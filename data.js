/* =========================================================
   Interview Navigator — DevOps Engineer
   Question dataset. Each topic has:
     - id, name, icon (emoji), description
     - kind: "intro" | "projects" | "qa"
     - items: [ ... ]  (cards or Q&A with follow-ups)
   ========================================================= */

const TOPICS = [
  /* ============================================================ */
  {
    id: "introduction",
    name: "Introduction",
    icon: "👋",
    kind: "intro",
    description:
      "Foundational concepts every DevOps engineer should be able to speak to in the first 5 minutes of an interview.",
    items: [
      {
        title: "What is DevOps?",
        body:
          "<p><b>DevOps</b> is a cultural and engineering practice that unifies software development (Dev) and IT operations (Ops) to enable teams to build, test, and release software faster and more reliably. Rather than being a specific tool or role, DevOps represents a fundamental shift in how organizations approach the software delivery lifecycle.</p><p>Traditional development followed a waterfall model where developers wrote code and handed it off to operations teams, who were then responsible for deploying and maintaining it. This separation created bottlenecks, mutual finger-pointing, and slow release cycles — often only quarterly or annual. DevOps eliminates these silos by fostering shared ownership across the entire lifecycle: developers understand the operational impact of their code, and operations engineers contribute to development tooling and practices.</p><p>The practice rests on four foundational pillars:</p><ul><li><b>Automation</b> — of builds, tests, deployments, infrastructure provisioning, and monitoring.</li><li><b>Collaboration</b> — between developers, operations, security, QA, and product stakeholders.</li><li><b>Continuous feedback</b> — through metrics, logs, traces, and blameless incident reviews.</li><li><b>Shared responsibility</b> — teams collectively own the health and reliability of their services in production.</li></ul><p>When implemented effectively, DevOps enables organizations to progress from monthly or quarterly releases to multiple daily deployments while simultaneously improving system reliability. Research by DORA (DevOps Research and Assessment) has consistently shown that faster-releasing teams experience fewer incidents — because smaller, more frequent changes are inherently easier to test, review, and debug.</p>",
      },
      {
        title: "Core DevOps Principles (CALMS)",
        body:
          "<p><b>CALMS</b> is a widely used framework that captures the five pillars of a mature DevOps practice. Each letter represents a principle that reinforces the others.</p><p><b>C — Culture:</b> The foundation of DevOps. Without a culture of trust, collaboration, and shared responsibility between Dev and Ops, no amount of tooling can deliver sustainable results. Culture is often the hardest pillar to establish and the most important to protect.</p><p><b>A — Automation:</b> Everything repeatable should be automated: builds, tests, deployments, infrastructure provisioning, monitoring configuration, security scanning, and even routine operational tasks. Automation removes human error, enables scale, and frees engineers to focus on higher-value work.</p><p><b>L — Lean:</b> Borrowed from Lean manufacturing, this principle emphasizes eliminating waste, working in small batches, and delivering value continuously. In DevOps, this translates to short-lived feature branches, small pull requests, and frequent releases rather than large, risky, quarterly launches.</p><p><b>M — Measurement:</b> Improvements cannot be made to what is not measured. Mature teams track meaningful metrics — DORA metrics, Service Level Indicators (SLIs), Service Level Objectives (SLOs), and business KPIs — rather than vanity metrics like lines of code committed.</p><p><b>S — Sharing:</b> Knowledge, tooling, ownership, and responsibility are shared across teams. Post-mortems are blameless and public. Runbooks are documented and accessible. On-call rotations include developers alongside operations engineers.</p>",
      },
      {
        title: "The DevOps Lifecycle",
        body:
          "<p>The DevOps lifecycle is commonly represented as an infinity loop to emphasize its continuous, iterative nature. Unlike traditional waterfall development, which treats deployment as an endpoint, DevOps treats operations as a source of ongoing feedback that flows back into planning.</p><p>The lifecycle consists of eight stages:</p><ol><li><b>Plan</b> — define requirements, prioritize backlogs, design changes.</li><li><b>Code</b> — write and review software changes.</li><li><b>Build</b> — compile source code and produce deployable artifacts.</li><li><b>Test</b> — validate functionality, performance, and security through automated tests.</li><li><b>Release</b> — package the artifact and prepare it for deployment.</li><li><b>Deploy</b> — promote the artifact into production environments.</li><li><b>Operate</b> — run and maintain the software in production.</li><li><b>Monitor</b> — observe system behavior and user impact, then feed insights back to planning.</li></ol><p>What distinguishes the DevOps lifecycle from traditional software delivery is the extent of <b>automation</b> at every stage and the depth of <b>observability</b> across the entire pipeline. When a deployment introduces a regression, metrics and traces surface the issue within minutes rather than days. This tight feedback loop allows teams to iterate quickly with confidence.</p>",
      },
      {
        title: "Key DORA Metrics",
        body:
          "<p>The <b>DORA metrics</b> were developed by Google's DevOps Research and Assessment team based on a multi-year study of thousands of engineering organizations. They are widely regarded as the most reliable indicators of software delivery performance because they balance speed with stability — improving one at the expense of the other is not possible over time.</p><p>The four metrics are:</p><ol><li><b>Deployment Frequency</b> — how often the organization deploys code to production. A measure of throughput and delivery speed.</li><li><b>Lead Time for Changes</b> — the elapsed time from a code commit being merged to that change running in production. A measure of end-to-end delivery efficiency.</li><li><b>Change Failure Rate</b> — the percentage of deployments that cause an incident, require a rollback, or result in degraded service. A measure of quality and stability.</li><li><b>Mean Time to Restore (MTTR)</b> — the average time required to restore service after a production incident. A measure of resilience and recovery capability.</li></ol><p>DORA classifies teams into four performance categories: Low, Medium, High, and Elite. <b>Elite performers</b> deploy multiple times per day, have lead times under one hour, keep their change failure rate below 15%, and restore service in under an hour. Low performers, by contrast, deploy less than once every six months and take weeks to recover from incidents.</p><p>A counter-intuitive but well-documented finding is that faster teams are also more stable. Smaller, more frequent changes are easier to test, review, and debug — so speed and reliability reinforce rather than oppose each other.</p>",
      },
      {
        title: "DevOps vs SRE vs Platform Engineering",
        body:
          "<p>These three disciplines are closely related and often overlap in practice, but they represent distinct approaches to solving different problems.</p><p><b>DevOps</b> is the broadest of the three — a cultural movement and a set of practices aimed at accelerating and improving software delivery through collaboration between Dev and Ops. It is deliberately implementation-agnostic; there is no single correct way to \"do DevOps.\"</p><p><b>Site Reliability Engineering (SRE)</b> is a specific implementation of DevOps pioneered by Google. It treats reliability as a first-class engineering discipline with concrete principles:</p><ul><li><b>Error budgets</b> — a quantified allowance for downtime derived from the SLO. When exhausted, feature work pauses in favor of reliability work.</li><li><b>Toil reduction</b> — measuring and systematically eliminating repetitive manual operational work.</li><li><b>The 50% rule</b> — SREs should not spend more than half their time on operations; the remainder is dedicated to engineering solutions that reduce future operational load.</li></ul><p><b>Platform Engineering</b> is a more recent discipline that has grown significantly since 2020. Instead of each product team independently building its own CI/CD, observability, and infrastructure tooling, a dedicated platform team builds an <b>Internal Developer Platform (IDP)</b> — a self-service, opinionated \"golden path\" that product teams consume. The goal is to reduce cognitive load on developers and improve developer velocity through productized internal tooling.</p><p>In large organizations, all three often coexist: SRE owns reliability, Platform Engineering owns developer productivity, and DevOps provides the cultural framework that binds them together.</p>",
      },
      {
        title: "The Toolchain at a Glance",
        body:
          "<p>A modern DevOps toolchain spans the entire software delivery lifecycle. Understanding how tools fit together — rather than simply knowing their names — is what distinguishes an experienced DevOps engineer.</p><p><b>Version Control Systems (VCS):</b> Git is the universal standard. It is typically hosted on <b>GitHub</b>, <b>GitLab</b>, or <b>Bitbucket</b>, each of which provides pull-request workflows, code review, and often built-in CI/CD.</p><p><b>Continuous Integration and Delivery (CI/CD):</b> <b>Jenkins</b> remains dominant in enterprise environments due to its plugin ecosystem. Newer teams often prefer <b>GitHub Actions</b> or <b>GitLab CI</b> for tighter SCM integration and simpler configuration. For Kubernetes-based deployments, CI (which builds artifacts) is commonly paired with a GitOps tool such as <b>ArgoCD</b> or <b>Flux</b>.</p><p><b>Configuration Management:</b> <b>Ansible</b> is the most widely used because it is agentless and low-friction. <b>Chef</b> and <b>Puppet</b> remain in mature environments but are less common in new projects.</p><p><b>Infrastructure as Code (IaC):</b> <b>Terraform</b> is the industry standard for cloud-agnostic provisioning. <b>Pulumi</b> is preferred by teams that want to use general-purpose programming languages. <b>CloudFormation</b> is used for AWS-specific scenarios that require deep integration.</p><p><b>Containers and Orchestration:</b> <b>Docker</b> (or <b>containerd</b> at the runtime level) is used for building and running container images. <b>Kubernetes</b> is the dominant orchestrator; managed alternatives include AWS <b>ECS/Fargate</b>, Azure Container Apps, and Google Cloud Run.</p><p><b>Observability:</b> The typical open-source stack combines <b>Prometheus</b> and <b>Grafana</b> for metrics, <b>Loki</b> or the <b>ELK stack</b> for logs, and <b>Tempo</b> or <b>Jaeger</b> for distributed traces. <b>Datadog</b>, <b>New Relic</b>, and <b>Dynatrace</b> are common commercial alternatives.</p><p><b>Cloud Providers:</b> <b>AWS</b> is the market leader, <b>Azure</b> is dominant in Microsoft-centric enterprises, and <b>GCP</b> is strong in data and machine learning workloads.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "projects",
    name: "Projects",
    icon: "🚀",
    kind: "projects",
    description:
      "Realistic DevOps portfolio projects you can talk about with confidence — pick 1–2 and know them cold.",
    items: [
      {
        title: "End-to-End CI/CD for a Microservices App",
        summary:
          "A fully automated CI/CD pipeline for a three-service application (two Node.js services and one Python worker) running on Kubernetes. The pipeline replaced a manual, error-prone deployment process with automated build, test, security scanning, and progressive delivery — reducing deployment time from hours to minutes while adding built-in security gates.",
        tech: ["GitHub", "Jenkins", "Docker", "SonarQube", "Trivy", "Helm", "Kubernetes", "ArgoCD"],
        highlights: [
          "<b>Webhook-triggered pipeline:</b> A GitHub webhook triggers the Jenkins pipeline automatically on every pull request to the <code>main</code> branch, providing fast feedback to developers within minutes.",
          "<b>Parallel testing stages:</b> Unit tests, static analysis using SonarQube, and Static Application Security Testing (SAST) run in parallel, reducing total pipeline duration from approximately 15 minutes to under 5 minutes.",
          "<b>Secure container builds:</b> Multi-stage Dockerfiles produce runtime images under 80 MB. Every image is scanned by Trivy for known CVEs before promotion; high-severity vulnerabilities fail the build with no exceptions.",
          "<b>GitOps deployment model:</b> Each service has a versioned Helm chart in a dedicated <code>k8s-config</code> repository. ArgoCD continuously reconciles the cluster state against this repository, meaning no cluster credentials are ever exposed to the CI system.",
          "<b>Progressive delivery:</b> Blue/green rollouts are managed by argo-rollouts. New pods must pass readiness probes for a defined stabilization period; failure triggers an automatic rollback to the previous version, effectively eliminating deploy-related outages.",
        ],
      },
      {
        title: "Infrastructure-as-Code AWS Landing Zone",
        summary:
          "A multi-account AWS Landing Zone provisioned entirely through Terraform, designed to move an organization from a single shared AWS account into a properly segmented multi-account architecture. The project established consistent guardrails, least-privilege IAM, and reproducible infrastructure across all environments.",
        tech: ["Terraform", "AWS Organizations", "S3", "DynamoDB", "IAM", "VPC", "EKS", "RDS"],
        highlights: [
          "<b>Reusable Terraform modules:</b> Independent, versioned modules for VPC, EKS, RDS, and IAM. Each environment workspace (dev, stage, prod) consumes the same module code with environment-specific variables, ensuring consistency across environments.",
          "<b>Secure remote state:</b> Terraform state is stored in a versioned, KMS-encrypted S3 bucket, with DynamoDB providing state locking to prevent concurrent modifications by multiple engineers.",
          "<b>Organizational structure:</b> AWS Organizations is configured with Security, Shared Services, Dev, and Prod Organizational Units. Service Control Policies (SCPs) at the OU level enforce guardrails such as region restrictions and prohibition of public S3 buckets.",
          "<b>Security-as-code in CI:</b> Every pull request runs <code>tfsec</code> and <code>Checkov</code> to detect misconfigurations. Non-compliant changes are blocked at merge time, shifting security left instead of relying on periodic audits.",
          "<b>Controlled apply workflow:</b> Atlantis is used to orchestrate plan/apply workflows. Engineers open pull requests; Atlantis posts the Terraform plan as a comment for review, and applies are triggered only after explicit approval.",
        ],
      },
      {
        title: "Kubernetes Observability Stack",
        summary:
          "A unified metrics, logs, and traces stack deployed on Amazon EKS, providing a single pane of glass for engineering and operations teams. The stack replaced fragmented, siloed monitoring tools with correlated telemetry and SLO-based alerting, significantly reducing incident detection and resolution times.",
        tech: ["Prometheus", "Grafana", "Loki", "Tempo", "OpenTelemetry", "AlertManager", "Kubernetes"],
        highlights: [
          "<b>Kubernetes-native metrics:</b> Deployed via the <code>kube-prometheus-stack</code> Helm chart, which bundles Prometheus, Grafana, and AlertManager with pre-configured dashboards, recording rules, and alerts appropriate for Kubernetes workloads.",
          "<b>Unified telemetry:</b> Logs are ingested by Loki using a label-indexed design that is more cost-effective at scale than full-text indexing. Distributed traces are captured by Tempo. Application services are instrumented with OpenTelemetry SDKs, ensuring trace IDs propagate consistently across service boundaries.",
          "<b>SLO-based alerting:</b> Service Level Objectives are defined for each critical user journey, with burn-rate alerts in Grafana. Fast burn rates (exhausting the monthly error budget within hours) page on-call engineers via PagerDuty; slower burn rates generate Slack notifications for asynchronous investigation.",
          "<b>Multi-tenant access control:</b> Grafana teams and folder-level permissions ensure that each product team sees only their own dashboards, enabling self-service without cross-team noise.",
        ],
      },
      {
        title: "Zero-Downtime Deployment with GitOps",
        summary:
          "Migration of a legacy push-based deployment pipeline to a pull-based GitOps model using ArgoCD. Beyond the tooling change, the project shifted the team's mental model from imperative deployments to declarative desired-state reconciliation, dramatically improving deployment reliability and rollback speed.",
        tech: ["ArgoCD", "Helm", "Kustomize", "Kubernetes", "GitHub Actions"],
        highlights: [
          "<b>Separation of concerns:</b> Application source code lives in the service repository, which builds container images and updates image tags via automated commits. Kubernetes manifests are maintained in a dedicated <code>k8s-config</code> repository, providing a clean audit trail of all changes to production.",
          "<b>Environment templating:</b> ArgoCD ApplicationSets generate per-environment Application resources (dev, stage, prod) from a single template using generators, eliminating YAML duplication across environments.",
          "<b>PR-based promotions:</b> Environment promotions are performed via pull requests that update the image tag in the target environment's overlay. Rollback is a single command (<code>argocd app rollback</code>) that reverts the manifest state.",
          "<b>Measurable business outcome:</b> Mean Time to Restore (MTTR) for deployment-related incidents dropped from approximately 45 minutes to under 5 minutes, driven by declarative state and instant, one-command rollbacks.",
        ],
      },
      {
        title: "Immutable Base AMIs with Packer + Ansible",
        summary:
          "A weekly automated pipeline that produces hardened, pre-configured Amazon Machine Images (AMIs) for use across the organization's EC2 fleet. The project established an immutable infrastructure pattern where every instance starts from a validated golden image, replacing the previous approach of configuring vanilla AMIs at boot time.",
        tech: ["Packer", "Ansible", "AWS", "Jenkins", "InSpec"],
        highlights: [
          "<b>Image build pipeline:</b> Packer provisions a temporary EC2 instance from the Amazon Linux 2 base image, applies configuration, and snapshots the result into a new AMI. The entire process runs unattended in Jenkins on a weekly schedule.",
          "<b>Provisioning with Ansible:</b> Ansible playbooks install standard operational agents (CloudWatch Agent, AWS Systems Manager Agent, Falco for runtime security) and apply CIS Benchmark hardening across the operating system.",
          "<b>Compliance validation:</b> InSpec compliance tests execute against the newly built AMI before it is published. If any control fails, the AMI is rejected and the build fails, preventing non-compliant images from reaching production.",
          "<b>Full traceability:</b> Each published AMI is tagged with the git SHA of the Packer configuration that produced it. Jenkins attaches the CVE scan report as a build artifact, ensuring every AMI in production can be traced back to its exact source code and security posture at build time.",
        ],
      },
    ],
  },

  /* ============================================================ */
  {
    id: "ai",
    name: "AI in Your Project",
    icon: "🤖",
    kind: "qa",
    description:
      "How you used AI in your DevOps work — a story you can tell in interviews. Cursor, GitHub Copilot, Claude, ChatGPT for coding, review, IaC, incident response, and toolchain automation.",
    items: [
      {
        q: "How have you used AI tools in your DevOps work?",
        difficulty: "easy",
        a: "<p>AI-assisted tooling has become a core part of my day-to-day DevOps workflow. Rather than treating AI as a novelty, I use it deliberately in specific parts of my workflow where it provides clear, measurable productivity gains without compromising quality.</p><p><b>Areas where I actively use AI:</b></p><ul><li><b>Writing Infrastructure-as-Code:</b> I use <b>Cursor</b> and <b>GitHub Copilot</b> to accelerate Terraform, Bicep, and Kubernetes YAML authoring. AI is exceptional at producing well-structured boilerplate — a new module, a Helm chart, or a Kubernetes manifest — that I then review and adjust. This typically reduces the time to draft new infrastructure code by 40–60%.</li><li><b>Understanding unfamiliar code:</b> When I inherit a Jenkins pipeline, a Terraform module, or a legacy shell script, I ask AI to explain what it does before I make changes. This dramatically shortens the ramp-up time and reduces the risk of misinterpreting complex logic.</li><li><b>Writing scripts and one-liners:</b> For quick bash, PowerShell, or jq/awk manipulations, describing what I want in natural language and asking AI to generate the command is faster than looking up syntax, and the result is easy to verify.</li><li><b>Troubleshooting and root-cause analysis:</b> Pasting a stack trace, a <code>kubectl describe pod</code> output, or a Terraform error into a chat interface produces a structured breakdown of likely causes and next steps, which I then validate against the actual system.</li><li><b>Documentation and communication:</b> Turning bullet points into clear runbook documentation, drafting post-incident reviews, and writing release notes — all activities where AI improves consistency and saves time.</li><li><b>Refactoring and code review:</b> Asking AI to review a diff or refactor a function produces useful suggestions that I evaluate as another perspective.</li></ul><p><b>My guiding principle:</b> AI is a powerful accelerator for tasks I already understand well enough to verify the output. I do not use AI to generate code I could not confidently produce and review myself, because that would introduce risk without an appropriate safety net.</p>",
      },
      {
        q: "Which AI tools do you use and why?",
        difficulty: "easy",
        a: "<p>I use different AI tools for different purposes, choosing based on the strengths of each and the task at hand.</p><p><b>Coding and IDE:</b></p><ul><li><b>Cursor:</b> My primary editor. Its agent-mode allows me to describe a multi-step task (\"refactor this Terraform module to use for_each\") and have the AI execute changes across files, which I then review commit-by-commit. It is significantly more powerful than autocomplete-only assistants for larger refactoring tasks.</li><li><b>GitHub Copilot:</b> Excellent as an inline autocomplete engine when I know exactly what I want to type but want to type less. Works well for boilerplate, repetitive code, and simple functions. I use it when I want low-latency inline suggestions rather than agentic actions.</li></ul><p><b>Reasoning, planning, and one-off tasks:</b></p><ul><li><b>Claude (Anthropic):</b> My preferred choice for open-ended reasoning — designing architectures, planning complex refactors, drafting long-form documentation, or analyzing a large log dump. It handles long context and structured explanations very well.</li><li><b>ChatGPT (OpenAI):</b> Similar use cases, with strong performance across many tasks. I also use it for image-related work when needed.</li></ul><p><b>Terminal and shell:</b></p><ul><li><b>Warp AI</b> or <b>GitHub Copilot for CLI:</b> Occasional use for terminal-native command suggestions — \"how do I find files modified in the last hour and larger than 1 MB?\" — where I want the answer in the same terminal where I will run it.</li></ul><p><b>Chat-based tools embedded in cloud platforms:</b></p><ul><li><b>Amazon Q, Azure Copilot, Google Cloud AI:</b> When working within a specific cloud console, the platform-specific AI assistant is often the most accurate source for that platform's specific behaviors, service limits, and error messages.</li></ul><p><b>How I choose:</b> Cursor for anything involving code I am editing. Claude or ChatGPT for planning, explaining, or analyzing content outside the editor. Platform-specific AI when I am deep inside a specific cloud console. The right tool depends on the friction — I want AI where I am working, not in a separate window I have to context-switch to.</p>",
      },
      {
        q: "Can you give a specific example of how AI accelerated your work?",
        difficulty: "medium",
        a: "<p>Yes — a concrete example was building the <b>Interview Navigator</b> project itself, which is a good illustration of how AI accelerates a portfolio-quality project without compromising ownership of the output.</p><p><b>The project:</b> A web-based interview preparation tool for DevOps Engineer positions. It covers 15+ topics (Linux, Git, Docker, Kubernetes, Terraform, Ansible, AWS, Azure, GCP, Helm, ArgoCD, Jenkins, GitHub Actions, SonarQube, and more) with detailed written explanations of interview-style questions, follow-up questions in draggable side windows, dark/light theme, and category filters.</p><p><b>How AI contributed:</b></p><ol><li><b>Scaffolding the initial UI:</b> I described the desired UX (\"sidebar of topics, main content area, draggable follow-up windows, dark theme by default\") and used AI to generate the initial HTML, CSS, and JavaScript. This gave me a working prototype in under an hour that would have taken me most of a day from scratch.</li><li><b>Redesigning the layout iteratively:</b> When I decided I wanted small side-by-side cards instead of a linear list, I described the desired end state and had AI restructure the CSS Grid and template rendering. Each iteration was a single conversation turn.</li><li><b>Generating comprehensive Q&A content:</b> The main value of the app is its 150+ interview questions across topics. I used AI to draft the initial content in a structured written-explanation style, following a template I defined (introduction paragraph, main explanation, examples, comparison tables, follow-ups). I reviewed and refined every answer, adjusted anything that felt inaccurate or awkward, and added my own perspectives where the AI's generic answer missed context.</li><li><b>Deployment and Git setup:</b> Creating the .gitignore, README.md, initializing the Git repository, and pushing to GitHub — AI walked me through the exact commands to run, including recovery from the initial push conflict when the remote had a pre-existing README.</li></ol><p><b>What AI did not do:</b></p><ul><li>Decide the scope or the audience.</li><li>Decide the visual design direction.</li><li>Judge which topics matter for a DevOps engineer.</li><li>Write anything I could not have written myself — I reviewed every line.</li></ul><p><b>Time saved:</b> A project of this scope would have taken me perhaps 40+ hours to build alone. With AI assistance, I completed it in roughly a day of focused work spread across a few sessions. More importantly, the AI-assisted workflow let me spend most of my time on the parts that required judgment — content structure, topic prioritization, tone, and quality — rather than typing boilerplate.</p>",
      },
      {
        q: "How do you use AI for writing Infrastructure-as-Code?",
        difficulty: "medium",
        a: "<p>Infrastructure-as-Code (IaC) is one of the areas where AI assistance has the highest impact for DevOps work, because IaC is verbose, repetitive, and follows well-known patterns that AI models have seen extensively.</p><p><b>Concrete workflow patterns I use:</b></p><ol><li><b>Generating a new module from a description.</b> Example: \"Create a Terraform module for a highly available web tier on AWS — ALB with HTTP → HTTPS redirect, ASG across 3 AZs, launch template using Amazon Linux 2023, target group with health checks, CloudWatch alarms for high CPU and 5xx errors.\" The AI produces a structured module with <code>variables.tf</code>, <code>main.tf</code>, <code>outputs.tf</code>, and <code>versions.tf</code> that I then review and refine.</li><li><b>Converting between IaC languages.</b> Example: \"Convert this ARM template to Bicep\" or \"Convert this CloudFormation to Terraform\". This is a task AI handles very well because the semantic content is preserved even when syntax differs.</li><li><b>Generating Kubernetes manifests from application requirements.</b> Example: \"Create a Kubernetes manifest set for a stateless Node.js application requiring 3 replicas, an Ingress with TLS via cert-manager, HPA scaling 3–10 pods based on CPU, PodDisruptionBudget of minAvailable 2, and a NetworkPolicy that only allows ingress from the ingress-nginx namespace.\" The AI produces the deployment, service, ingress, HPA, PDB, and NetworkPolicy YAML — all wired correctly.</li><li><b>Producing Helm chart templates.</b> The scaffolding produced by <code>helm create</code> is a starting point, but customizing values.yaml, templates, and _helpers.tpl for a specific application is a task AI handles well.</li><li><b>Explaining and refactoring inherited code.</b> \"What does this Terraform module do?\" or \"Refactor this module to use for_each instead of count\" are queries where AI reliably delivers useful, correct results.</li></ol><p><b>Guardrails I always apply:</b></p><ul><li><b>Always run <code>terraform plan</code> before <code>apply</code>.</b> AI-generated Terraform is quite reliable, but the plan output is where I catch anything wrong.</li><li><b>Diff against known-good baselines.</b> When AI modifies existing modules, I diff the changes carefully and reason about each modification.</li><li><b>Version-pin providers and modules.</b> AI may reference outdated resource attribute names if not given current provider version context. Pinning versions catches these mismatches.</li><li><b>Validate with static analysis.</b> I run <code>tflint</code>, <code>checkov</code>, and <code>terraform validate</code> on AI-generated code before committing.</li><li><b>Never let AI apply changes autonomously to production.</b> AI generates code; a human reviews and applies.</li></ul>",
      },
      {
        q: "How do you use AI for incident response and troubleshooting?",
        difficulty: "medium",
        a: "<p>AI assistance is particularly valuable during incident response because incidents are inherently novel and often require synthesizing information across multiple sources under time pressure. The right prompting can accelerate root-cause analysis significantly, while poor use of AI can send you down wrong paths.</p><p><b>Ways I use AI during incidents:</b></p><ol><li><b>Interpreting unfamiliar error messages.</b> Kubernetes, Terraform, and cloud APIs produce error messages that are often opaque. Pasting the exact error text into a chat interface — with any relevant context like the resource type and operation — usually produces a structured explanation of what the error means and common causes.</li><li><b>Parsing complex logs and metrics.</b> When faced with a long, dense log dump — for example, application logs during a memory leak or the output of <code>kubectl describe pod</code> for a crashing pod — AI is very good at summarizing patterns, identifying anomalies, and pointing to likely culprits.</li><li><b>Suggesting diagnostic commands.</b> \"What commands can I run to check if this pod is failing due to memory pressure versus CPU throttling versus a downstream dependency?\" The AI produces a checklist I can work through systematically.</li><li><b>Drafting the incident summary.</b> After the incident is resolved, I brief AI on what happened and have it draft the post-incident review — timeline, root cause, contributing factors, corrective actions. I then edit for accuracy and add nuance the AI could not know.</li><li><b>Generating queries for observability tools.</b> \"Write a PromQL query that shows the pods in the payments namespace with memory usage above 80% of their limit over the last 30 minutes.\" AI produces the query, which I run and verify.</li></ol><p><b>What I explicitly do NOT do with AI during incidents:</b></p><ul><li><b>Take AI suggestions as ground truth.</b> AI can hallucinate — invent plausible-sounding but wrong command flags, non-existent APIs, or incorrect explanations. During an incident, I verify every AI suggestion against actual system behavior before acting on it.</li><li><b>Skip fundamentals.</b> Reading the actual logs, running the actual commands, and consulting the actual dashboards remains primary. AI supplements that work; it does not replace it.</li><li><b>Feed sensitive information to public AI services.</b> Customer data, credentials, and internal system details never go into public chat interfaces. For those, I use enterprise-scoped AI tools with appropriate data handling.</li></ul><p><b>The framing I keep in mind:</b> AI is a very fast, well-read junior engineer who has read every documentation page and every Stack Overflow answer, but has never actually operated the specific system you are looking at. Its suggestions are usually good starting points, and it is worth an extra minute of verification before acting during an incident.</p>",
      },
      {
        q: "How do you handle AI hallucinations and inaccuracies?",
        difficulty: "medium",
        a: "<p><b>Hallucination</b> — AI producing content that sounds correct but is factually wrong — is the single most important risk in AI-assisted engineering. Managing it is a discipline that separates effective AI users from those who accidentally introduce bugs and security issues.</p><p><b>Where hallucinations commonly appear in DevOps use:</b></p><ul><li>Non-existent or renamed provider resource attributes in Terraform.</li><li>Command flags that never existed or existed only in older versions of a tool.</li><li>API endpoints or SDK method names that sound right but are not real.</li><li>Kubernetes API resources with incorrect apiVersion or kind combinations.</li><li>Fabricated command output presented as if quoted from the AI's execution.</li><li>Invented documentation citations.</li></ul><p><b>My practical mitigation strategies:</b></p><ol><li><b>Verify against official docs for anything unfamiliar.</b> If AI suggests a flag or resource attribute I do not immediately recognize, I check the tool's documentation before using it. Providers evolve; AI training data has cutoffs.</li><li><b>Run the code, do not just read it.</b> A Terraform plan will fail immediately if a resource attribute does not exist. A shell script will fail immediately if a command does not exist. Running is verification.</li><li><b>Use type-checked and lint-checked contexts.</b> Cursor and Copilot benefit enormously from language servers, TypeScript, Python type hints, and linters. These catch invalid method calls the moment they are generated.</li><li><b>Ask the AI to cite sources.</b> For claims that sound important — a specific SLA number, a particular service limit — I ask the AI to identify where the fact came from. AI responses that cannot be traced to real sources are treated as suspicious.</li><li><b>Prefer models with current training or live search.</b> Some AI tools have access to search or up-to-date documentation. For tools that change quickly (Kubernetes, Terraform providers), I prefer those over models with older cutoffs.</li><li><b>Match the AI's confidence to the stakes.</b> Low-stakes work — a one-off script, a draft of a runbook — tolerates some verification-later. High-stakes work — production IaC changes, security configurations, database migrations — demands verification before commit.</li></ol><p><b>Cultural rule I apply to myself and share with teams:</b> Treat AI output like a pull request from a fast but slightly untrusted contributor. You would not merge a PR without reading it and running the tests. Apply the same discipline to AI output.</p><p>With this discipline, hallucinations become an occasional inconvenience rather than a systemic risk. Without it, they become a substantial source of production incidents.</p>",
      },
      {
        q: "How do you use AI for code reviews?",
        difficulty: "medium",
        a: "<p>AI assistance in code review works well as a supplemental reviewer — providing a fast, methodical, well-read second opinion that human reviewers can build on. It does not replace human review, but it improves the signal reviewers see.</p><p><b>How I use AI in review workflows:</b></p><ol><li><b>Self-review before submission.</b> Before opening a PR, I paste the diff into an AI chat and ask for a review. Common prompts: \"Review this diff for correctness, edge cases, and style. Flag anything unclear or risky.\" This catches issues I might have missed and reduces reviewer burden.</li><li><b>Reviewing others' PRs.</b> When I am assigned a PR in an unfamiliar area, I ask AI to explain the intent of the changes and identify potential concerns. It gives me a starting map of the review, but I then verify each concern against the actual code.</li><li><b>Focused security review.</b> For changes that touch authentication, authorization, cryptography, or input validation, I ask AI specifically about security implications. AI is generally strong at recognizing common vulnerability patterns (SQL injection, XSS, insecure deserialization, timing attacks).</li><li><b>Test case suggestions.</b> \"What edge cases should I test for this function?\" produces useful lists that often reveal cases I would not have thought of.</li></ol><p><b>Automated AI code review in the pipeline:</b> Several tools now provide AI-driven review integrated with GitHub, GitLab, and Bitbucket:</p><ul><li><b>Cursor Bugbot</b> and other AI reviewers run on every PR and post inline comments on likely issues.</li><li><b>Snyk Code</b>, <b>DeepSource</b>, and <b>Codacy</b> use AI-assisted analysis for their static analysis reports.</li><li>These tools produce feedback that is more contextual than traditional static analyzers because they can reason about intent, not just pattern-match.</li></ul><p><b>Limitations I have observed:</b></p><ul><li>AI reviewers can be noisy — they flag many minor style issues alongside genuine bugs. Tuning is important.</li><li>AI reviewers often miss architectural concerns that require domain knowledge of the specific system.</li><li>AI review is best at surface-level correctness (null checks, missing error handling, obvious typos) and pattern-based issues; humans remain essential for higher-level design judgment.</li></ul><p><b>My rule:</b> Every PR gets human review; some PRs also get AI review. The AI review is a helpful first pass — not the last word.</p>",
      },
      {
        q: "How do you write good prompts for AI-assisted DevOps work?",
        difficulty: "medium",
        a: "<p>The quality of AI output is roughly proportional to the quality of the prompt. Vague prompts produce vague, generic answers; specific prompts with concrete context produce useful, actionable results. This is a skill worth developing intentionally.</p><p><b>Elements of a good technical prompt:</b></p><ol><li><b>Explicit goal.</b> \"I want to accomplish X\" — clear about the desired outcome.</li><li><b>Relevant context.</b> Technology stack versions, cloud provider, constraints, existing patterns. \"We use Terraform 1.7 with the AWS provider 5.x, targeting eu-west-1, with all state in a shared S3 backend.\"</li><li><b>Sample of existing style.</b> Paste a snippet of your existing code so the AI mirrors your conventions. \"Match the style of this existing module: [paste code].\"</li><li><b>Constraints and non-goals.</b> \"Do not use root credentials.\" \"Keep the module compatible with our shared module structure.\" \"Do not introduce new dependencies.\"</li><li><b>Expected output format.</b> \"Produce three files: main.tf, variables.tf, and outputs.tf, with variable descriptions and defaults.\"</li></ol><p><b>Example — poor prompt:</b><br><code>\"Give me a Terraform module for an EKS cluster.\"</code></p><p>This produces a generic module that may or may not match my needs, uses arbitrary defaults, and follows random style choices.</p><p><b>Example — improved prompt:</b><br><code>\"Create a Terraform module for a production EKS cluster on AWS (eu-west-1). Requirements: private cluster (no public API endpoint), three node groups (system, general, spot), IRSA enabled, EKS control-plane logs sent to CloudWatch, cluster encryption with a KMS CMK, and CNI configured with prefix delegation for higher pod density. Follow this variable style: [paste example from an existing module]. Output as three files: main.tf, variables.tf, outputs.tf. Use provider version 5.x and Terraform 1.7 syntax.\"</code></p><p>This produces a module tailored to my exact requirements, in my style, with the right versioning.</p><p><b>Additional techniques I use:</b></p><ul><li><b>Iterative refinement.</b> Do not expect the first response to be final. Iterate: \"Add IRSA role for cluster-autoscaler.\" \"Change the node groups to use launch templates.\" \"Move variable defaults to a locals block.\"</li><li><b>Ask for reasoning, not just code.</b> \"Explain the trade-off before making the change.\" This surfaces whether the AI understands the problem or just pattern-matched.</li><li><b>Include failure cases.</b> When something did not work, paste the error and the code that produced it. The AI can then reason about the specific problem rather than guessing.</li><li><b>Use structured formats.</b> Tables, checklists, numbered steps produce output that is easier to consume and review than free-form prose.</li><li><b>Break large tasks into stages.</b> Complex multi-step changes go better when done in stages, each with its own review, than in one massive request.</li></ul><p><b>The overarching principle:</b> Communicate with AI the way you would communicate with a competent contractor who is fast, well-read, but unfamiliar with your specific system. Give them the same context you would give a human, and expect similar quality of output.</p>",
      },
      {
        q: "How do you handle security and data privacy when using AI tools?",
        difficulty: "medium",
        a: "<p>AI tools introduce real security and privacy considerations, particularly around what data is sent to model providers, how that data may be used, and how AI-generated output is trusted. Every enterprise DevOps team needs an intentional policy on this.</p><p><b>The primary concerns:</b></p><ol><li><b>Data leaving your environment.</b> Anything sent to a public AI service becomes visible to that service's operator and may — depending on the provider's terms — be used for training future models. This is unacceptable for customer data, credentials, proprietary code, or internal architectural details.</li><li><b>Prompt injection and supply chain risk.</b> AI-generated code may reference non-existent packages, or worse, packages that a malicious actor has typosquatted. Blindly running <code>pip install</code> or <code>npm install</code> on AI-generated commands is a real risk.</li><li><b>Credentials in prompts.</b> It is surprisingly easy to accidentally paste an API key or password into a chat interface while asking for help. These credentials should be assumed compromised the moment they cross that boundary.</li></ol><p><b>Practical safeguards I apply:</b></p><ol><li><b>Enterprise-scoped AI tools for sensitive work.</b> Use Cursor's enterprise mode, GitHub Copilot for Business/Enterprise, ChatGPT Enterprise, or Claude Enterprise — all of which have contractual guarantees that prompts and code are not used for training and are handled with enterprise data protection.</li><li><b>Redact before pasting.</b> When asking about an error or a log, remove real hostnames, IPs, user IDs, and any customer data. Substitute placeholders like <code>&lt;host&gt;</code>, <code>&lt;user-id&gt;</code>, <code>&lt;customer-uuid&gt;</code>.</li><li><b>Never paste credentials or secrets.</b> If a secret leaked into a prompt, rotate it immediately — treat it as compromised.</li><li><b>Vet AI-suggested dependencies.</b> Before running <code>pip install</code>, <code>npm install</code>, or downloading any binary an AI suggests, verify the package exists and is genuine. Prefer packages you or your team already use.</li><li><b>Run AI-generated code in a sandboxed context first.</b> A container or throwaway VM catches AI-generated commands that would misbehave (deleting things unexpectedly, exfiltrating data). Never run untrusted AI commands as root on your workstation.</li><li><b>Static analysis on AI code before commit.</b> Tools like SonarQube, Snyk, and Trivy catch security issues in AI-generated code the same way they catch them in human-written code. AI does not exempt code from your normal security pipeline.</li><li><b>Enterprise usage policies.</b> The organization should have an explicit policy — which AI tools are approved, what data classes can and cannot be sent to which tools, and how AI-generated code is reviewed before merging. Awareness training for engineers is essential.</li></ol><p><b>Emerging models help:</b> Local and on-premise AI (Llama, Mistral, self-hosted OpenAI-compatible endpoints) allow sensitive work to happen entirely inside your network. For highly regulated environments, this is often the only acceptable option, at the cost of some quality relative to leading frontier models.</p>",
      },
      {
        q: "How do you see AI changing the role of DevOps engineers going forward?",
        difficulty: "medium",
        a: "<p>AI is reshaping the DevOps role meaningfully, but not in the direction of replacement. The engineers I see thriving are the ones who use AI as a force multiplier for judgment work, rather than either ignoring it or over-trusting it.</p><p><b>What is changing:</b></p><ol><li><b>The volume of routine work is dropping.</b> Writing standard IaC, drafting Kubernetes manifests, producing shell scripts, generating boilerplate — all of this is faster with AI. Engineers who previously spent a large fraction of their time on such tasks now spend less.</li><li><b>The value of judgment and architecture is rising.</b> Deciding what to build, how systems should interact, what the failure modes are, what the security posture should be — none of this is automated. AI accelerates execution, but the person making the design decisions still needs to understand what they are deciding.</li><li><b>The bar for junior engineers is rising.</b> Basic script-writing and manifest-authoring — traditionally training tasks for juniors — are now trivial with AI. Juniors need to develop higher-level skills faster: understanding systems, evaluating trade-offs, and reviewing AI output critically.</li><li><b>Observability and troubleshooting are becoming more efficient.</b> AI is very good at pattern-recognition across logs and metrics, and increasingly integrates directly into observability tools. This shortens time-to-detection and time-to-diagnosis for incidents.</li><li><b>New failure modes are emerging.</b> Over-trusted AI output leads to bugs and security issues at higher rates than well-reviewed human code. Teams that use AI without discipline are seeing this now. Teams that use AI with discipline are getting the benefits without the downsides.</li></ol><p><b>What is NOT changing:</b></p><ul><li>The need for engineers to understand the systems they operate. Someone must read the AI output and know whether it is right.</li><li>The value of ownership and accountability. AI does not get paged at 3 AM.</li><li>The importance of communication, judgment, and collaboration — the things that are fundamentally human.</li><li>The need for engineers to keep learning. AI tools evolve rapidly; effective engineers adapt.</li></ul><p><b>My own trajectory:</b> I am investing time in becoming more effective at using AI — better prompts, better verification, faster iteration — while also deepening my understanding of the systems I work with. The two go together: the more you understand the underlying system, the more effectively you can direct and verify AI. I expect that in 3–5 years, being fluent with AI-assisted workflows will be a baseline expectation for DevOps engineers, similar to being fluent with Git today.</p><p><b>The mindset I recommend:</b> Treat AI as a highly capable collaborator, apply the same discipline you would to any collaborator's work, and focus your own time on the parts of the job that require judgment. The engineers who do this will be significantly more productive; the engineers who resist AI entirely, or who use it uncritically, will find themselves at a disadvantage.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "linux",
    name: "Linux",
    icon: "🐧",
    kind: "qa",
    description:
      "Linux fundamentals — process, files, permissions, networking, and troubleshooting. The bread and butter of DevOps.",
    items: [
      {
        q: "How do you troubleshoot a Linux server with high CPU usage?",
        difficulty: "medium",
        a: "<p>Troubleshooting high CPU usage follows a systematic top-down approach: identify the offending process first, then progressively narrow down the root cause.</p><p><b>Step 1 — Identify the process:</b> Use <code>top</code> or <code>htop</code> to display processes sorted by CPU consumption. The interactive view shows the process ID (PID), user, command, and CPU percentage in real time. For a scriptable output, use:</p><pre>ps -eo pid,ppid,cmd,%cpu,%mem --sort=-%cpu | head</pre><p><b>Step 2 — Analyze the process behavior:</b> Once the offending PID is identified, deeper analysis depends on the process type. For multi-threaded processes, <code>pidstat -u -p &lt;PID&gt; 1</code> shows per-thread CPU usage. To observe what system calls the process is making, <code>strace -p &lt;PID&gt;</code> attaches to the process. For CPU profiling at the function level, <code>perf top -p &lt;PID&gt;</code> provides a sampling profile that highlights hot code paths.</p><p><b>Step 3 — Distinguish CPU-bound from I/O-bound workloads:</b> The <code>uptime</code> command shows the load average, but this metric alone is misleading. A high load with low CPU utilization typically indicates I/O wait rather than actual CPU saturation. Confirm using <code>vmstat 1</code> or the CPU breakdown in <code>top</code>: high <code>%wa</code> (I/O wait) points to disk or network bottlenecks, while high <code>%us</code> and <code>%sy</code> indicate genuine CPU pressure.</p><p><b>Step 4 — Correlate with application logs:</b> High CPU is often a symptom of an underlying issue — a Java garbage collection loop, a runaway cron job, an infinite loop introduced in a recent deployment, or excessive logging. Reviewing application logs alongside system metrics typically reveals the root cause.</p>",
        followups: [
          {
            q: "What's the difference between load average and CPU usage?",
            a: "<p>These two metrics measure different things and are commonly confused.</p><p><b>CPU usage percentage</b> represents the proportion of time the CPU is actively executing instructions rather than sitting idle. It is a direct measure of CPU utilization and is bounded between 0% and 100% per core.</p><p><b>Load average</b> represents the average number of processes that are either <b>running on the CPU</b> or <b>waiting in the run queue for CPU time</b>, plus processes in an <b>uninterruptible sleep state</b> (typically waiting on I/O). It is reported over three time windows: the last 1, 5, and 15 minutes.</p><p>On a 4-core system:</p><ul><li>A load average of <b>4</b> indicates full CPU utilization — one process running on each core with none queued.</li><li>A load average of <b>8</b> indicates 2× oversubscription — twice as much work as the system can process at once.</li><li>A load average of <b>2</b> indicates 50% utilization — CPU is available.</li></ul><p>The critical distinction: a system can have <b>high load average with low CPU utilization</b>. This almost always indicates I/O contention — processes are stuck waiting for disk or network operations to complete, so the CPU appears idle but the run queue is deep. Diagnosing this correctly requires looking at both metrics together, not either one in isolation.</p>",
          },
          {
            q: "How do you distinguish CPU-bound vs I/O-bound processes?",
            a: "<p>The distinction is made by analyzing the CPU time breakdown, which is visible in <code>top</code>, <code>vmstat 1</code>, or <code>mpstat -P ALL 1</code>. The breakdown categorizes CPU time by activity type:</p><ul><li><b>%us (user):</b> Time spent executing user-space code. High values indicate <b>CPU-bound</b> workloads executing application logic.</li><li><b>%sy (system):</b> Time spent executing kernel code on behalf of user processes. High values often indicate heavy system-call activity (e.g., many small file operations).</li><li><b>%wa (I/O wait):</b> Time the CPU is idle but has processes waiting for I/O to complete. High values indicate <b>I/O-bound</b> workloads — the CPU is not the bottleneck; storage or network is.</li><li><b>%si / %hi (soft/hard interrupts):</b> Time spent servicing interrupts. High values typically indicate heavy network packet processing.</li><li><b>%id (idle):</b> Time the CPU has no work to do.</li></ul><p>For I/O-bound investigations, the next diagnostic tool is <code>iostat -x 1</code>, which shows per-device metrics including queue depth (<code>avgqu-sz</code>), average wait time (<code>await</code>), and service time (<code>svctm</code>). When <code>await</code> is significantly higher than <code>svctm</code>, the storage device is saturated and requests are queuing up.</p>",
          },
          {
            q: "Show the exact command to find the top 5 memory-consuming processes.",
            a: "<p>The most reliable command uses <code>ps</code> sorted by resident set size (RSS):</p><pre>ps -eo pid,user,rss,vsz,cmd --sort=-rss | head -6</pre><p>The output includes six lines: one header and the top five processes by memory. Including both <b>RSS</b> and <b>VSZ</b> is useful because they measure different things:</p><ul><li><b>RSS (Resident Set Size)</b> — physical memory actually consumed by the process.</li><li><b>VSZ (Virtual Size)</b> — total virtual memory allocated, including memory that has been mapped but not actively used.</li></ul><p>A process with very high VSZ but low RSS is typically not a memory pressure concern — it has mapped a lot of address space but is only using a small portion. For interactive analysis, <code>top -o %MEM</code> or pressing <code>M</code> within <code>top</code> achieves the same sorted view in real time.</p>",
          },
        ],
      },
      {
        q: "Explain Linux file permissions (chmod, chown, umask).",
        difficulty: "easy",
        a: "<p>Linux file permissions control who can perform which operations on files and directories. The permission model consists of three permission classes and three permission types.</p><p><b>Permission classes:</b></p><ul><li><b>Owner (u):</b> The user who owns the file.</li><li><b>Group (g):</b> The group associated with the file.</li><li><b>Others (o):</b> All users who are neither the owner nor in the group.</li></ul><p><b>Permission types:</b></p><ul><li><b>Read (r) = 4:</b> View file contents or list directory contents.</li><li><b>Write (w) = 2:</b> Modify file contents or add/remove entries in a directory.</li><li><b>Execute (x) = 1:</b> Execute a file or traverse into a directory.</li></ul><p>Permissions are typically expressed as a three-digit octal number where each digit represents the sum of permissions for owner, group, and others respectively.</p><p><b>chmod</b> changes file permissions. For example, <code>chmod 755 script.sh</code> grants <code>rwx</code> (7 = 4+2+1) to the owner, <code>r-x</code> (5 = 4+1) to the group, and <code>r-x</code> to others — a common permission for executable scripts.</p><p><b>chown</b> changes file ownership. <code>chown user:group file</code> sets both the owner and the group; <code>chown user file</code> changes only the owner; <code>chown :group file</code> changes only the group.</p><p><b>umask</b> defines default permissions for newly created files and directories. It works as a subtractive mask against the system defaults (666 for files, 777 for directories). A common umask of <code>022</code> results in new files with permissions <code>644</code> (rw-r--r--) and new directories with <code>755</code> (rwxr-xr-x).</p><p>For more granular access control that cannot be expressed with the standard owner/group/other model, Linux supports <b>Access Control Lists (ACLs)</b> via <code>setfacl</code> and <code>getfacl</code>, which allow per-user or per-group permissions on a file.</p>",
        followups: [
          {
            q: "What is the SUID bit and when is it used?",
            a: "<p>The <b>SUID (Set User ID)</b> bit is a special permission that causes an executable to run with the privileges of its <b>owner</b> rather than the user who invoked it. When set on a file owned by root, any user who executes the file runs it with root privileges for the duration of the execution.</p><p>The canonical example is <code>/usr/bin/passwd</code>. Regular users need to change their own passwords, but password hashes are stored in <code>/etc/shadow</code>, which is owned by root and unreadable by regular users. The <code>passwd</code> binary is owned by root with the SUID bit set, allowing it to modify <code>/etc/shadow</code> on behalf of the invoking user.</p><p><b>Setting SUID:</b> The command <code>chmod u+s file</code> enables SUID. In directory listings, the SUID bit appears as an <code>s</code> in place of the owner's execute bit — e.g., <code>-rwsr-xr-x</code>.</p><p><b>Security considerations:</b> SUID binaries represent a significant attack surface. A vulnerability in an SUID-root binary can allow local users to gain root privileges. Well-managed systems periodically audit SUID binaries with:</p><pre>find / -perm -4000 -type f 2&gt;/dev/null</pre><p>Any unexpected entry in the result deserves investigation. A related bit, <b>SGID (Set Group ID)</b>, does the same thing for the group class. On directories, SGID causes new files created within to inherit the directory's group.</p>",
          },
          {
            q: "Difference between hard link and soft (symbolic) link?",
            a: "<p>Both are ways to reference a file by an additional name, but they operate at different levels of the filesystem.</p><p><b>Hard link:</b> A hard link is an additional directory entry that points to the <b>same underlying inode</b> as the original file. Since it references the inode directly, all hard links to a file are equivalent — none is more \"original\" than another. The file's data is only released when the inode's link count reaches zero (i.e., all hard links are deleted).</p><p><b>Constraints of hard links:</b></p><ul><li>Cannot span filesystems (inodes are filesystem-specific).</li><li>Cannot link directories (would create cycles that break filesystem traversal).</li></ul><p><b>Soft link (symbolic link):</b> Created with <code>ln -s target link</code>, a soft link is a small file whose contents are a <b>path</b> to the target. It is a reference by name, not by inode.</p><p><b>Characteristics of soft links:</b></p><ul><li>Can span filesystems.</li><li>Can point to directories.</li><li>Can point to nonexistent targets (dangling links).</li><li>Break if the target is moved or deleted.</li></ul><p><b>When to use each:</b> Hard links are appropriate when the second name must be indistinguishable from the first at the filesystem level — rare in typical system administration. Soft links are appropriate for most other use cases, such as versioned binary paths (e.g., <code>/usr/local/bin/python</code> pointing to a specific version) or configuration symlinks in package management.</p>",
          },
        ],
      },
      {
        q: "What is a systemd unit and how do you create one?",
        difficulty: "medium",
        a: "<p><b>systemd</b> is the init system used by most modern Linux distributions. It runs as PID 1 — the first process started by the kernel — and is responsible for starting, stopping, and supervising all other system services. It replaces the older SysV init and Upstart systems, offering parallel service startup, dependency management, and unified logging.</p><p>A <b>unit</b> is a systemd configuration file that describes a manageable resource. Unit types include:</p><ul><li><b>.service</b> — a background process (the most common).</li><li><b>.timer</b> — a scheduled trigger (systemd's cron replacement).</li><li><b>.socket</b> — a socket-activated service.</li><li><b>.mount</b> / <b>.automount</b> — filesystem mounts.</li><li><b>.target</b> — a grouping of units (analogous to SysV runlevels).</li></ul><p>To create a service unit for a custom application, place a file at <code>/etc/systemd/system/myapp.service</code>:</p><pre>[Unit]\nDescription=My App\nAfter=network.target\n\n[Service]\nType=simple\nUser=myapp\nExecStart=/usr/local/bin/myapp\nRestart=on-failure\nRestartSec=5\n\n[Install]\nWantedBy=multi-user.target</pre><p><b>Section breakdown:</b></p><ul><li><b>[Unit]:</b> Metadata and dependencies. <code>After=network.target</code> ensures the service starts only after networking is available.</li><li><b>[Service]:</b> How the service is executed. <code>Type=simple</code> is appropriate for foreground processes. <code>User=</code> runs the service as a non-root user. <code>Restart=on-failure</code> instructs systemd to automatically restart the service on abnormal exit.</li><li><b>[Install]:</b> Enablement configuration. <code>WantedBy=multi-user.target</code> means the service starts during normal boot.</li></ul><p>After creating the unit file:</p><pre>systemctl daemon-reload    # reload systemd's unit index\nsystemctl enable --now myapp    # start now and enable on boot</pre>",
        followups: [
          {
            q: "How do you check service logs with systemd?",
            a: "<p>systemd captures the standard output and standard error of every service into a centralized structured log store called the <b>journal</b>. The <code>journalctl</code> command is the standard interface to query it.</p><p><b>Common queries:</b></p><pre>journalctl -u myapp -f                    # tail live logs for a unit\njournalctl -u myapp --since \"1 hour ago\"  # time-filtered\njournalctl -u myapp --since today          # today's logs\njournalctl -p err -u myapp                # only error-level logs\njournalctl -u myapp -n 100                 # last 100 lines\njournalctl -k                              # kernel messages only</pre><p><b>Advantages over traditional log files:</b> The journal is structured (fields can be filtered), indexed (fast queries even on large logs), and unified (all services in one queryable store). Logs are automatically rotated based on disk usage and configurable retention.</p>",
          },
          {
            q: "What are the systemd `Type=` values and when to use each?",
            a: "<p>The <code>Type=</code> directive in the <code>[Service]</code> section tells systemd how to determine that a service has finished starting. Choosing the correct type is important for accurate dependency ordering.</p><ul><li><b>simple (default):</b> The main process runs in the foreground. systemd considers the service ready as soon as it is spawned. Appropriate for most modern applications that do not explicitly signal readiness.</li><li><b>forking:</b> The process forks a child and the parent exits. systemd considers the service started once the parent exits. Used for legacy daemons; <code>PIDFile=</code> is typically required.</li><li><b>oneshot:</b> The process runs, performs its work, and exits. Suitable for scripts and setup tasks. Often combined with <code>RemainAfterExit=yes</code> so systemd remembers the unit as \"active\" after the process exits.</li><li><b>notify:</b> The application explicitly signals readiness by calling <code>sd_notify(READY=1)</code>. This is the most reliable option for services that other units depend on, because systemd only considers the service ready when the application confirms it. Used by nginx, Docker, and other production-grade services.</li><li><b>idle:</b> The service starts only after all other queued jobs have completed. Rarely used outside boot ordering scenarios.</li></ul><p><b>Guideline:</b> For new applications, prefer <code>notify</code> when explicit readiness signaling is possible, or <code>simple</code> otherwise. The <code>forking</code> type should not be used for new code.</p>",
          },
        ],
      },
      {
        q: "How do you find files modified in the last 24 hours over 100MB?",
        difficulty: "easy",
        a: "<p>The <code>find</code> command supports combining multiple search criteria. To locate files modified within the last 24 hours that are larger than 100 MB:</p><pre>find /var/log -type f -mtime -1 -size +100M -exec ls -lh {} \\;</pre><p><b>Flag breakdown:</b></p><ul><li><b>/var/log</b> — the search root; adjust to any directory or use <code>/</code> for a filesystem-wide search.</li><li><b>-type f</b> — restricts results to regular files (excluding directories, symlinks, etc.).</li><li><b>-mtime -1</b> — modification time less than 1 day ago. The sign is significant: <code>-mtime 1</code> means <i>exactly</i> 1 day ago (24–48 hours), <code>-mtime -1</code> means less than 1 day, and <code>-mtime +1</code> means more than 1 day.</li><li><b>-size +100M</b> — size greater than 100 megabytes. Suffixes include <code>k</code> (KB), <code>M</code> (MB), <code>G</code> (GB).</li><li><b>-exec ls -lh {} \\;</b> — runs <code>ls -lh</code> on each matching file for human-readable sizes. The <code>{}</code> is replaced with each file path, and <code>\\;</code> terminates the command.</li></ul><p>An equivalent, simpler alternative is <code>-ls</code>, which prints similar information without invoking an external command:</p><pre>find /var/log -type f -mtime -1 -size +100M -ls</pre><p>This command is frequently used when investigating unexpected disk usage, since large recently modified files are common culprits (unrotated logs, core dumps, or misconfigured application output).</p>",
        followups: [
          {
            q: "What's the difference between -mtime, -atime, and -ctime?",
            a: "<p>All three flags filter files by timestamp, but each timestamp represents a different type of file activity:</p><ul><li><b>mtime (modification time):</b> When the <i>content</i> of the file was last changed. This is what most users mean by \"when the file was modified.\"</li><li><b>atime (access time):</b> When the file was last <i>read</i>. Many filesystems now mount with <code>relatime</code> or <code>noatime</code> to avoid the overhead of updating this on every read operation.</li><li><b>ctime (change time):</b> When the file's <i>inode</i> was last changed. This includes changes to content <i>and</i> to metadata such as permissions, ownership, or file name. Note that despite the name, <code>ctime</code> is <b>not</b> the file creation time.</li></ul><p>Historically, Linux did not track file creation time. Modern filesystems such as ext4, XFS, and Btrfs support a fourth timestamp — <b>btime (birth time)</b> — which is the actual creation time. It is not accessible through <code>find</code> but can be viewed with <code>stat</code>.</p>",
          },
        ],
      },
      {
        q: "How do you check what process is using port 8080?",
        difficulty: "easy",
        a: "<p>The modern preferred tool is <code>ss</code> (socket statistics), which is significantly faster than the older <code>netstat</code>, especially on busy systems:</p><pre>ss -ltnp | grep :8080</pre><p><b>Flag breakdown:</b></p><ul><li><b>-l</b> — show only listening sockets.</li><li><b>-t</b> — TCP sockets only (use <b>-u</b> for UDP).</li><li><b>-n</b> — do not resolve hostnames or port names (much faster).</li><li><b>-p</b> — show the process using each socket. Requires root or the <code>CAP_NET_ADMIN</code> capability.</li></ul><p>An alternative when <code>ss</code> is unavailable or additional detail is needed is <code>lsof</code>:</p><pre>lsof -iTCP:8080 -sTCP:LISTEN -n -P</pre><p><code>lsof</code> is slower but provides a comprehensive view of the process, including the user, PID, and any additional open file descriptors. Both tools require appropriate permissions to display process information.</p>",
        followups: [
          {
            q: "How to kill a process holding a port?",
            a: "<p>The most direct approach uses <code>fuser</code>, which combines process lookup and signaling in a single command:</p><pre>fuser -k 8080/tcp</pre><p>By default, <code>fuser -k</code> sends <b>SIGKILL</b>, which terminates the process immediately without allowing cleanup. To send a gentler signal first, use <code>-TERM</code>:</p><pre>fuser -k -TERM 8080/tcp</pre><p>Alternatively, using <code>lsof</code> to find the PID and then killing it explicitly:</p><pre>kill $(lsof -t -iTCP:8080 -sTCP:LISTEN)</pre><p><b>Best practice:</b> Always attempt a graceful shutdown first (<code>SIGTERM</code>, which is what <code>kill</code> sends by default) before escalating to <code>SIGKILL</code> (<code>kill -9</code>). SIGKILL cannot be caught or handled by the process, so it prevents cleanup of temporary files, network connections, and in-memory state.</p>",
          },
        ],
      },
      {
        q: "Explain the boot process of a Linux system.",
        difficulty: "hard",
        a: "<p>The Linux boot process is a series of handoffs from hardware to firmware to the kernel and finally to userspace. Understanding each stage is essential for diagnosing boot failures.</p><ol><li><b>BIOS / UEFI:</b> The system firmware runs the Power-On Self-Test (POST), initializes hardware, and loads the bootloader from a designated boot device. Modern systems use UEFI, which supports larger disks and provides Secure Boot; legacy systems use BIOS.</li><li><b>Bootloader (GRUB):</b> The bootloader — most commonly GRUB (GRand Unified Bootloader) on Linux — presents a boot menu, loads the selected kernel image and initramfs into memory, and transfers execution to the kernel entry point.</li><li><b>Kernel initialization:</b> The kernel decompresses itself, initializes essential hardware drivers, sets up memory management, and mounts the root filesystem in read-only mode. It then executes the first userspace process (PID 1).</li><li><b>initramfs execution:</b> The initramfs is a temporary in-memory root filesystem containing minimal drivers and userspace tools required to mount the real root filesystem. This handles complex root configurations such as LVM, encrypted LUKS volumes, iSCSI, or NFS. Once the real root is mounted, the kernel performs a <code>pivot_root</code> and hands off to the real init.</li><li><b>init / systemd:</b> PID 1 — systemd on modern distributions — reads its unit files and brings up services according to their dependencies. It progresses through boot targets (e.g., <code>basic.target</code> → <code>multi-user.target</code> → <code>graphical.target</code>), starting services in parallel where possible.</li><li><b>Login:</b> Once all services required by the default target are running, a login prompt is presented — either <code>getty</code> on a text console or a display manager for graphical sessions.</li></ol><p><b>Practical debugging value:</b> Knowing which stage a boot failure occurs in indicates which log or interface to consult. Firmware issues appear on the console before GRUB; GRUB errors appear at the menu; kernel issues appear in <code>dmesg</code>; systemd issues appear in the journal (<code>journalctl -b</code>); and application startup issues appear in per-service logs.</p>",
        followups: [
          {
            q: "What is initramfs and why do we need it?",
            a: "<p><b>initramfs (initial RAM filesystem)</b> is a temporary root filesystem loaded into memory by the bootloader alongside the kernel. Its purpose is to provide the environment needed to mount the actual root filesystem.</p><p><b>The bootstrapping problem it solves:</b> To run any userspace program, the kernel must have a root filesystem mounted. However, the root filesystem may reside on storage that itself requires drivers or setup to access — for example:</p><ul><li>A logical volume managed by LVM.</li><li>An encrypted LUKS volume that requires a passphrase.</li><li>A remote iSCSI target.</li><li>An NFS share.</li><li>A RAID array requiring assembly.</li></ul><p>If all the drivers and tools required for these scenarios were compiled into the kernel, kernel images would be enormous and inflexible. initramfs solves this by packaging exactly the drivers and utilities needed for a given system into a small cpio archive.</p><p><b>Boot sequence:</b> The bootloader loads both the kernel and the initramfs image into RAM. The kernel mounts initramfs as a temporary root, executes its <code>/init</code> script, which loads necessary modules and mounts the real root filesystem. The kernel then performs a <code>pivot_root</code> syscall to switch roots, and initramfs is discarded from memory.</p><p>The initramfs image is generated per system (e.g., by <code>mkinitcpio</code>, <code>dracut</code>, or <code>update-initramfs</code>), containing only the modules and tools relevant to that system's storage configuration.</p>",
          },
        ],
      },
      {
        q: "What is eBPF and why is it important for modern DevOps?",
        difficulty: "hard",
        a: "<p><b>eBPF (extended Berkeley Packet Filter)</b> is a Linux kernel technology that allows sandboxed programs to run inside the kernel without modifying kernel source code or loading kernel modules. eBPF has become one of the most transformative Linux technologies of the last decade and is the foundation for a new generation of observability, networking, and security tools.</p><p><b>How it works:</b> A user compiles a small program (typically in a restricted C dialect) that is verified by the kernel's eBPF verifier — which proves the program is safe (no infinite loops, no invalid memory access) — and then loads it into the kernel. The program is attached to a hook point such as a system call, a network packet event, a kernel function trace, or a scheduler event. When the hook fires, the eBPF program runs in the kernel context with near-native performance.</p><p><b>Why it matters:</b></p><ul><li><b>Zero-overhead observability:</b> eBPF programs can capture detailed events (function calls, network packets, disk I/O) without the overhead of traditional tracing tools like <code>strace</code>. This enables always-on production observability.</li><li><b>No kernel modules needed:</b> Historically, deep kernel visibility required custom modules — risky, hard to distribute, and fragile across kernel versions. eBPF eliminates that need.</li><li><b>Programmable networking:</b> eBPF replaces iptables and complex network configurations with programmable data planes. Cilium (a CNCF project) uses eBPF as its primary datapath for Kubernetes networking and service mesh.</li><li><b>Runtime security:</b> Tools like Falco, Tetragon, and Tracee use eBPF to monitor system calls and detect anomalous behavior — a container spawning an unexpected shell, a process reading /etc/shadow, an unusual outbound connection.</li></ul><p><b>Tools built on eBPF that every DevOps engineer should know:</b></p><ul><li><b>Cilium:</b> Kubernetes CNI plugin providing networking, load balancing, and network policies via eBPF. Now the default in many managed Kubernetes offerings.</li><li><b>Hubble:</b> Cilium's observability layer showing service-to-service flows.</li><li><b>Pixie:</b> Auto-instrumented Kubernetes observability using eBPF — no code changes required.</li><li><b>Falco:</b> Runtime security detecting suspicious behavior via kernel events.</li><li><b>bpftrace:</b> A high-level tracing language for ad-hoc eBPF programs — the modern equivalent of DTrace.</li><li><b>Parca / Pyroscope:</b> Continuous profiling using eBPF, showing CPU flame graphs for entire clusters.</li></ul><p><b>Practical example:</b> Running <code>opensnoop-bpfcc</code> (from BCC tools) traces every file open across the system in real time, showing which process opened which file with what result — invaluable for troubleshooting elusive I/O issues without recompiling anything.</p><p><b>Learning path:</b> Start by using eBPF-powered tools (Cilium, Falco, bpftrace) to solve real problems. Only later, if needed, dive into writing custom eBPF programs.</p>",
      },
      {
        q: "Explain systemd and its role in modern Linux.",
        difficulty: "medium",
        a: "<p><b>systemd</b> is the init system and service manager used by essentially all modern Linux distributions (Red Hat, CentOS, Ubuntu, Debian, SUSE, Fedora, Arch, and most container images derived from these). It replaced SysV init and Upstart during the 2010s and now handles far more than just starting services at boot.</p><p><b>What systemd manages:</b></p><ul><li><b>Services (units):</b> Long-running processes with lifecycle management (start, stop, restart, reload, enable).</li><li><b>Sockets:</b> Socket-activated services that only start when connections arrive.</li><li><b>Timers:</b> The systemd replacement for cron, with more expressive scheduling and better logging.</li><li><b>Mount units:</b> Filesystem mounts as first-class objects with dependencies.</li><li><b>Targets:</b> Groupings of units representing system states (multi-user.target, graphical.target, emergency.target). Rough equivalent of SysV runlevels.</li><li><b>Journald:</b> Structured logging that captures stdout/stderr of every service, complete with metadata, and is queryable with <code>journalctl</code>.</li><li><b>Logind:</b> User session and login management.</li><li><b>Networkd/Resolved (optional):</b> Network configuration and DNS resolution.</li></ul><p><b>Unit files:</b> The declarative configuration format for systemd. A minimal service unit:</p><pre># /etc/systemd/system/myapp.service\n[Unit]\nDescription=My Application\nAfter=network.target\nRequires=postgresql.service\n\n[Service]\nType=simple\nUser=myapp\nExecStart=/usr/local/bin/myapp\nRestart=on-failure\nRestartSec=5s\nEnvironment=CONFIG_PATH=/etc/myapp/config.yaml\n\n[Install]\nWantedBy=multi-user.target</pre><p><b>Key management commands:</b></p><pre>systemctl start myapp        # start service\nsystemctl enable myapp       # enable at boot\nsystemctl status myapp       # show status\nsystemctl daemon-reload      # reload unit files after editing\nsystemctl list-units --failed  # list failed services\nsystemctl list-timers        # list scheduled timers\njournalctl -u myapp -f       # tail service logs\njournalctl -u myapp --since \"1 hour ago\"</pre><p><b>Why systemd matters for DevOps:</b></p><ul><li>Managing services on VMs and bare metal — even in the cloud, many workloads run under systemd.</li><li>Writing custom services for internal tools, background jobs, or agents.</li><li>Interpreting logs and troubleshooting failed services with <code>journalctl</code>.</li><li>Using systemd timers as a robust cron replacement with better error handling.</li><li>Setting up sandboxed services using systemd's security hardening features (<code>PrivateTmp</code>, <code>ProtectSystem</code>, <code>NoNewPrivileges</code>, <code>CapabilityBoundingSet</code>) — an often-underused but powerful security capability.</li></ul>",
      },
      {
        q: "What are immutable Linux distributions like Bottlerocket and Talos?",
        difficulty: "hard",
        a: "<p><b>Immutable Linux distributions</b> are a growing category of operating systems designed for container hosts. They replace the traditional model of a mutable server (where administrators install packages, edit configs, and apply patches over time) with an atomic, image-based model that dramatically improves security, reliability, and manageability.</p><p><b>Core principles of immutable OS:</b></p><ul><li>The root filesystem is <b>read-only</b> at runtime. Configuration and data live in tightly scoped writable partitions.</li><li>Updates apply as <b>whole-image swaps</b> — the entire OS is replaced atomically, not upgraded package-by-package.</li><li><b>Rollback</b> is trivial: the previous image is kept and can be booted if the new one fails.</li><li>The system has <b>no traditional shell access</b> (or heavily restricted access) — administration happens through APIs.</li><li>The OS ships only what is needed to run containers — no compilers, no editors, minimal utilities. This drastically reduces attack surface.</li></ul><p><b>Common immutable distributions:</b></p><ul><li><b>Amazon Bottlerocket:</b> Open source, purpose-built for hosting containers. Widely used as the default node AMI for AWS EKS. API-driven configuration (no SSH by default — access via <code>apiclient</code> in a control container).</li><li><b>Talos Linux:</b> Purpose-built Linux distribution for running Kubernetes. Managed entirely via <code>talosctl</code> and a gRPC API. Ships with no shell — the OS is effectively an appliance.</li><li><b>Flatcar Container Linux:</b> The community successor to CoreOS Container Linux. Automated updates with A/B partition swaps.</li><li><b>Fedora CoreOS:</b> Red Hat's minimal, automatically-updating OS for containers.</li><li><b>Google Container-Optimized OS (COS):</b> Google's version for GKE.</li><li><b>openSUSE MicroOS:</b> SUSE's transactional-update-based immutable OS.</li></ul><p><b>Why they matter for modern DevOps:</b></p><ol><li><b>Security:</b> A read-only root filesystem cannot be tampered with. Minimal package set means fewer CVEs. No shell means no interactive persistence for attackers.</li><li><b>Consistency:</b> Every node runs bit-identical software. Configuration drift is impossible because there is nothing to drift.</li><li><b>Fleet-scale patching:</b> Rolling out an OS update is atomic and reversible. Bad updates roll back automatically.</li><li><b>Operational simplification:</b> No configuration management (Ansible, Puppet) is needed for node OS. All state is either in Kubernetes or in the node's declarative configuration.</li></ol><p><b>Trade-off:</b> Loss of ad-hoc SSH troubleshooting. Teams accustomed to logging in and running arbitrary commands must adapt to API-driven or debug-container-based workflows. This is intentional — the trade-off is well worth the security and reliability gains at scale.</p>",
      },
      {
        q: "How do you harden a Linux server for production?",
        difficulty: "medium",
        a: "<p>Server hardening is the practice of reducing a system's attack surface and enforcing strong security defaults. Hardening is layered — no single control is sufficient, and a mature server applies many overlapping protections.</p><p><b>1. Keep the OS patched.</b> Enable automated security updates (<code>unattended-upgrades</code> on Debian/Ubuntu, <code>dnf-automatic</code> on Fedora/RHEL). Monitor for kernel CVEs; use live patching (Canonical Livepatch, Red Hat kpatch) for zero-downtime kernel fixes.</p><p><b>2. Minimize installed software.</b> Remove packages not needed on the system (<code>apt purge</code>). Every installed package is a potential attack surface. For container hosts, consider immutable OSes (Bottlerocket, Talos) that ship only what's needed.</p><p><b>3. Enforce strong SSH configuration.</b></p><pre># /etc/ssh/sshd_config\nPermitRootLogin no\nPasswordAuthentication no\nPubkeyAuthentication yes\nMaxAuthTries 3\nClientAliveInterval 300\nClientAliveCountMax 2\nAllowUsers deploy admin\nX11Forwarding no</pre><ul><li>Disable password authentication entirely — SSH keys or certificates only.</li><li>Do not allow root login directly.</li><li>Use <code>fail2ban</code> or <code>sshguard</code> to rate-limit failed attempts.</li><li>Consider MFA via <code>libpam-google-authenticator</code> or SSH certificates from a bastion.</li></ul><p><b>4. Configure the firewall.</b> Use <code>ufw</code>, <code>firewalld</code>, or direct <code>nftables</code> to deny by default and open only required ports. Restrict administrative ports (SSH) to specific management networks or via VPN/bastion.</p><p><b>5. Enable Mandatory Access Control.</b></p><ul><li><b>SELinux</b> (RHEL family): Set to <code>enforcing</code> mode. Provides fine-grained policy control per process.</li><li><b>AppArmor</b> (Debian/Ubuntu family): Simpler profile-based confinement.</li><li>Both restrict what a compromised process can do even if it runs as root.</li></ul><p><b>6. Least-privilege user setup.</b> Non-root users for services (<code>User=</code> in systemd units). No shared passwords. Sudoers configured with per-command permissions where possible, not blanket <code>ALL=(ALL) ALL</code>.</p><p><b>7. Kernel hardening.</b> Enable common protections via <code>/etc/sysctl.d/</code>:</p><pre>kernel.dmesg_restrict = 1\nkernel.kptr_restrict = 2\nnet.ipv4.conf.all.rp_filter = 1\nnet.ipv4.tcp_syncookies = 1\nnet.ipv6.conf.all.accept_ra = 0</pre><p><b>8. Filesystem protections.</b> Mount <code>/tmp</code>, <code>/var/tmp</code>, and <code>/home</code> with <code>noexec,nosuid,nodev</code> where appropriate. Encrypt disks with LUKS. Set correct permissions on sensitive files (<code>chmod 600 /etc/shadow</code>).</p><p><b>9. Audit and logging.</b> Enable <code>auditd</code> for kernel-level audit trails of sensitive operations. Ship logs to a central SIEM (Elastic, Splunk, Datadog). Set up file integrity monitoring with AIDE or Tripwire.</p><p><b>10. Vulnerability scanning.</b> Run periodic scans with OpenSCAP, Lynis, or commercial tools (Nessus, Qualys) to detect misconfigurations against benchmarks (CIS, DISA STIG).</p><p><b>11. Runtime security.</b> For container hosts, run Falco or Tetragon to detect suspicious runtime behavior — unexpected shells, privilege escalations, unusual outbound connections.</p><p><b>Automation:</b> All of the above should be codified in Ansible/Puppet/Chef/OpenSCAP tailored roles so hardening is reproducible across every server, not applied ad-hoc.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "git",
    name: "Git & Version Control",
    icon: "🌿",
    kind: "qa",
    description:
      "Git workflows, branching strategies, and the everyday commands that make or break your PR hygiene.",
    items: [
      {
        q: "Difference between `git merge` and `git rebase`?",
        difficulty: "medium",
        a: "<p>Both <code>git merge</code> and <code>git rebase</code> integrate changes from one branch into another, but they produce fundamentally different commit histories.</p><p><b>git merge</b> creates a new commit — a <i>merge commit</i> — that has two parents: the tip of each integrated branch. This preserves the complete history of both branches, including the fact that development happened in parallel. The result is visible as a graph structure in <code>git log --graph</code>, showing that a feature branch existed and was integrated at a specific point.</p><p><b>git rebase</b> replays the commits of the source branch onto the tip of the target branch, one at a time. The result is a linear history that looks as if all the work was done sequentially on the latest state of the target branch. However, because each replayed commit has a new parent, its SHA changes — Git rewrites history rather than preserving it.</p><p><b>Comparison:</b></p><ul><li><b>Merge:</b> Preserves history and context, non-destructive, but produces a non-linear graph.</li><li><b>Rebase:</b> Produces clean linear history, ideal for code review, but rewrites commit SHAs — problematic for shared branches.</li></ul><p><b>Recommended workflow:</b></p><ol><li>Rebase feature branches onto <code>main</code> before opening a pull request to produce a clean, reviewable history.</li><li>Merge (or squash-merge) the pull request into <code>main</code> according to team convention.</li><li>Never rebase a branch that other developers are working on — it forces them to reconcile history divergence and can lead to lost commits.</li></ol>",
        followups: [
          {
            q: "What is interactive rebase and when is it useful?",
            a: "<p><b>Interactive rebase</b> — invoked with <code>git rebase -i &lt;base&gt;</code> — opens an editor listing each commit in the range to be replayed, with a keyword next to each. Modifying the keyword controls what happens to that commit:</p><ul><li><b>pick</b> — apply the commit as-is (default).</li><li><b>reword</b> — apply the commit but edit its message.</li><li><b>edit</b> — pause after applying, allowing further edits before continuing.</li><li><b>squash</b> — combine the commit with the previous one, merging their messages.</li><li><b>fixup</b> — like squash, but discard the commit's message.</li><li><b>drop</b> — remove the commit entirely.</li></ul><p>Interactive rebase is commonly used to clean up a feature branch before opening a pull request. Work-in-progress commits like \"fix typo\" or \"wip\" can be squashed into their logical parent commits, producing a series of clean, self-contained changes that are easier for reviewers to evaluate.</p><p>Because interactive rebase rewrites history, it should only be used on local branches that have not yet been shared with others.</p>",
          },
          {
            q: "How do you recover a commit you accidentally lost via reset --hard?",
            a: "<p>The <code>git reflog</code> command records every movement of HEAD in the local repository — every commit, checkout, reset, rebase, and merge. Even when a <code>git reset --hard</code> appears to have destroyed work, the commits still exist in the object database; they are simply no longer reachable from any branch reference.</p><p><b>Recovery procedure:</b></p><ol><li>Run <code>git reflog</code> to display the history of HEAD movements. Each entry shows a SHA, a reference (e.g., <code>HEAD@{2}</code>), and the action that produced it.</li><li>Identify the SHA of the state to recover — typically the entry just before the <code>reset --hard</code> action.</li><li>Restore the state by creating a new branch or checking out the SHA:</li></ol><pre>git branch recovered &lt;sha&gt;\n# or\ngit reset --hard &lt;sha&gt;</pre><p>Reflog entries are retained for approximately 90 days by default (configurable via <code>gc.reflogExpire</code>), providing a substantial window for recovery. This mechanism makes destructive local operations far less risky than they appear.</p>",
          },
        ],
      },
      {
        q: "Explain Gitflow vs Trunk-Based Development.",
        difficulty: "medium",
        a: "<p><b>Gitflow</b> and <b>Trunk-Based Development (TBD)</b> are two fundamentally different branching strategies, each suited to different development contexts.</p><p><b>Gitflow</b> uses multiple long-lived branches with defined roles:</p><ul><li><code>main</code> (or <code>master</code>) — always reflects production-released code.</li><li><code>develop</code> — the integration branch for active development.</li><li><code>feature/*</code> — short-lived branches for individual features, merged into <code>develop</code>.</li><li><code>release/*</code> — stabilization branches created from <code>develop</code> when preparing a release.</li><li><code>hotfix/*</code> — emergency fix branches created from <code>main</code>.</li></ul><p>Gitflow is well-suited to products with scheduled releases and multiple supported versions in the field — traditional shipped software, desktop applications, or embedded systems. However, it introduces significant ceremony: branches live for weeks, merges become painful, and continuous delivery is difficult.</p><p><b>Trunk-Based Development</b> uses a single long-lived branch (<code>main</code>) as the source of truth. Developers either commit directly to <code>main</code> or use very short-lived feature branches lasting hours to at most a day. There is no <code>develop</code> branch. Releases are cut directly from <code>main</code> or via release tags.</p><p>TBD requires two supporting practices to work safely:</p><ul><li><b>Strong CI:</b> Every commit must be validated by comprehensive automated tests before merging.</li><li><b>Feature flags:</b> Incomplete features are guarded by runtime toggles rather than isolated on long-lived branches.</li></ul><p><b>Industry direction:</b> DORA research consistently identifies TBD as characteristic of elite-performing software delivery organizations. It enables true continuous delivery and eliminates the coordination overhead of long-lived branches. Gitflow remains appropriate primarily for versioned software that requires multiple maintained releases.</p>",
        followups: [
          {
            q: "What is a feature flag and why is it central to trunk-based?",
            a: "<p>A <b>feature flag</b> (also called a feature toggle) is a runtime configuration that gates the execution of a code path. It is typically implemented as a conditional check backed by a configuration store:</p><pre>if (flag.enabled(\"new-checkout\", user)) {\n  renderNewCheckout();\n} else {\n  renderLegacyCheckout();\n}</pre><p><b>Role in trunk-based development:</b> Feature flags allow incomplete or experimental code to be merged into <code>main</code> and deployed to production without exposing the functionality to users. This decouples two events that have historically been tied together:</p><ul><li><b>Deploy:</b> Code is running in production.</li><li><b>Release:</b> The functionality is visible to users.</li></ul><p>This separation enables continuous integration of small changes without requiring long-lived branches to isolate in-progress work.</p><p><b>Advanced patterns enabled by flags:</b></p><ul><li><b>Gradual rollouts</b> — release to 5%, then 25%, then 100% of users while monitoring metrics.</li><li><b>Targeted rollouts</b> — release only to specific user segments (beta users, specific regions).</li><li><b>Kill switches</b> — instantly disable a problematic feature without a deployment.</li><li><b>A/B experiments</b> — run competing implementations simultaneously.</li></ul><p><b>Common tooling:</b> LaunchDarkly, Unleash, Flagsmith, or Split.io for managed platforms; database-backed toggles for simpler cases.</p><p><b>Operational consideration:</b> Flag hygiene is critical. Stale flags accumulate as technical debt, turning code into a maze of conditional branches. Mature teams treat flag removal as an ongoing engineering task and enforce time-to-live policies.</p>",
          },
        ],
      },
      {
        q: "What is `git cherry-pick` and when do you use it?",
        difficulty: "easy",
        a: "<p><code>git cherry-pick &lt;sha&gt;</code> applies the changes introduced by a specific commit onto the current branch, creating a new commit with the same content but a different SHA. Unlike merge or rebase, cherry-pick copies a single commit rather than integrating an entire branch.</p><p><b>Common use cases:</b></p><ul><li><b>Back-porting hotfixes:</b> A critical bug is fixed on <code>main</code>, but a released version on <code>release/1.2</code> also needs the fix. The fix commit can be cherry-picked from <code>main</code> onto the release branch, producing a targeted patch release without pulling in unrelated changes.</li><li><b>Recovering isolated commits:</b> When a commit from an abandoned or discarded branch contains useful work, it can be cherry-picked onto an active branch.</li><li><b>Selective integration:</b> Applying specific commits from a fork or contributor branch without merging the entire branch.</li></ul><p><b>Considerations:</b> Cherry-picking creates a duplicate commit — the original and the cherry-picked version have the same changes but different SHAs. Excessive cherry-picking between long-lived branches can complicate history tracking and merge attempts. When the same commit exists in both branches, subsequent merges may produce spurious conflicts.</p>",
        followups: [
          {
            q: "How do you cherry-pick a range of commits?",
            a: "<p>Two range syntaxes are available depending on whether the starting commit is included:</p><pre>git cherry-pick A..B      # excludes A, includes B (and all between)\ngit cherry-pick A^..B     # includes A and B (and all between)</pre><p>The <code>^</code> suffix references the parent of commit A, effectively shifting the range boundary to include A itself. This distinction is a common source of confusion because Git's range syntax is exclusive on the left endpoint by default.</p><p>For non-contiguous commits, pass multiple SHAs:</p><pre>git cherry-pick &lt;sha1&gt; &lt;sha2&gt; &lt;sha3&gt;</pre><p>If conflicts occur during a range cherry-pick, Git pauses on the conflicting commit. After resolving, use <code>git cherry-pick --continue</code> to proceed, or <code>--abort</code> to cancel the entire operation.</p>",
          },
        ],
      },
      {
        q: "How do `git reset --soft`, `--mixed`, and `--hard` differ?",
        difficulty: "medium",
        a: "<p>All three variants of <code>git reset</code> move the <b>HEAD</b> pointer and the current branch reference to a specified target commit. They differ in what they do to two additional structures: the <b>index (staging area)</b> and the <b>working tree (files on disk)</b>.</p><ul><li><b>--soft:</b> Moves HEAD only. The index and working tree are unchanged. Any differences between the old and new HEAD appear as <b>staged changes</b>, ready to be committed. Useful for squashing several commits into one by resetting to their common base and re-committing.</li><li><b>--mixed (default):</b> Moves HEAD and resets the index, but leaves the working tree unchanged. Differences appear as <b>unstaged changes</b>. Useful for un-staging changes without losing them.</li><li><b>--hard:</b> Moves HEAD, resets the index, and resets the working tree. <b>All uncommitted changes are permanently lost.</b> This is the destructive variant.</li></ul><p><b>Summary table:</b></p><pre>Variant      HEAD moved   Index reset   Working tree reset\n--soft       Yes          No            No\n--mixed      Yes          Yes           No\n--hard       Yes          Yes           Yes</pre><p><b>Recovery from --hard:</b> If <code>git reset --hard</code> is used accidentally, the abandoned commits can typically be recovered via <code>git reflog</code>, as long as reflog retention (~90 days by default) has not expired.</p>",
        followups: [
          {
            q: "Difference between `git reset` and `git revert`?",
            a: "<p>The two commands both undo changes, but through fundamentally different mechanisms.</p><p><b>git reset</b> rewrites history by moving the branch pointer backwards. Commits after the reset point become unreachable from the branch reference. This is safe on local, unpushed branches but destructive on shared branches — collaborators who have already pulled the removed commits will experience history divergence.</p><p><b>git revert</b> creates a <b>new commit</b> whose contents are the inverse of the target commit. History is fully preserved: both the original commit and the revert commit remain in the log, in order. The state of the codebase changes, but no existing commits are modified or removed.</p><p><b>Rule of thumb:</b></p><ul><li>On local, unpushed branches: either is safe; <code>reset</code> is often more convenient.</li><li>On shared branches (especially <code>main</code>): <b>always use <code>revert</code></b>. Rewriting shared history breaks other developers' clones and can cause data loss.</li></ul>",
          },
        ],
      },
      {
        q: "What is a `.gitignore` and how does it work for already-tracked files?",
        difficulty: "easy",
        a: "<p><code>.gitignore</code> is a text file containing glob patterns that Git uses to identify files it should <b>not</b> track. Matching files are excluded from <code>git status</code> output and cannot be added to the index via <code>git add</code>.</p><p><b>Critical limitation:</b> <code>.gitignore</code> only affects <b>untracked</b> files. Once a file has been committed to the repository, Git continues to track it regardless of what patterns are added to <code>.gitignore</code>. This is a frequent source of confusion — for example, a developer accidentally commits a <code>.env</code> file, later adds it to <code>.gitignore</code>, and observes that Git still shows changes to it.</p><p><b>Removing a tracked file from Git while keeping it locally:</b></p><pre>git rm --cached path/to/file\necho \"path/to/file\" &gt;&gt; .gitignore\ngit commit -m \"stop tracking file\"</pre><p>The <code>--cached</code> flag removes the file from the Git index (staging area) but leaves it on disk. After the commit, subsequent modifications to the file are ignored.</p><p><b>Security consideration:</b> If the removed file contained secrets, credentials, or sensitive data, removing it from HEAD is insufficient. The file remains in the repository's history and can be recovered by anyone with clone access. Complete removal requires rewriting history with tools like <code>git filter-repo</code> or BFG Repo-Cleaner. More importantly, any exposed secret must be <b>rotated immediately</b>, since it must be assumed to be compromised.</p><p><b>Pattern precedence:</b> Patterns can be scoped globally (<code>~/.gitignore_global</code>), per-repository (<code>.gitignore</code> at any level), or per-user (<code>.git/info/exclude</code>). Local <code>.git/info/exclude</code> is useful for personal preferences that should not be committed to the shared repository.</p>",
      },
      {
        q: "What is `git bisect` and how do you use it to find a bug?",
        difficulty: "medium",
        a: "<p><b><code>git bisect</code></b> is a binary search tool for finding the exact commit that introduced a bug. When you know that code worked at some earlier commit but is broken at HEAD, bisect systematically narrows down the offending commit in O(log n) steps by testing a middle commit and asking whether it is good or bad, then repeating on the remaining half.</p><p><b>Basic workflow:</b></p><pre>git bisect start\ngit bisect bad HEAD           # current commit is broken\ngit bisect good v1.5.0        # earlier commit that was working\n\n# Git now checks out a commit halfway between good and bad.\n# Run your reproducing test:\nnpm test                       # or any command that identifies the bug\n\n# Tell Git the result:\ngit bisect good                # if this commit is fine\n# OR\ngit bisect bad                 # if this commit exhibits the bug\n\n# Git checks out the next candidate. Repeat until:\n# \"aaaabbbb1234... is the first bad commit\"\n\ngit bisect reset               # return to your original branch</pre><p><b>Automated bisect:</b> If a test script can determine good vs bad automatically, use <code>git bisect run</code> to let Git run the test on each candidate:</p><pre>git bisect start HEAD v1.5.0\ngit bisect run ./test-repro.sh</pre><p>The script must exit 0 for good, non-zero for bad, and 125 to indicate the commit cannot be tested (skip it). This is astonishingly powerful — with a reliable reproduction script, you can find a bug in a repository with 100,000 commits in about 17 automated steps.</p><p><b>When bisect shines:</b></p><ul><li>A performance regression appeared \"sometime this month\".</li><li>A test that passes in v1.0 fails in v2.0 with hundreds of commits between them.</li><li>A subtle bug is intermittent and you have no idea where it was introduced.</li></ul><p><b>Practical tips:</b></p><ul><li>Make sure your reproduction test is reliable before starting. Bisect is only as good as your test's reliability.</li><li>Use <code>--skip</code> for commits that cannot be tested (build broken for unrelated reasons).</li><li>If a bug was introduced by a merge commit, bisect will point to the merge; use <code>git log -m -p &lt;merge&gt;</code> to see the actual changes.</li><li>For large repositories, checking out commits can be slow. <code>git bisect</code> respects the current worktree — combine with worktrees to isolate the bisect from your ongoing work.</li></ul>",
      },
      {
        q: "What are Git worktrees and when are they useful?",
        difficulty: "medium",
        a: "<p><b>Git worktrees</b> allow you to have multiple working directories checked out from a single Git repository simultaneously — each on a different branch or commit. Unlike cloning the repo multiple times, worktrees share the underlying <code>.git</code> object database, so they consume much less disk space and enable seamless work across branches.</p><p><b>Basic usage:</b></p><pre>git worktree add ../myproject-hotfix hotfix/urgent-bug\n# Creates a new directory ../myproject-hotfix with the hotfix branch\n# checked out. You can cd into it and work independently.\n\ngit worktree list\n# /home/user/myproject           abc1234 [main]\n# /home/user/myproject-hotfix    def5678 [hotfix/urgent-bug]\n\ngit worktree remove ../myproject-hotfix\n# Cleans up when done</pre><p><b>Common use cases:</b></p><ul><li><b>Emergency hotfix without stashing:</b> You are mid-feature on a branch with uncommitted changes. A production hotfix is needed. Rather than stashing your work, create a worktree for the hotfix branch, fix and merge it, then return to your original worktree with your work intact.</li><li><b>Running tests on multiple branches in parallel:</b> A CI-like local workflow where you build/test one branch while working on another.</li><li><b>Comparing behavior across versions:</b> Run the same script or benchmark on two different commits side-by-side.</li><li><b>Working with git bisect:</b> Isolate the bisect operation to its own worktree so your main worktree remains on your feature branch.</li><li><b>Documentation vs code branches:</b> Keep a persistent worktree for a docs branch while your main worktree tracks main.</li></ul><p><b>Constraints:</b></p><ul><li>You cannot check out the same branch in two worktrees simultaneously (Git prevents this to avoid divergent histories on the same branch).</li><li>Worktrees are lightweight but not free — each has its own working files. For a very large repo, disk usage can still be significant.</li><li>Deleting a worktree's directory manually (without <code>git worktree remove</code>) leaves stale references; use <code>git worktree prune</code> to clean them.</li></ul><p>Worktrees have been a Git feature since 2.5 (2015) but remain underused. Once you get used to them, they change how you work on complex repositories.</p>",
      },
      {
        q: "How do you sign Git commits and why does it matter?",
        difficulty: "medium",
        a: "<p><b>Signed commits</b> cryptographically prove that a specific author created a specific commit. Without signing, anyone can set <code>user.email</code> to any value and impersonate any contributor — Git does not verify identity by default. In security-conscious environments (open source projects, regulated industries, supply-chain-sensitive codebases), signing commits is essential.</p><p><b>Three signing methods:</b></p><ol><li><b>GPG:</b> The original and most established method. Uses a GPG key associated with your email.</li><li><b>SSH keys:</b> Newer method (Git 2.34+, 2021). Uses your existing SSH key, so no separate GPG setup is required. This is now the most convenient option.</li><li><b>X.509 (S/MIME):</b> Certificate-based, common in corporate PKI environments.</li></ol><p><b>Setting up SSH commit signing (recommended for most users):</b></p><pre># Configure Git to use SSH signing\ngit config --global gpg.format ssh\ngit config --global user.signingkey ~/.ssh/id_ed25519.pub\ngit config --global commit.gpgsign true\ngit config --global tag.gpgsign true\n\n# Create an allowed_signers file for local verification\necho \"you@example.com $(cat ~/.ssh/id_ed25519.pub)\" \\\n  &gt; ~/.config/git/allowed_signers\ngit config --global gpg.ssh.allowedSignersFile \\\n  ~/.config/git/allowed_signers</pre><p><b>Upload your public signing key to your Git host:</b></p><ul><li>GitHub: Settings → SSH and GPG keys → New SSH key → choose \"Signing Key\" type.</li><li>GitLab and Bitbucket have equivalent settings.</li></ul><p>Once configured, every commit is automatically signed. GitHub displays a green \"Verified\" badge on signed commits — an easily-visible signal that the commit's authorship is cryptographically confirmed.</p><p><b>Why signing matters:</b></p><ul><li><b>Supply chain security:</b> Attackers who compromise a Git host or CI system could theoretically inject commits. Signed commits from verified developers cannot be forged without also compromising the signing key.</li><li><b>Compliance:</b> Many regulated environments require signed commits as part of software supply chain governance (SLSA, in-toto).</li><li><b>Trust in open source:</b> Contributors' identities can be cryptographically confirmed, mitigating impersonation attacks.</li><li><b>DCO (Developer Certificate of Origin):</b> Some projects require signed-off-by lines (<code>git commit -s</code>) attesting the contributor's right to submit the code. This is a legal signal, not cryptographic, but often paired with signing.</li></ul><p><b>Enforcement:</b> GitHub and GitLab allow branch protection rules that require signed commits. When enabled, unsigned pushes are rejected — providing organizational-level enforcement.</p>",
      },
      {
        q: "What are Git hooks and how are they used in modern workflows?",
        difficulty: "medium",
        a: "<p><b>Git hooks</b> are scripts that Git executes automatically at specific points in its workflow — before a commit is created, after a merge completes, before a push happens, and so on. They are a powerful mechanism for automating checks, enforcing conventions, and integrating Git with external systems.</p><p><b>Hook locations:</b> Git looks for hooks in the <code>.git/hooks/</code> directory of each repository. Sample hooks are pre-populated but disabled (named <code>*.sample</code>). To activate a hook, rename it and ensure it is executable.</p><p><b>Common client-side hooks:</b></p><ul><li><b>pre-commit:</b> Runs before the commit is created. Perfect for linting, formatting, static analysis, and rejecting commits that fail checks. If the hook exits non-zero, the commit is aborted.</li><li><b>commit-msg:</b> Validates the commit message — e.g., enforcing Conventional Commits format or requiring an issue ID.</li><li><b>pre-push:</b> Runs before <code>git push</code>. Useful for running tests before pushing to avoid broken commits landing in the remote.</li><li><b>post-merge / post-checkout:</b> Run after merges or checkouts. Commonly used to trigger dependency updates (<code>npm install</code>) or database migrations.</li></ul><p><b>Server-side hooks (on the Git server):</b></p><ul><li><b>pre-receive:</b> Runs on the server before accepting a push. Used to enforce policies like signed commits, message format, or branch permissions.</li><li><b>update:</b> Runs once per branch being updated. Enables fine-grained rules per branch.</li><li><b>post-receive:</b> Runs after a push completes. Common for triggering downstream automation (CI, deployments, notifications).</li></ul><p><b>The challenge with plain <code>.git/hooks/</code>:</b> They are not versioned — every developer must install them manually, and there is no way to ensure everyone has the same hooks. Modern tooling solves this by managing hooks in the repository itself.</p><p><b>Popular hook managers:</b></p><ul><li><b>pre-commit</b> (the tool, <code>pre-commit.com</code>): Python-based framework with a <code>.pre-commit-config.yaml</code> in the repo. Language-agnostic; supports hooks for Python (Black, Ruff), JavaScript (ESLint, Prettier), Terraform (tflint, terraform fmt), Docker (hadolint), YAML (yamllint), secrets scanning (gitleaks), and hundreds more.<pre># .pre-commit-config.yaml\nrepos:\n  - repo: https://github.com/pre-commit/pre-commit-hooks\n    rev: v4.6.0\n    hooks:\n      - id: trailing-whitespace\n      - id: end-of-file-fixer\n      - id: check-yaml\n  - repo: https://github.com/gitleaks/gitleaks\n    rev: v8.18.0\n    hooks:\n      - id: gitleaks</pre><pre>pre-commit install    # installs hooks into .git/hooks/\npre-commit run --all-files</pre></li><li><b>Husky (Node.js ecosystem):</b> Popular in JavaScript/TypeScript projects. Configured in <code>package.json</code> and installed automatically via <code>npm install</code>.</li><li><b>lefthook (Go-based):</b> Modern, fast alternative that works across languages.</li></ul><p><b>Typical enforcement chain:</b></p><ol><li>Hooks in the developer's local repo run on <code>git commit</code> (client-side).</li><li>Server-side hooks enforce policies for anything reaching the central repo.</li><li>CI pipelines run the same checks in a controlled environment, catching anything that bypassed local hooks.</li></ol><p>Together, these three layers create a robust quality/security gate that catches most issues before they reach production.</p>",
      },
      {
        q: "Explain trunk-based development vs GitFlow — when to use each?",
        difficulty: "medium",
        a: "<p><b>Trunk-Based Development (TBD)</b> and <b>GitFlow</b> are the two dominant branching models, representing fundamentally different philosophies about how software should be delivered. The industry's mainstream has been shifting from GitFlow toward TBD, particularly for cloud-native and continuously-deployed applications.</p><p><b>GitFlow (Vincent Driessen, 2010):</b> A structured branching model with multiple long-lived branches serving specific roles.</p><ul><li><code>main</code>: Production releases only. Every commit is a tagged release.</li><li><code>develop</code>: Integration branch where all feature work merges.</li><li><code>feature/*</code>: Branched from <code>develop</code>, merged back when complete.</li><li><code>release/*</code>: Branched from <code>develop</code> for a release, receives release-hardening commits, then merges to both <code>main</code> and <code>develop</code>.</li><li><code>hotfix/*</code>: Branched from <code>main</code> for urgent production fixes, merged to both <code>main</code> and <code>develop</code>.</li></ul><p><b>Trunk-Based Development:</b> Everyone commits to a single main branch (\"trunk\"), continuously, in very small increments (ideally daily or more often). Feature branches, if used, live for hours to a day maximum.</p><ul><li><code>main</code> (trunk): The single source of truth. Always deployable.</li><li>Optional very-short-lived feature branches for review purposes.</li><li>Release branches only for supported long-tail versions (rare in cloud-native contexts).</li><li>Feature flags gate incomplete work in production rather than long-lived branches gating it in Git.</li></ul><p><b>Comparison:</b></p><table><tr><th>Aspect</th><th>GitFlow</th><th>Trunk-Based Development</th></tr><tr><td>Branch complexity</td><td>High — 5+ branch types with rules</td><td>Very low — usually just main</td></tr><tr><td>Merge conflicts</td><td>Frequent, painful (long-lived branches diverge)</td><td>Rare (short-lived changes)</td></tr><tr><td>Release cadence</td><td>Fits scheduled releases (weekly, monthly)</td><td>Fits continuous deployment (multiple daily)</td></tr><tr><td>Feature flags</td><td>Not required</td><td>Essential — for hiding incomplete work</td></tr><tr><td>CI/CD requirements</td><td>Moderate</td><td>Strong — extensive automated testing</td></tr><tr><td>Best for</td><td>Versioned products, mobile apps, on-prem software</td><td>SaaS, web apps, cloud-native services</td></tr></table><p><b>Why trunk-based has won for most cloud-native work:</b> Research from Google (DORA reports) consistently shows that teams using TBD deploy more frequently, have lower lead time for changes, lower change failure rate, and shorter time to restore service — the four key DORA metrics. Long-lived branches accumulate divergence and merge conflicts; short-lived changes integrate cheaply.</p><p><b>When GitFlow still makes sense:</b></p><ul><li>Software with strict release schedules and versioned support (e.g., open source libraries with multiple supported major versions).</li><li>Products shipped to customers rather than deployed to a SaaS environment (mobile apps in stores, on-prem software).</li><li>Regulated environments requiring formal release gates and audit trails per version.</li></ul><p><b>When TBD is essential:</b></p><ul><li>SaaS and web applications deploying multiple times a day.</li><li>Cloud-native microservices with independent deployment cadences.</li><li>Any team practicing continuous deployment.</li></ul><p><b>Practical modern approach:</b> A GitHub Flow variant is dominant — main is always deployable, short-lived feature branches for PR review, merged via squash-merge to keep main history clean. This is essentially TBD with a small nod to PR-based code review.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "githubactions",
    name: "GitHub Actions",
    icon: "🐙",
    kind: "qa",
    description:
      "GitHub's native CI/CD platform — workflows, actions, runners, secrets, matrix builds, reusable workflows, and OIDC-based cloud authentication.",
    items: [
      {
        q: "What is GitHub Actions and how does it work?",
        difficulty: "easy",
        a: "<p><b>GitHub Actions</b> is GitHub's native, integrated CI/CD platform. It enables workflows — automated pipelines for building, testing, and deploying code — to be defined as YAML files inside the same repository as the code they operate on. Workflows are triggered by events happening in the repository (a push, a pull request, a scheduled cron, or manual invocation).</p><p><b>Core building blocks:</b></p><ul><li><b>Workflow:</b> The top-level automation file, stored in <code>.github/workflows/*.yml</code>. Each workflow defines when it runs (events) and what it does (jobs).</li><li><b>Event:</b> The trigger that starts a workflow — <code>push</code>, <code>pull_request</code>, <code>schedule</code>, <code>workflow_dispatch</code> (manual), <code>release</code>, or one of dozens of other event types.</li><li><b>Job:</b> A collection of steps that execute together on the same runner. Multiple jobs in a workflow run in parallel by default, but can be sequenced using <code>needs</code>.</li><li><b>Step:</b> A single unit of work within a job — either a shell command (<code>run:</code>) or a reusable action (<code>uses:</code>).</li><li><b>Action:</b> A pre-packaged unit of functionality — for example, <code>actions/checkout@v4</code> checks out repository code, <code>actions/setup-node@v4</code> installs Node.js. Actions can be authored by anyone and published to the GitHub Marketplace.</li><li><b>Runner:</b> The compute environment executing the job. GitHub provides hosted runners (Linux, Windows, macOS), or you can register self-hosted runners.</li></ul><p><b>Minimal workflow example:</b></p><pre>name: CI\n\non:\n  push:\n    branches: [main]\n  pull_request:\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n      - run: npm ci\n      - run: npm test</pre><p><b>Comparison with alternatives:</b> GitHub Actions is often chosen for its tight GitHub integration, marketplace of pre-built actions, generous free tier for public repositories, and a simpler mental model than Jenkins. Azure Pipelines offers similar functionality with more enterprise-oriented features. Jenkins remains preferred for self-hosted environments with extensive customization needs.</p>",
      },
      {
        q: "Explain the different types of GitHub Actions triggers/events.",
        difficulty: "medium",
        a: "<p>GitHub Actions workflows respond to a wide variety of triggers, each suited to different automation scenarios. The <code>on:</code> key in a workflow file declares the events that will start it.</p><p><b>Common event types:</b></p><ul><li><b>push:</b> Triggered when commits are pushed to specified branches or tags. Can be filtered by branches, tags, or paths.<pre>on:\n  push:\n    branches: [main, 'releases/**']\n    paths: ['src/**', '!**.md']</pre></li><li><b>pull_request:</b> Triggered on pull request activity (opened, synchronized, closed, etc.). The workflow runs against the merge commit of the base and head branches.</li><li><b>pull_request_target:</b> Similar to <code>pull_request</code> but runs in the context of the base repository. Used carefully because it has access to secrets — a common security consideration for forked PRs.</li><li><b>schedule:</b> Cron-based scheduled execution using standard cron syntax. Useful for periodic maintenance, dependency updates, or scheduled deployments.<pre>on:\n  schedule:\n    - cron: '0 3 * * *'   # daily at 3 AM UTC</pre></li><li><b>workflow_dispatch:</b> Manual trigger from the GitHub UI or API. Can accept typed inputs.<pre>on:\n  workflow_dispatch:\n    inputs:\n      environment:\n        type: choice\n        options: [dev, stage, prod]</pre></li><li><b>workflow_call:</b> Enables the workflow to be called by another workflow — the foundation for reusable workflows.</li><li><b>workflow_run:</b> Triggered when another workflow completes, enabling chains of workflows.</li><li><b>repository_dispatch:</b> Triggered by external API calls. Used for external integrations or cross-repo automation.</li><li><b>release:</b> Triggered by release lifecycle events (published, edited, deleted).</li><li><b>issues / issue_comment / pull_request_review:</b> Triggered by activity in issues, comments, and reviews. Useful for automation bots (auto-labeling, first-time-contributor greetings).</li></ul><p><b>Filters:</b> Most events support filters — branches, tags, paths, activity types — to precisely control when workflows run. Careful use of filters is important for efficient CI usage and avoiding unnecessary runs.</p>",
      },
      {
        q: "Difference between GitHub-hosted runners and self-hosted runners?",
        difficulty: "medium",
        a: "<p>GitHub Actions supports two types of runners for executing jobs: GitHub-hosted and self-hosted. Each has different trade-offs suited to different scenarios.</p><p><b>GitHub-hosted runners:</b> Fully managed virtual machines provided by GitHub, spun up on demand for each job and discarded after execution.</p><ul><li><b>Available images:</b> Ubuntu (latest, 22.04, 20.04), Windows (latest, 2022, 2019), macOS (latest, 14, 13).</li><li><b>Pre-installed software:</b> Common developer tools — Docker, Node, Python, Go, Java, various databases, cloud CLIs (aws, az, gcloud).</li><li><b>Ephemeral:</b> Each job gets a fresh VM. No state persists between jobs.</li><li><b>Free tier for public repos; per-minute billing for private repos.</b></li><li><b>Constraints:</b> Limited compute (2-core, 7 GB RAM by default; larger runners available at higher cost), no access to internal networks by default, job timeout of 6 hours.</li></ul><p><b>Self-hosted runners:</b> Compute you provide and register with GitHub. GitHub dispatches jobs to your runners.</p><ul><li><b>Deployed anywhere:</b> On-premises servers, cloud VMs, Kubernetes clusters, or Raspberry Pis.</li><li><b>Full control:</b> Choose the OS, hardware, network access, and pre-installed software.</li><li><b>Access to internal resources:</b> Runners inside your network can reach internal APIs, databases, and services.</li><li><b>Cost:</b> No GitHub charge per minute — you pay only for the underlying infrastructure. Cost-effective for high volume.</li><li><b>Responsibility:</b> You manage OS patches, runner software updates, and cleanup between jobs. For security, ephemeral runners (fresh VM per job) are strongly recommended.</li></ul><p><b>When to use self-hosted:</b></p><ul><li>Jobs requiring internal network access (private artifact repositories, on-prem databases).</li><li>Jobs needing specialized hardware (GPUs, ARM, large memory).</li><li>High-volume workloads where GitHub-hosted minutes are cost-prohibitive.</li><li>Compliance requirements that prevent public cloud execution.</li></ul><p><b>When to use GitHub-hosted:</b> Any workload that doesn't specifically require self-hosted. Simpler, no maintenance overhead, and secure by default.</p><p><b>Runner scale sets on Kubernetes:</b> A modern pattern is running self-hosted runners as ephemeral pods on a Kubernetes cluster via the Actions Runner Controller (ARC). This combines the flexibility of self-hosted with cloud elasticity and per-job isolation.</p>",
        followups: [
          {
            q: "What is Actions Runner Controller (ARC)?",
            a: "<p><b>Actions Runner Controller (ARC)</b> is a Kubernetes operator that manages self-hosted GitHub Actions runners as ephemeral pods. It gives you the isolation and freshness of GitHub-hosted runners with the flexibility and cost of self-hosted infrastructure.</p><p><b>How it works:</b></p><ol><li>You install ARC in a Kubernetes cluster.</li><li>You define a <b>RunnerScaleSet</b> resource pointing to a specific GitHub repository or organization.</li><li>When a workflow job requires a matching runner, GitHub Actions dispatches it; ARC creates a pod for that job.</li><li>The pod runs the job, then terminates. A fresh pod is created for the next job.</li></ol><p><b>Benefits over static self-hosted runners:</b></p><ul><li><b>Per-job isolation:</b> No state leakage between builds. Every job starts fresh.</li><li><b>Automatic scaling:</b> Runners scale up on demand and back to zero when idle.</li><li><b>Kubernetes-native:</b> Uses standard Kubernetes primitives (Pods, RBAC, networking), integrates with the platform team's existing tooling.</li><li><b>Cost-effective at scale:</b> Pay only for cluster capacity consumed.</li></ul><p>ARC is the recommended pattern for organizations running GitHub Actions at meaningful scale on internal infrastructure.</p>",
          },
        ],
      },
      {
        q: "What is a matrix build in GitHub Actions?",
        difficulty: "easy",
        a: "<p>A <b>matrix build</b> is a GitHub Actions feature that runs the same job multiple times with different combinations of variables. It is used to test code against many versions, operating systems, or configurations in parallel — a common requirement for libraries and cross-platform applications.</p><p><b>Basic example:</b></p><pre>jobs:\n  test:\n    runs-on: ${{ matrix.os }}\n    strategy:\n      matrix:\n        os: [ubuntu-latest, macos-latest, windows-latest]\n        node: [18, 20, 22]\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ matrix.node }}\n      - run: npm ci\n      - run: npm test</pre><p>This produces <b>9 parallel jobs</b> — 3 operating systems × 3 Node.js versions — each testing the code independently.</p><p><b>Advanced matrix features:</b></p><ul><li><b>include:</b> Add specific extra combinations beyond the cross-product.</li><li><b>exclude:</b> Remove specific combinations that should not run.</li><li><b>fail-fast:</b> Whether to cancel remaining matrix jobs when one fails. Default is <code>true</code>; set to <code>false</code> to always see all results.</li><li><b>max-parallel:</b> Limit how many matrix jobs can run simultaneously, useful for rate-limited resources.</li></ul><pre>strategy:\n  fail-fast: false\n  matrix:\n    os: [ubuntu-latest, macos-latest]\n    node: [18, 20]\n    include:\n      - os: ubuntu-latest\n        node: 22    # extra combination beyond cross-product\n    exclude:\n      - os: macos-latest\n        node: 18    # skip this combination</pre><p><b>Common use cases:</b> Testing across OS/language-version combinations, deploying to multiple environments in parallel, running the same build across different architecture targets (amd64, arm64).</p>",
      },
      {
        q: "How do you manage secrets in GitHub Actions?",
        difficulty: "medium",
        a: "<p>Secrets in GitHub Actions are encrypted values that can be referenced in workflows without exposing them in the repository. They are the standard mechanism for storing sensitive data such as API tokens, deployment credentials, and third-party service keys.</p><p><b>Three scopes of secrets:</b></p><ul><li><b>Repository secrets:</b> Available to workflows in a single repository. Configured in Settings → Secrets and variables → Actions.</li><li><b>Environment secrets:</b> Scoped to a named environment (e.g., <code>production</code>). Only workflows targeting that environment can access them. Enables environment-specific credentials and additional protection like required reviewers.</li><li><b>Organization secrets:</b> Available across many repositories in an organization. Access can be limited to specific repositories.</li></ul><p><b>Usage in workflows:</b></p><pre>jobs:\n  deploy:\n    runs-on: ubuntu-latest\n    environment: production\n    steps:\n      - name: Deploy\n        env:\n          API_TOKEN: ${{ secrets.PROD_API_TOKEN }}\n        run: |\n          curl -H \"Authorization: Bearer $API_TOKEN\" https://api.example.com/deploy</pre><p>Secrets are automatically masked in workflow logs — if a secret value appears in log output, it is replaced with <code>***</code>.</p><p><b>Better alternative for cloud authentication — OIDC:</b> For authenticating to AWS, Azure, or GCP, GitHub Actions can use <b>OpenID Connect (OIDC)</b> federation instead of storing long-lived credentials as secrets. The workflow obtains a short-lived token from the cloud provider on demand.</p><p><b>Benefits of OIDC over stored credentials:</b></p><ul><li>No long-lived credentials to rotate or leak.</li><li>Fine-grained trust — the cloud provider verifies the exact workflow, branch, and environment.</li><li>Automatic scoping to specific repositories and workflows.</li><li>Full audit trail via the cloud provider's logging.</li></ul><p><b>Additional best practices:</b></p><ul><li>Use the least-privilege principle — each secret should have the minimum scope necessary.</li><li>Enable required reviewers on production environments so secrets are only accessible after approval.</li><li>Use <code>pull_request_target</code> with extreme caution — forks cannot access secrets in <code>pull_request</code> events, but <code>pull_request_target</code> exposes them.</li><li>For dynamic secrets (rotated credentials), integrate with HashiCorp Vault, AWS Secrets Manager, or Azure Key Vault via marketplace actions.</li></ul>",
        followups: [
          {
            q: "What is OIDC federation and why is it preferred over stored cloud credentials?",
            a: "<p><b>OpenID Connect (OIDC) federation</b> allows GitHub Actions workflows to authenticate to external services — most commonly AWS, Azure, or GCP — using short-lived tokens issued by GitHub itself, rather than long-lived credentials stored as secrets.</p><p><b>How it works:</b></p><ol><li>You configure a trust relationship in the cloud provider — for example, an IAM role in AWS with a trust policy that trusts GitHub's OIDC provider (<code>token.actions.githubusercontent.com</code>).</li><li>The trust policy includes conditions that scope the trust to specific repositories, branches, or environments.</li><li>At workflow runtime, the job requests a signed JWT from GitHub's OIDC provider using the <code>id-token: write</code> permission.</li><li>The workflow presents this JWT to the cloud provider, which verifies its signature and the claims (repository, branch, environment) against the trust policy.</li><li>The cloud provider returns short-lived credentials scoped to the requested role.</li></ol><p><b>Example — authenticating to AWS via OIDC:</b></p><pre>permissions:\n  id-token: write\n  contents: read\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: aws-actions/configure-aws-credentials@v4\n        with:\n          role-to-assume: arn:aws:iam::123456789012:role/gha-deploy\n          aws-region: us-east-1\n      - run: aws s3 sync ./build s3://my-bucket/</pre><p><b>Why it is preferred:</b></p><ul><li>No credentials stored in GitHub — nothing to leak or rotate.</li><li>Very fine-grained trust — the cloud provider can restrict access to specific workflows and branches.</li><li>Automatic expiration — tokens live only for the duration of the job.</li><li>Full audit trail in the cloud provider showing which workflow assumed which role.</li></ul><p>OIDC is now the recommended pattern for all cloud authentication from GitHub Actions.</p>",
          },
        ],
      },
      {
        q: "What are reusable workflows and how do they differ from composite actions?",
        difficulty: "medium",
        a: "<p>GitHub Actions provides two mechanisms for reusing automation logic across many workflows: <b>reusable workflows</b> and <b>composite actions</b>. They serve overlapping but distinct purposes.</p><p><b>Reusable workflows:</b> Full workflow files that other workflows can call as if they were a job. Defined with the <code>workflow_call</code> trigger.</p><pre># .github/workflows/build.yml (reusable)\non:\n  workflow_call:\n    inputs:\n      node-version:\n        type: string\n        default: '20'\n    secrets:\n      NPM_TOKEN:\n        required: true\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ inputs.node-version }}\n      - run: npm ci\n        env:\n          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}</pre><pre># .github/workflows/ci.yml (caller)\njobs:\n  call-build:\n    uses: ./.github/workflows/build.yml\n    secrets:\n      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}\n    with:\n      node-version: '22'</pre><p><b>Composite actions:</b> Reusable sequences of <i>steps</i> — smaller units than a full job — packaged as a directory with an <code>action.yml</code> file. Consumed via <code>uses:</code>.</p><pre># .github/actions/deploy/action.yml (composite)\nname: 'Deploy to K8s'\ninputs:\n  image:\n    required: true\nruns:\n  using: composite\n  steps:\n    - shell: bash\n      run: kubectl set image deployment/app app=${{ inputs.image }}</pre><pre>steps:\n  - uses: ./.github/actions/deploy\n    with:\n      image: myapp:${{ github.sha }}</pre><p><b>When to use each:</b></p><ul><li><b>Composite actions:</b> When encapsulating a small sequence of steps (a few shell commands, a specific setup task). They plug into existing jobs. Cannot contain <code>if</code> conditions at the top level or matrices.</li><li><b>Reusable workflows:</b> When encapsulating a complete job or multi-job pipeline. Support matrix strategies, conditional execution, secrets, and environments. Consumed as jobs rather than steps.</li></ul><p><b>Benefit for enterprises:</b> Both mechanisms enable central DevOps teams to publish standardized building blocks — canonical build, test, and deploy pipelines — that product teams consume consistently. Centralized updates propagate to all consumers automatically.</p>",
      },
      {
        q: "How do you cache dependencies in GitHub Actions?",
        difficulty: "easy",
        a: "<p>Dependency installation is one of the slowest parts of most CI pipelines. GitHub Actions provides caching to dramatically reduce build times by reusing previously downloaded dependencies across workflow runs.</p><p><b>Two approaches:</b></p><p><b>1. Built-in caching via setup actions:</b> Most language-specific setup actions have built-in caching support. This is the recommended approach for common languages.</p><pre>- uses: actions/setup-node@v4\n  with:\n    node-version: '20'\n    cache: 'npm'    # automatically caches based on package-lock.json</pre><pre>- uses: actions/setup-python@v5\n  with:\n    python-version: '3.12'\n    cache: 'pip'</pre><pre>- uses: actions/setup-java@v4\n  with:\n    distribution: 'temurin'\n    java-version: '21'\n    cache: 'maven'</pre><p>These actions automatically compute a cache key from the relevant lockfile (<code>package-lock.json</code>, <code>requirements.txt</code>, <code>pom.xml</code>), restore matching caches, and save updated caches after installation.</p><p><b>2. Explicit caching with actions/cache:</b> For more control or non-standard caches.</p><pre>- uses: actions/cache@v4\n  id: cache\n  with:\n    path: |\n      ~/.cache/pip\n      ~/.venv\n    key: pip-${{ runner.os }}-${{ hashFiles('requirements.txt') }}\n    restore-keys: |\n      pip-${{ runner.os }}-\n\n- if: steps.cache.outputs.cache-hit != 'true'\n  run: pip install -r requirements.txt</pre><p><b>Cache key strategy:</b></p><ul><li>Include a stable prefix (identifying the cache purpose).</li><li>Include the runner OS to prevent cross-platform mismatches.</li><li>Include a hash of the dependency manifest — the cache is only valid if the manifest is unchanged.</li><li>Use <code>restore-keys</code> for fallback keys that will partially restore a cache (e.g., an older version of dependencies) if the exact key does not match.</li></ul><p><b>Cache limits:</b> GitHub imposes a 10 GB limit per repository for total cache storage. Older caches are evicted automatically when the limit is reached, so a well-designed cache strategy focuses on the most impactful, most frequently used caches (typically dependency directories).</p>",
      },
      {
        q: "How do you deploy to Kubernetes from GitHub Actions?",
        difficulty: "medium",
        a: "<p>Deploying to Kubernetes from GitHub Actions is a common pattern for modern cloud-native pipelines. The workflow typically builds a container image, pushes it to a registry, and updates Kubernetes manifests to reference the new image.</p><p><b>Example workflow — build, push, deploy to AKS via OIDC:</b></p><pre>name: Deploy to AKS\n\non:\n  push:\n    branches: [main]\n\npermissions:\n  id-token: write\n  contents: read\n\nenv:\n  IMAGE: myacr.azurecr.io/myapp\n\njobs:\n  build-and-deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Azure login (OIDC)\n        uses: azure/login@v2\n        with:\n          client-id: ${{ secrets.AZURE_CLIENT_ID }}\n          tenant-id: ${{ secrets.AZURE_TENANT_ID }}\n          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}\n\n      - name: Build and push image\n        run: |\n          az acr login -n myacr\n          docker build -t $IMAGE:${{ github.sha }} .\n          docker push $IMAGE:${{ github.sha }}\n\n      - name: Get AKS credentials\n        uses: azure/aks-set-context@v3\n        with:\n          resource-group: prod-rg\n          cluster-name: prod-aks\n\n      - name: Deploy\n        run: |\n          kubectl set image deployment/myapp \\\n            myapp=$IMAGE:${{ github.sha }}\n          kubectl rollout status deployment/myapp --timeout=5m</pre><p><b>Recommended patterns:</b></p><ul><li><b>OIDC for cloud authentication:</b> Avoid long-lived credentials by using OIDC federation to AWS/Azure/GCP.</li><li><b>Immutable image tags:</b> Tag images with the commit SHA rather than <code>latest</code>, ensuring deployments are reproducible and rollbacks are straightforward.</li><li><b>Deployment strategies:</b> For zero-downtime deployments, use <code>kubectl rollout</code> with proper readiness probes, or Argo Rollouts / Flagger for blue-green and canary patterns.</li><li><b>GitOps as an alternative:</b> Instead of the pipeline directly running <code>kubectl</code>, update Kubernetes manifests in a separate GitOps repository. ArgoCD or Flux syncs the changes to the cluster. This removes cluster access from the CI system.</li><li><b>Multi-environment workflow:</b> Use <code>environment:</code> on jobs to gate production deployments with required reviewers.</li></ul>",
      },
      {
        q: "How do you implement approvals for production deployments in GitHub Actions?",
        difficulty: "medium",
        a: "<p>GitHub Actions supports deployment approvals through <b>environments</b>, a feature that provides gating, secret scoping, and audit trails for jobs that target specific environments.</p><p><b>Setting up environment-based approvals:</b></p><ol><li>In the repository, navigate to Settings → Environments and create an environment (e.g., <code>production</code>).</li><li>Configure protection rules on the environment:<ul><li><b>Required reviewers:</b> Specify up to 6 users or teams whose approval is required before jobs targeting this environment can run.</li><li><b>Wait timer:</b> Add a mandatory delay (0–43,200 minutes) before jobs can start.</li><li><b>Deployment branches:</b> Restrict which branches can deploy to this environment.</li></ul></li><li>Optionally, add environment-scoped secrets and variables that are only accessible to workflows targeting this environment.</li></ol><p><b>Using the environment in a workflow:</b></p><pre>jobs:\n  deploy-prod:\n    runs-on: ubuntu-latest\n    environment: production   # <-- triggers approval gate\n    needs: deploy-stage\n    steps:\n      - uses: actions/checkout@v4\n      - name: Deploy to production\n        run: ./deploy.sh</pre><p><b>Approval flow:</b></p><ol><li>The workflow runs, and the <code>deploy-prod</code> job pauses when reached.</li><li>GitHub sends a notification to the required reviewers.</li><li>Reviewers see the pending deployment in the workflow run UI and can approve or reject.</li><li>Upon approval, the job resumes execution.</li><li>The approval, reviewer identity, and any provided comment are captured in the workflow's audit log.</li></ol><p><b>Additional patterns for robust production deployments:</b></p><ul><li><b>Staged progression:</b> Chain jobs so that <code>deploy-prod</code> depends on <code>deploy-stage</code> via <code>needs</code>, ensuring staging deployment succeeds before production is even attempted.</li><li><b>Deployment history:</b> Environments provide a full history of deployments per environment, useful for tracking releases and rollbacks.</li><li><b>Multiple approver policies:</b> For sensitive environments, require multiple approvers or approval from specific teams.</li><li><b>Automated rollback:</b> Include a post-deployment health check step, and if it fails, automatically trigger a rollback deployment.</li></ul>",
      },
      {
        q: "What is the difference between GitHub Actions and GitLab CI?",
        difficulty: "medium",
        a: "<p><b>GitHub Actions</b> and <b>GitLab CI</b> are the native CI/CD platforms of GitHub and GitLab respectively. Both are configuration-as-code YAML-based platforms with substantial feature overlap, but each has design differences worth understanding.</p><p><b>Similarities:</b></p><ul><li>YAML pipeline configuration stored in the repository.</li><li>Managed runners (SaaS) or self-hosted runners.</li><li>Secret management, environment-scoped credentials.</li><li>Matrix builds, parallel jobs, and dependencies.</li><li>Deployment approvals and environment gates.</li></ul><p><b>Key differences:</b></p><ul><li><b>File location:</b> GitHub uses multiple files in <code>.github/workflows/</code>. GitLab traditionally uses a single <code>.gitlab-ci.yml</code>, though multi-file support is available via <code>include:</code>.</li><li><b>Job model:</b> In GitLab, jobs are grouped into named <b>stages</b> (build, test, deploy) that execute sequentially, with jobs within a stage running in parallel. In GitHub Actions, jobs are organized within a workflow and ordered via <code>needs:</code>. GitLab's stage model is often considered simpler for straightforward pipelines.</li><li><b>Reusable components:</b> GitHub uses composite actions and reusable workflows. GitLab uses <code>include:</code>, templates, and job extensions (<code>extends:</code>).</li><li><b>Marketplace:</b> GitHub has a large marketplace of third-party actions. GitLab has a template ecosystem and CI/CD Catalog, but the third-party ecosystem is smaller.</li><li><b>Integrated features:</b> GitLab bundles many DevSecOps features (security scanning, container scanning, license compliance) into the platform itself. GitHub relies more on integrated third-party actions and its own security features (CodeQL, Dependabot).</li><li><b>Self-hosted:</b> Both support self-hosted runners. GitLab additionally offers a fully self-hosted platform option (GitLab Community/Enterprise Edition) that includes CI/CD, whereas GitHub's self-hosted option (GitHub Enterprise Server) is significantly more expensive.</li></ul><p><b>When to choose which:</b> The choice usually follows where your code lives. Both platforms are capable enough that CI/CD differences rarely drive the decision — organizational adoption of GitHub vs GitLab as the source platform is the primary driver.</p>",
      },
      {
        q: "How do you troubleshoot a failing GitHub Actions workflow?",
        difficulty: "medium",
        a: "<p>Debugging failed workflows involves inspecting logs, understanding the failure signal, and reproducing the environment locally when needed. A systematic approach:</p><p><b>1. Read the workflow run summary and job logs.</b> Navigate to the Actions tab of the repository. The run summary shows which job failed. Expanding a job displays its log, with the failing step highlighted. Each step's output is preserved and searchable.</p><p><b>2. Enable step debug and runner diagnostic logs.</b> When default logs are not sufficient, enable verbose logging by setting two repository secrets:</p><ul><li><code>ACTIONS_STEP_DEBUG</code> = <code>true</code> — shows debug output for each step, including which files are being loaded and expression evaluations.</li><li><code>ACTIONS_RUNNER_DEBUG</code> = <code>true</code> — shows runner-level diagnostics including infrastructure setup.</li></ul><p>These flags produce substantially more log output; enable them only when investigating.</p><p><b>3. Re-run failed jobs.</b> GitHub offers two re-run options:</p><ul><li><b>Re-run all jobs</b> — starts the entire workflow from scratch.</li><li><b>Re-run failed jobs</b> — reruns only the jobs that failed, reusing successful upstream job outputs.</li></ul><p><b>4. Add temporary debugging steps.</b> Insert <code>run:</code> steps that print environment variables, list files, or check tool versions before the failing step:</p><pre>- name: Debug environment\n  run: |\n    env | sort\n    node --version\n    ls -la\n    pwd</pre><p><b>5. SSH into the runner with tmate.</b> The <code>mxschmitt/action-tmate</code> action opens an interactive SSH session into the runner, allowing live investigation. Use only for private repos and remove after debugging.</p><pre>- name: Debug via SSH\n  if: failure()\n  uses: mxschmitt/action-tmate@v3</pre><p><b>6. Reproduce locally with act.</b> The <code>act</code> CLI tool (github.com/nektos/act) runs GitHub Actions workflows locally in Docker containers, enabling quick iteration without pushing commits.</p><p><b>7. Check for common failure patterns:</b></p><ul><li>Missing or mistyped secrets.</li><li>Permission issues — the workflow's <code>permissions</code> block may need adjustment.</li><li>Action version pinning — an updated action may have introduced a breaking change.</li><li>Runner image changes — GitHub updates hosted runner images periodically; pre-installed software versions may shift.</li><li>Cache corruption — occasionally, restoring a broken cache causes downstream failures. Clear the cache from the Actions cache UI.</li></ul>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "docker",
    name: "Docker",
    icon: "🐳",
    kind: "qa",
    description:
      "Container fundamentals — images, layers, networking, storage, and the day-to-day of running containerized workloads.",
    items: [
      {
        q: "What is the difference between a Docker image and a container?",
        difficulty: "easy",
        a: "<p>An <b>image</b> and a <b>container</b> represent two different concepts in Docker: a template and a running instance of that template.</p><p><b>Docker image:</b> An image is an immutable, read-only template that contains everything needed to run an application: application code, runtime, libraries, environment variables, and configuration. Internally, an image is a stack of filesystem layers plus a metadata manifest that specifies the default command, exposed ports, working directory, and other runtime parameters. Images are stored in registries (Docker Hub, ECR, GCR, private registries) and identified by name and tag (e.g., <code>nginx:1.25</code>).</p><p><b>Docker container:</b> A container is a running (or stopped) instance of an image. When a container is started, Docker adds a thin writable layer on top of the image's read-only layers and creates isolated namespaces for the container's processes, network interfaces, and filesystem view. Multiple containers can run from the same image, each with its own writable layer and namespaces.</p><p><b>Analogy:</b> The relationship is analogous to object-oriented programming: an image is a class, and a container is an instance of that class. The image defines what should happen; the container is a live execution of it.</p><p><b>Key implications:</b> Because images are immutable, they can be versioned, distributed, and reproduced reliably. Because containers add a writable layer, changes to a container's filesystem do not persist after it is removed unless explicitly captured (e.g., via volumes or by committing the container to a new image).</p>",
        followups: [
          {
            q: "How do Docker image layers work?",
            a: "<p>Docker images are constructed as a stack of read-only <b>layers</b>. Each layer represents the filesystem changes introduced by a single instruction in the Dockerfile (such as <code>RUN</code>, <code>COPY</code>, or <code>ADD</code>). When multiple instructions modify the same file, each layer records only the diff from the previous state.</p><p><b>Layer caching:</b> During builds, Docker caches each layer based on the instruction and its inputs. If an instruction and its inputs are unchanged from a previous build, Docker reuses the cached layer instead of re-executing the instruction. This dramatically speeds up incremental builds — a code change that only affects the final <code>COPY</code> step does not invalidate the layers for installing dependencies.</p><p><b>Optimization principle:</b> Order Dockerfile instructions from least likely to change to most likely to change. Base image selection and dependency installation should occur early; source code copying should occur late. This maximizes cache hits and minimizes rebuild time.</p><pre># Effective ordering\nFROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./       # dependency manifest first\nRUN npm ci --production      # dependency install (cached unless manifest changes)\nCOPY . .                     # source code last (changes most often)\nCMD [\"node\", \"server.js\"]</pre><p>Layers are also deduplicated across images — if two images share a base layer, that layer is stored only once on disk, saving significant space.</p>",
          },
          {
            q: "What is a multi-stage build and why use it?",
            a: "<p>A <b>multi-stage build</b> is a Dockerfile pattern that uses multiple <code>FROM</code> statements, each starting a new build stage. Artifacts can be copied from earlier stages into later ones using <code>COPY --from=&lt;stage&gt;</code>. The final image contains only the contents of the final stage.</p><p><b>Primary use case:</b> Separating build-time dependencies from runtime dependencies. Compilers, build tools, and intermediate artifacts are required to produce the application but should not be present in the final image, as they inflate size and increase the attack surface.</p><pre>FROM golang:1.22 AS build\nWORKDIR /src\nCOPY . .\nRUN go build -o /app ./cmd/api\n\nFROM gcr.io/distroless/base-debian12\nCOPY --from=build /app /app\nENTRYPOINT [\"/app\"]</pre><p><b>Benefits:</b></p><ul><li><b>Smaller images:</b> The runtime image contains only the compiled binary and minimal OS libraries, often reducing size from 800 MB to under 30 MB.</li><li><b>Reduced attack surface:</b> No compilers, package managers, or build tools are available in the running container.</li><li><b>Faster deployments:</b> Smaller images pull and start faster, particularly relevant at scale.</li><li><b>Single Dockerfile:</b> The complete build process is documented in one file, simplifying maintenance.</li></ul><p>Multi-stage builds are considered a best practice for all production images.</p>",
          },
        ],
      },
      {
        q: "How do you keep Docker images small and secure?",
        difficulty: "medium",
        a: "<p>Reducing image size and improving security are closely related goals: smaller images have fewer components, fewer components mean fewer vulnerabilities, and less attack surface. Several established practices work together to achieve both.</p><p><b>1. Choose a minimal base image.</b> Use <code>alpine</code>, <code>distroless</code>, or <code>scratch</code> where possible instead of full-featured distributions. Alpine adds ~5 MB; distroless images contain no shell or package manager at all.</p><p><b>2. Use multi-stage builds.</b> Separate build tooling from runtime. The final image should contain only what is required to execute the application.</p><p><b>3. Minimize the number of layers.</b> Combine related <code>RUN</code> commands into a single instruction and clean up temporary files within the same layer. For example, on Debian-based images:</p><pre>RUN apt-get update &amp;&amp; \\\n    apt-get install -y --no-install-recommends curl &amp;&amp; \\\n    rm -rf /var/lib/apt/lists/*</pre><p><b>4. Use .dockerignore.</b> Exclude files that should not be sent to the Docker build context (<code>node_modules</code>, <code>.git</code>, local logs, environment files). This speeds up builds and prevents accidental inclusion of secrets or bloat.</p><p><b>5. Pin base image versions.</b> Use specific tags such as <code>FROM node:20.11.1-alpine</code> rather than <code>FROM node:latest</code>. The <code>latest</code> tag is a moving target and produces non-reproducible builds. For maximum reproducibility, pin by digest: <code>FROM node@sha256:...</code>.</p><p><b>6. Run as a non-root user.</b> Create a dedicated user in the Dockerfile and switch to it with <code>USER</code>. Running as root is unnecessary for most workloads and increases the impact of a container escape.</p><p><b>7. Scan images in CI.</b> Integrate vulnerability scanners such as <b>Trivy</b>, <b>Grype</b>, or <b>Snyk</b> into the pipeline. Fail builds on high-severity CVEs to prevent known-vulnerable images from reaching production.</p><p><b>8. Sign images.</b> Use <b>cosign</b> or Docker Content Trust to sign images and verify signatures at deployment time, preventing tampered images from being deployed.</p>",
        followups: [
          {
            q: "Difference between COPY and ADD?",
            a: "<p>Both <code>COPY</code> and <code>ADD</code> transfer files from the build context into the image, but <code>ADD</code> has additional behavior that can be surprising.</p><p><b>COPY</b> simply copies files or directories from the build context to the specified destination in the image. It has no side effects and is the most predictable option.</p><p><b>ADD</b> supports the same operation but adds two features:</p><ol><li><b>Automatic archive extraction:</b> If the source is a local tar archive (recognized by extension), ADD extracts it into the destination rather than copying it as a file.</li><li><b>Remote URL support:</b> ADD can fetch files from URLs directly into the image (though this is discouraged in favor of explicit <code>RUN curl</code> or <code>wget</code> for better cache control).</li></ol><p><b>Recommendation:</b> Use <code>COPY</code> by default. Its behavior is explicit and predictable. Use <code>ADD</code> only when its archive-extraction behavior is specifically desired — and even then, the intent is clearer when using an explicit extraction step.</p>",
          },
          {
            q: "Why avoid running containers as root?",
            a: "<p>Containers provide isolation through Linux namespaces, but the isolation is not absolute. If a container process runs as root and a vulnerability allows escape from the container — through a kernel bug, misconfigured volume mount, or exposed Docker socket — that root process obtains root access on the host system.</p><p><b>Defense in depth:</b> Running as a non-root user does not prevent all escapes, but it significantly reduces the impact of many. A compromised non-root container process is limited to the privileges of that user, both inside the container and in the event of an escape.</p><p><b>Configuring a non-root user in a Dockerfile:</b></p><pre>FROM node:20-alpine\nRUN addgroup -S app &amp;&amp; adduser -S app -G app\nWORKDIR /home/app\nCOPY --chown=app:app . .\nUSER app\nCMD [\"node\", \"server.js\"]</pre><p><b>Additional hardening measures</b> in production include: dropping all Linux capabilities and adding back only what is required, mounting the root filesystem as read-only, using seccomp profiles to restrict system calls, and configuring AppArmor or SELinux policies.</p>",
          },
        ],
      },
      {
        q: "Difference between CMD and ENTRYPOINT?",
        difficulty: "medium",
        a: "<p>Both <code>CMD</code> and <code>ENTRYPOINT</code> specify what runs when a container starts, but they serve different purposes and interact with each other.</p><p><b>ENTRYPOINT</b> defines the executable that <i>always</i> runs when the container starts. It is intended to be the fixed command of the container — the thing the container fundamentally does.</p><p><b>CMD</b> provides <i>default arguments</i> to the ENTRYPOINT, or a default command if no ENTRYPOINT is defined. Arguments passed to <code>docker run</code> override CMD but do not affect ENTRYPOINT.</p><p><b>Example:</b></p><pre>ENTRYPOINT [\"nginx\"]\nCMD [\"-g\", \"daemon off;\"]</pre><p><b>Behavior:</b></p><ul><li><code>docker run image</code> → runs <code>nginx -g \"daemon off;\"</code> (ENTRYPOINT + CMD)</li><li><code>docker run image -v</code> → runs <code>nginx -v</code> (ENTRYPOINT + overridden CMD)</li><li><code>docker run --entrypoint sh image</code> → runs <code>sh -g \"daemon off;\"</code> (overridden ENTRYPOINT + CMD)</li></ul><p><b>Design guidance:</b> Use <code>ENTRYPOINT</code> for the primary purpose of the container (e.g., the application binary). Use <code>CMD</code> for default arguments that users may reasonably want to customize. This produces containers that behave like executables, where <code>docker run image [args]</code> works intuitively.</p><p>For containers intended as flexible environments rather than dedicated applications (e.g., a development shell), CMD alone is often more appropriate, with no ENTRYPOINT.</p>",
        followups: [
          {
            q: "What is the difference between exec and shell form?",
            a: "<p>Docker instructions like <code>CMD</code>, <code>ENTRYPOINT</code>, and <code>RUN</code> support two syntactic forms.</p><p><b>Exec form:</b> A JSON array — <code>CMD [\"executable\", \"arg1\", \"arg2\"]</code>. Docker executes the command directly, without invoking a shell. The process becomes PID 1 inside the container and receives signals directly, so <code>SIGTERM</code> from <code>docker stop</code> reaches the application immediately.</p><p><b>Shell form:</b> A plain string — <code>CMD executable arg1 arg2</code>. Docker executes the command via <code>/bin/sh -c \"executable arg1 arg2\"</code>. The shell becomes PID 1 and receives signals, which the shell does not forward to the child process. This causes <code>docker stop</code> to time out and eventually SIGKILL the container, preventing graceful shutdown.</p><p><b>Comparison:</b></p><ul><li><b>Exec form:</b> Direct execution, correct signal propagation, no shell features (variable expansion, wildcards, pipes).</li><li><b>Shell form:</b> Shell features available, but signals do not reach the application — poor for graceful shutdown.</li></ul><p><b>Recommendation:</b> Use exec form for production images to ensure clean shutdown behavior. If shell features are genuinely needed, invoke the shell explicitly in exec form:</p><pre>CMD [\"sh\", \"-c\", \"echo $HOME &amp;&amp; exec myapp\"]</pre><p>The <code>exec</code> builtin replaces the shell process with the application, preserving correct signal handling.</p>",
          },
        ],
      },
      {
        q: "How do you persist data in Docker?",
        difficulty: "easy",
        a: "<p>By default, any data written inside a container is stored in its writable layer and is lost when the container is removed. To persist data beyond a container's lifecycle, Docker provides three mechanisms, each with different trade-offs.</p><ol><li><b>Named volumes (recommended for most use cases):</b> Volumes are storage objects managed entirely by Docker, stored under <code>/var/lib/docker/volumes/</code> on Linux.<pre>docker volume create data\ndocker run -v data:/var/lib/mysql mysql:8</pre>Volumes are portable, decoupled from the host filesystem structure, and easier to back up. They are the preferred option for production workloads such as databases.</li><li><b>Bind mounts:</b> A directory on the host is mounted directly into the container.<pre>docker run -v /host/path:/container/path myapp</pre>Bind mounts couple the container to a specific host path. They are commonly used during development (for example, mounting source code for hot-reload) but are less portable in production.</li><li><b>tmpfs mounts:</b> An in-memory filesystem for the container. Data is not persisted at all — it exists only for the lifetime of the container and is stored in host RAM, not on disk.<pre>docker run --tmpfs /tmp myapp</pre>tmpfs is useful for storing temporary files, sensitive data that should not touch disk, or improving performance for scratch space.</li></ol><p><b>Storage drivers</b> such as <code>overlay2</code> (default on Linux) manage how layers and the writable layer are structured on disk. Volume plugins allow integration with external storage systems like NFS, Ceph, or cloud block storage.</p>",
        followups: [
          {
            q: "Named volume vs bind mount — when to use each?",
            a: "<p><b>Named volumes</b> are the preferred choice in production because Docker fully manages their lifecycle. Their contents are opaque to the host filesystem, avoiding accidental interference. They are portable between hosts, straightforward to back up, and integrate cleanly with volume plugins for network or cloud storage. Databases, message queues, and any stateful production service should use named volumes.</p><p><b>Bind mounts</b> are appropriate primarily in development contexts. Mounting a local source code directory into a container enables hot-reload workflows: changes on the host are immediately visible inside the container. Bind mounts are also useful for injecting configuration files or exposing host-specific data. However, they create tight coupling between the container and the host's directory structure, and host permission and SELinux/AppArmor labels can create difficult-to-debug issues.</p><p><b>Summary:</b> Named volumes for production data; bind mounts for development source code and host-specific inputs.</p>",
          },
        ],
      },
      {
        q: "What are multi-arch Docker images and how do you build them?",
        difficulty: "medium",
        a: "<p><b>Multi-arch (multi-platform) images</b> are container images that can run natively on multiple CPU architectures — most commonly <code>linux/amd64</code> (Intel/AMD x86_64) and <code>linux/arm64</code> (Apple Silicon, AWS Graviton, Raspberry Pi 4+). A single image reference resolves to the correct binary at pull time based on the requesting host's architecture.</p><p><b>Why multi-arch matters:</b></p><ul><li><b>Apple Silicon developers, Intel-based CI runners:</b> Developers on M1/M2/M3 Macs run ARM64 natively; CI often uses AMD64 runners. Multi-arch images let both work without emulation slowdowns.</li><li><b>AWS Graviton cost savings:</b> Graviton (ARM64) instances are typically 20% cheaper than equivalent Intel instances. Multi-arch images let workloads run on either without recompilation.</li><li><b>Raspberry Pi and edge devices:</b> ARM-based edge devices become first-class deployment targets.</li></ul><p><b>How multi-arch images are structured:</b> Rather than a single image, a multi-arch image is a <b>manifest list</b> — a small JSON document that maps each architecture to its own image digest. When <code>docker pull nginx:latest</code> runs, Docker automatically fetches the manifest matching the host's platform.</p><pre>docker manifest inspect nginx:latest\n# Shows amd64, arm64, arm/v7, s390x, and more entries</pre><p><b>Building multi-arch images with <code>docker buildx</code>:</b></p><pre># Enable buildx (Docker Buildx uses BuildKit under the hood)\ndocker buildx create --name mybuilder --use\ndocker buildx inspect --bootstrap\n\n# Build for multiple platforms and push to a registry\ndocker buildx build \\\n  --platform linux/amd64,linux/arm64 \\\n  -t myregistry/myapp:v1.2.0 \\\n  --push \\\n  .</pre><p>Buildx uses QEMU emulation to build architectures that don't match the host, or (preferably) runs the build natively on real ARM64 hardware for speed.</p><p><b>Native multi-arch builds (fastest):</b> Some CI systems (GitHub Actions, AWS CodeBuild, Buildkite) expose native ARM64 runners. Building each architecture on native hardware is dramatically faster than QEMU emulation — often 5–10× faster for compilation-heavy workloads.</p><p><b>Tips for reliable multi-arch:</b></p><ul><li>Use base images that themselves are multi-arch (official Alpine, Debian, Ubuntu, distroless are all multi-arch).</li><li>Test builds on both architectures — a package available on AMD64 may not exist on ARM64.</li><li>Pin exact digests (<code>@sha256:...</code>) rather than mutable tags for reproducibility.</li><li>In Dockerfiles, avoid hard-coding architecture-specific binary URLs. Use build args like <code>$TARGETPLATFORM</code> and <code>$TARGETARCH</code> that Buildx sets automatically.</li></ul>",
      },
      {
        q: "What is BuildKit and how does it improve Docker builds?",
        difficulty: "medium",
        a: "<p><b>BuildKit</b> is a modern build engine that replaced the legacy Docker builder in Docker 23.0+ and is the default engine today. It dramatically improves build performance, capabilities, and security compared to the old builder.</p><p><b>Key improvements over legacy builder:</b></p><ol><li><b>Parallel stage execution:</b> BuildKit builds independent stages of a multi-stage Dockerfile in parallel, rather than sequentially. On multi-stage Dockerfiles with heavy CPU or I/O per stage, this alone can cut build times in half.</li><li><b>Smart layer caching:</b> BuildKit's cache is content-addressable and includes dependency analysis. It caches more aggressively and invalidates more precisely than the legacy builder.</li><li><b>Cache mounts:</b> Persistent caches for dependencies (npm, pip, Go modules, apt) survive across builds without polluting image layers.<pre>RUN --mount=type=cache,target=/root/.npm \\\n    npm ci</pre>The npm cache is stored outside the image layer graph and reused between builds, dramatically speeding up dependency-heavy builds.</li><li><b>Secret mounts:</b> Build-time secrets can be exposed to a single RUN command without ending up in image layers.<pre>RUN --mount=type=secret,id=npmrc,target=/root/.npmrc \\\n    npm install</pre>Historically, secrets in Dockerfiles were a common leakage vector; secret mounts solve this cleanly.</li><li><b>SSH agent forwarding:</b> Access private Git repositories at build time without embedding SSH keys in the image.<pre>RUN --mount=type=ssh git clone git@github.com:private/repo.git</pre></li><li><b>Improved output formats:</b> Produce OCI images, Docker images, cache-only outputs, or local filesystem outputs — flexible integration with pipelines.</li><li><b>Multi-platform builds (buildx):</b> Native support for cross-platform builds without additional tooling.</li></ol><p><b>Enabling BuildKit:</b> On modern Docker (23.0+), BuildKit is the default. On older versions:</p><pre>DOCKER_BUILDKIT=1 docker build .\n# Or set 'features.buildkit: true' in /etc/docker/daemon.json</pre><p><b>Rewriting a Dockerfile for BuildKit — example:</b></p><pre># syntax=docker/dockerfile:1.7\nFROM node:20-alpine AS build\nWORKDIR /app\n\nCOPY package*.json ./\nRUN --mount=type=cache,target=/root/.npm \\\n    npm ci --prefer-offline\n\nCOPY . .\nRUN npm run build\n\nFROM nginx:1.25-alpine\nCOPY --from=build /app/dist /usr/share/nginx/html\nEXPOSE 80\nCMD [\"nginx\", \"-g\", \"daemon off;\"]</pre><p>The <code># syntax=</code> directive opts into the latest Dockerfile frontend, enabling all modern features.</p>",
      },
      {
        q: "What are distroless images and why are they important?",
        difficulty: "medium",
        a: "<p><b>Distroless images</b> are minimal container images that contain only your application and its direct runtime dependencies — no shell, no package manager, no coreutils, no BusyBox, no OS distribution. Google created the concept with the <code>gcr.io/distroless</code> images, and Chainguard has since produced <code>cgr.dev/chainguard/*</code> as a hardened commercial alternative.</p><p><b>What is missing (compared to a typical Alpine or Debian image):</b></p><ul><li>No <code>bash</code>, <code>sh</code>, or any shell.</li><li>No <code>ls</code>, <code>cat</code>, <code>ps</code>, or other utilities.</li><li>No package manager (<code>apt</code>, <code>apk</code>).</li><li>Only what the runtime absolutely requires (glibc, ca-certificates, tzdata, and the language runtime like Java, Python, or Node).</li></ul><p><b>Why this matters — security implications:</b></p><ol><li><b>Drastically reduced attack surface.</b> A distroless image typically has 50–90% fewer CVEs than a full-OS image, simply because there are far fewer packages to be vulnerable.</li><li><b>No shell means no lateral movement.</b> If an attacker exploits a vulnerability in your application, they cannot spawn a shell, download tools, or run diagnostic commands. The exploit is contained to whatever the compromised process can do directly.</li><li><b>Smaller image size.</b> Faster pulls, faster deployments, faster autoscaling. A Java distroless image is often 100–200 MB smaller than an equivalent Debian-based image.</li><li><b>Immutable, auditable base.</b> Distroless images are rebuilt frequently with patched dependencies, and the base is straightforward to audit.</li></ol><p><b>Example — a Go application with distroless:</b></p><pre>FROM golang:1.22 AS build\nWORKDIR /src\nCOPY . .\nRUN CGO_ENABLED=0 go build -o /out/app ./cmd/app\n\nFROM gcr.io/distroless/static-debian12:nonroot\nCOPY --from=build /out/app /app\nUSER nonroot\nENTRYPOINT [\"/app\"]</pre><p><b>Example — a Python application:</b></p><pre>FROM python:3.12-slim AS build\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --user -r requirements.txt\nCOPY . .\n\nFROM gcr.io/distroless/python3-debian12:nonroot\nCOPY --from=build /root/.local /home/nonroot/.local\nCOPY --from=build /app /app\nENV PYTHONPATH=/home/nonroot/.local/lib/python3.12/site-packages\nWORKDIR /app\nUSER nonroot\nCMD [\"main.py\"]</pre><p><b>Trade-off — debugging challenges:</b> The biggest downside is that debugging is harder. When something breaks in production, you cannot <code>kubectl exec</code> into the pod and poke around with a shell because there is no shell. Solutions:</p><ul><li><b>Kubernetes ephemeral debug containers:</b> Attach a debug container with tools alongside the running pod without modifying the image.<pre>kubectl debug -it &lt;pod&gt; --image=nicolaka/netshoot --target=&lt;container&gt;</pre></li><li><b>Sidecar debug containers:</b> A separate container in the same pod for troubleshooting.</li><li><b>Distroless debug variants:</b> <code>gcr.io/distroless/static-debian12:debug</code> includes a shell — use only for debugging environments.</li></ul><p>Distroless has become the standard for production-grade containers in security-conscious organizations. The debugging trade-off is real but manageable, and the security benefit is substantial.</p>",
      },
      {
        q: "What are container runtimes and how do containerd, CRI-O, and Podman compare to Docker?",
        difficulty: "hard",
        a: "<p>\"Container runtime\" is often confused because the term applies at multiple layers. A modern container stack has <b>high-level runtimes</b> (management, image handling, orchestration integration) and <b>low-level runtimes</b> (actual process creation and namespace/cgroup setup). Understanding the stack helps interpret error messages and choose the right runtime for a use case.</p><p><b>Low-level runtimes</b> — implement the OCI Runtime Specification:</p><ul><li><b>runc:</b> The de facto reference implementation. Used under the hood by nearly everything.</li><li><b>crun:</b> A faster, lighter C-based reimplementation. Default in some Red Hat/Fedora environments.</li><li><b>gVisor / Kata Containers:</b> Sandbox runtimes that trade performance for stronger isolation (user-space kernel or micro-VM per container). Used for multi-tenant workloads.</li></ul><p><b>High-level runtimes</b> — provide image management, lifecycle, and CRI compatibility:</p><ul><li><b>Docker Engine (dockerd):</b> The original. Provides a rich CLI, Docker API, image building, networking, and volume management. Uses containerd internally.</li><li><b>containerd:</b> A CNCF graduated project that manages the full container lifecycle. Historically extracted from Docker. Now the runtime used by most managed Kubernetes services (EKS, GKE, AKS).</li><li><b>CRI-O:</b> An alternative container runtime purpose-built for Kubernetes. Lightweight, focused only on CRI (Container Runtime Interface). Preferred in Red Hat OpenShift.</li><li><b>Podman:</b> Red Hat's daemonless, rootless Docker alternative. Compatible with Docker CLI (<code>alias docker=podman</code> works for most commands). Excellent for development on RHEL/Fedora.</li></ul><p><b>Kubernetes and container runtimes:</b> Since Kubernetes 1.24 (2022), the built-in Docker shim has been removed. Kubernetes now requires a CRI-compliant runtime — the practical choices are <b>containerd</b> and <b>CRI-O</b>. Docker Engine can still be used indirectly via <code>cri-dockerd</code>, but this is not the recommended path.</p><p><b>Comparison for common scenarios:</b></p><table><tr><th>Scenario</th><th>Best choice</th></tr><tr><td>Local development (any OS)</td><td>Docker Desktop or Podman Desktop</td></tr><tr><td>CI/CD image building</td><td>Docker (Buildx), or BuildKit standalone, or Kaniko for rootless</td></tr><tr><td>Kubernetes production nodes</td><td>containerd (dominant) or CRI-O</td></tr><tr><td>Rootless development on Linux</td><td>Podman</td></tr><tr><td>Multi-tenant isolation</td><td>gVisor or Kata Containers (as OCI runtime for containerd/CRI-O)</td></tr></table><p><b>Why the CRI-O/containerd shift matters:</b> Kubernetes never needed Docker's full feature set — it only needed the parts that Docker delegated to containerd anyway. Removing the extra Docker layer simplified the stack, reduced attack surface, and enabled Kubernetes to move faster on runtime features. From a Kubernetes user's perspective, the change is largely invisible; <code>kubectl exec</code>, image pulls, and normal operations behave the same. The migration primarily affected node-level tooling.</p><p><b>What this means for DevOps engineers:</b></p><ul><li>Docker on your laptop remains fine for building images.</li><li>In Kubernetes, expect containerd or CRI-O — <code>docker ps</code> on a node no longer shows containers; use <code>crictl ps</code> instead.</li><li>OCI image standards mean images built with Docker run seamlessly on any CRI-compliant runtime — there is no lock-in.</li></ul>",
      },
      {
        q: "What is Docker Compose and when should you use it versus Kubernetes?",
        difficulty: "medium",
        a: "<p><b>Docker Compose</b> is a tool for defining and running multi-container applications on a single host, using a declarative YAML configuration. It fills the gap between running individual <code>docker run</code> commands and full-blown container orchestration.</p><p><b>Basic Compose file:</b></p><pre># docker-compose.yml (Compose spec — no version field needed in v2+)\nservices:\n  web:\n    build: .\n    image: myapp:dev\n    ports:\n      - \"8080:8080\"\n    environment:\n      DATABASE_URL: postgres://postgres:secret@db:5432/app\n    depends_on:\n      db:\n        condition: service_healthy\n    volumes:\n      - .:/app\n\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_PASSWORD: secret\n      POSTGRES_DB: app\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U postgres\"]\n      interval: 5s\n      retries: 5\n\n  redis:\n    image: redis:7-alpine\n\nvolumes:\n  pgdata:</pre><pre>docker compose up -d       # start all services\ndocker compose logs -f     # follow logs\ndocker compose down        # tear down (add -v to remove volumes)</pre><p><b>Key features:</b> multi-service networking (each service is DNS-resolvable by name), dependency ordering with health checks, per-service scaling, environment variable interpolation, and override files (<code>docker-compose.override.yml</code>) for local customization.</p><p><b>When Docker Compose is the right choice:</b></p><ul><li><b>Local development environments:</b> Spin up your entire application stack (web, db, cache, message broker) with one command. This is Compose's flagship use case and where it truly shines.</li><li><b>Integration testing in CI:</b> Run your app plus its dependencies for end-to-end tests.</li><li><b>Simple single-host deployments:</b> Small applications, side projects, self-hosted tools where Kubernetes is massive overkill.</li><li><b>Prototypes and demos:</b> Quick, understandable stack definitions.</li></ul><p><b>When Kubernetes is the right choice instead:</b></p><ul><li><b>Multi-host deployments:</b> Compose runs on a single host; Kubernetes distributes across many.</li><li><b>High availability requirements:</b> Kubernetes has native self-healing, health probes, rolling updates, and multi-zone scheduling.</li><li><b>Auto-scaling:</b> Horizontal Pod Autoscaler and Cluster Autoscaler are core Kubernetes features; Compose has no equivalent.</li><li><b>Production workloads at scale:</b> The rich Kubernetes ecosystem (Ingress controllers, service mesh, GitOps, observability integrations) has no Compose equivalent.</li></ul><p><b>What about Docker Swarm?</b> Docker Swarm is Docker's built-in orchestrator — essentially Compose extended to multiple hosts. It was competitive with Kubernetes in 2016–2017 but has effectively been abandoned by the industry. It is still functional and simpler than Kubernetes, but Kubernetes has won the orchestration war so decisively that Swarm should not be chosen for new deployments.</p><p><b>A practical modern workflow:</b> Use Compose for local development, and Kubernetes for staging and production. Tools like <b>Kompose</b> can convert Compose files to Kubernetes manifests as a starting point, though production Kubernetes usually needs more configuration than a simple conversion produces.</p>",
      },
      {
        q: "Explain Docker networking modes.",
        difficulty: "medium",
        a: "<p>Docker provides several networking drivers, each suited to different use cases. The choice of mode determines how a container communicates with other containers, the host, and external networks.</p><ul><li><b>bridge (default):</b> Docker creates an internal virtual network (typically <code>docker0</code>) and assigns each container an IP address within it. Containers on the same bridge network can communicate with each other by container name (DNS resolution is built-in on user-defined bridges). External access requires explicit port publishing with <code>-p host:container</code>.</li><li><b>host:</b> The container shares the host's network namespace. It has no isolated IP or ports — its services bind directly to the host's interfaces. This eliminates NAT overhead and can improve performance, but sacrifices network isolation. Not available on Docker Desktop for Mac or Windows.</li><li><b>none:</b> The container has no network interfaces other than loopback. Used for offline batch processing or when networking is provided by an external mechanism.</li><li><b>overlay:</b> Enables multi-host networking. Containers on different Docker hosts can communicate as if on the same network, using VXLAN encapsulation. Used by Docker Swarm and can be created manually for other multi-host scenarios.</li><li><b>macvlan:</b> Assigns each container its own MAC address and makes it appear as a physical device on the host's network. Useful for legacy applications that expect to be directly on the physical LAN.</li></ul><p><b>User-defined networks:</b> Custom bridge networks created with <code>docker network create</code> are recommended over the default bridge, as they provide automatic DNS resolution between containers by name, better isolation, and cleaner teardown.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "kubernetes",
    name: "Kubernetes",
    icon: "☸️",
    kind: "qa",
    description:
      "Comprehensive CKA-aligned coverage — control plane, workloads, scheduling, storage, networking, security, cluster maintenance, and troubleshooting. From beginner to senior/CKA exam level.",
    items: [
      {
        q: "What is a Pod and why does Kubernetes group containers into Pods?",
        difficulty: "easy",
        a: "<p>A <b>Pod</b> is the smallest deployable unit — one or more containers that <b>share</b>:</p><ul><li>Network namespace (same IP, same localhost).</li><li>IPC namespace.</li><li>Storage volumes.</li></ul><p>Grouping is useful for tightly-coupled helpers (a <b>sidecar</b> like a log shipper, or an <b>init container</b> for setup) that must live and die together with the main app.</p>",
        followups: [
          {
            q: "What is a sidecar container? Give a real example.",
            a: "<p>A helper container that runs alongside the main app in the same Pod. Common examples:</p><ul><li><b>Envoy / Istio proxy</b> — service mesh data plane.</li><li><b>Fluent Bit</b> — tails app logs and ships to Loki/Elasticsearch.</li><li><b>Cloud SQL Auth Proxy</b> — authenticates connections to a managed DB.</li></ul>",
          },
          {
            q: "Init container vs sidecar?",
            a: "<p><b>Init containers</b> run to completion <i>before</i> app containers start — used for setup (schema migrations, waiting for a dependency, pulling secrets).</p><p><b>Sidecars</b> run alongside the app for its entire lifetime.</p>",
          },
        ],
      },
      {
        q: "Difference between Deployment, StatefulSet, and DaemonSet?",
        difficulty: "medium",
        a: "<p>Kubernetes provides several workload controllers for managing pods, each designed for a different type of application. The three most common are Deployment, StatefulSet, and DaemonSet — the choice among them depends on whether the pods are interchangeable, need stable identity, or need to run on every node.</p><ul><li><b>Deployment:</b> Manages a set of identical, stateless replicas. Any pod is interchangeable with any other, allowing free scaling and safe rolling updates. Supports rolling updates and rollbacks natively. Suitable for stateless web APIs, background workers, and any application where individual pod identity does not matter.</li><li><b>StatefulSet:</b> Manages pods that require stable network identity (pods are named deterministically as <code>pod-0</code>, <code>pod-1</code>, ...) and stable persistent storage per pod (each pod gets its own PersistentVolumeClaim). Pods are started, stopped, and updated in order. Suitable for databases, message brokers like Kafka, and coordination systems like ZooKeeper — anywhere individual pod identity or dedicated storage matters.</li><li><b>DaemonSet:</b> Ensures exactly one pod runs on every node (or every node matching a selector). Automatically adds pods when new nodes join and removes them when nodes leave. Suitable for node-level agents: log collectors (Fluent Bit), metrics exporters (node_exporter), CNI plugins, and security agents.</li></ul>",
        followups: [
          {
            q: "When would you choose StatefulSet over Deployment?",
            a: "<p>Any time pods need <b>stable identity</b> or <b>durable per-pod storage</b>:</p><ul><li>Databases where each replica is a distinct role (primary/replica).</li><li>Distributed systems needing peer discovery by name (Zookeeper, Cassandra).</li><li>Systems that write to a per-pod PVC that must survive rescheduling.</li></ul>",
          },
          {
            q: "How do rolling updates work in a Deployment?",
            a: "<p>K8s creates a new ReplicaSet with the new spec, then gradually scales it up while scaling the old one down, respecting <code>maxSurge</code> (extra pods during rollout) and <code>maxUnavailable</code> (pods that can be down).</p><pre>strategy:\n  type: RollingUpdate\n  rollingUpdate:\n    maxSurge: 25%\n    maxUnavailable: 0</pre><p>Rollback with <code>kubectl rollout undo deploy/api</code>.</p>",
          },
        ],
      },
      {
        q: "Explain Service types: ClusterIP, NodePort, LoadBalancer, ExternalName.",
        difficulty: "medium",
        a: "<p>A Kubernetes <b>Service</b> is an abstraction that provides a stable network endpoint for a dynamic set of pods. Because pods are ephemeral — their IPs change as they are rescheduled — clients need something more stable to connect to. Services solve this by providing a stable virtual IP (or DNS name) that automatically load-balances traffic across the pods matched by a label selector.</p><p>Kubernetes defines four Service types, each intended for a different exposure scenario:</p><ul><li><b>ClusterIP (default):</b> Assigns the service a virtual IP that is only reachable from inside the cluster. This is the standard type for internal service-to-service communication.</li><li><b>NodePort:</b> Opens the same static port (in the range 30000–32767) on every node in the cluster. External traffic to any <code>&lt;nodeIP&gt;:&lt;nodePort&gt;</code> is forwarded to the service. Useful for basic external exposure without a cloud load balancer.</li><li><b>LoadBalancer:</b> Provisions an external load balancer through the cloud provider (AWS ELB, Azure Load Balancer, GCP Load Balancer). The LB's public IP is registered as the service's external IP. Each LoadBalancer service typically consumes one cloud load balancer, which can be expensive at scale.</li><li><b>ExternalName:</b> Maps the service to an external DNS name via a CNAME record. No proxying occurs — this is purely a DNS alias, useful for referencing external systems by an internal service name.</li></ul><p><b>Modern practice:</b> Rather than creating a LoadBalancer service per HTTP endpoint, production setups typically expose HTTP/HTTPS via a single <b>Ingress controller</b> (NGINX, Traefik, AWS ALB) or the newer <b>Gateway API</b>, both of which sit in front of ClusterIP services and route traffic based on host and path rules. This dramatically reduces cloud load balancer costs.</p>",
        followups: [
          {
            q: "Ingress vs LoadBalancer vs Gateway API?",
            a: "<p><b>LoadBalancer</b>: one cloud LB per service — expensive at scale.</p><p><b>Ingress</b>: one LB shared across many services with host/path routing (NGINX, Traefik, ALB).</p><p><b>Gateway API</b>: the newer, more expressive successor to Ingress — role-oriented (GatewayClass / Gateway / HTTPRoute), supports L4 & L7, and is the future standard.</p>",
          },
        ],
      },
      {
        q: "How do you troubleshoot a Pod stuck in `CrashLoopBackOff`?",
        difficulty: "medium",
        a: "<ol><li><code>kubectl describe pod &lt;name&gt;</code> — check Events, exit code, restart count.</li><li><code>kubectl logs &lt;name&gt; --previous</code> — logs from the crashed container.</li><li>Check <b>liveness/readiness</b> probes — misconfigured probes are a top cause.</li><li>Verify <b>resources</b> — OOMKilled? Insufficient memory?</li><li>Verify <b>configmaps/secrets</b> mounted correctly and <b>env vars</b>.</li><li><code>kubectl debug</code> or an ephemeral container to poke inside.</li></ol>",
        followups: [
          {
            q: "What is the difference between liveness, readiness, and startup probes?",
            a: "<ul><li><b>Liveness</b>: is the container alive? Failing → restart.</li><li><b>Readiness</b>: can it serve traffic? Failing → removed from Service endpoints (not restarted).</li><li><b>Startup</b>: has it finished starting? Once it succeeds, liveness/readiness take over. Great for slow-starting apps to avoid premature liveness kills.</li></ul>",
          },
          {
            q: "What does exit code 137 mean?",
            a: "<p>128 + 9 (SIGKILL). Almost always means the kernel OOM-killer terminated the container — set higher <code>resources.limits.memory</code> or fix the memory leak. Confirm with <code>kubectl describe</code> under Events (<code>OOMKilled</code>).</p>",
          },
        ],
      },
      {
        q: "What are Requests vs Limits, and how does the scheduler use them?",
        difficulty: "medium",
        a: "<p>Kubernetes resource management is controlled by two parallel settings on each container: <b>requests</b> and <b>limits</b>. Together they determine scheduling decisions, runtime enforcement, and the container's Quality of Service (QoS) class.</p><p><b>Requests:</b> The guaranteed minimum amount of a resource that the container needs. The Kubernetes scheduler uses requests when deciding which node to place a pod on — a pod will only be scheduled onto a node that has enough <i>unrequested</i> capacity to satisfy every container's requests. Requests do not enforce a runtime cap; they are purely a scheduling signal.</p><p><b>Limits:</b> The hard maximum a container may consume at runtime. Enforcement differs by resource type:</p><ul><li><b>CPU limits</b> are enforced by throttling — the container cannot exceed its limit, but is not killed.</li><li><b>Memory limits</b> are enforced by termination — if the container exceeds its memory limit, the kernel's OOM killer terminates it (visible as exit code 137).</li></ul><p><b>QoS classes:</b> Based on the relationship between requests and limits, Kubernetes assigns each pod to one of three QoS classes, which determine eviction priority when a node is under resource pressure:</p><ul><li><b>Guaranteed:</b> Requests equal limits for every resource on every container. These pods are evicted last.</li><li><b>Burstable:</b> Requests are set but less than limits (or only some resources have requests set). Evicted after BestEffort but before Guaranteed.</li><li><b>BestEffort:</b> Neither requests nor limits are set on any container. Evicted first when the node runs low on resources.</li></ul>",
      },
      {
        q: "Explain how ConfigMaps and Secrets work.",
        difficulty: "easy",
        a: "<p><b>ConfigMaps</b> and <b>Secrets</b> are two Kubernetes objects designed to decouple configuration data from container images. Both store key-value pairs and can be consumed by pods either as environment variables or as files mounted through a volume.</p><p><b>ConfigMap:</b> Stores non-sensitive configuration data — application settings, feature flags, connection strings without credentials, or entire configuration files. Values are stored in plain text in etcd. Consumed by pods in one of two ways:</p><ul><li>Injected as environment variables via <code>env.valueFrom.configMapKeyRef</code>.</li><li>Mounted as files inside a volume, where each key becomes a file name and its value becomes the file content.</li></ul><p><b>Secret:</b> Structurally identical to a ConfigMap, but intended for sensitive data — passwords, API tokens, TLS certificates. Secret values are <b>base64-encoded</b>, which is <i>not</i> encryption — it is only an encoding format that provides no confidentiality against anyone with access to the API or etcd. To provide actual protection, additional measures are required:</p><ol><li>Enable <b>encryption at rest</b> for etcd using a KMS provider (AWS KMS, Azure Key Vault, GCP KMS, or HashiCorp Vault).</li><li>Restrict access via RBAC — only workloads and users that need a secret should be able to read it.</li><li>Never commit Secret YAML to Git in plain form.</li></ol><p><b>Recommended practice:</b> For production systems, use the <b>External Secrets Operator</b> to synchronize Kubernetes Secrets from an external secrets management system (AWS Secrets Manager, HashiCorp Vault, Azure Key Vault). This centralizes secret lifecycle management, provides automatic rotation, and ensures secrets never touch Git.</p>",
        followups: [
          {
            q: "Are Kubernetes Secrets secure by default?",
            a: "<p>No — they are just <b>base64-encoded</b>, not encrypted. To make them secure:</p><ol><li>Enable <b>encryption at rest</b> for etcd (using a KMS provider).</li><li>Restrict access via <b>RBAC</b>.</li><li>Never commit secret YAML to git — use <b>Sealed Secrets</b>, <b>SOPS</b>, or an <b>External Secrets Operator</b>.</li></ol>",
          },
        ],
      },

      /* ============ CORE CONCEPTS / CONTROL PLANE ============ */
      {
        q: "Explain the Kubernetes cluster architecture (control plane vs worker nodes).",
        difficulty: "easy",
        a: "<p>A Kubernetes cluster is split into two roles:</p><p><b>Control Plane (master)</b> — makes global decisions and stores cluster state. Components:</p><ul><li><b>kube-apiserver</b> — front door for all cluster ops.</li><li><b>etcd</b> — distributed KV store, source of truth.</li><li><b>kube-scheduler</b> — assigns pods to nodes.</li><li><b>kube-controller-manager</b> — runs reconciliation loops.</li><li><b>cloud-controller-manager</b> — talks to cloud APIs.</li></ul><p><b>Worker Nodes</b> — run application pods. Components:</p><ul><li><b>kubelet</b> — agent that makes containers run.</li><li><b>kube-proxy</b> — implements Service networking.</li><li><b>Container Runtime</b> (containerd, CRI-O).</li></ul>",
        followups: [
          {
            q: "What happens when you run `kubectl apply -f pod.yaml`?",
            a: "<ol><li>kubectl authenticates & sends the YAML to <b>kube-apiserver</b>.</li><li>API server validates via <b>admission controllers</b>, then persists the desired state to <b>etcd</b>.</li><li><b>kube-scheduler</b> notices an unbound pod, picks a node using filters+scores, writes the binding back.</li><li>The chosen node's <b>kubelet</b> sees the assignment (via the API server watch), tells the container runtime to pull the image and start the container.</li><li>kubelet reports status; API server updates etcd.</li></ol>",
          },
          {
            q: "Can the control plane and worker roles run on the same node?",
            a: "<p>Yes — in small dev clusters (kind, minikube) they do. In production, keep control plane nodes dedicated (tainted with <code>node-role.kubernetes.io/control-plane:NoSchedule</code>) so workload pods don't compete with etcd for I/O.</p>",
          },
        ],
      },
      {
        q: "What is etcd and what does it store?",
        difficulty: "medium",
        a: "<p><b>etcd</b> is a distributed, consistent key-value store used as Kubernetes' <b>single source of truth</b>. It stores:</p><ul><li>All cluster objects (Pods, Deployments, ConfigMaps, Secrets, etc.).</li><li>Cluster configuration and metadata.</li><li>Service discovery data.</li></ul><p>etcd uses the <b>Raft consensus algorithm</b> — writes need a quorum (majority) of nodes. For HA, always run an <b>odd number</b> of etcd members (3 or 5); 3 tolerates 1 failure, 5 tolerates 2.</p>",
        followups: [
          {
            q: "How do you back up etcd?",
            a: "<pre>ETCDCTL_API=3 etcdctl snapshot save /backup/etcd.db \\\n  --endpoints=https://127.0.0.1:2379 \\\n  --cacert=/etc/kubernetes/pki/etcd/ca.crt \\\n  --cert=/etc/kubernetes/pki/etcd/server.crt \\\n  --key=/etc/kubernetes/pki/etcd/server.key</pre><p>Verify with <code>etcdctl snapshot status /backup/etcd.db</code>. Automate this in a nightly cron and offsite the snapshot.</p>",
          },
          {
            q: "How do you restore etcd from a snapshot?",
            a: "<ol><li>Stop the kube-apiserver on all control plane nodes.</li><li><code>etcdctl snapshot restore /backup/etcd.db --data-dir /var/lib/etcd-new</code></li><li>Update the etcd static pod manifest (<code>/etc/kubernetes/manifests/etcd.yaml</code>) to point <code>--data-dir</code> to the new directory.</li><li>Restart etcd, then the kube-apiserver.</li><li>Verify with <code>kubectl get nodes</code>.</li></ol>",
          },
          {
            q: "Why must etcd have an odd number of members?",
            a: "<p>Raft requires a <b>majority quorum</b> for writes. With 3 members, quorum = 2 (tolerates 1 loss). With 4 members, quorum still = 3 (still tolerates only 1 loss — adding a 4th doesn't help). Odd counts maximize fault tolerance for the same infrastructure cost.</p>",
          },
        ],
      },
      {
        q: "What is the role of the kube-apiserver?",
        difficulty: "easy",
        a: "<p>The <b>kube-apiserver</b> is the front-end of the Kubernetes control plane. Every component (kubectl, kubelet, controller-manager, scheduler) talks to it. Its responsibilities:</p><ul><li><b>Authenticate & Authorize</b> requests (via authn plugins + RBAC).</li><li><b>Validate & Mutate</b> requests through admission controllers.</li><li><b>Persist</b> objects to etcd.</li><li><b>Serve watches</b> so components can react to changes.</li></ul><p>It's the only component that talks directly to etcd — everything else goes through the API server.</p>",
        followups: [
          {
            q: "How do you scale the kube-apiserver?",
            a: "<p>The API server is stateless and horizontally scalable. In HA setups, run 3+ replicas behind a load balancer. Each connects to the same etcd cluster. Watches are load-balanced across replicas; writes still hit etcd's leader.</p>",
          },
        ],
      },
      {
        q: "What does the kube-scheduler do and how does it pick a node?",
        difficulty: "medium",
        a: "<p>The scheduler assigns unscheduled Pods to Nodes in a two-phase process:</p><ol><li><b>Filtering</b> — eliminate nodes that can't run the pod (insufficient resources, unmatched selectors, taints without matching tolerations, PV zone mismatch, etc.).</li><li><b>Scoring</b> — rank the remaining nodes (spread across zones, image-locality, resource balance) and pick the highest score. Ties broken randomly.</li></ol><p>The scheduler only writes a <b>binding</b> — it never starts containers. The kubelet on the chosen node does that.</p>",
        followups: [
          {
            q: "Can you run multiple schedulers or customize scheduling?",
            a: "<p>Yes — three options:</p><ul><li><b>Scheduler Profiles</b>: multiple named profiles in the default scheduler, each with different plugin configs. Pods choose a profile via <code>spec.schedulerName</code>.</li><li><b>Custom scheduler</b>: deploy a second scheduler binary with a different name.</li><li><b>Scheduling framework plugins</b>: extend filtering/scoring in Go via out-of-tree plugins.</li></ul>",
          },
          {
            q: "How would you manually schedule a pod to a specific node?",
            a: "<p>Two options:</p><ul><li><b>nodeName</b> in pod spec — bypasses the scheduler entirely (rarely recommended).</li><li><b>nodeSelector</b> or <b>nodeAffinity</b> targeting a label like <code>kubernetes.io/hostname: node-1</code>.</li></ul>",
          },
        ],
      },
      {
        q: "What does the kube-controller-manager do?",
        difficulty: "medium",
        a: "<p>The controller manager runs the built-in <b>reconciliation loops</b> that drive cluster state toward the desired spec. Notable controllers inside it:</p><ul><li><b>Node controller</b> — detects unreachable nodes.</li><li><b>Deployment / ReplicaSet controller</b> — maintains desired pod count.</li><li><b>Endpoints/EndpointSlice controller</b> — keeps Service endpoints up to date.</li><li><b>ServiceAccount & Token controllers</b> — create default SAs and their tokens.</li><li><b>Job/CronJob, StatefulSet, DaemonSet controllers</b>.</li></ul><p>Each controller watches the API server, computes a diff between desired and actual state, and takes action.</p>",
      },
      {
        q: "What is the kubelet and how does it manage pods?",
        difficulty: "medium",
        a: "<p><b>kubelet</b> is the node-level agent. It:</p><ul><li>Watches the API server for pods assigned to its node.</li><li>Tells the container runtime (via <b>CRI</b>) to pull images and start/stop containers.</li><li>Runs <b>liveness/readiness/startup probes</b>.</li><li>Reports pod & node status back to the API server.</li><li>Manages mounted volumes via CSI plugins.</li></ul><p>kubelet is <i>not</i> a container itself — it runs as a systemd service on every node (control plane + workers).</p>",
        followups: [
          {
            q: "What are Static Pods?",
            a: "<p>Pods managed directly by the <b>kubelet</b>, not the API server. kubelet watches a manifest directory (default <code>/etc/kubernetes/manifests/</code>) and creates a pod for each YAML file it finds. The API server sees them as <b>mirror pods</b> (read-only).</p><p>Used to bootstrap control plane components themselves — kubeadm places <code>kube-apiserver.yaml</code>, <code>etcd.yaml</code>, etc., in that folder.</p>",
          },
        ],
      },
      {
        q: "What is kube-proxy and what modes does it support?",
        difficulty: "medium",
        a: "<p><b>kube-proxy</b> runs on every worker node and implements the <b>Service abstraction</b> at the network layer. When traffic hits a ClusterIP, kube-proxy load-balances it to a healthy backend Pod.</p><p>Modes:</p><ul><li><b>iptables</b> (default): programs iptables NAT rules. Simple, no user-space hop, but rule evaluation is O(n) with the number of services.</li><li><b>IPVS</b>: kernel-level L4 load balancer, hash-table lookup (O(1)), supports more LB algorithms (rr, lc, sh). Better at large scale (thousands of services).</li><li><b>nftables</b> (newer, beta): successor to iptables, better performance.</li><li><b>userspace</b>: legacy, avoid.</li></ul>",
        followups: [
          {
            q: "How would you switch a cluster from iptables to IPVS?",
            a: "<ol><li>Ensure IPVS kernel modules are loaded on every node (<code>ip_vs</code>, <code>ip_vs_rr</code>, <code>ip_vs_wrr</code>, <code>ip_vs_sh</code>, <code>nf_conntrack</code>).</li><li>Edit the kube-proxy ConfigMap: set <code>mode: \"ipvs\"</code>.</li><li>Rollout the kube-proxy DaemonSet: <code>kubectl rollout restart ds/kube-proxy -n kube-system</code>.</li></ol>",
          },
        ],
      },

      /* ============ WORKLOADS & SCHEDULING ============ */
      {
        q: "What is a Namespace and when should you use one?",
        difficulty: "easy",
        a: "<p>Namespaces provide a <b>logical scope</b> for names — a way to divide a cluster into virtual sub-clusters. They enable:</p><ul><li><b>Multi-tenancy</b> — dev, staging, prod, or team-per-namespace isolation.</li><li><b>RBAC scoping</b> — grant a user access only to their namespace.</li><li><b>ResourceQuotas & LimitRanges</b> — cap CPU/memory per namespace.</li><li><b>Network policies</b> can select by namespace.</li></ul><p>Not namespaced: Nodes, PersistentVolumes, StorageClasses, ClusterRoles, ClusterRoleBindings.</p>",
        followups: [
          {
            q: "How do you enforce resource limits at the namespace level?",
            a: "<pre>apiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: team-a-quota\n  namespace: team-a\nspec:\n  hard:\n    requests.cpu: \"10\"\n    requests.memory: 20Gi\n    limits.cpu: \"20\"\n    pods: \"50\"</pre><p>Combine with a <b>LimitRange</b> to set per-pod defaults so users don't have to specify requests/limits explicitly.</p>",
          },
        ],
      },
      {
        q: "What is a ReplicaSet and how does it relate to a Deployment?",
        difficulty: "easy",
        a: "<p>A <b>ReplicaSet</b> keeps a specified number of identical Pods running. It uses a <b>label selector</b> to identify which pods it owns.</p><p>You almost never create a ReplicaSet directly. Instead you create a <b>Deployment</b>, which manages ReplicaSets for you:</p><ul><li>Each Deployment revision → a new ReplicaSet.</li><li>Rolling updates work by scaling the new RS up and the old RS down.</li><li>Rollback = point back to a previous RS.</li></ul>",
      },
      {
        q: "What is a Job and CronJob?",
        difficulty: "easy",
        a: "<p><b>Job</b>: runs pods until a specified number of them terminate successfully. Used for <b>batch/one-off work</b> — DB migrations, backups, data processing.</p><p><b>CronJob</b>: creates Jobs on a schedule (cron syntax). Used for periodic tasks — nightly reports, cache warm-ups.</p><pre>apiVersion: batch/v1\nkind: CronJob\nmetadata: { name: nightly-backup }\nspec:\n  schedule: \"0 2 * * *\"\n  jobTemplate:\n    spec:\n      template:\n        spec:\n          restartPolicy: OnFailure\n          containers:\n          - name: backup\n            image: my/backup:1.2</pre>",
        followups: [
          {
            q: "How do you handle Job retries and parallelism?",
            a: "<ul><li><b>backoffLimit</b>: max retries before marking Job failed.</li><li><b>completions</b>: how many successful pods needed.</li><li><b>parallelism</b>: how many pods can run at once.</li><li><b>activeDeadlineSeconds</b>: hard timeout for the whole Job.</li><li><b>ttlSecondsAfterFinished</b>: auto-delete completed Jobs.</li></ul>",
          },
        ],
      },
      {
        q: "Explain Taints and Tolerations.",
        difficulty: "medium",
        a: "<p><b>Taints</b> are applied to <i>Nodes</i> and repel pods that don't <i>tolerate</i> them. <b>Tolerations</b> are applied to <i>Pods</i> and allow them to be scheduled onto matching tainted nodes.</p><p>Effect values:</p><ul><li><b>NoSchedule</b>: pods without matching toleration won't be scheduled.</li><li><b>PreferNoSchedule</b>: soft version.</li><li><b>NoExecute</b>: also evicts existing pods that don't tolerate it.</li></ul><pre>kubectl taint nodes gpu-node dedicated=gpu:NoSchedule</pre><p>Pod that tolerates it:</p><pre>tolerations:\n- key: dedicated\n  operator: Equal\n  value: gpu\n  effect: NoSchedule</pre><p><b>Note</b>: toleration only <i>allows</i> — use <b>nodeAffinity</b> to actively <i>attract</i> pods.</p>",
        followups: [
          {
            q: "What's the difference between taints/tolerations and nodeAffinity?",
            a: "<p><b>Taints/tolerations (node-repels-pod)</b>: keep unwanted pods off a node. Pods opt-in to run there.</p><p><b>NodeAffinity (pod-attracts-node)</b>: pod chooses which nodes it wants to run on.</p><p>They compose: taint a GPU node <code>dedicated=gpu:NoSchedule</code>, and set nodeAffinity on GPU pods to prefer <code>hardware=gpu</code>. Result: only GPU pods on GPU nodes, and GPU pods prefer them.</p>",
          },
        ],
      },
      {
        q: "Compare NodeSelector, NodeAffinity, and PodAffinity.",
        difficulty: "medium",
        a: "<ul><li><b>nodeSelector</b>: simplest form — pod runs on nodes with matching labels. Hard requirement, exact match only.</li><li><b>nodeAffinity</b>: richer — supports <code>In</code>, <code>NotIn</code>, <code>Exists</code>, <code>Gt</code>, <code>Lt</code>. Has <b>requiredDuringScheduling</b> (hard) and <b>preferredDuringScheduling</b> (soft, weighted) modes.</li><li><b>podAffinity / podAntiAffinity</b>: schedule based on <i>other pods</i>' labels — e.g. \"run near a Redis pod\" (affinity) or \"never on the same node as another replica\" (anti-affinity, for HA).</li></ul>",
        followups: [
          {
            q: "How do you spread replicas across zones?",
            a: "<p>Use <b>Pod Topology Spread Constraints</b> — the modern replacement for podAntiAffinity + topology keys:</p><pre>topologySpreadConstraints:\n- maxSkew: 1\n  topologyKey: topology.kubernetes.io/zone\n  whenUnsatisfiable: DoNotSchedule\n  labelSelector:\n    matchLabels:\n      app: api</pre><p>This keeps replicas evenly spread across zones (max 1 replica difference).</p>",
          },
        ],
      },
      {
        q: "What is a Priority Class and how does preemption work?",
        difficulty: "hard",
        a: "<p><b>PriorityClass</b> assigns a numeric priority to pods. Higher-priority pods are scheduled first.</p><p><b>Preemption</b>: when a high-priority pod can't schedule due to insufficient resources, the scheduler evicts (preempts) lower-priority pods to make room.</p><pre>apiVersion: scheduling.k8s.io/v1\nkind: PriorityClass\nmetadata: { name: critical }\nvalue: 1000000\nglobalDefault: false\ndescription: \"Critical system pods\"</pre><p>System-reserved names <code>system-cluster-critical</code> and <code>system-node-critical</code> are used for essential components (CoreDNS, kube-proxy).</p>",
      },
      {
        q: "What is a PodDisruptionBudget (PDB)?",
        difficulty: "medium",
        a: "<p>A <b>PDB</b> limits how many pods of an app can be <b>voluntarily</b> disrupted at once (e.g. during a node drain, upgrade). It doesn't protect against involuntary failures (node crash).</p><pre>apiVersion: policy/v1\nkind: PodDisruptionBudget\nspec:\n  minAvailable: 2   # or maxUnavailable\n  selector:\n    matchLabels: { app: api }</pre><p>During <code>kubectl drain</code>, the eviction API respects the PDB — draining blocks until the PDB allows the eviction.</p>",
        followups: [
          {
            q: "When should you set minAvailable vs maxUnavailable?",
            a: "<p><b>minAvailable</b>: use when you know exact minimum needed (\"always keep 2 running\"). Static.</p><p><b>maxUnavailable</b>: use when replica count varies — expressed as % (\"never lose more than 25%\"). Scales with your HPA.</p>",
          },
        ],
      },
      {
        q: "How does the Horizontal Pod Autoscaler (HPA) work?",
        difficulty: "medium",
        a: "<p>HPA scales the number of pod replicas based on observed metrics — CPU, memory, or custom metrics.</p><p><b>Formula</b>: <code>desiredReplicas = ceil(currentReplicas × currentMetric / targetMetric)</code></p><pre>apiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: api\n  minReplicas: 2\n  maxReplicas: 20\n  metrics:\n  - type: Resource\n    resource:\n      name: cpu\n      target:\n        type: Utilization\n        averageUtilization: 70</pre><p>Requires the <b>metrics-server</b> (for resource metrics) or a custom metrics adapter (for Prometheus).</p>",
        followups: [
          {
            q: "HPA vs VPA vs Cluster Autoscaler — when to use each?",
            a: "<ul><li><b>HPA</b>: scales <i>replica count</i> (horizontal). Best for stateless workloads.</li><li><b>VPA</b>: adjusts <i>requests/limits</i> per pod (vertical). Restarts pods to apply changes. Good for workloads that can't be horizontally scaled.</li><li><b>Cluster Autoscaler</b>: scales the <i>number of nodes</i>. Adds nodes when pods are Pending due to insufficient capacity, removes underutilized nodes.</li></ul><p>They compose: HPA + Cluster Autoscaler is the classic pair. HPA + VPA together is tricky (they can fight over CPU).</p>",
          },
        ],
      },

      /* ============ STORAGE ============ */
      {
        q: "Explain PersistentVolume (PV) and PersistentVolumeClaim (PVC) — the storage lifecycle.",
        difficulty: "medium",
        a: "<p><b>PV</b> is a piece of storage in the cluster (a raw disk, NFS export, cloud volume). Cluster-scoped, provisioned by admin (static) or by a StorageClass (dynamic).</p><p><b>PVC</b> is a <i>request</i> for storage by a user (namespaced). Specifies size, access mode, and optionally a StorageClass.</p><p><b>Binding</b>: control plane matches a PVC to a suitable PV.</p><p><b>Lifecycle</b>: Available → Bound → Released → (Retained / Deleted / Recycled based on <code>persistentVolumeReclaimPolicy</code>).</p><p>Pod uses the PVC via <code>volumes.persistentVolumeClaim.claimName</code>.</p>",
        followups: [
          {
            q: "What are the PV reclaim policies?",
            a: "<ul><li><b>Retain</b>: PV kept after PVC delete — admin must manually clean up. Safest for prod.</li><li><b>Delete</b>: PV (and underlying cloud disk) deleted. Common default for dynamic provisioning.</li><li><b>Recycle</b>: (deprecated) does a rm -rf and makes PV Available again.</li></ul>",
          },
          {
            q: "What are the volume access modes?",
            a: "<ul><li><b>ReadWriteOnce (RWO)</b>: mount as R/W by one <i>node</i>.</li><li><b>ReadOnlyMany (ROX)</b>: mount R/O by many nodes.</li><li><b>ReadWriteMany (RWX)</b>: mount R/W by many nodes (NFS, CephFS, EFS).</li><li><b>ReadWriteOncePod (RWOP)</b>: mount R/W by exactly one <i>pod</i>. Newer, useful for singletons.</li></ul>",
          },
        ],
      },
      {
        q: "What is a StorageClass and how does dynamic provisioning work?",
        difficulty: "medium",
        a: "<p>A <b>StorageClass</b> defines a <i>type</i> of storage the cluster can dynamically provision — e.g. \"fast-ssd\" (io2), \"cheap-hdd\" (gp3), \"nfs\".</p><pre>apiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata: { name: fast }\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: gp3\n  iops: \"3000\"\nreclaimPolicy: Delete\nvolumeBindingMode: WaitForFirstConsumer\nallowVolumeExpansion: true</pre><p>When a PVC references a StorageClass, the corresponding <b>CSI driver</b> creates the underlying volume and a matching PV automatically.</p>",
        followups: [
          {
            q: "What is `volumeBindingMode: WaitForFirstConsumer` and why does it matter?",
            a: "<p>With <b>Immediate</b> (default), the PV is provisioned as soon as the PVC is created — before the scheduler picks a node. If the volume is zonal (EBS), the pod may end up in a zone with no capacity.</p><p><b>WaitForFirstConsumer</b>: delays provisioning until a pod using the PVC is scheduled. The scheduler picks a node first, then the PV is created in the correct zone. Almost always the right choice.</p>",
          },
          {
            q: "How do you resize a PVC?",
            a: "<p>If the StorageClass has <code>allowVolumeExpansion: true</code>:</p><ol><li><code>kubectl edit pvc data</code> → increase <code>spec.resources.requests.storage</code>.</li><li>The CSI driver expands the underlying disk.</li><li>Some file systems require the pod to restart for the FS-level resize (\"FileSystemResizePending\" condition on the PVC).</li></ol>",
          },
        ],
      },
      {
        q: "What is CSI (Container Storage Interface)?",
        difficulty: "medium",
        a: "<p><b>CSI</b> is the standard interface between Kubernetes and storage backends. Before CSI, storage drivers were in-tree (compiled into Kubernetes). CSI moved them out — each vendor now ships a <b>CSI driver</b> as a set of pods.</p><p>A CSI driver typically has:</p><ul><li><b>Controller plugin</b>: creates/deletes/attaches volumes (talks to the cloud API).</li><li><b>Node plugin</b>: DaemonSet on every node — mounts the volume into the pod's filesystem.</li></ul><p>Examples: <code>ebs.csi.aws.com</code>, <code>disk.csi.azure.com</code>, <code>pd.csi.storage.gke.io</code>, <code>rook-ceph.rbd.csi.ceph.com</code>.</p>",
      },
      {
        q: "Why does StatefulSet use `volumeClaimTemplates` instead of a plain PVC?",
        difficulty: "hard",
        a: "<p>Each StatefulSet pod needs its <b>own</b> persistent volume (a database replica's data is unique to that replica). A plain PVC referenced in the pod spec would be shared by all replicas.</p><p><b>volumeClaimTemplates</b> tells the StatefulSet controller: \"for every replica, create a PVC named <code>&lt;template&gt;-&lt;podname&gt;-N</code>\".</p><pre>volumeClaimTemplates:\n- metadata: { name: data }\n  spec:\n    accessModes: [\"ReadWriteOnce\"]\n    storageClassName: fast\n    resources:\n      requests: { storage: 20Gi }</pre><p>When the pod is rescheduled, it keeps its identity (pod-0) and reattaches its original PVC.</p>",
      },

      /* ============ NETWORKING ============ */
      {
        q: "Explain the Kubernetes networking model.",
        difficulty: "medium",
        a: "<p>Kubernetes assumes a <b>flat network</b> where:</p><ol><li>Every Pod gets its <b>own IP</b>.</li><li>Pods can communicate with all other Pods, on any node, <b>without NAT</b>.</li><li>Nodes can communicate with all Pods, without NAT.</li><li>The IP a Pod sees itself as = the IP others see.</li></ol><p>Kubernetes doesn't implement this itself — it delegates to a <b>CNI plugin</b> (Calico, Cilium, Flannel, Weave). Services and their ClusterIPs are then layered on top by kube-proxy.</p>",
      },
      {
        q: "What is CNI and name some popular plugins.",
        difficulty: "easy",
        a: "<p><b>CNI (Container Network Interface)</b> is a spec that defines how a container runtime configures pod networking. When a pod starts, kubelet invokes the CNI plugin (a binary in <code>/opt/cni/bin</code>) which sets up the pod's network namespace, IP, and routes.</p><p>Popular CNI plugins:</p><ul><li><b>Calico</b>: BGP-based, supports NetworkPolicies out of the box, very scalable.</li><li><b>Cilium</b>: eBPF-based, extremely fast, rich observability, has its own service mesh.</li><li><b>Flannel</b>: simple, overlay networks (VXLAN). Good for small clusters.</li><li><b>Weave Net</b>: mesh with encryption.</li><li><b>AWS VPC CNI</b>: pods get real VPC IPs on EKS.</li></ul>",
      },
      {
        q: "How does CoreDNS work in a Kubernetes cluster?",
        difficulty: "medium",
        a: "<p><b>CoreDNS</b> runs as a Deployment in <code>kube-system</code> and provides in-cluster DNS. Every pod's <code>/etc/resolv.conf</code> points to CoreDNS's ClusterIP.</p><p>Service DNS pattern: <code>&lt;service&gt;.&lt;namespace&gt;.svc.cluster.local</code></p><ul><li><code>api.prod.svc.cluster.local</code> — resolves to the ClusterIP.</li><li>Pods in the same namespace can use just <code>api</code>.</li><li>Headless service returns each pod's A record separately.</li></ul><p>Non-cluster names (e.g. <code>google.com</code>) are forwarded upstream. Configurable via the <code>coredns</code> ConfigMap (<b>Corefile</b>).</p>",
        followups: [
          {
            q: "How do you debug a DNS resolution problem?",
            a: "<ol><li>Confirm CoreDNS pods are running: <code>kubectl get pods -n kube-system -l k8s-app=kube-dns</code>.</li><li>Run a debug pod: <code>kubectl run tmp --image=nicolaka/netshoot --rm -it -- sh</code></li><li>Inside: <code>nslookup kubernetes.default.svc.cluster.local</code></li><li>Check <code>cat /etc/resolv.conf</code> — should point to the kube-dns ClusterIP.</li><li>Check CoreDNS logs: <code>kubectl logs -n kube-system -l k8s-app=kube-dns</code>.</li><li>Inspect the Corefile ConfigMap for misconfigured forwarders.</li></ol>",
          },
        ],
      },
      {
        q: "What is a Headless Service and when do you use it?",
        difficulty: "medium",
        a: "<p>A Service with <code>clusterIP: None</code>. Instead of a load-balanced ClusterIP, DNS returns <b>each backend pod's IP</b> directly (via A records for each Endpoints entry).</p><p>Used when:</p><ul><li>You want <b>direct pod addressing</b> — e.g. StatefulSet peers (Cassandra, Kafka, ZooKeeper) discover each other by DNS.</li><li>Client-side load balancing (e.g. gRPC).</li><li>You want to consume a Service via its underlying IPs.</li></ul>",
      },
      {
        q: "Endpoints vs EndpointSlices — what changed?",
        difficulty: "hard",
        a: "<p><b>Endpoints</b> (v1): a single object per Service listing all backend pod IPs. Doesn't scale — every kube-proxy re-processes the entire object on any change (thundering herd on large services).</p><p><b>EndpointSlices</b> (v1, GA in 1.21): the same info split into multiple slices (default 100 endpoints per slice). Updates only push changed slices, dramatically reducing control-plane load in large clusters. Also carries richer info (topology hints, protocols).</p><p>Modern kube-proxy uses EndpointSlices; the legacy Endpoints object is kept in sync for backward compatibility.</p>",
      },
      {
        q: "What is a NetworkPolicy and how does it work?",
        difficulty: "medium",
        a: "<p>A NetworkPolicy defines <b>allowed ingress/egress traffic</b> for a set of pods (selected by label). By default, all pods are non-isolated (all traffic allowed). Once any policy selects a pod, that direction becomes <b>deny-by-default</b> — only explicitly allowed traffic passes.</p><pre>apiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata: { name: api-allow-web }\nspec:\n  podSelector:\n    matchLabels: { app: api }\n  policyTypes: [\"Ingress\"]\n  ingress:\n  - from:\n    - podSelector:\n        matchLabels: { app: web }\n    ports:\n    - port: 8080</pre><p><b>Requires a CNI that implements policies</b> (Calico, Cilium, Weave). Flannel alone won't enforce them.</p>",
        followups: [
          {
            q: "How do you write a default-deny policy for a namespace?",
            a: "<pre>apiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: default-deny-all\n  namespace: prod\nspec:\n  podSelector: {}\n  policyTypes: [\"Ingress\", \"Egress\"]</pre><p>Empty podSelector = all pods. Empty ingress+egress = deny everything. Then add specific allow policies on top.</p>",
          },
        ],
      },
      {
        q: "Explain Ingress in depth — Ingress resource vs Ingress controller.",
        difficulty: "medium",
        a: "<p><b>Ingress resource</b>: a Kubernetes object (YAML) describing HTTP routing rules — host/path → Service.</p><p><b>Ingress controller</b>: a pod actually implementing those rules (an L7 reverse proxy). Common controllers: NGINX, Traefik, HAProxy, AWS ALB Controller, Istio Gateway.</p><p>Without a controller, an Ingress resource does nothing. The controller watches Ingress objects and reconfigures its proxy.</p><pre>apiVersion: networking.k8s.io/v1\nkind: Ingress\nspec:\n  ingressClassName: nginx\n  rules:\n  - host: api.example.com\n    http:\n      paths:\n      - path: /\n        pathType: Prefix\n        backend:\n          service:\n            name: api\n            port: { number: 80 }\n  tls:\n  - hosts: [\"api.example.com\"]\n    secretName: api-tls</pre>",
      },

      /* ============ SECURITY ============ */
      {
        q: "Explain RBAC — Role, ClusterRole, RoleBinding, ClusterRoleBinding.",
        difficulty: "medium",
        a: "<p>RBAC controls who can do what.</p><ul><li><b>Role</b>: set of permissions <b>within a namespace</b>.</li><li><b>ClusterRole</b>: set of permissions <b>cluster-wide</b> (or for cluster-scoped resources like Nodes).</li><li><b>RoleBinding</b>: binds a Role (or ClusterRole) to subjects (User, Group, ServiceAccount) in a namespace.</li><li><b>ClusterRoleBinding</b>: binds a ClusterRole to subjects cluster-wide.</li></ul><pre># Read-only pods in the 'app' namespace\napiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata: { namespace: app, name: pod-reader }\nrules:\n- apiGroups: [\"\"]\n  resources: [\"pods\", \"pods/log\"]\n  verbs: [\"get\", \"list\", \"watch\"]\n---\nkind: RoleBinding\nmetadata: { namespace: app, name: alice-reads }\nsubjects:\n- kind: User\n  name: alice\nroleRef:\n  kind: Role\n  name: pod-reader\n  apiGroup: rbac.authorization.k8s.io</pre>",
        followups: [
          {
            q: "Can you use a ClusterRole in a RoleBinding?",
            a: "<p>Yes — very common pattern. Define a reusable ClusterRole (e.g. <code>view</code>), then bind it via RoleBinding in each namespace. This scopes the ClusterRole's permissions to that single namespace.</p>",
          },
          {
            q: "How do you debug RBAC issues (`kubectl auth can-i`)?",
            a: "<pre>kubectl auth can-i list pods --namespace prod\nkubectl auth can-i list pods --namespace prod --as alice\nkubectl auth can-i list pods --namespace prod \\\n  --as system:serviceaccount:prod:api\nkubectl auth can-i --list --as alice</pre><p>Great for verifying policies without waiting for a real failure.</p>",
          },
        ],
      },
      {
        q: "What is a ServiceAccount and how do pods use it?",
        difficulty: "easy",
        a: "<p>A <b>ServiceAccount (SA)</b> is an identity for pods (not humans). Every pod runs as a SA — if you don't specify one, it uses <code>default</code> in the pod's namespace.</p><p>A SA is granted permissions via RBAC. Its token is projected into the pod at <code>/var/run/secrets/kubernetes.io/serviceaccount/token</code>.</p><pre>apiVersion: v1\nkind: ServiceAccount\nmetadata: { name: reader, namespace: app }\n---\napiVersion: v1\nkind: Pod\nspec:\n  serviceAccountName: reader\n  containers: [...]</pre>",
        followups: [
          {
            q: "How have SA tokens changed in modern Kubernetes?",
            a: "<p>Historically, each SA had a long-lived <b>Secret</b> auto-created holding its token. In 1.24+, this stopped — tokens are now <b>bound, time-limited, audience-scoped</b> and projected via <code>TokenRequest</code> API. Auto-refreshed by kubelet every ~1 hour.</p><p>You can still create a legacy Secret manually if needed, but it's not recommended.</p>",
          },
        ],
      },
      {
        q: "What is a SecurityContext?",
        difficulty: "medium",
        a: "<p>SecurityContext defines privilege and access controls at pod or container level.</p><pre>securityContext:\n  runAsNonRoot: true\n  runAsUser: 1000\n  runAsGroup: 3000\n  fsGroup: 2000\n  readOnlyRootFilesystem: true\n  allowPrivilegeEscalation: false\n  capabilities:\n    drop: [\"ALL\"]\n  seccompProfile:\n    type: RuntimeDefault</pre><p>Best-practice hardened baseline: non-root, read-only rootfs, all caps dropped, no priv escalation, RuntimeDefault seccomp.</p>",
      },
      {
        q: "What is Pod Security Admission (PSA)?",
        difficulty: "hard",
        a: "<p><b>PSA</b> is the built-in admission plugin (GA in 1.25) that enforces the <b>Pod Security Standards</b>. It replaced the deprecated PodSecurityPolicy (PSP).</p><p>Three standards:</p><ul><li><b>privileged</b>: unrestricted (host access allowed).</li><li><b>baseline</b>: minimally restrictive — prevents known privilege escalations.</li><li><b>restricted</b>: heavily restricted — non-root, seccomp default, no caps, etc.</li></ul><p>Enforced per namespace via labels:</p><pre>apiVersion: v1\nkind: Namespace\nmetadata:\n  name: prod\n  labels:\n    pod-security.kubernetes.io/enforce: restricted\n    pod-security.kubernetes.io/audit: restricted\n    pod-security.kubernetes.io/warn: restricted</pre>",
        followups: [
          {
            q: "PSA vs OPA/Gatekeeper vs Kyverno?",
            a: "<p><b>PSA</b>: built-in, only enforces the standard PSS profiles. Cheap, easy.</p><p><b>Gatekeeper</b> (OPA): flexible policy-as-code in Rego. Complex but very powerful.</p><p><b>Kyverno</b>: policies in YAML (native K8s feel), supports validate/mutate/generate. Easier learning curve than Gatekeeper.</p><p>Common stack: PSA for baseline + Kyverno for org-specific rules.</p>",
          },
        ],
      },
      {
        q: "How does Kubernetes authenticate users?",
        difficulty: "hard",
        a: "<p>Kubernetes has <b>no built-in user database</b>. It relies on external auth via one or more of:</p><ul><li><b>X.509 client certs</b>: kubectl uses a cert signed by the cluster CA. CN = username, O = groups.</li><li><b>Static token file / bootstrap tokens</b>: legacy, mostly for bootstrap.</li><li><b>OIDC</b>: production standard — integrate with Okta, Google, Azure AD, Keycloak.</li><li><b>Webhook authentication</b>: API server calls out to a custom webhook.</li><li><b>ServiceAccount tokens</b>: for in-cluster pods (JWTs signed by the cluster).</li></ul><p>After authentication, RBAC (or Webhook / ABAC / Node authorizer) decides <i>authorization</i>.</p>",
      },
      {
        q: "What is admission control? Give examples.",
        difficulty: "hard",
        a: "<p>Admission controllers are plugins that intercept API requests <b>after authentication/authorization but before persistence</b> to etcd. They can <b>validate</b> or <b>mutate</b> objects.</p><p>Built-in examples:</p><ul><li><b>NamespaceLifecycle</b>: blocks creating objects in terminating namespaces.</li><li><b>LimitRanger</b>: applies default resource requests/limits.</li><li><b>ResourceQuota</b>: enforces namespace quotas.</li><li><b>ServiceAccount</b>: automounts SA tokens.</li><li><b>PodSecurity</b>: enforces PSS profiles.</li></ul><p><b>Dynamic admission</b>: <code>ValidatingAdmissionWebhook</code> and <code>MutatingAdmissionWebhook</code> — your own logic via HTTPS callouts. Used by Istio (sidecar injection), OPA/Gatekeeper, Kyverno.</p>",
        followups: [
          {
            q: "Difference between validating and mutating webhooks?",
            a: "<p><b>Mutating</b> runs first — can modify the object (e.g. inject a sidecar). <b>Validating</b> runs after — can only accept or reject. Ordering matters: never validate a field that a later mutating webhook will change.</p>",
          },
        ],
      },
      {
        q: "How do you rotate cluster TLS certificates?",
        difficulty: "hard",
        a: "<p>With <b>kubeadm</b>-managed clusters:</p><pre>kubeadm certs check-expiration                # see when certs expire\nkubeadm certs renew all                       # renew all\nsystemctl restart kubelet                     # or restart control plane pods</pre><p>Kubeadm auto-renews non-kubelet certs on every <code>kubeadm upgrade</code>.</p><p><b>Kubelet client certificates</b> auto-rotate if <code>rotateCertificates: true</code> and <code>serverTLSBootstrap: true</code> are set. New CSRs need to be approved (<code>kubectl certificate approve</code>).</p>",
      },

      /* ============ CLUSTER MAINTENANCE ============ */
      {
        q: "How do you upgrade a Kubernetes cluster with kubeadm?",
        difficulty: "hard",
        a: "<p>Upgrade one <b>minor version at a time</b> (never skip). Order: control plane first, then workers.</p><pre># On the first control plane node:\nsudo apt-mark unhold kubeadm\nsudo apt-get install -y kubeadm=1.29.x-*\nsudo apt-mark hold kubeadm\nsudo kubeadm upgrade plan\nsudo kubeadm upgrade apply v1.29.x\n\n# Then kubelet+kubectl on the same node:\nkubectl drain cp-1 --ignore-daemonsets\nsudo apt-get install -y kubelet=1.29.x-* kubectl=1.29.x-*\nsudo systemctl daemon-reload &amp;&amp; sudo systemctl restart kubelet\nkubectl uncordon cp-1\n\n# Other control plane nodes: kubeadm upgrade node (not apply)\n# Then each worker: drain, upgrade kubelet, uncordon.</pre>",
        followups: [
          {
            q: "What does `kubectl drain` do exactly?",
            a: "<p><code>kubectl drain node-1 --ignore-daemonsets --delete-emptydir-data</code>:</p><ol><li>Marks node <b>unschedulable</b> (cordon).</li><li>Evicts all pods (respecting PDBs).</li><li>DaemonSet pods are skipped (they exist per-node by design) unless <code>--ignore-daemonsets</code> is set.</li></ol><p>After maintenance, <code>kubectl uncordon node-1</code> makes it schedulable again.</p>",
          },
          {
            q: "How would you handle a cluster upgrade in production with zero downtime?",
            a: "<ol><li>Pre-flight: check API deprecations (<code>kubectl deprecations</code> / <code>pluto</code>).</li><li>Backup etcd.</li><li>Confirm PDBs on critical workloads.</li><li>Roll control plane one node at a time.</li><li>Roll worker nodes with drain + upgrade + uncordon.</li><li>Or use <b>node group replacement</b>: create new node group on new version, drain and delete old.</li></ol>",
          },
        ],
      },
      {
        q: "How do you safely take a node out for maintenance?",
        difficulty: "medium",
        a: "<pre># 1. Cordon: no new pods scheduled\nkubectl cordon node-1\n\n# 2. Drain: evict existing pods (respecting PDBs)\nkubectl drain node-1 --ignore-daemonsets --delete-emptydir-data\n\n# 3. Do OS patches, kernel updates, reboot ...\n\n# 4. Bring the node back\nkubectl uncordon node-1</pre><p>Watch for pods stuck evicting (PDB blocks) — either raise <code>maxUnavailable</code> temporarily or wait.</p>",
      },
      {
        q: "What is a CertificateSigningRequest (CSR) and when is it used?",
        difficulty: "hard",
        a: "<p>A CSR is a K8s object representing a request for a certificate signed by the cluster CA. Used for:</p><ul><li><b>Kubelet serving cert rotation</b> — kubelet auto-generates CSRs, admin approves.</li><li>Adding a new user: generate a private key + CSR, submit as a K8s CSR, approve, then extract the signed cert.</li><li>Bootstrap tokens for kubeadm-joining nodes.</li></ul><pre>kubectl get csr\nkubectl certificate approve node-csr-abcd</pre>",
      },

      /* ============ TROUBLESHOOTING ============ */
      {
        q: "How do you debug a Pod stuck in `Pending`?",
        difficulty: "medium",
        a: "<p><code>kubectl describe pod &lt;name&gt;</code> — look at <b>Events</b>. Common reasons:</p><ul><li><b>Insufficient resources</b>: no node has enough CPU/mem for the pod's <code>requests</code>. Fix: add nodes (or Cluster Autoscaler), lower requests.</li><li><b>Unschedulable due to taints</b>: pod lacks matching toleration.</li><li><b>NodeSelector/Affinity</b> matches no node.</li><li><b>PVC not bound</b>: no PV available or dynamic provisioning failed.</li><li><b>Volume topology mismatch</b>: EBS in wrong zone.</li></ul>",
      },
      {
        q: "How do you debug `ImagePullBackOff` / `ErrImagePull`?",
        difficulty: "easy",
        a: "<p><code>kubectl describe pod</code> — Events show the exact error:</p><ul><li><b>Wrong image name/tag</b>: typo, missing tag, or tag deleted from registry.</li><li><b>Private registry, no imagePullSecret</b>: create the secret, reference it in the pod spec (or the pod's ServiceAccount).</li><li><b>Rate-limited</b> (Docker Hub anonymous limits): authenticate.</li><li><b>Wrong architecture</b>: <code>exec format error</code> — image built for amd64 but running on arm64.</li></ul>",
      },
      {
        q: "How do you debug a Service that isn't routing traffic?",
        difficulty: "hard",
        a: "<ol><li>Verify the Service exists: <code>kubectl get svc</code>.</li><li>Verify Endpoints are populated: <code>kubectl get endpoints &lt;svc&gt;</code>. Empty? → selector doesn't match any pod's labels.</li><li>Confirm backend pods are <code>Ready</code> (readiness probe passing) — unready pods are removed from Endpoints.</li><li>Test from inside the cluster: <code>kubectl run tmp --image=nicolaka/netshoot --rm -it -- curl svc-name.ns:port</code>.</li><li>Check NetworkPolicies: any policy blocking the traffic?</li><li>Check <code>kube-proxy</code> logs on the node.</li></ol>",
        followups: [
          {
            q: "Endpoints is empty even though the pod is Running — what next?",
            a: "<p>Almost always a label mismatch between Service selector and Pod labels.</p><pre>kubectl get svc api -o jsonpath='{.spec.selector}'\nkubectl get pods --show-labels</pre><p>Also check the pod's readiness — pods that fail the readiness probe won't appear in Endpoints.</p>",
          },
        ],
      },
      {
        q: "How would you troubleshoot a broken control plane component?",
        difficulty: "hard",
        a: "<p>In kubeadm clusters, control plane components run as <b>static pods</b> managed by kubelet from <code>/etc/kubernetes/manifests/</code>.</p><ol><li>SSH to the control plane node.</li><li>Check kubelet logs: <code>journalctl -u kubelet -f</code>.</li><li>Check the component's container logs: <code>crictl ps</code> → <code>crictl logs &lt;id&gt;</code>.</li><li>Validate the static manifest YAML (indentation errors are common).</li><li>Check certs: <code>kubeadm certs check-expiration</code>.</li><li>Verify etcd is healthy: <code>etcdctl endpoint health</code>.</li></ol>",
      },

      /* ============ ADVANCED / SENIOR ============ */
      {
        q: "What are Custom Resource Definitions (CRDs)?",
        difficulty: "medium",
        a: "<p>A CRD extends the Kubernetes API with your own resource types. Once installed, you can <code>kubectl apply -f mything.yaml</code> just like any built-in resource. K8s stores them in etcd and gives you a full API (list, watch, RBAC, etc.).</p><pre>apiVersion: apiextensions.k8s.io/v1\nkind: CustomResourceDefinition\nmetadata: { name: pgdatabases.acme.io }\nspec:\n  group: acme.io\n  scope: Namespaced\n  names: { plural: pgdatabases, singular: pgdatabase, kind: PGDatabase }\n  versions:\n  - name: v1\n    served: true\n    storage: true\n    schema:\n      openAPIV3Schema: { ... }</pre><p>By themselves, CRDs are just data. To make them <i>do</i> something you write a <b>controller/operator</b>.</p>",
      },
      {
        q: "What is the Operator pattern?",
        difficulty: "hard",
        a: "<p>An <b>Operator</b> = <b>CRD + custom controller</b> that encodes operational knowledge for a complex application (a database, a message queue, etc.).</p><p>The controller watches the CRD and reconciles reality toward the desired spec — provisioning storage, running failovers, applying schema migrations, taking backups.</p><p>Examples: <b>Prometheus Operator</b>, <b>PostgreSQL (CloudNativePG / Zalando)</b>, <b>Elasticsearch (ECK)</b>, <b>Strimzi (Kafka)</b>.</p><p>Build one with the <b>Operator SDK</b> or <b>Kubebuilder</b>.</p>",
        followups: [
          {
            q: "Explain the reconciliation loop pattern.",
            a: "<ol><li>Watch a resource (via the API server's watch API).</li><li>On each event, read <b>desired state</b> (spec) and <b>actual state</b> (status + real world).</li><li>Compute the diff.</li><li>Take action to bring actual closer to desired.</li><li>Update status.</li><li>Requeue for re-processing if not converged.</li></ol><p>Controllers must be <b>idempotent</b> and <b>level-triggered</b> — they should work correctly even if they miss events.</p>",
          },
        ],
      },
      {
        q: "Difference between `kubectl apply`, `create`, `replace`, and `patch`.",
        difficulty: "medium",
        a: "<ul><li><b>create</b>: creates the object; fails if it exists. Imperative.</li><li><b>replace</b>: replaces the whole object; fails if it doesn't exist. Wipes fields not in the file.</li><li><b>patch</b>: partial update (strategic merge, JSON merge, or JSON patch).</li><li><b>apply</b>: declarative — computes the diff between your YAML and current state, applies only the diff. Tracks its own field ownership via annotations (<b>client-side apply</b>) or Server-Side Apply managers.</li></ul>",
        followups: [
          {
            q: "What is Server-Side Apply?",
            a: "<p>An API-server-side implementation of <code>apply</code> (GA in 1.22). The server tracks which controller/user \"owns\" each field via <b>field managers</b>. Multiple controllers can safely modify different fields of the same object without stomping on each other. Recommended for controllers/operators.</p>",
          },
        ],
      },
      {
        q: "How would you design a highly-available multi-master cluster?",
        difficulty: "hard",
        a: "<ol><li><b>3+ control plane nodes</b> spread across AZs, each running <code>kube-apiserver</code>, <code>controller-manager</code>, <code>scheduler</code>, and an etcd member.</li><li><b>Load balancer</b> (external NLB, HAProxy, or keepalived+VIP) in front of the 3 API servers, with health-checked TCP:6443.</li><li><b>Stacked etcd</b> (etcd on the control plane nodes) is simplest; <b>external etcd</b> (dedicated etcd cluster) isolates blast radius.</li><li>Worker nodes point kubelet at the LB hostname, not a specific master.</li><li>PodDisruptionBudgets on control plane components (managed cluster) and any critical add-ons (CoreDNS, ingress controllers).</li><li>Regular etcd backups + tested restore runbook.</li></ol>",
      },
      {
        q: "How does leader election work for controllers?",
        difficulty: "hard",
        a: "<p>Only one instance of the controller-manager (and scheduler) should be <i>active</i> — leader election ensures that. It uses a <b>Lease</b> object (<code>coordination.k8s.io/v1</code>):</p><ul><li>Each replica tries to acquire the Lease by updating it with its identity + renewTime.</li><li>The one who gets the update becomes leader and continuously renews.</li><li>If it stops renewing, another instance takes over after the lease expires.</li></ul><pre>kubectl -n kube-system get lease</pre><p>Custom controllers implement the same via <code>client-go/tools/leaderelection</code>.</p>",
      },
      {
        q: "How does the HPA calculate desired replicas?",
        difficulty: "hard",
        a: "<p>Every ~15s the HPA controller:</p><ol><li>Fetches current metric values from metrics-server / custom metrics API.</li><li>Computes <code>desiredReplicas = ceil(currentReplicas × (currentMetric / targetMetric))</code>.</li><li>Applies tolerance (default 10%) — no change if within tolerance.</li><li>Applies scaling policies (max scale-up rate, stabilization window for scale-down to avoid flapping).</li><li>Writes new <code>spec.replicas</code> on the target Deployment.</li></ol><p>Multiple metrics: HPA computes desired for each and picks the <b>max</b>.</p>",
      },

    ],
  },

  /* ============================================================ */
  {
    id: "helm",
    name: "Helm",
    icon: "⎈",
    kind: "qa",
    description:
      "Helm — the package manager for Kubernetes. Charts, templates, values, releases, hooks, dependencies, and best practices.",
    items: [
      {
        q: "What is Helm and why is it needed for Kubernetes?",
        difficulty: "easy",
        a: "<p><b>Helm</b> is the de facto package manager for Kubernetes. It solves a problem that emerges quickly when working with Kubernetes at scale: managing collections of related YAML manifests across environments, versions, and installations becomes unwieldy without abstraction.</p><p><b>The problem Helm solves:</b> A production Kubernetes application typically consists of multiple related resources — Deployments, Services, ConfigMaps, Secrets, Ingresses, ServiceAccounts, HorizontalPodAutoscalers, and more. Deploying the same application to dev, staging, and production requires environment-specific values (image tags, replica counts, resource limits, hostnames). Duplicating YAML files per environment is error-prone; using tools like <code>sed</code> or <code>envsubst</code> is fragile; and installing off-the-shelf software (PostgreSQL, Redis, Prometheus, Ingress-NGINX) demands hundreds of lines of manifest engineering.</p><p><b>What Helm provides:</b></p><ul><li><b>Charts:</b> Packages that bundle related Kubernetes manifests as templates with default values. A single chart can produce hundreds of resources with one command.</li><li><b>Templating engine:</b> Go template–based rendering that substitutes values into manifest templates, enabling one chart to serve many environments.</li><li><b>Releases:</b> Each installation of a chart is a named release with a version history. Helm tracks what was deployed and enables rollbacks.</li><li><b>Repositories:</b> Public and private stores of charts. The Bitnami, Artifact Hub, and community repositories host thousands of curated charts for common software.</li><li><b>Lifecycle management:</b> <code>helm install</code>, <code>helm upgrade</code>, <code>helm rollback</code>, and <code>helm uninstall</code> operate on a release as a unit.</li></ul><p><b>Typical workflow:</b></p><pre>helm repo add bitnami https://charts.bitnami.com/bitnami\nhelm repo update\nhelm install my-db bitnami/postgresql --namespace data \\\n  --set auth.postgresPassword=secret \\\n  --set primary.persistence.size=50Gi\nhelm upgrade my-db bitnami/postgresql --values prod-values.yaml\nhelm rollback my-db 1</pre><p>Helm is now considered a fundamental Kubernetes tool, on par with <code>kubectl</code> for any team running non-trivial workloads.</p>",
      },
      {
        q: "Explain the structure of a Helm chart.",
        difficulty: "medium",
        a: "<p>A <b>Helm chart</b> is a directory with a defined structure that Helm knows how to interpret. Running <code>helm create mychart</code> scaffolds the standard layout, which every chart follows:</p><pre>mychart/\n├── Chart.yaml           # Chart metadata (name, version, appVersion)\n├── values.yaml          # Default configuration values\n├── charts/              # Subchart dependencies (embedded or downloaded)\n├── templates/           # Kubernetes manifest templates\n│   ├── deployment.yaml\n│   ├── service.yaml\n│   ├── ingress.yaml\n│   ├── configmap.yaml\n│   ├── _helpers.tpl     # Reusable template snippets\n│   └── NOTES.txt        # Message shown after install\n├── crds/                # Custom Resource Definitions (installed first)\n└── .helmignore          # Files to exclude from packaging</pre><p><b>Key files explained:</b></p><ul><li><b>Chart.yaml:</b> The manifest of the chart itself. Contains <code>name</code>, <code>version</code> (the chart's own version, in SemVer), <code>appVersion</code> (the version of the application packaged), <code>description</code>, and any dependencies.</li><li><b>values.yaml:</b> Default values for all templated fields. Users override these at install time with <code>--set</code> flags or a custom <code>values.yaml</code>. The best-designed charts expose sensible defaults for common needs and enable customization for advanced cases.</li><li><b>templates/:</b> The heart of the chart. Each file is a Kubernetes manifest interspersed with Go template directives (<code>{{ .Values.image.repository }}</code>, <code>{{ if .Values.ingress.enabled }}</code>). Helm renders these templates using the merged values and applies them to the cluster.</li><li><b>_helpers.tpl:</b> A conventional file containing reusable template snippets (named templates) — for example, a common label block used across every manifest. Referenced with <code>{{ include \"mychart.labels\" . }}</code>.</li><li><b>NOTES.txt:</b> A templated message printed after successful install/upgrade. Used to tell users how to access the deployed application.</li><li><b>crds/:</b> Custom Resource Definitions. Helm installs these before rendering the rest of the chart, ensuring the CRDs exist before manifests that reference them.</li></ul>",
      },
      {
        q: "What is the difference between values.yaml, --set, and --values in Helm?",
        difficulty: "medium",
        a: "<p>Helm merges configuration values from multiple sources with a well-defined precedence, allowing flexible customization at different levels.</p><p><b>Precedence (lowest to highest):</b></p><ol><li><b>Chart's default values.yaml:</b> Ships with the chart. Defines the baseline configuration.</li><li><b>Parent chart's values (for subcharts):</b> A parent chart can override subchart defaults.</li><li><b>-f / --values flag:</b> One or more YAML files provided at the command line. Later files override earlier ones.</li><li><b>--set / --set-string / --set-file flags:</b> Command-line overrides for individual keys. Applied last, so they win.</li></ol><p><b>Practical usage of each:</b></p><ul><li><b>values.yaml (chart default):</b> Should contain sensible defaults for a typical installation. Well-maintained charts have this heavily commented, documenting each option.</li><li><b>Custom -f values file:</b> The preferred approach for real deployments. Store the file in version control alongside your GitOps configuration.<pre># prod-values.yaml\nreplicaCount: 5\nimage:\n  tag: 1.4.2\nresources:\n  limits:\n    cpu: 1000m\n    memory: 2Gi</pre><pre>helm upgrade myapp ./mychart -f prod-values.yaml</pre></li><li><b>--set for one-off overrides:</b> Useful for CI/CD pipelines that pass dynamic values (like the current image tag) or for ad-hoc changes.<pre>helm upgrade myapp ./mychart -f prod-values.yaml \\\n  --set image.tag=${{ github.sha }}</pre></li></ul><p><b>Special --set variants:</b></p><ul><li><code>--set-string</code>: Forces the value to be treated as a string (important for values that look numeric or boolean, like a version tag \"1.0\" or \"true\").</li><li><code>--set-file</code>: Reads the value from a local file. Useful for embedding certificates, keys, or config blobs without escaping issues.</li></ul><p><b>Best practice:</b> Store per-environment values in versioned <code>-f values-<env>.yaml</code> files, and reserve <code>--set</code> only for CI-supplied dynamic values (image tags, build numbers).</p>",
      },
      {
        q: "How does Helm's templating engine work?",
        difficulty: "medium",
        a: "<p>Helm's templating engine is built on <b>Go text/template</b>, augmented with the <b>Sprig</b> function library and Helm-specific helpers. It takes template files from a chart's <code>templates/</code> directory and merges them with a values object to produce final Kubernetes manifests.</p><p><b>Template basics:</b></p><ul><li><b>{{ ... }}:</b> Delimits template expressions. Anything inside is evaluated at render time.</li><li><b>.Values:</b> The merged values object (defaults + user overrides).</li><li><b>.Release:</b> Metadata about the current release (<code>.Release.Name</code>, <code>.Release.Namespace</code>).</li><li><b>.Chart:</b> Metadata from Chart.yaml.</li><li><b>.Files:</b> Access to non-template files in the chart (config files, certificates).</li><li><b>.Capabilities:</b> Information about the cluster (Kubernetes version, available API resources).</li></ul><p><b>Example template snippet (templates/deployment.yaml):</b></p><pre>apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: {{ include \"mychart.fullname\" . }}\n  labels:\n    {{- include \"mychart.labels\" . | nindent 4 }}\nspec:\n  replicas: {{ .Values.replicaCount }}\n  selector:\n    matchLabels:\n      {{- include \"mychart.selectorLabels\" . | nindent 6 }}\n  template:\n    metadata:\n      labels:\n        {{- include \"mychart.selectorLabels\" . | nindent 8 }}\n    spec:\n      containers:\n        - name: {{ .Chart.Name }}\n          image: \"{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}\"\n          resources:\n            {{- toYaml .Values.resources | nindent 12 }}\n          {{- if .Values.env }}\n          env:\n            {{- range $key, $value := .Values.env }}\n            - name: {{ $key }}\n              value: {{ $value | quote }}\n            {{- end }}\n          {{- end }}</pre><p><b>Key concepts illustrated:</b></p><ul><li><b>{{ include \"template.name\" . }}:</b> Invokes a named template (defined in <code>_helpers.tpl</code>) with the current context.</li><li><b>{{- ... -}}:</b> The minus signs trim whitespace before/after the directive, keeping rendered YAML clean.</li><li><b>| pipe operators:</b> Chain functions. <code>{{ .Values.tag | default \"latest\" | quote }}</code> takes the tag, applies default \"latest\" if empty, and wraps in quotes.</li><li><b>{{ if / range / with }}:</b> Control flow — conditionals, iteration, and scope changes.</li><li><b>toYaml + nindent:</b> Serialize a values object into YAML and indent by a fixed amount — essential for embedding structured values from values.yaml into manifest templates.</li></ul><p><b>Rendering (dry-run):</b> <code>helm template mychart ./mychart -f prod-values.yaml</code> renders the templates without applying them, showing the final YAML that Helm would send to Kubernetes.</p>",
        followups: [
          {
            q: "What are named templates and _helpers.tpl?",
            a: "<p><b>Named templates</b> are reusable snippets defined once and included throughout a chart, avoiding duplication. They are the equivalent of functions in a general-purpose language. By convention, they live in <code>templates/_helpers.tpl</code> — files prefixed with underscore are treated as non-rendered partials rather than manifest sources.</p><p><b>Example _helpers.tpl:</b></p><pre>{{/*\nCommon labels applied to every resource in this chart\n*/}}\n{{- define \"mychart.labels\" -}}\napp.kubernetes.io/name: {{ include \"mychart.name\" . }}\nhelm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}\napp.kubernetes.io/instance: {{ .Release.Name }}\napp.kubernetes.io/version: {{ .Chart.AppVersion | quote }}\napp.kubernetes.io/managed-by: {{ .Release.Service }}\n{{- end -}}\n\n{{/*\nSelector labels — a subset of common labels used in selectors\n*/}}\n{{- define \"mychart.selectorLabels\" -}}\napp.kubernetes.io/name: {{ include \"mychart.name\" . }}\napp.kubernetes.io/instance: {{ .Release.Name }}\n{{- end -}}\n\n{{/*\nFully qualified name for resources\n*/}}\n{{- define \"mychart.fullname\" -}}\n{{- $name := default .Chart.Name .Values.nameOverride }}\n{{- printf \"%s-%s\" .Release.Name $name | trunc 63 | trimSuffix \"-\" }}\n{{- end -}}</pre><p><b>Usage from a template:</b></p><pre>metadata:\n  name: {{ include \"mychart.fullname\" . }}\n  labels:\n    {{- include \"mychart.labels\" . | nindent 4 }}</pre><p><b>Why include not template?</b> Both invoke a named template, but <code>include</code> returns the result as a string, allowing further pipe operations like <code>| nindent 4</code>. <code>template</code> writes output directly and cannot be piped. Modern charts almost always use <code>include</code>.</p>",
          },
        ],
      },
      {
        q: "How do you manage subchart/chart dependencies in Helm?",
        difficulty: "medium",
        a: "<p>A Helm chart can depend on other charts, allowing large applications to be composed from smaller pieces. For example, an application chart might depend on <code>postgresql</code>, <code>redis</code>, and <code>rabbitmq</code> charts rather than reinventing them.</p><p><b>Declaring dependencies in Chart.yaml:</b></p><pre>apiVersion: v2\nname: my-app\nversion: 1.0.0\nappVersion: \"2.5.0\"\n\ndependencies:\n  - name: postgresql\n    version: \"13.2.0\"\n    repository: \"https://charts.bitnami.com/bitnami\"\n    condition: postgresql.enabled\n    alias: primary-db\n  - name: redis\n    version: \"18.6.1\"\n    repository: \"https://charts.bitnami.com/bitnami\"\n    condition: redis.enabled\n    tags:\n      - cache</pre><p><b>Key options for dependencies:</b></p><ul><li><b>condition:</b> A path in values.yaml that must be truthy for the dependency to be installed. Enables optional subcharts.</li><li><b>tags:</b> Group dependencies for bulk enable/disable via <code>tags:</code> in values.yaml.</li><li><b>alias:</b> Install the same chart multiple times under different names (e.g., separate PostgreSQL instances for two microservices).</li><li><b>import-values:</b> Import specific values from subcharts into the parent's scope for cleaner referencing.</li></ul><p><b>Building dependencies:</b> After declaring dependencies, run:</p><pre>helm dependency update</pre><p>This downloads each dependency chart as a <code>.tgz</code> file into the <code>charts/</code> subdirectory and creates a <code>Chart.lock</code> file capturing exact resolved versions — similar to <code>package-lock.json</code> or <code>Cargo.lock</code>.</p><p><b>Overriding subchart values:</b> Values for a subchart are provided in the parent's values.yaml under a top-level key matching the subchart name (or alias):</p><pre># parent values.yaml\npostgresql:\n  enabled: true\n  auth:\n    postgresPassword: secret\n    database: myapp\n  primary:\n    persistence:\n      size: 20Gi\n\nredis:\n  enabled: true\n  auth:\n    enabled: false</pre><p><b>Best practices:</b></p><ul><li>Pin dependency versions precisely — avoid version ranges that could pull in breaking changes.</li><li>Vet subcharts from external repos for security before adoption.</li><li>Use conditions and tags to make subcharts optional; this makes the chart flexible across environments (e.g., use managed cloud databases in production but embed a PostgreSQL subchart for dev).</li></ul>",
      },
      {
        q: "What are Helm hooks and when should you use them?",
        difficulty: "medium",
        a: "<p><b>Helm hooks</b> are annotations that mark specific manifests to run at defined points in a release's lifecycle — before/after install, upgrade, rollback, or delete. Hooks enable operations that must happen at particular moments during a Helm operation, such as database migrations or backups.</p><p><b>Hook events:</b></p><ul><li><code>pre-install</code>: Runs after templates are rendered but before any resources are created.</li><li><code>post-install</code>: Runs after all resources are loaded.</li><li><code>pre-upgrade</code> / <code>post-upgrade</code>: Same points during an upgrade.</li><li><code>pre-delete</code> / <code>post-delete</code>: Same points during uninstall.</li><li><code>pre-rollback</code> / <code>post-rollback</code>: Same points during rollback.</li><li><code>test</code>: Runs when <code>helm test</code> is invoked, for validating the release.</li></ul><p><b>Common use case — database migration Job that must complete before the application starts:</b></p><pre>apiVersion: batch/v1\nkind: Job\nmetadata:\n  name: {{ include \"mychart.fullname\" . }}-migrate\n  annotations:\n    \"helm.sh/hook\": pre-upgrade,pre-install\n    \"helm.sh/hook-weight\": \"-5\"\n    \"helm.sh/hook-delete-policy\": before-hook-creation,hook-succeeded\nspec:\n  template:\n    spec:\n      restartPolicy: OnFailure\n      containers:\n        - name: migrate\n          image: \"{{ .Values.image.repository }}:{{ .Values.image.tag }}\"\n          command: [\"./manage.py\", \"migrate\"]</pre><p><b>Key hook annotations:</b></p><ul><li><b>helm.sh/hook:</b> The event(s) this hook responds to. Multiple values can be comma-separated.</li><li><b>helm.sh/hook-weight:</b> Ordering within a hook phase. Lower values run first. Useful when multiple hooks respond to the same event.</li><li><b>helm.sh/hook-delete-policy:</b> When the hook's resource should be deleted — <code>before-hook-creation</code> (delete previous instance before running), <code>hook-succeeded</code> (delete after success), <code>hook-failed</code> (delete after failure).</li></ul><p><b>Trade-offs and warnings:</b></p><ul><li>Hooks are managed outside the normal release lifecycle — they are not tracked as part of the release. This means Helm cannot roll them back automatically.</li><li>Long-running hook Jobs delay the entire Helm operation. Design hooks to be idempotent and reasonably fast.</li><li>For database migrations, consider whether a hook is the right pattern — some teams prefer separate migration tooling triggered by their CI/CD pipeline for finer control.</li></ul>",
      },
      {
        q: "How does Helm handle upgrades and rollbacks?",
        difficulty: "medium",
        a: "<p>Helm treats each installation of a chart as a versioned <b>release</b>. Every upgrade produces a new revision, and each revision is preserved in the release history, enabling straightforward rollback to any previous state.</p><p><b>Upgrade flow:</b></p><pre>helm upgrade my-app ./mychart -f values.yaml</pre><ol><li>Helm renders the new manifests using the current values.</li><li>It computes the diff between the previous revision's manifests and the new ones.</li><li>It applies changes to the cluster — creating new resources, updating changed ones, and deleting resources that are no longer part of the release.</li><li>Release metadata is updated with a new revision number.</li></ol><p><b>Viewing release history:</b></p><pre>helm history my-app\nREVISION  UPDATED                   STATUS      CHART      APP VERSION  DESCRIPTION\n1         2024-01-15 10:00:00       superseded  myapp-1.0  2.0.0        Install complete\n2         2024-01-15 14:30:00       superseded  myapp-1.1  2.1.0        Upgrade complete\n3         2024-01-16 09:15:00       deployed    myapp-1.2  2.2.0        Upgrade complete</pre><p><b>Rollback:</b></p><pre>helm rollback my-app 2       # revert to revision 2\nhelm rollback my-app         # revert to the last successful revision</pre><p>Rollback performs the same diffing operation in reverse — resources introduced in the current revision are removed, changed resources are reverted, and previously deleted resources are re-created.</p><p><b>Important considerations for upgrades:</b></p><ul><li><b>--atomic:</b> If the upgrade fails, automatically roll back to the previous revision. Highly recommended for CI/CD pipelines.<pre>helm upgrade my-app ./mychart --atomic --timeout 5m</pre></li><li><b>--wait:</b> Block until all resources reach a ready state (or timeout). Ensures the upgrade actually completed successfully before returning.</li><li><b>--dry-run:</b> Show what would be applied without making changes. Useful for reviewing upgrades before executing.</li><li><b>Immutable fields:</b> Some Kubernetes fields cannot be changed after creation (e.g., a Service's <code>clusterIP</code>, a StatefulSet's <code>selector</code>). Helm upgrades that attempt such changes will fail. Sometimes the resource must be deleted and recreated manually.</li><li><b>Release history retention:</b> By default, Helm retains 10 revisions. Older revisions are pruned. Configure with <code>--history-max</code>.</li></ul>",
      },
      {
        q: "How do you write a Helm chart for your own application?",
        difficulty: "medium",
        a: "<p>Writing a chart for a custom application is a common task. The standard approach uses <code>helm create</code> to scaffold a chart, then customizes the templates and values to match the application's needs.</p><p><b>Step 1 — Scaffold:</b></p><pre>helm create my-service\ncd my-service\ntree\n# Chart.yaml, values.yaml, templates/, .helmignore</pre><p><code>helm create</code> generates a working chart for a simple stateless web application — a Deployment, Service, Ingress, and ServiceAccount — with best-practice labeling and helper templates.</p><p><b>Step 2 — Customize Chart.yaml:</b></p><pre>apiVersion: v2\nname: my-service\ndescription: A microservice for order processing\ntype: application\nversion: 0.1.0        # chart version — bump on each change\nappVersion: \"1.0.0\"   # the app version this chart deploys\nmaintainers:\n  - name: Platform Team\n    email: platform@example.com</pre><p><b>Step 3 — Design values.yaml:</b> Expose parameters that will vary between environments. Provide sensible defaults, document each option in comments, and structure related values into nested objects.</p><pre>image:\n  repository: myregistry/my-service\n  tag: \"\"           # empty defaults to .Chart.AppVersion\n  pullPolicy: IfNotPresent\n\nreplicaCount: 3\n\nresources:\n  requests:\n    cpu: 100m\n    memory: 128Mi\n  limits:\n    cpu: 500m\n    memory: 512Mi\n\nservice:\n  type: ClusterIP\n  port: 80\n\ningress:\n  enabled: false\n  className: nginx\n  hosts:\n    - host: my-service.example.com\n      paths:\n        - path: /\n          pathType: Prefix\n\nenv: {}\n\npostgresql:\n  enabled: false\n  # subchart config here</pre><p><b>Step 4 — Adjust templates:</b> The scaffolded templates cover common needs. Extend them for application-specific requirements — probes, volumes, sidecars, service account permissions, PDBs, HPAs, etc.</p><p><b>Step 5 — Test locally:</b></p><pre>helm lint ./my-service                       # syntax check\nhelm template ./my-service -f values-dev.yaml # render templates\nhelm install my-service ./my-service --dry-run --debug\nhelm install my-service ./my-service --namespace test</pre><p><b>Step 6 — Package and publish (optional):</b></p><pre>helm package ./my-service\nhelm push my-service-0.1.0.tgz oci://my-registry/charts</pre><p><b>Best practices:</b> Include a chart README, use standard <code>app.kubernetes.io/*</code> labels via a helpers template, expose reasonable but not excessive configurability (charts with 200+ values become unmaintainable), version the chart with SemVer and bump on every change.</p>",
      },
      {
        q: "What is the difference between Helm and Kustomize?",
        difficulty: "medium",
        a: "<p><b>Helm</b> and <b>Kustomize</b> are the two dominant tools for managing Kubernetes manifests. They solve overlapping problems with fundamentally different philosophies.</p><p><b>Helm — templating-based:</b> Manifests are Go templates with placeholder expressions. At render time, values are merged into templates to produce final YAML. The template language allows loops, conditionals, and function calls — full programmatic control.</p><p><b>Kustomize — overlay-based:</b> Manifests are plain, valid YAML files. Kustomize applies patches (overlays) declaratively to a base set of manifests, producing environment-specific variants without ever templating text. It ships built into <code>kubectl</code> as <code>kubectl apply -k</code>.</p><p><b>Comparison table:</b></p><table><tr><th>Aspect</th><th>Helm</th><th>Kustomize</th></tr><tr><td>Approach</td><td>Text templating</td><td>YAML patching</td></tr><tr><td>Base file validity</td><td>Templates are not valid YAML</td><td>All files are valid Kubernetes YAML</td></tr><tr><td>Packaging</td><td>Yes (charts, versioning, repositories)</td><td>No — just a directory structure</td></tr><tr><td>Release tracking</td><td>Yes — full history, rollback, upgrade</td><td>No — Kustomize just renders, kubectl applies</td></tr><tr><td>Learning curve</td><td>Steeper — Go templates, Sprig, helpers</td><td>Gentler — just patches and merges</td></tr><tr><td>External charts</td><td>Rich ecosystem of pre-built charts</td><td>Fewer pre-built bases</td></tr><tr><td>Complex logic</td><td>Fully supported via templating</td><td>Limited to strategic merge patches and JSON patches</td></tr></table><p><b>When to prefer Helm:</b></p><ul><li>Packaging complex applications with dozens of configurable options.</li><li>Distributing charts publicly or across teams.</li><li>Consuming off-the-shelf charts for common software (Postgres, Redis, Prometheus).</li><li>Needing release lifecycle management (install/upgrade/rollback).</li></ul><p><b>When to prefer Kustomize:</b></p><ul><li>Simpler applications with mostly-static manifests plus environment overlays.</li><li>Preference for keeping all manifests as valid YAML for editor tooling and reviews.</li><li>GitOps workflows where ArgoCD or Flux applies manifests directly.</li><li>Avoiding a dedicated templating language.</li></ul><p><b>Combining both:</b> A common pattern is to use Helm to render a chart, then apply Kustomize patches on top for organization-specific tweaks. ArgoCD supports this natively via <code>helm.template</code> + <code>kustomize.patches</code>.</p>",
      },
      {
        q: "What is Helm chart security and how do you handle it?",
        difficulty: "hard",
        a: "<p>Helm charts run arbitrary Kubernetes resources with the permissions of the user running Helm, and they often reference container images from external registries. This makes chart security an important consideration, particularly for charts sourced from public repositories.</p><p><b>Key security concerns:</b></p><ul><li><b>Malicious or vulnerable charts:</b> A public chart could install resources granting cluster-admin privileges, deploy compromised container images, or exfiltrate data.</li><li><b>Secret handling:</b> Charts often accept passwords and keys as values. If these are committed to Git in plain text, they leak.</li><li><b>Image provenance:</b> The container images referenced by a chart may have their own vulnerabilities.</li><li><b>Cluster RBAC exposure:</b> Some charts request wide RBAC permissions (ClusterRoleBindings) that may be inappropriate.</li></ul><p><b>Security practices:</b></p><ol><li><b>Verify chart source:</b> Prefer charts from established, active repositories — Bitnami, official upstream projects, or your organization's private repo. Vet third-party charts by reviewing their templates before installation.</li><li><b>Pin chart versions:</b> Always specify explicit versions in <code>dependencies</code> and <code>helm install --version</code>. Never use \"latest\" or version ranges that could pull in unreviewed changes.</li><li><b>Chart signing and provenance:</b> Helm supports signing charts with GPG keys, producing a <code>.prov</code> file alongside the packaged chart. Verify signatures with <code>helm install --verify</code>.<pre>helm package --sign --key 'user@example.com' ./mychart\nhelm install --verify my-app ./mychart-1.0.0.tgz</pre></li><li><b>Scan chart contents:</b> Tools like <code>checkov</code>, <code>kubesec</code>, and <code>trivy config</code> analyze rendered manifests for security issues — privileged containers, missing resource limits, permissive RBAC.<pre>helm template ./mychart | checkov -f -\nhelm template ./mychart | trivy config -</pre></li><li><b>Scan referenced images:</b> Chart values reference images; scan those images with Trivy, Grype, or your registry's built-in scanner.</li><li><b>Handle secrets securely:</b><ul><li>Never commit plaintext secrets in values.yaml.</li><li>Use <b>Helm Secrets</b> (SOPS-based encryption) or <b>External Secrets Operator</b> to fetch secrets at runtime from Vault, AWS Secrets Manager, or Azure Key Vault.</li><li>For CI/CD, inject sensitive values via <code>--set</code> from environment variables sourced from a secure store.</li></ul></li><li><b>Restrict Tiller-era permissions (Helm 2 only):</b> Helm 2 required a server-side component (Tiller) with cluster-admin privileges. Helm 3 removed this — Helm now uses the caller's kubeconfig credentials directly, following normal RBAC. Migrate off Helm 2 if you haven't already.</li><li><b>Cluster-side policy enforcement:</b> Use OPA Gatekeeper or Kyverno to enforce cluster policies (no privileged containers, mandatory labels, image registry allowlists) regardless of what Helm charts try to install.</li></ol>",
      },
      {
        q: "How do you use Helm in a GitOps workflow with ArgoCD or Flux?",
        difficulty: "medium",
        a: "<p>GitOps tools like <b>ArgoCD</b> and <b>Flux</b> integrate deeply with Helm, using charts as the source of manifests while managing the delivery lifecycle themselves. This changes how Helm is used: rather than running <code>helm install</code> or <code>helm upgrade</code> from a CI pipeline, the GitOps controller renders and applies Helm charts based on Git state.</p><p><b>ArgoCD + Helm pattern:</b></p><pre>apiVersion: argoproj.io/v1alpha1\nkind: Application\nmetadata:\n  name: my-app\n  namespace: argocd\nspec:\n  project: default\n  source:\n    repoURL: https://charts.bitnami.com/bitnami\n    chart: postgresql\n    targetRevision: 13.2.0\n    helm:\n      releaseName: my-db\n      valueFiles:\n        - values-prod.yaml\n      values: |\n        auth:\n          postgresPassword: from-vault\n        primary:\n          persistence:\n            size: 50Gi\n  destination:\n    server: https://kubernetes.default.svc\n    namespace: data\n  syncPolicy:\n    automated:\n      prune: true\n      selfHeal: true</pre><p>ArgoCD internally runs <code>helm template</code> (not <code>helm install</code>) — it renders the chart to plain manifests, then applies them via its own reconciliation engine. This gives ArgoCD full ownership of the release lifecycle while leveraging Helm as a packaging and templating mechanism.</p><p><b>Flux + Helm pattern:</b> Flux uses a dedicated <code>HelmRelease</code> custom resource with an underlying <code>HelmController</code>. Unlike ArgoCD, Flux does run actual <code>helm install/upgrade</code> operations inside the cluster, preserving Helm release history and enabling <code>helm rollback</code>.</p><pre>apiVersion: helm.toolkit.fluxcd.io/v2\nkind: HelmRelease\nmetadata:\n  name: my-app\n  namespace: default\nspec:\n  interval: 5m\n  chart:\n    spec:\n      chart: my-service\n      version: '1.2.x'\n      sourceRef:\n        kind: HelmRepository\n        name: my-charts\n  values:\n    replicaCount: 3\n    image:\n      tag: 1.2.5</pre><p><b>Best practices for Helm + GitOps:</b></p><ul><li><b>Chart in a separate repo:</b> Store the chart in a dedicated repository or under <code>charts/</code> in the application repo. Version the chart with SemVer.</li><li><b>Environment-specific values in a config repo:</b> A separate GitOps repo holds environment-specific <code>values-*.yaml</code> files referenced by ArgoCD Applications. This separates concerns cleanly.</li><li><b>Automated sync with self-heal:</b> Enables the platform to correct drift automatically and eliminates manual <code>helm upgrade</code> commands.</li><li><b>Progressive delivery:</b> Combine Helm with Argo Rollouts or Flagger for canary/blue-green deployments driven by GitOps state.</li></ul>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "jenkins",
    name: "Jenkins & CI/CD",
    icon: "🔧",
    kind: "qa",
    description:
      "Pipelines, agents, shared libraries, and pragmatic patterns for reliable delivery automation.",
    items: [
      {
        q: "What is a Jenkinsfile and why use Declarative Pipeline?",
        difficulty: "easy",
        a: "<p>A <b>Jenkinsfile</b> is a text file that defines a Jenkins build pipeline as code. It is committed to the application's source repository alongside the code it builds, providing several important properties:</p><ul><li><b>Versioned:</b> Every change to the pipeline is tracked in Git alongside the code.</li><li><b>Reviewable:</b> Pipeline changes go through the same pull request and review process as code changes.</li><li><b>Reproducible:</b> The pipeline definition is co-located with the code it builds, so any historical build can be reconstructed.</li><li><b>Portable:</b> Multiple Jenkins instances or environments can execute the same pipeline definition.</li></ul><p>Jenkins supports two pipeline syntaxes:</p><p><b>Declarative Pipeline</b> uses a structured, rigid syntax with the outer form <code>pipeline { agent ... stages { ... } }</code>. It enforces a defined structure, provides comprehensive validation, and integrates cleanly with the Jenkins UI (Blue Ocean, stage view, restart-from-stage). It is designed to be readable and maintainable by teams.</p><p><b>Scripted Pipeline</b> is essentially raw Groovy code with more flexibility but significantly less structure. It is powerful for complex logic but harder to maintain and review.</p><p><b>Guidance:</b> Start with Declarative for all new pipelines. When escape hatches to Groovy are needed for edge cases, embed a <code>script { }</code> block within a Declarative pipeline rather than converting the entire pipeline to Scripted.</p>",
        followups: [
          {
            q: "Show a minimal Declarative Jenkinsfile.",
            a: "<p>A minimal but production-representative Declarative Jenkinsfile:</p><pre>pipeline {\n  agent any\n\n  stages {\n    stage('Build') {\n      steps { sh 'mvn -B clean package' }\n    }\n    stage('Test') {\n      steps { sh 'mvn test' }\n    }\n    stage('Deploy') {\n      when { branch 'main' }\n      steps { sh './deploy.sh' }\n    }\n  }\n\n  post {\n    failure {\n      mail to: 'team@example.com',\n           subject: \"FAIL: ${env.JOB_NAME} #${env.BUILD_NUMBER}\"\n    }\n  }\n}</pre><p><b>Key elements:</b></p><ul><li><b>agent</b> — specifies where the pipeline runs; <code>any</code> uses any available agent.</li><li><b>stages</b> — logical phases visible in the UI (Build, Test, Deploy).</li><li><b>when</b> — conditional execution; here, Deploy runs only on the <code>main</code> branch.</li><li><b>post</b> — actions triggered by build outcomes (<code>always</code>, <code>success</code>, <code>failure</code>, <code>unstable</code>).</li></ul>",
          },
          {
            q: "What is a Shared Library?",
            a: "<p>A <b>Shared Library</b> is a reusable collection of Groovy code — custom pipeline steps, classes, and utility functions — stored in a dedicated Git repository and imported into pipelines via the <code>@Library</code> annotation:</p><pre>@Library('my-shared-library') _\n\npipeline {\n  agent any\n  stages {\n    stage('Deploy') {\n      steps {\n        dockerBuildAndPush(image: 'myapp', tag: env.GIT_COMMIT)\n        slackNotify(channel: '#deploys', status: 'success')\n      }\n    }\n  }\n}</pre><p><b>Purpose:</b> Shared Libraries follow the Don't-Repeat-Yourself principle. Common build operations — Docker build and push, Slack notifications, artifact promotion, security scanning — are defined once in the library and consumed by many pipelines.</p><p><b>Structure:</b> A Shared Library repository has a well-defined directory layout: <code>vars/</code> for global variables and custom steps, <code>src/</code> for Groovy classes following standard Java package conventions, and <code>resources/</code> for non-code files that steps can load. Libraries are typically versioned by Git tag and pinned per pipeline.</p>",
          },
        ],
      },
      {
        q: "How would you design a CI/CD pipeline for a microservices application?",
        difficulty: "hard",
        a: "<p>A robust CI/CD pipeline for a microservices application consists of clearly separated stages that progress from source code to production deployment, with quality and security gates at each transition.</p><p><b>1. Trigger:</b> A webhook from the source repository (GitHub, GitLab, Bitbucket) triggers the pipeline on two events — every pull request (for validation) and every merge to <code>main</code> (for full delivery).</p><p><b>2. Build:</b> Install dependencies from the lockfile and compile the code. Producing a build artifact should be deterministic — identical inputs must produce identical outputs.</p><p><b>3. Test (parallel execution):</b> Multiple validation activities run concurrently to minimize pipeline duration:</p><ul><li>Unit tests.</li><li>Linting and code style checks.</li><li>Static analysis (SonarQube, ESLint, RuboCop, etc.).</li><li>License compliance scanning.</li></ul><p><b>4. Security scanning:</b> Two categories of security checks:</p><ul><li><b>SAST</b> (Static Application Security Testing): tools like Semgrep or SonarQube analyze source code for security patterns.</li><li><b>SCA</b> (Software Composition Analysis): tools like Snyk or Trivy scan lockfiles for known vulnerabilities in dependencies.</li></ul><p><b>5. Package:</b> Build a container image using a multi-stage Dockerfile. Push the image to a registry with the Git commit SHA as the tag for full traceability.</p><p><b>6. Image scanning:</b> Scan the built image with Trivy or Grype. Fail the build on high-severity CVEs unless explicitly allowlisted.</p><p><b>7. Deploy to dev:</b> Apply Helm or Kustomize manifests, or update the GitOps repository for ArgoCD to reconcile.</p><p><b>8. Integration tests:</b> Run end-to-end tests against the deployed dev environment to validate cross-service behavior.</p><p><b>9. Promotion:</b> Environment promotions (dev → stage → prod) are performed via pull requests to the GitOps repository that bump the image tag. Production promotions require manual approval and, ideally, an automated soak period in staging first.</p>",
        followups: [
          {
            q: "What is GitOps and how does it change deployment?",
            a: "<p><b>GitOps</b> is a deployment methodology where a Git repository is the single source of truth for the desired state of a system, and an automated operator continuously reconciles the actual system state to match the repository.</p><p>The name reflects the core principle: operations (deployments, configuration changes, rollbacks) are performed by making changes to Git, not by imperatively running commands against the target system.</p><p><b>Push vs pull deployment models:</b></p><ul><li><b>Traditional push model:</b> A CI/CD pipeline holds cluster credentials and imperatively executes deployment commands (e.g., <code>kubectl apply</code>). If credentials are compromised, an attacker can deploy anything.</li><li><b>GitOps pull model:</b> An operator (ArgoCD, Flux) runs inside the target cluster, watching the Git repository. When changes are detected, the operator reconciles the cluster to match. No credentials leave the cluster.</li></ul><p><b>Additional benefits of GitOps:</b></p><ul><li><b>Drift correction:</b> Manual changes to the cluster are automatically reverted, or the operator alerts on divergence.</li><li><b>Auditability:</b> Every production change corresponds to a Git commit, providing a complete audit trail.</li><li><b>Simple rollback:</b> Reverting a commit reverts the deployment.</li><li><b>Consistent environments:</b> Multiple environments can be defined declaratively from the same source of truth.</li></ul>",
          },
          {
            q: "Difference between blue/green, canary, and rolling deployments?",
            a: "<p>These three deployment strategies represent different trade-offs between deployment risk, resource cost, and operational complexity.</p><p><b>Rolling deployment (Kubernetes default):</b> The new version replaces the old version incrementally, one pod (or batch of pods) at a time. Traffic gradually shifts as new pods become ready and old pods terminate. This approach is resource-efficient but has a moderate blast radius — a bad deploy affects some percentage of users before the issue is detected. Rollback requires deploying the previous version, which is also incremental.</p><p><b>Blue/Green deployment:</b> Two complete environments are maintained. The current live version (blue) serves all traffic while the new version (green) is deployed and validated in parallel. A traffic switch (via load balancer or DNS) atomically cuts over to green. Rollback is instantaneous — switch back to blue. Downside: infrastructure cost is roughly doubled during the transition.</p><p><b>Canary deployment:</b> A small percentage of traffic (e.g., 1%) is directed to the new version while the majority continues on the old version. Application metrics (error rate, latency, business KPIs) are monitored automatically. If the canary is healthy, traffic is progressively shifted (1% → 10% → 50% → 100%). If problems are detected, traffic is reverted before broad exposure. This strategy has the lowest risk but requires sophisticated traffic-shifting infrastructure (a service mesh like Istio/Linkerd, an ingress controller with weighted routing, or a tool like Argo Rollouts or Flagger).</p><p><b>Selection guidance:</b> Rolling for low-risk deployments and stateless services. Blue/green when instant rollback matters and cost is acceptable. Canary for high-risk changes or when automated metric-based validation is available.</p>",
          },
        ],
      },
      {
        q: "Master vs Agent architecture in Jenkins?",
        difficulty: "easy",
        a: "<p>Jenkins uses a distributed architecture consisting of a controller (formerly called \"master\") and one or more agents (formerly called \"slaves\").</p><p><b>Controller:</b> The controller is the central component responsible for:</p><ul><li>Scheduling build jobs and dispatching them to agents.</li><li>Serving the web UI.</li><li>Storing configuration, job definitions, and build history.</li><li>Managing plugins and system settings.</li><li>Coordinating user authentication and authorization.</li></ul><p><b>Agents:</b> Agents are workers that execute the actual build steps. They connect to the controller via one of several protocols — SSH, JNLP (Java Network Launch Protocol), or WebSockets — and receive work from the controller's queue. Agents can be:</p><ul><li>Static VMs or physical machines with agent software installed.</li><li>Dynamically provisioned cloud instances (EC2, GCP).</li><li>Ephemeral Kubernetes pods created per build.</li></ul><p><b>Best practice:</b> The controller should be treated as a scheduling and coordination layer only — no builds should run on it directly. Build execution should happen exclusively on agents, ideally ephemeral ones spawned in Kubernetes, so each build starts in a clean, isolated environment. This improves security (no accumulated state or credentials), reproducibility (identical starting conditions), and scalability (agents come and go with demand).</p>",
        followups: [
          {
            q: "Why run Jenkins agents on Kubernetes?",
            a: "<p>Running Jenkins agents as Kubernetes pods offers substantial advantages over static agent machines:</p><ul><li><b>Ephemerality:</b> Each build runs in a freshly created pod that is destroyed on completion. There is no state carried between builds — no leaked credentials, no cached artifacts polluting subsequent builds, no drift from manual changes.</li><li><b>Elastic scaling:</b> The Jenkins Kubernetes plugin creates pods on demand as jobs enter the queue. When demand is low, no agents exist. When demand spikes, dozens can spin up in parallel. This eliminates the fixed cost of idle agent VMs.</li><li><b>Cost efficiency:</b> Combined with the Kubernetes cluster autoscaler, the underlying nodes scale up and down with build demand. Costs align with actual workload.</li><li><b>Toolchain isolation:</b> Each pipeline specifies the container image its agents should use. A Node.js pipeline uses a Node.js image; a Java pipeline uses a JDK image. No shared installation, no version conflicts.</li><li><b>Consistent environments:</b> Because agents are containers, the build environment is defined declaratively and versioned alongside the pipeline.</li></ul>",
          },
        ],
      },
      {
        q: "What is Jenkins Shared Library and how does it help?",
        difficulty: "medium",
        a: "<p>A <b>Jenkins Shared Library</b> is a versioned collection of Groovy code that can be loaded and used across many Jenkins pipelines. It is the primary mechanism for DRY (\"Don't Repeat Yourself\") in Jenkins-based CI/CD at scale — enabling a central platform team to provide reusable pipeline building blocks that many product teams consume.</p><p><b>Why shared libraries matter:</b> Without them, each project's Jenkinsfile tends to duplicate the same logic — build, test, publish, deploy, notify — with small variations. As the number of pipelines grows, keeping them consistent (security patches, tool version updates, new required steps) becomes impossible. Shared libraries centralize the logic.</p><p><b>Standard structure:</b></p><pre>my-shared-library/\n├── src/                       # Groovy classes (utility code)\n│   └── org/example/\n│       └── DeployHelper.groovy\n├── vars/                      # Global pipeline variables (steps)\n│   ├── buildAndPublish.groovy\n│   ├── deployToKubernetes.groovy\n│   └── notifySlack.groovy\n└── resources/                 # Non-Groovy resources (templates, configs)\n    └── deploy-template.yaml</pre><p><b>The <code>vars/</code> directory</b> is the most important. Each Groovy file in <code>vars/</code> becomes a globally-callable step in every pipeline that loads the library. For example, <code>vars/deployToKubernetes.groovy</code>:</p><pre>def call(Map config) {\n  script {\n    sh \"\"\"\n      kubectl set image deployment/${config.deployment} \\\n        ${config.container}=${config.image} \\\n        --namespace=${config.namespace}\n      kubectl rollout status deployment/${config.deployment} \\\n        --namespace=${config.namespace} --timeout=5m\n    \"\"\"\n  }\n}</pre><p>Consuming pipelines can then simply call:</p><pre>@Library('my-shared-library@main') _\n\npipeline {\n  agent any\n  stages {\n    stage('Deploy') {\n      steps {\n        deployToKubernetes(\n          deployment: 'my-app',\n          container:  'app',\n          image:      \"myregistry/my-app:${env.BUILD_NUMBER}\",\n          namespace:  'production'\n        )\n      }\n    }\n  }\n}</pre><p><b>Configuring a shared library:</b> Registered globally in Jenkins under Manage Jenkins → Configure System → Global Pipeline Libraries. The library points to a Git repository, and versions are pinned per-pipeline using the <code>@Library</code> annotation (e.g., <code>@Library('my-lib@v1.2')</code>).</p><p><b>Best practices:</b></p><ul><li><b>Version your library.</b> Pipelines should pin to specific tags or branches, not a mutable <code>main</code>. Breaking changes deserve major version bumps.</li><li><b>Test the library.</b> Use JenkinsPipelineUnit or spock to unit-test your shared library code.</li><li><b>Keep it small.</b> A library with 50 well-designed steps is more valuable than one with 500 poorly-scoped ones.</li><li><b>Document extensively.</b> Consumers need to know what each step does, what parameters it accepts, and how to configure it.</li><li><b>Publish a template pipeline.</b> A reference Jenkinsfile that demonstrates the library in action helps teams onboard quickly.</li></ul>",
      },
      {
        q: "What is Configuration as Code (JCasC) in Jenkins?",
        difficulty: "medium",
        a: "<p><b>Jenkins Configuration as Code (JCasC)</b> is a plugin that allows the entire Jenkins configuration to be defined declaratively in YAML files. Historically, Jenkins was configured through its web UI and stored settings in XML files under the JENKINS_HOME directory — making Jenkins instances snowflakes that could not be reliably reproduced.</p><p><b>What JCasC configures:</b></p><ul><li>Global system configuration (Jenkins URL, admin address, executor counts).</li><li>Authentication and authorization (LDAP, SAML, matrix-based security).</li><li>Cloud configurations (Kubernetes cloud, EC2 agents, Docker cloud).</li><li>Tool installations (JDK, Maven, Node, custom tools).</li><li>Credentials (with references to secrets, not actual values).</li><li>Global libraries.</li><li>Plugin-specific configuration.</li><li>Views and folder structures.</li></ul><p><b>Example <code>jenkins.yaml</code>:</b></p><pre>jenkins:\n  systemMessage: \"Managed by JCasC — do not modify via UI\"\n  numExecutors: 0\n  mode: EXCLUSIVE\n\n  securityRealm:\n    ldap:\n      configurations:\n        - server: ldaps://ldap.example.com\n          rootDN: dc=example,dc=com\n\n  authorizationStrategy:\n    roleBased:\n      roles:\n        global:\n          - name: admin\n            permissions:\n              - Overall/Administer\n            assignments:\n              - platform-team\n\n  clouds:\n    - kubernetes:\n        name: k8s-cloud\n        serverUrl: https://kubernetes.default\n        namespace: jenkins-agents\n        templates:\n          - name: default-jnlp\n            containers:\n              - name: jnlp\n                image: jenkins/inbound-agent:latest\n\ntool:\n  jdk:\n    installations:\n      - name: openjdk-21\n        properties:\n          - installSource:\n              installers:\n                - adoptOpenJdkInstaller:\n                    id: jdk-21+35</pre><p><b>Applying configuration:</b> The plugin looks for configuration files in a directory specified by <code>CASC_JENKINS_CONFIG</code> environment variable, or auto-loads from <code>$JENKINS_HOME/casc_configs</code>. Changes can be applied at startup or reloaded via the UI (\"Configuration as Code\" → \"Apply new configuration\").</p><p><b>Key benefits:</b></p><ol><li><b>Reproducibility:</b> A new Jenkins instance can be provisioned identical to production in minutes, without manual UI clicks.</li><li><b>Version control:</b> Configuration lives in Git alongside pipeline definitions. Changes are reviewable in PRs.</li><li><b>Disaster recovery:</b> A destroyed Jenkins can be rebuilt from configuration + backup of jobs.</li><li><b>Multi-instance consistency:</b> Dev, staging, and production Jenkins instances stay in sync via the same YAML.</li><li><b>Auditability:</b> Configuration changes are Git commits with authorship and history.</li></ol><p><b>Combined with the Job DSL plugin or Jenkinsfile-based pipelines:</b> JCasC handles system configuration, and Job DSL or SCM-based Jenkinsfiles handle job definitions. Together they provide a fully code-driven Jenkins setup where the entire installation is reproducible from Git.</p>",
      },
      {
        q: "How do you run Jenkins on Kubernetes?",
        difficulty: "medium",
        a: "<p>Running Jenkins on Kubernetes has become the standard pattern for modern Jenkins deployments. The Kubernetes plugin allows Jenkins to dynamically provision agent pods on demand — one pod per build — providing per-build isolation, elastic scaling, and clean-up-after-use semantics that traditional static agents cannot match.</p><p><b>Architecture:</b></p><ul><li><b>Jenkins controller</b> runs as a StatefulSet in Kubernetes with a PersistentVolume for JENKINS_HOME.</li><li><b>Jenkins agents</b> are pods created on demand by the Kubernetes plugin. Each pod is destroyed after the build completes.</li><li>The controller communicates with agents via JNLP over the internal Kubernetes network.</li></ul><p><b>Installation via Helm:</b> The most common way to deploy is with the official Jenkins Helm chart:</p><pre>helm repo add jenkins https://charts.jenkins.io\nhelm install jenkins jenkins/jenkins \\\n  --namespace jenkins --create-namespace \\\n  -f values.yaml</pre><p><b>Example values.yaml:</b></p><pre>controller:\n  image:\n    tag: latest-jdk21\n  resources:\n    requests: { cpu: 500m, memory: 2Gi }\n    limits:   { cpu: 2000m, memory: 4Gi }\n  installPlugins:\n    - kubernetes\n    - workflow-aggregator\n    - git\n    - configuration-as-code\n  JCasC:\n    defaultConfig: true\n    configScripts:\n      welcome: |\n        jenkins:\n          systemMessage: \"Jenkins on Kubernetes\"\n  serviceType: LoadBalancer\n\npersistence:\n  size: 20Gi\n  storageClass: gp3\n\nagent:\n  enabled: true\n  image:\n    repository: jenkins/inbound-agent\n    tag: latest</pre><p><b>Defining agents in pipelines:</b></p><pre>pipeline {\n  agent {\n    kubernetes {\n      yaml '''\n        apiVersion: v1\n        kind: Pod\n        spec:\n          containers:\n            - name: maven\n              image: maven:3.9-eclipse-temurin-21\n              command: [\"cat\"]\n              tty: true\n            - name: docker\n              image: docker:24-cli\n              command: [\"cat\"]\n              tty: true\n              volumeMounts:\n                - name: docker-sock\n                  mountPath: /var/run/docker.sock\n          volumes:\n            - name: docker-sock\n              hostPath:\n                path: /var/run/docker.sock\n      '''\n    }\n  }\n  stages {\n    stage('Build') {\n      steps {\n        container('maven') {\n          sh 'mvn -B package'\n        }\n      }\n    }\n    stage('Publish') {\n      steps {\n        container('docker') {\n          sh 'docker build -t myapp .'\n        }\n      }\n    }\n  }\n}</pre><p><b>Key benefits of running on Kubernetes:</b></p><ul><li><b>Per-build agent isolation:</b> No dependency conflicts between builds, no state leakage.</li><li><b>Elastic scaling:</b> Agent pods scale to zero when idle. Cluster autoscaler can provision new nodes for spikes.</li><li><b>Rich agent configuration:</b> Every build can specify its exact tools via a pod template — different JDKs, different Node versions, GPU nodes, etc.</li><li><b>Efficient resource usage:</b> No overprovisioned static agents sitting idle.</li><li><b>Cost:</b> Pay only for the resources actually used, especially valuable when combined with Spot nodes for builds.</li></ul><p><b>Common pitfalls:</b></p><ul><li>Persistent volumes for JENKINS_HOME must be reliably backed up — losing them means losing all job history and configuration.</li><li>Jenkins upgrades require careful testing due to plugin compatibility.</li><li>Docker builds inside Kubernetes are tricky — prefer Kaniko, buildah, or BuildKit rootless over Docker-in-Docker.</li></ul>",
      },
      {
        q: "What is the difference between Declarative and Scripted Pipeline?",
        difficulty: "medium",
        a: "<p>Jenkins Pipeline as Code supports two syntaxes: <b>Declarative</b> and <b>Scripted</b>. Both compile to the same underlying execution engine, but they differ significantly in style, structure, and audience.</p><p><b>Declarative Pipeline:</b> Introduced in 2017 with a structured, opinionated DSL. Enforces a top-down layout with predefined sections.</p><pre>pipeline {\n  agent { label 'linux' }\n\n  environment {\n    NODE_ENV = 'production'\n  }\n\n  options {\n    timeout(time: 30, unit: 'MINUTES')\n    disableConcurrentBuilds()\n  }\n\n  stages {\n    stage('Build') {\n      steps {\n        sh 'npm ci'\n        sh 'npm run build'\n      }\n    }\n\n    stage('Test') {\n      parallel {\n        stage('Unit') {\n          steps { sh 'npm test' }\n        }\n        stage('Lint') {\n          steps { sh 'npm run lint' }\n        }\n      }\n    }\n\n    stage('Deploy') {\n      when { branch 'main' }\n      steps {\n        sh './deploy.sh'\n      }\n    }\n  }\n\n  post {\n    always  { junit '**/test-results.xml' }\n    failure { slackSend(color: 'danger', message: 'Build failed') }\n  }\n}</pre><p><b>Characteristics of Declarative:</b></p><ul><li>Predefined structure: <code>agent</code>, <code>environment</code>, <code>options</code>, <code>stages</code>, <code>post</code>, etc.</li><li>Validated at parse time — many errors caught before the build starts.</li><li>Better UI integration (Blue Ocean, pipeline visualization).</li><li>Easier for people new to Jenkins.</li><li>Groovy code allowed only inside a <code>script { }</code> block within a <code>steps { }</code> block.</li></ul><p><b>Scripted Pipeline:</b> The original syntax — arbitrary Groovy code with a few Jenkins-specific functions.</p><pre>node('linux') {\n  try {\n    stage('Build') {\n      sh 'npm ci'\n      sh 'npm run build'\n    }\n\n    stage('Test') {\n      parallel(\n        Unit: { sh 'npm test' },\n        Lint: { sh 'npm run lint' }\n      )\n    }\n\n    if (env.BRANCH_NAME == 'main') {\n      stage('Deploy') {\n        sh './deploy.sh'\n      }\n    }\n  } catch (e) {\n    slackSend(color: 'danger', message: 'Build failed')\n    throw e\n  } finally {\n    junit '**/test-results.xml'\n  }\n}</pre><p><b>Characteristics of Scripted:</b></p><ul><li>Full power of Groovy — any control flow, functions, classes.</li><li>Handles complex logic that would be awkward in Declarative.</li><li>Less structure means less consistency across pipelines.</li><li>Errors are runtime-only.</li><li>Steeper learning curve.</li></ul><p><b>When to use which:</b></p><ul><li><b>Declarative is the default recommendation.</b> Its structure makes pipelines predictable, easier to maintain, and easier for others to understand. It handles the vast majority of real-world CI/CD needs.</li><li><b>Scripted is appropriate for edge cases</b> requiring complex dynamic logic — for example, dynamically building the pipeline shape based on the contents of the source repository.</li><li><b>Mixed:</b> You can drop into scripted mode inside Declarative using <code>script { }</code> blocks when specific complex logic requires it — the best of both worlds.</li></ul><p><b>The industry has clearly settled on Declarative for new pipelines,</b> with Scripted retained for legacy pipelines and specific advanced cases.</p>",
      },
      {
        q: "How does Jenkins compare to modern alternatives like Tekton or Argo Workflows?",
        difficulty: "medium",
        a: "<p>Jenkins remains the most widely-deployed CI/CD tool but is increasingly supplemented or replaced by cloud-native alternatives designed specifically for Kubernetes-first workflows. Understanding where Jenkins excels and where alternatives may fit better is a valuable skill for choosing tooling.</p><p><b>The alternatives:</b></p><ul><li><b>Tekton:</b> Kubernetes-native CI/CD framework. Every pipeline is a Kubernetes Custom Resource (Pipeline, PipelineRun, Task). Runs pipelines as pods on Kubernetes. Part of the CDF (Continuous Delivery Foundation).</li><li><b>Argo Workflows:</b> A general workflow engine (originally for ML pipelines) that also works well for CI/CD. Also Kubernetes-native.</li><li><b>GitHub Actions:</b> SaaS CI/CD tightly integrated with GitHub.</li><li><b>GitLab CI:</b> Integrated with GitLab, uses runners on Kubernetes or dedicated hosts.</li><li><b>Buildkite, CircleCI, TeamCity, Bamboo:</b> Other commercial CI systems.</li></ul><p><b>Jenkins strengths:</b></p><ul><li><b>Maturity:</b> 15+ years of production use, thousands of plugins, huge community, well-understood operational patterns.</li><li><b>Flexibility:</b> Can build absolutely anything — mobile apps, embedded firmware, Windows software, mainframe workloads, not just cloud-native web apps.</li><li><b>Self-hosted:</b> Full control over infrastructure, credentials, and data — important for regulated industries.</li><li><b>Extensibility:</b> If a plugin doesn't exist, you can write one. Jenkins is Java, so any Java library can be used.</li><li><b>Widespread expertise:</b> Every serious DevOps engineer has worked with Jenkins. Hiring is easier.</li></ul><p><b>Jenkins weaknesses (relative to modern alternatives):</b></p><ul><li><b>Not cloud-native by design:</b> Jenkins was designed for VM-era CI. Kubernetes-native alternatives feel more coherent when everything is containers.</li><li><b>Operational burden:</b> Plugins conflict, upgrades break things, Groovy is a legacy learning curve, and the SPOF controller architecture creates scaling headaches.</li><li><b>Security posture:</b> The plugin ecosystem includes many unmaintained plugins with CVEs. Keeping Jenkins current is ongoing work.</li><li><b>UI shows its age:</b> Even with Blue Ocean, the Jenkins UI feels dated compared to GitHub Actions or Argo.</li></ul><p><b>Tekton strengths:</b></p><ul><li>First-class Kubernetes citizen — every pipeline is a set of CRDs, RBAC applies naturally, GitOps-friendly.</li><li>Stateless controllers — no persistent controller state to back up.</li><li>Reusable Tasks published in <b>Tekton Hub</b> — a growing catalog.</li><li>Great fit when Kubernetes is already the primary compute substrate.</li></ul><p><b>Tekton weaknesses:</b></p><ul><li>Younger ecosystem — fewer integrations than Jenkins.</li><li>YAML-heavy — some pipelines feel more verbose than the equivalent Jenkinsfile.</li><li>UI story historically weak (though improving via Tekton Dashboard and Elafros).</li></ul><p><b>When Jenkins remains the right choice:</b></p><ul><li>You already have significant Jenkins investment and it's working.</li><li>You need to build non-container workloads (Windows software, mobile, embedded).</li><li>You need mature plugins for specific integrations (SAP, legacy ERP, specific mobile toolchains).</li><li>You need self-hosted with air-gapped operation.</li></ul><p><b>When to look at alternatives:</b></p><ul><li>Greenfield project with cloud-native focus → GitHub Actions or Tekton.</li><li>Everything already runs on Kubernetes → Tekton or Argo Workflows.</li><li>Fully SaaS preference → GitHub Actions or CircleCI.</li><li>Struggling with Jenkins operational overhead and want to modernize → migrate progressively to GitHub Actions or Tekton.</li></ul><p><b>Practical reality:</b> Many organizations run multiple CI systems simultaneously — Jenkins for legacy workloads, GitHub Actions for cloud-native new work. Migration is gradual and driven by team needs rather than being a one-time cutover.</p>",
      },
      {
        q: "How do you manage secrets in Jenkins pipelines?",
        difficulty: "medium",
        a: "<p>Secrets must never be hardcoded in Jenkinsfiles or checked into source control. Jenkins provides several mechanisms for secure secret management.</p><p><b>Credentials Binding:</b> Store secrets in Jenkins credential store and inject them into pipeline steps using the <code>withCredentials</code> block:</p><pre>withCredentials([usernamePassword(\n    credentialsId: 'dockerhub',\n    usernameVariable: 'DOCKER_USER',\n    passwordVariable: 'DOCKER_PASS')]) {\n  sh 'echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin'\n}</pre><p>The credential values are exposed as environment variables only within the block, and Jenkins automatically masks them in the build log.</p><p><b>Supported credential types:</b> Username/password, secret text, secret file, SSH private key, and certificates.</p><p><b>External secret managers:</b> For enterprise environments, integrate Jenkins with a dedicated secret manager rather than storing secrets in Jenkins itself:</p><ul><li><b>HashiCorp Vault</b> — via the HashiCorp Vault plugin, which fetches secrets at pipeline runtime.</li><li><b>AWS Secrets Manager</b> — via the AWS Secrets Manager plugin.</li><li><b>Azure Key Vault</b> — via the Azure Key Vault plugin.</li><li><b>Kubernetes Secrets</b> — mounted as environment variables in agent pods.</li></ul><p><b>Advantages of external secret managers:</b> Centralized secret lifecycle management, rotation policies, fine-grained access control, and audit logs for every secret access. Secrets are fetched at runtime and never persisted in Jenkins itself.</p><p><b>Additional practices:</b> Rotate credentials regularly, use short-lived tokens where possible (e.g., IAM roles instead of static access keys), and periodically audit which pipelines have access to which credentials.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "argocd",
    name: "ArgoCD & GitOps",
    icon: "🚢",
    kind: "qa",
    description:
      "ArgoCD — declarative GitOps for Kubernetes. Applications, sync strategies, waves, hooks, projects, and multi-cluster management.",
    items: [
      {
        q: "What is ArgoCD and what is GitOps?",
        difficulty: "easy",
        a: "<p><b>GitOps</b> is an operational model where Git is the single source of truth for both application code and infrastructure state. A cluster's desired state is declared in a Git repository, and a controller continuously reconciles the actual state of the cluster with what Git says it should be. This approach was popularized by Weaveworks and has become the dominant pattern for Kubernetes-based delivery.</p><p><b>Four principles of GitOps:</b></p><ol><li><b>Declarative:</b> The entire system is described declaratively (Kubernetes manifests, Helm charts, Kustomize overlays), not imperatively.</li><li><b>Versioned and immutable:</b> Desired state is stored in Git, with full history and immutability guarantees.</li><li><b>Pulled automatically:</b> Approved changes are automatically pulled from Git and applied to the cluster by an in-cluster agent — no external push required.</li><li><b>Continuously reconciled:</b> A controller ensures the cluster's actual state matches Git; any drift is corrected or reported.</li></ol><p><b>ArgoCD</b> is a Kubernetes-native GitOps continuous delivery controller, originally created at Intuit and now a graduated CNCF project. It watches Git repositories, renders manifests (raw YAML, Helm charts, Kustomize overlays, or JSONNet), and applies them to one or more Kubernetes clusters.</p><p><b>Key benefits of ArgoCD-driven GitOps:</b></p><ul><li><b>Audit trail:</b> Every cluster change is a Git commit with author, timestamp, and code review history.</li><li><b>Reproducibility:</b> A cluster can be re-created by pointing a new ArgoCD instance at the same Git repo.</li><li><b>Drift detection:</b> ArgoCD compares live cluster state to Git and reports differences.</li><li><b>Automatic reconciliation:</b> Configurable self-healing corrects drift automatically.</li><li><b>Separation of duties:</b> CI systems no longer need direct cluster credentials — they push changes to Git, and ArgoCD applies them.</li></ul><p><b>Alternatives:</b> Flux CD is the other major CNCF GitOps controller, with a similar philosophy but different architecture. ArgoCD is generally chosen for its rich UI and application-centric model; Flux is chosen for its lighter footprint and stronger Helm release management.</p>",
      },
      {
        q: "How does ArgoCD architecture work?",
        difficulty: "medium",
        a: "<p>ArgoCD runs as a set of Kubernetes controllers and services inside a cluster (typically its own <code>argocd</code> namespace). It watches Git repositories, computes desired state, and reconciles that state with a target cluster — which can be the same cluster it runs in or one or more external clusters.</p><p><b>Core components:</b></p><ul><li><b>API Server:</b> Exposes the ArgoCD API used by the UI, CLI, and any external integrations. Handles authentication, authorization, and multi-tenancy via AppProjects.</li><li><b>Repository Server:</b> Clones Git repositories and generates rendered manifests. It supports plain manifests, Helm, Kustomize, JSONNet, and custom config-management plugins.</li><li><b>Application Controller:</b> The heart of ArgoCD. Compares the desired state (from the repo server) against the live state in the target cluster, computes the difference, and performs sync operations. Runs continuously.</li><li><b>Redis:</b> Caches rendered manifests, sync statuses, and other transient state.</li><li><b>Dex (optional):</b> An identity broker for SSO integration with OIDC/SAML providers (Google, Okta, GitHub, Azure AD).</li><li><b>Notifications Controller (optional):</b> Sends alerts on sync events to Slack, email, webhooks.</li><li><b>ApplicationSet Controller (optional):</b> Templating for many similar Applications — for example, one Application per environment or per cluster.</li></ul><p><b>Data flow for a sync operation:</b></p><ol><li>The Application controller wakes on a schedule (default every 3 minutes) or on a webhook from Git.</li><li>It asks the repo server for the current rendered manifests based on the Application's spec (repo URL, path, revision, Helm values or Kustomize overlays).</li><li>It compares those manifests to what is currently in the cluster.</li><li>Any drift is reported as \"OutOfSync\". If auto-sync is enabled, it applies changes to bring the cluster into agreement with Git.</li><li>Sync status is updated in the API, UI, and notifications.</li></ol><p><b>Multi-cluster:</b> A single ArgoCD instance can manage many downstream clusters by adding them as targets with <code>argocd cluster add</code>. This centralizes GitOps management across a fleet of clusters.</p>",
      },
      {
        q: "What is an ArgoCD Application and how is it defined?",
        difficulty: "medium",
        a: "<p>The <b>Application</b> is the fundamental unit of ArgoCD. It ties together a source (Git repository, chart, or plugin) with a destination (cluster and namespace) and specifies the sync policy. Applications are themselves Kubernetes custom resources (CRDs) stored in the ArgoCD namespace.</p><p><b>Basic Application manifest:</b></p><pre>apiVersion: argoproj.io/v1alpha1\nkind: Application\nmetadata:\n  name: my-app\n  namespace: argocd\nspec:\n  project: default\n  source:\n    repoURL: https://github.com/example/config-repo.git\n    targetRevision: main\n    path: apps/my-app/overlays/prod\n  destination:\n    server: https://kubernetes.default.svc\n    namespace: production\n  syncPolicy:\n    automated:\n      prune: true\n      selfHeal: true\n    syncOptions:\n      - CreateNamespace=true\n      - ApplyOutOfSyncOnly=true\n    retry:\n      limit: 5\n      backoff:\n        duration: 5s\n        factor: 2\n        maxDuration: 3m</pre><p><b>Fields explained:</b></p><ul><li><b>source:</b> Where the desired state lives.<ul><li><code>repoURL</code>: The Git repository URL (or Helm repository).</li><li><code>targetRevision</code>: Branch, tag, or commit SHA to sync from.</li><li><code>path</code>: Subdirectory within the repo.</li><li><code>helm</code> / <code>kustomize</code> / <code>plugin</code>: Rendering configuration for those source types.</li></ul></li><li><b>destination:</b> Where to apply the manifests.<ul><li><code>server</code>: Kubernetes API server URL. <code>https://kubernetes.default.svc</code> is the ArgoCD's own cluster.</li><li><code>namespace</code>: Target namespace.</li></ul></li><li><b>project:</b> Which AppProject scope this Application belongs to. Projects enforce allowed sources, destinations, and RBAC.</li><li><b>syncPolicy:</b> How and when the Application reconciles.<ul><li><code>automated.prune</code>: Delete resources removed from Git.</li><li><code>automated.selfHeal</code>: Correct live cluster drift automatically.</li><li><code>syncOptions</code>: Fine-grained flags (create namespace, replace-strategy, skip-schema-validation).</li><li><code>retry</code>: Retry configuration for failed syncs.</li></ul></li></ul><p><b>Multi-source Applications:</b> ArgoCD 2.6+ supports multiple sources per Application, allowing patterns like a Helm chart from one repo with values files from another.</p>",
      },
      {
        q: "Explain sync policies and self-heal in ArgoCD.",
        difficulty: "medium",
        a: "<p>ArgoCD's <b>sync policy</b> controls how and when the desired state (Git) is reconciled with the live state (cluster). Two orthogonal knobs shape this behavior.</p><p><b>1. Manual vs Automated sync:</b></p><ul><li><b>Manual sync (default):</b> ArgoCD detects drift and marks the Application as OutOfSync, but does not apply changes automatically. An operator must click \"Sync\" in the UI or run <code>argocd app sync</code>. This is appropriate for environments requiring change control or human review.</li><li><b>Automated sync:</b> ArgoCD applies changes as soon as they are detected in Git. Suitable for dev and staging environments, and for production teams comfortable with fully automated deployment.</li></ul><pre>syncPolicy:\n  automated: {}       # enable automatic sync</pre><p><b>2. Prune (with automated only):</b> Whether to delete resources that were removed from Git.</p><ul><li>Without <code>prune: true</code>, ArgoCD will apply new/changed resources but leave orphaned ones (that existed in an earlier revision) in place.</li><li>With <code>prune: true</code>, orphaned resources are deleted as part of the sync. This is generally the intended GitOps behavior — the cluster exactly matches Git.</li></ul><pre>syncPolicy:\n  automated:\n    prune: true</pre><p><b>3. Self-Heal:</b> The most impactful setting. With self-heal enabled, ArgoCD reverts any live drift back to Git state.</p><ul><li>Without self-heal, ArgoCD applies Git changes but does not react to changes made directly in the cluster (e.g., <code>kubectl edit</code>). The Application shows as OutOfSync but is not auto-corrected.</li><li>With self-heal, any manual <code>kubectl</code> change is reverted automatically within the next sync cycle. This enforces Git as absolute source of truth.</li></ul><pre>syncPolicy:\n  automated:\n    prune: true\n    selfHeal: true</pre><p><b>When to enable each:</b></p><ul><li><b>Dev/staging:</b> Full auto (<code>automated.prune: true, selfHeal: true</code>) for maximum GitOps discipline.</li><li><b>Production (mature GitOps):</b> Same as dev/staging — enforces the discipline that all changes flow through code review.</li><li><b>Production (early adoption):</b> Manual sync or automated without self-heal, allowing operators to correct drift by hand while the culture shifts.</li></ul><p><b>Preventing sync temporarily:</b> <code>argocd app sync --dry-run</code> to preview, or use the <code>Sync Windows</code> feature on AppProjects to restrict sync to specific hours or block sync during freeze periods.</p>",
      },
      {
        q: "What are sync waves and sync phases in ArgoCD?",
        difficulty: "medium",
        a: "<p>ArgoCD needs a way to control the order in which resources are applied to the cluster. For example, a database StatefulSet must exist before the applications that depend on it, and a Namespace must exist before objects inside it. ArgoCD provides two ordering mechanisms.</p><p><b>1. Sync Phases</b> — three fixed lifecycle phases:</p><ul><li><b>PreSync:</b> Runs before any regular resources. Used for setup like database migrations or prerequisites.</li><li><b>Sync:</b> The normal application phase where all standard resources are applied.</li><li><b>PostSync:</b> Runs after all regular resources are healthy. Used for smoke tests, notifications, or cleanup.</li></ul><p>Resources are assigned to phases via annotations:</p><pre>apiVersion: batch/v1\nkind: Job\nmetadata:\n  name: db-migrate\n  annotations:\n    argocd.argoproj.io/hook: PreSync\n    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded\nspec: ...</pre><p><b>2. Sync Waves</b> — fine-grained ordering within a phase. Each resource can be assigned a wave number (any integer, positive or negative). Resources in lower-numbered waves are applied first, and ArgoCD waits for each wave to be healthy before proceeding to the next.</p><pre>metadata:\n  annotations:\n    argocd.argoproj.io/sync-wave: \"-2\"    # applied earliest</pre><p><b>Common wave assignments in a typical application:</b></p><ul><li><b>Wave -3:</b> Namespaces, CRDs.</li><li><b>Wave -2:</b> ServiceAccounts, RBAC roles.</li><li><b>Wave -1:</b> ConfigMaps, Secrets.</li><li><b>Wave 0 (default):</b> Regular Deployments, Services, StatefulSets.</li><li><b>Wave 1:</b> Ingresses, ServiceMonitors.</li><li><b>Wave 2:</b> HorizontalPodAutoscalers (which need Deployments to already exist).</li></ul><p><b>Health checks between waves:</b> ArgoCD uses built-in health assessments for standard Kubernetes resources (Deployments Ready, StatefulSets Ready, Jobs Complete) to determine when a wave is done. Custom resources can define their own health checks via Lua scripts in ArgoCD's config.</p>",
      },
      {
        q: "What is an AppProject in ArgoCD?",
        difficulty: "medium",
        a: "<p>An <b>AppProject</b> in ArgoCD is a logical grouping and multi-tenancy boundary. Every Application belongs to a project, and the project defines what that Application is allowed to do. In small setups a single <code>default</code> project may suffice, but at scale AppProjects become essential for governance.</p><p><b>What AppProjects control:</b></p><ol><li><b>Source repositories:</b> Which Git repositories Applications in this project may source manifests from.</li><li><b>Destinations:</b> Which clusters and namespaces Applications may deploy to.</li><li><b>Cluster resource whitelist/blacklist:</b> Which cluster-scoped resources Applications may create/modify (e.g., can this project create ClusterRoles?).</li><li><b>Namespace resource whitelist/blacklist:</b> Which namespace-scoped resources are allowed.</li><li><b>RBAC:</b> Which users and groups can access this project and what actions they can perform (get/sync/override/delete).</li><li><b>Sync windows:</b> Time-based restrictions on when syncs can occur — useful for production freezes or maintenance windows.</li><li><b>Signature keys:</b> If commit signing is required, which GPG keys are trusted.</li></ol><p><b>Example AppProject:</b></p><pre>apiVersion: argoproj.io/v1alpha1\nkind: AppProject\nmetadata:\n  name: team-payments\n  namespace: argocd\nspec:\n  description: Payments team applications\n\n  sourceRepos:\n    - https://github.com/example/payments-config.git\n    - https://github.com/example/shared-charts.git\n\n  destinations:\n    - server: https://kubernetes.default.svc\n      namespace: payments-*\n    - server: https://staging.k8s.example.com\n      namespace: payments-*\n\n  clusterResourceWhitelist:\n    - group: ''\n      kind: Namespace\n\n  namespaceResourceBlacklist:\n    - group: ''\n      kind: ResourceQuota\n\n  roles:\n    - name: developer\n      policies:\n        - p, proj:team-payments:developer, applications, get, team-payments/*, allow\n        - p, proj:team-payments:developer, applications, sync, team-payments/*, allow\n      groups:\n        - example-org:payments-developers\n\n  syncWindows:\n    - kind: deny\n      schedule: '0 22 * * 5'\n      duration: 60h\n      applications:\n        - '*'\n      manualSync: false</pre><p><b>Multi-tenant pattern:</b> A common enterprise setup uses one AppProject per team or business unit, each restricted to its own repos and namespaces. This lets many teams share a single ArgoCD instance safely.</p>",
      },
      {
        q: "What is ApplicationSet and when should you use it?",
        difficulty: "medium",
        a: "<p><b>ApplicationSet</b> is an ArgoCD feature that generates multiple ArgoCD Applications from a template plus a generator. It solves the problem of managing many similar Applications — for example, deploying the same microservice to 20 clusters, or creating one Application per environment for each of 50 services.</p><p><b>Why not just define Applications manually?</b> With a few Applications, manual definition is fine. With dozens or hundreds — spanning environments, clusters, and services — the overhead becomes prohibitive and error-prone. ApplicationSet uses generators to enumerate the desired combinations and produces Applications programmatically.</p><p><b>Available generators:</b></p><ul><li><b>List generator:</b> Static list of parameter sets — the simplest generator.</li><li><b>Cluster generator:</b> Iterates over clusters registered with ArgoCD.</li><li><b>Git generator:</b> Iterates over directories or files in a Git repository — one Application per directory, for instance.</li><li><b>Matrix generator:</b> Combines two generators to produce the cross-product (e.g., every service × every environment).</li><li><b>Merge generator:</b> Combines multiple generators by merging their outputs on a key.</li><li><b>Pull Request generator:</b> Creates ephemeral Applications for each open PR — useful for preview environments.</li><li><b>SCM Provider generator:</b> Discovers repositories in a GitHub/GitLab organization automatically.</li></ul><p><b>Example — one Application per cluster:</b></p><pre>apiVersion: argoproj.io/v1alpha1\nkind: ApplicationSet\nmetadata:\n  name: guestbook-per-cluster\n  namespace: argocd\nspec:\n  generators:\n    - clusters: {}          # iterates over all registered clusters\n  template:\n    metadata:\n      name: '{{name}}-guestbook'\n    spec:\n      project: default\n      source:\n        repoURL: https://github.com/example/guestbook.git\n        targetRevision: HEAD\n        path: guestbook\n      destination:\n        server: '{{server}}'\n        namespace: guestbook\n      syncPolicy:\n        automated:\n          prune: true\n          selfHeal: true</pre><p><b>Example — matrix (per service × per environment):</b></p><pre>generators:\n  - matrix:\n      generators:\n        - git:\n            repoURL: https://github.com/example/services.git\n            directories:\n              - path: services/*\n        - list:\n            elements:\n              - env: dev\n              - env: staging\n              - env: prod</pre><p><b>Common use cases:</b></p><ul><li>Fleet management — deploying the same platform components to many clusters.</li><li>Multi-environment applications — dev/staging/prod for each microservice.</li><li>Preview environments — ephemeral deployments per pull request.</li><li>Auto-discovery — deploying every service in a monorepo automatically.</li></ul>",
      },
      {
        q: "How does ArgoCD handle Helm charts and Kustomize?",
        difficulty: "medium",
        a: "<p>ArgoCD natively supports the two most popular Kubernetes manifest tools — <b>Helm</b> and <b>Kustomize</b> — rendering them internally rather than delegating to <code>helm install</code> or <code>kubectl kustomize</code>.</p><p><b>Helm handling:</b></p><p>When an Application's source is a Helm chart, ArgoCD invokes <code>helm template</code> to render the chart to plain manifests, then applies them via its own reconciliation. It does <b>not</b> use <code>helm install</code>, so Helm's release tracking is not used — ArgoCD is the sole source of state.</p><pre>source:\n  repoURL: https://github.com/example/charts.git\n  targetRevision: main\n  path: charts/my-app\n  helm:\n    releaseName: my-app\n    valueFiles:\n      - values-prod.yaml\n    parameters:\n      - name: image.tag\n        value: v1.2.3\n    values: |\n      replicaCount: 5\n      ingress:\n        enabled: true</pre><p>ArgoCD supports:</p><ul><li>Local charts in the same repo as configuration.</li><li>Remote Helm repositories referenced directly (with <code>chart:</code> instead of <code>path:</code>).</li><li>Multi-source Applications: chart from one repo, values files from another.</li><li>Helm hooks translated to ArgoCD sync phases (PreSync/PostSync).</li></ul><p><b>Trade-off:</b> Because ArgoCD does not run <code>helm install</code>, Helm's built-in rollback and release history are unavailable. Rollback is handled by ArgoCD's own history and by reverting Git commits.</p><p><b>Kustomize handling:</b></p><p>When the Application's <code>path</code> contains a <code>kustomization.yaml</code>, ArgoCD renders it with <code>kubectl kustomize</code>-equivalent logic.</p><pre>source:\n  repoURL: https://github.com/example/config-repo.git\n  targetRevision: main\n  path: overlays/prod\n  kustomize:\n    images:\n      - myapp:v1.2.3\n    commonLabels:\n      team: platform\n    namePrefix: prod-</pre><p>ArgoCD adds Kustomize customizations on top of what the kustomization.yaml provides, allowing per-Application overrides without modifying the base kustomization files.</p><p><b>Comparison — which to use:</b></p><ul><li><b>Helm:</b> Best for consuming off-the-shelf charts (Postgres, Prometheus), publishing your own charts, and complex templating needs.</li><li><b>Kustomize:</b> Best for organization-owned manifests with environment overlays, when you prefer plain YAML over templates.</li><li><b>Combined:</b> Use a Helm chart as the base and apply Kustomize patches on top — supported natively via Kustomize's <code>helmCharts</code> field or via ArgoCD's rendering.</li><li><b>Plain YAML:</b> For simple applications, plain manifests in a directory are the simplest option.</li></ul>",
      },
      {
        q: "How do you manage secrets with ArgoCD?",
        difficulty: "medium",
        a: "<p>ArgoCD itself does not solve secret management — it treats Secrets like any other Kubernetes resource. Since committing raw secrets to Git is unacceptable, the ecosystem provides several patterns for handling secrets in GitOps workflows.</p><p><b>Pattern 1 — External Secrets Operator (ESO, recommended):</b></p><p>Store the actual secret value in an external secret manager (Vault, AWS Secrets Manager, Azure Key Vault, GCP Secret Manager). Commit only a reference to Git in the form of an <code>ExternalSecret</code> custom resource. ESO watches these references and creates real Kubernetes Secrets in the cluster.</p><pre>apiVersion: external-secrets.io/v1beta1\nkind: ExternalSecret\nmetadata:\n  name: db-credentials\nspec:\n  refreshInterval: 1h\n  secretStoreRef:\n    name: aws-secretsmanager\n    kind: SecretStore\n  target:\n    name: db-credentials\n  data:\n    - secretKey: password\n      remoteRef:\n        key: prod/database\n        property: password</pre><p><b>Benefits:</b> Secrets never touch Git, centralized rotation is possible, and access is auditable in the external system.</p><p><b>Pattern 2 — Sealed Secrets:</b></p><p>A Bitnami controller (Sealed Secrets) encrypts a regular Secret into a <code>SealedSecret</code> custom resource that <i>can</i> safely be committed to Git. Only the in-cluster controller (holding the private key) can decrypt it back into a real Secret.</p><pre>kubeseal --format yaml < db-secret.yaml > db-secret-sealed.yaml\n# The sealed version is safe to commit</pre><p><b>Trade-off:</b> Secrets are cluster-specific (each cluster has its own key). Rotation requires re-sealing.</p><p><b>Pattern 3 — SOPS + Helm/Kustomize plugins:</b></p><p>Encrypt secrets in Git using <b>SOPS</b> (Secrets OPerationS) with a KMS-backed key. A custom config-management plugin decrypts them at ArgoCD render time. Popular tools include <code>argocd-vault-plugin</code>, <code>helm-secrets</code>, and <code>kustomize-sops</code>.</p><p><b>Pattern 4 — Vault Agent Injector:</b></p><p>HashiCorp Vault provides a mutating webhook that injects secrets into pods at runtime from Vault, mounted as files or environment variables. No Kubernetes Secret objects are used at all.</p><p><b>Anti-patterns to avoid:</b></p><ul><li>Committing plaintext Secrets to Git — even to a \"private\" repo.</li><li>Storing secrets as ArgoCD Application <code>parameters</code> — these are visible in the ArgoCD UI/API.</li><li>Using <code>--set</code> in a CI pipeline that logs secrets in build output.</li></ul><p><b>Best practice for most teams:</b> External Secrets Operator with a cloud-native secret manager (AWS Secrets Manager, Azure Key Vault, or GCP Secret Manager) provides the cleanest, most auditable solution and integrates seamlessly with cloud IAM.</p>",
      },
      {
        q: "How does ArgoCD compare with Flux CD?",
        difficulty: "medium",
        a: "<p><b>ArgoCD</b> and <b>Flux CD</b> are the two dominant GitOps controllers for Kubernetes, both CNCF graduated projects with very similar core capabilities. They differ in architecture, philosophy, and operational feel.</p><p><b>Similarities:</b></p><ul><li>Pull-based GitOps — controllers running in the cluster fetch state from Git.</li><li>Support for Helm, Kustomize, and plain manifests.</li><li>Multi-cluster management.</li><li>SSO integration, RBAC, and multi-tenancy.</li><li>Notification hooks and webhook integrations.</li></ul><p><b>Key differences:</b></p><table><tr><th>Aspect</th><th>ArgoCD</th><th>Flux CD</th></tr><tr><td>UI</td><td>Rich web UI showing Application status, drift, sync history, and topology graphs</td><td>Minimal UI (via Weave GitOps or third-party dashboards); primarily CLI/kubectl</td></tr><tr><td>Core abstraction</td><td>Application CRD — a full unit of deployment</td><td>Composed of Source (GitRepository, HelmRepository) + Kustomization or HelmRelease CRDs</td></tr><tr><td>Multi-source</td><td>Multi-source Applications (2.6+)</td><td>Naturally supports many sources via separate Source resources</td></tr><tr><td>Helm handling</td><td>Uses <code>helm template</code> — no real Helm release tracking</td><td>HelmController runs actual <code>helm install/upgrade</code>, preserving Helm history</td></tr><tr><td>Progressive delivery</td><td>Argo Rollouts (same project family) for canary/blue-green</td><td>Flagger (Flux project family) for canary/blue-green</td></tr><tr><td>Architecture</td><td>Monolithic-ish: several coupled components in one namespace</td><td>Highly modular: separate controllers for source, kustomize, helm, notifications, image automation</td></tr><tr><td>Image update automation</td><td>Not built in — use ArgoCD Image Updater project</td><td>Built-in Image Automation Controller</td></tr><tr><td>Learning curve</td><td>Easier for teams unfamiliar with GitOps — the UI helps discovery</td><td>Steeper — CLI-first, more moving parts to understand</td></tr></table><p><b>When to choose ArgoCD:</b> Teams that value a strong UI, prefer an application-centric model, want the Argo Rollouts progressive delivery ecosystem, or expect many operators to interact with GitOps without deep Kubernetes CLI expertise.</p><p><b>When to choose Flux:</b> Teams that prefer CLI-native workflows, want strong Helm release preservation, need built-in image automation, or appreciate a highly modular architecture that installs only the pieces they need.</p><p><b>In practice:</b> Both are excellent, and most organizations succeed with either. Choose based on which fits the team's culture and existing tooling.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "sonarqube",
    name: "SonarQube & Code Quality",
    icon: "🎯",
    kind: "qa",
    description:
      "SonarQube — static analysis and code quality gate for CI/CD. Rules, quality gates, coverage, security hotspots, and pipeline integration.",
    items: [
      {
        q: "What is SonarQube and what does it do?",
        difficulty: "easy",
        a: "<p><b>SonarQube</b> is a self-hosted platform for continuous inspection of code quality. It performs <b>static analysis</b> — examining source code without executing it — to identify bugs, code smells, security vulnerabilities, security hotspots, duplication, and insufficient test coverage. It is the most widely adopted static analysis platform in enterprise DevOps toolchains.</p><p><b>What SonarQube analyzes:</b></p><ul><li><b>Bugs:</b> Coding errors that will (or likely will) cause defects at runtime — null pointer dereferences, incorrect loop conditions, resource leaks.</li><li><b>Vulnerabilities:</b> Security flaws that could be exploited — SQL injection, cross-site scripting, hardcoded credentials, insecure deserialization.</li><li><b>Security Hotspots:</b> Code that is sensitive from a security perspective and requires manual review — for example, use of cryptographic functions where a reviewer must confirm proper usage.</li><li><b>Code Smells:</b> Maintainability issues — long methods, deeply nested code, dead code, poor naming, complex conditionals — that make code harder to change safely.</li><li><b>Duplications:</b> Blocks of code that appear multiple times, which are candidates for refactoring.</li><li><b>Test Coverage:</b> Percentage of code exercised by tests, computed from coverage reports produced by testing tools.</li></ul><p><b>Supported languages:</b> 30+ languages including Java, JavaScript/TypeScript, Python, C#, C/C++, Go, Ruby, PHP, Kotlin, Swift, Scala, Rust, and infrastructure-as-code languages (Terraform, CloudFormation, Kubernetes YAML).</p><p><b>Editions:</b></p><ul><li><b>Community Edition:</b> Open source, supports most common languages, no branch analysis.</li><li><b>Developer / Enterprise / Data Center Editions:</b> Commercial, add branch/PR analysis, advanced security rules, additional language support, and scalability features.</li></ul><p><b>Typical placement in DevOps:</b> A CI pipeline runs the SonarScanner against the code and pushes results to a SonarQube server. The server aggregates results, applies quality gate rules, and reports success/failure back to the pipeline. Failed quality gates block PR merges via GitHub, GitLab, Bitbucket, or Azure DevOps integrations.</p>",
      },
      {
        q: "What is a Quality Gate in SonarQube?",
        difficulty: "medium",
        a: "<p>A <b>Quality Gate</b> is a set of conditions that a code analysis must meet to be considered acceptable. It is SonarQube's mechanism for enforcing an objective quality bar in a CI/CD pipeline — code that fails the gate cannot merge or deploy.</p><p><b>How a quality gate works:</b></p><ol><li>The pipeline runs the SonarScanner, which uploads analysis results to SonarQube.</li><li>SonarQube evaluates the results against the configured Quality Gate.</li><li>The gate returns Passed or Failed.</li><li>The pipeline reads the result (e.g., via <code>sonar-quality-gate</code> plugin or webhook) and passes or fails the build accordingly.</li></ol><p><b>The default \"Sonar way\" quality gate focuses on new code:</b></p><ul><li>No new bugs.</li><li>No new vulnerabilities.</li><li>Code coverage on new code ≥ 80%.</li><li>Duplicated lines on new code ≤ 3%.</li><li>Security hotspots reviewed on new code = 100%.</li></ul><p><b>The \"clean as you code\" philosophy:</b> SonarQube deliberately focuses conditions on <i>new code</i> — code added or modified since the branch's baseline — rather than the entire codebase. This is intentional:</p><ul><li>It is practical: legacy codebases with thousands of pre-existing issues cannot pass a strict gate immediately, but new code can be held to a high standard.</li><li>It is progressive: as more code is touched over time, the total quality improves gradually.</li><li>It is fair: developers are held responsible for their new contributions, not for problems inherited from years ago.</li></ul><p><b>Custom quality gates:</b> Organizations can define their own gates for different projects or maturity levels. Common conditions include:</p><ul><li>Maximum issues by severity (Blocker, Critical, Major).</li><li>Reliability rating and Security rating on new code.</li><li>Technical debt ratio.</li><li>Specific rule violations that must not occur.</li></ul><p><b>Enforcing gates in CI:</b> Modern pipelines fail the build if the gate fails — typically via the SonarScanner's <code>-Dsonar.qualitygate.wait=true</code> flag, which blocks until analysis is complete and returns non-zero if the gate is red.</p>",
      },
      {
        q: "How do you integrate SonarQube into a CI/CD pipeline?",
        difficulty: "medium",
        a: "<p>Integrating SonarQube into a CI/CD pipeline follows a consistent pattern regardless of the CI system: run the appropriate scanner during the build, upload results to SonarQube, and gate the build on the quality gate result.</p><p><b>General components:</b></p><ol><li><b>SonarQube server:</b> A running instance (self-hosted or SonarCloud SaaS) that receives and stores analysis results.</li><li><b>Project configuration:</b> Project key, name, and analysis parameters set either in a <code>sonar-project.properties</code> file or passed as scanner arguments.</li><li><b>Scanner:</b> The right scanner for the language stack — <code>SonarScanner CLI</code>, <code>sonar-scanner-maven</code>, <code>sonar-scanner-gradle</code>, <code>SonarScanner for .NET</code>, or <code>SonarScanner for MSBuild</code>.</li><li><b>Authentication:</b> A user token generated in SonarQube, stored in the pipeline's secrets store.</li><li><b>Quality Gate check:</b> A step that fails the pipeline if the gate is red.</li></ol><p><b>Example — GitHub Actions with Maven:</b></p><pre>name: Build\n\non: [push, pull_request]\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0    # required for accurate new-code detection\n\n      - uses: actions/setup-java@v4\n        with:\n          distribution: 'temurin'\n          java-version: '21'\n          cache: 'maven'\n\n      - name: Build and test\n        run: mvn -B verify\n\n      - name: SonarQube scan\n        env:\n          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n          SONAR_HOST_URL: ${{ vars.SONAR_HOST_URL }}\n          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n        run: |\n          mvn sonar:sonar \\\n            -Dsonar.projectKey=example_my-app \\\n            -Dsonar.qualitygate.wait=true</pre><p><b>Example — Jenkins declarative pipeline:</b></p><pre>pipeline {\n  agent any\n  environment {\n    SONAR_TOKEN = credentials('sonar-token')\n  }\n  stages {\n    stage('Test') {\n      steps { sh 'mvn -B verify' }\n    }\n    stage('SonarQube') {\n      steps {\n        withSonarQubeEnv('SonarQube') {\n          sh 'mvn sonar:sonar -Dsonar.projectKey=my-app'\n        }\n      }\n    }\n    stage('Quality Gate') {\n      steps {\n        timeout(time: 5, unit: 'MINUTES') {\n          waitForQualityGate abortPipeline: true\n        }\n      }\n    }\n  }\n}</pre><p><b>Best practices:</b></p><ul><li>Perform a full <code>fetch-depth: 0</code> checkout so SonarQube can accurately identify the new-code baseline via Git blame.</li><li>Wait for the quality gate (<code>-Dsonar.qualitygate.wait=true</code>) so pipeline results reflect the actual gate outcome, not just \"scan submitted\".</li><li>Configure PR decoration to post analysis comments directly on GitHub/GitLab/Bitbucket PRs.</li><li>Use branch analysis to isolate results per feature branch and enable per-PR quality gates.</li></ul>",
      },
      {
        q: "How does SonarQube handle code coverage?",
        difficulty: "medium",
        a: "<p>SonarQube does not run tests or compute coverage itself. Instead, it <b>consumes coverage reports</b> produced by external testing tools during the build. Each language has one or more coverage report formats that SonarQube can import.</p><p><b>Common coverage tools and their SonarQube-supported formats:</b></p><ul><li><b>Java:</b> JaCoCo (XML report). Configuration: <code>sonar.coverage.jacoco.xmlReportPaths</code>.</li><li><b>JavaScript/TypeScript:</b> LCOV (Istanbul/nyc, Jest, Vitest). Configuration: <code>sonar.javascript.lcov.reportPaths</code>.</li><li><b>Python:</b> Coverage.py (Cobertura or XML). Configuration: <code>sonar.python.coverage.reportPaths</code>.</li><li><b>.NET:</b> OpenCover, dotCover, Coverlet, or Visual Studio native.</li><li><b>Go:</b> Native <code>go test -coverprofile</code>. Configuration: <code>sonar.go.coverage.reportPaths</code>.</li><li><b>Ruby:</b> SimpleCov (JSON format).</li></ul><p><b>Typical pipeline structure:</b></p><ol><li>Run tests with coverage instrumentation enabled.</li><li>Generate a coverage report in the appropriate format.</li><li>Run the SonarScanner, passing the report path.</li></ol><p><b>Example — JavaScript with Jest:</b></p><pre>npm test -- --coverage --coverageReporters=lcov\n\nsonar-scanner \\\n  -Dsonar.projectKey=my-app \\\n  -Dsonar.sources=src \\\n  -Dsonar.tests=src \\\n  -Dsonar.test.inclusions='**/*.test.ts' \\\n  -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info</pre><p><b>Example — Java with Maven and JaCoCo:</b></p><pre>&lt;plugin&gt;\n  &lt;groupId&gt;org.jacoco&lt;/groupId&gt;\n  &lt;artifactId&gt;jacoco-maven-plugin&lt;/artifactId&gt;\n  &lt;version&gt;0.8.11&lt;/version&gt;\n  &lt;executions&gt;\n    &lt;execution&gt;\n      &lt;goals&gt;&lt;goal&gt;prepare-agent&lt;/goal&gt;&lt;/goals&gt;\n    &lt;/execution&gt;\n    &lt;execution&gt;\n      &lt;id&gt;report&lt;/id&gt;\n      &lt;phase&gt;test&lt;/phase&gt;\n      &lt;goals&gt;&lt;goal&gt;report&lt;/goal&gt;&lt;/goals&gt;\n    &lt;/execution&gt;\n  &lt;/executions&gt;\n&lt;/plugin&gt;</pre><p><b>Coverage metrics in SonarQube:</b></p><ul><li><b>Line coverage:</b> Percentage of executable lines covered by at least one test.</li><li><b>Condition (branch) coverage:</b> Percentage of boolean branches taken by tests.</li><li><b>Overall coverage:</b> Weighted combination, typically the metric quality gates target.</li><li><b>New Code coverage:</b> Coverage specifically on new/modified lines — the metric that quality gates in the \"clean as you code\" model prioritize.</li></ul><p><b>Common target:</b> The default Sonar way quality gate requires <b>80% coverage on new code</b>, not on overall code. This is again a practical way to raise quality progressively without demanding immediate coverage of legacy code.</p>",
      },
      {
        q: "What are Bugs, Vulnerabilities, Code Smells, and Security Hotspots?",
        difficulty: "medium",
        a: "<p>SonarQube classifies issues into four distinct categories. Understanding the distinctions helps teams prioritize remediation and interpret analysis results correctly.</p><p><b>1. Bugs:</b> Code that will (or likely will) cause a defect at runtime. Bugs are objective correctness issues that ideally should never make it to production.</p><p><i>Examples:</i></p><ul><li><code>NullPointerException</code>-prone code (dereferencing a value that can be null).</li><li>Infinite loops.</li><li>Resource leaks (opened files or connections never closed).</li><li>Assigning a variable to itself.</li><li>Using <code>==</code> instead of <code>.equals()</code> on Java Strings.</li></ul><p><b>2. Vulnerabilities:</b> Security-relevant issues where the analyzer can definitively identify a security defect. These have known exploitation patterns.</p><p><i>Examples:</i></p><ul><li>SQL injection via string concatenation of user input.</li><li>Path traversal (unchecked filename inputs).</li><li>Hardcoded credentials or API keys.</li><li>Weak cryptographic algorithms (MD5, SHA1 for password hashing).</li><li>Cross-site scripting (XSS) — writing user input directly to HTML.</li></ul><p><b>3. Security Hotspots:</b> Security-sensitive code that requires human review. Unlike vulnerabilities, the analyzer cannot determine whether the code is actually flawed — the risk depends on context.</p><p><i>Examples:</i></p><ul><li>Cryptographic operations — the code uses AES, but is the mode appropriate for the use case?</li><li>Regular expressions with high complexity — could be safe or could be a ReDoS risk.</li><li>File permission changes — <code>chmod 777</code> is suspicious but may be intentional.</li><li>Random number generation — safe randomness vs weak randomness.</li></ul><p>Hotspots are shown separately in the UI and require an explicit review action, marking them either \"Safe\" (justified) or \"To Fix\" (real vulnerability). Quality gates typically require all hotspots to be reviewed.</p><p><b>4. Code Smells:</b> Maintainability issues that make code harder to understand, change, or test. They do not indicate bugs, but they accumulate as technical debt and slow future development.</p><p><i>Examples:</i></p><ul><li>Methods longer than 100 lines.</li><li>Cyclomatic complexity above a threshold.</li><li>Deeply nested control structures.</li><li>Dead code (unused variables, unreachable code).</li><li>Duplicated string literals that should be constants.</li><li>Poor naming (e.g., single-letter variables outside loops).</li></ul><p><b>Severity levels</b> apply across all categories: <b>Blocker</b> (highest), <b>Critical</b>, <b>Major</b>, <b>Minor</b>, <b>Info</b> (lowest).</p><p><b>Reliability, Security, and Maintainability ratings</b> (A–E) are derived from the counts and severities of these issues, providing high-level project-health indicators.</p>",
      },
      {
        q: "What is SonarLint and how does it differ from SonarQube?",
        difficulty: "easy",
        a: "<p><b>SonarLint</b> is a free IDE plugin that brings SonarQube's analysis capabilities directly into the developer's editor — VS Code, IntelliJ IDEA, Eclipse, and Visual Studio. It provides real-time feedback on the code being written, before it is committed or pushed.</p><p><b>Key differences:</b></p><table><tr><th>Aspect</th><th>SonarLint</th><th>SonarQube</th></tr><tr><td>Where it runs</td><td>Locally in the IDE, on the developer's machine</td><td>On a server, integrated with the CI pipeline</td></tr><tr><td>When it runs</td><td>As you type, immediately</td><td>On CI runs (per commit, PR, or nightly)</td></tr><tr><td>Analyzed code</td><td>Just the file(s) currently open</td><td>The entire codebase and its history</td></tr><tr><td>Rules used</td><td>Subset — most common rules; language-specific</td><td>Full rule set including cross-file and security dataflow analysis</td></tr><tr><td>Coverage</td><td>Not analyzed (no test runs)</td><td>Imports coverage from CI-run tests</td></tr><tr><td>Result storage</td><td>Local only</td><td>Persistent, shared, historical trending</td></tr></table><p><b>Connected Mode:</b> A key SonarLint feature. When configured to connect to a SonarQube server, SonarLint uses the exact same rules and quality profiles the server uses, ensuring the developer's IDE and the CI pipeline agree on what constitutes an issue. This dramatically reduces the surprise of \"passed locally, failed CI\".</p><p><b>How they work together in a typical DevOps workflow:</b></p><ol><li>Developer writes code in their IDE with SonarLint (Connected Mode).</li><li>SonarLint highlights issues as the developer types, allowing immediate fixes.</li><li>The developer commits and pushes.</li><li>CI runs the full SonarQube analysis, which catches cross-file, coverage, and duplication issues SonarLint cannot see locally.</li><li>The quality gate result determines whether the PR can merge.</li></ol><p>This combination — shift-left with SonarLint plus authoritative CI-time analysis with SonarQube — is the recommended pattern for teams using SonarQube.</p>",
      },
      {
        q: "What is Sonar's 'Clean as You Code' methodology?",
        difficulty: "medium",
        a: "<p><b>Clean as You Code (CAYC)</b> is SonarQube's recommended methodology for improving code quality progressively. It shifts the focus from cleaning up historical debt to keeping <i>new code</i> clean from the moment it is written. Over time, as more code is touched, the entire codebase converges toward high quality without demanding heroic cleanup campaigns.</p><p><b>Core principles:</b></p><ol><li><b>Focus on new code, not the whole project:</b> Quality gates evaluate metrics on new code (added or modified since a defined baseline) rather than the entire codebase. Legacy issues are visible but not blocking.</li><li><b>The Sonar way quality gate is the default target:</b> No new bugs, no new vulnerabilities, 80%+ coverage on new code, ≤3% duplication on new code, 100% hotspot review.</li><li><b>New Code Definition:</b> Each project or branch has an explicit definition of what counts as \"new\" — typically since the previous release version, since a fixed date, or since the previous analysis.</li><li><b>Author responsibility:</b> The developer who introduced an issue is expected to fix it, ideally before merging. The author is automatically identified via Git blame.</li></ol><p><b>Why this approach is effective:</b></p><ul><li><b>It is achievable:</b> Any team can commit to writing new clean code, even if their existing codebase has thousands of legacy issues. Setting a project-wide bar of \"zero bugs total\" is often unrealistic; setting a bar of \"zero new bugs\" is always achievable.</li><li><b>It is fair to developers:</b> Nobody is held responsible for issues inherited from prior years or from other teams.</li><li><b>It compounds naturally:</b> Any code that is refactored, modified, or extended becomes subject to the current quality standard. Over months and years, the entire codebase gradually reaches the target quality level as it is touched.</li><li><b>It reduces alert fatigue:</b> Rather than presenting developers with thousands of issues (mostly untouched legacy code), CAYC shows only the small set of issues introduced by recent changes — a manageable review load.</li></ul><p><b>Practical adoption steps:</b></p><ol><li>Set up SonarQube analysis on the main branch and every PR.</li><li>Define \"new code\" as everything since a stable baseline (e.g., the current release version).</li><li>Enable the default Sonar way quality gate.</li><li>Make failing gates block PR merges via GitHub/GitLab/Bitbucket integration.</li><li>Encourage SonarLint in Connected Mode for immediate feedback in IDEs.</li><li>Do not attempt to fix all legacy issues at once — allow them to be fixed opportunistically as code is touched.</li></ol>",
      },
      {
        q: "How do you customize SonarQube rules and quality profiles?",
        difficulty: "medium",
        a: "<p>SonarQube's analysis is driven by <b>rules</b> — hundreds of them per language — grouped into <b>Quality Profiles</b>. Each project analyzes against exactly one active profile per language. Customization allows teams to tune the ruleset to their coding conventions and risk tolerance.</p><p><b>Rules:</b> Each rule detects a specific issue pattern. Rules have:</p><ul><li>A unique key (e.g., <code>java:S1234</code>).</li><li>A category (Bug, Vulnerability, Code Smell, Security Hotspot).</li><li>A default severity (Blocker/Critical/Major/Minor/Info).</li><li>Tags for grouping (e.g., <code>owasp-a3</code>, <code>cwe</code>, <code>performance</code>).</li><li>Optional parameters (e.g., maximum method length threshold).</li></ul><p><b>Quality Profiles:</b> Named collections of active rules for a specific language.</p><ul><li>SonarQube ships with a default profile called <b>Sonar way</b> for each language — a curated set of the most impactful rules, updated by SonarSource with each release.</li><li>Custom profiles are typically created by copying Sonar way and adjusting from there.</li></ul><p><b>Common customizations:</b></p><ol><li><b>Disable rules that conflict with organizational conventions.</b> For example, a rule requiring the use of an interface may not fit a specific project.</li><li><b>Change severity levels</b> to reflect business impact. A rule marked \"Minor\" upstream may be \"Critical\" in your context, or vice versa.</li><li><b>Adjust rule parameters.</b> The \"maximum method length\" rule has a threshold parameter — 100 lines by default, but a team may lower it to 50.</li><li><b>Add rules not enabled in Sonar way.</b> SonarQube includes stricter rules that are off by default; teams with strong quality bars can enable them.</li><li><b>Language-specific coding standards.</b> For example, enable Google Java Style compliance rules or React best-practice rules for a JavaScript team.</li></ol><p><b>Managing profiles at scale:</b></p><ul><li>Profiles can be exported and imported as XML for version control and repeatability.</li><li>The <b>Compare</b> feature shows differences between profiles — useful when merging changes from upstream Sonar way updates into a custom profile.</li><li>The <b>Built-in profile inheritance</b> lets a custom profile inherit from Sonar way and add its own changes, automatically staying in sync with upstream additions.</li></ul><p><b>Best practice:</b> Start with the built-in Sonar way profile for each language and resist heavy customization initially. Only tune rules after observing real analysis results and identifying rules that generate false positives for your codebase. Over-customizing early leads to profiles that are hard to maintain.</p>",
      },
      {
        q: "What is PR decoration and branch analysis in SonarQube?",
        difficulty: "medium",
        a: "<p><b>Branch analysis</b> and <b>PR decoration</b> are commercial-edition features that make SonarQube far more useful for modern feature-branch and pull-request workflows. They enable per-branch and per-PR quality assessment with automatic feedback in the source hosting platform.</p><p><b>Branch analysis:</b> SonarQube can analyze not just the main branch, but any feature branch, computing:</p><ul><li>Issues, coverage, and duplication specific to that branch.</li><li>New code defined as changes relative to a target (typically <code>main</code> or the release branch).</li><li>Quality gate evaluation per branch.</li></ul><p>Each branch appears independently in the SonarQube UI, with its own history and quality gate status. This lets teams see the quality of work-in-progress before merging.</p><p><b>PR decoration:</b> When a pull/merge request is opened, SonarQube analyzes the PR against its target branch and posts analysis results back to the source hosting platform:</p><ul><li><b>GitHub:</b> Comments on the PR with the analysis summary, inline code comments on specific issues, and a check status that gates the merge.</li><li><b>GitLab:</b> Merge request comments and pipeline status integration.</li><li><b>Bitbucket:</b> Build status and code insights displayed directly in the PR view.</li><li><b>Azure DevOps:</b> Pull request comments and build validation policy.</li></ul><p><b>How it looks in practice:</b></p><ol><li>A developer opens a PR.</li><li>The CI pipeline runs, including the SonarScanner with PR-specific parameters (<code>sonar.pullrequest.key</code>, <code>sonar.pullrequest.branch</code>, <code>sonar.pullrequest.base</code>).</li><li>SonarQube analyzes only the PR's diff, comparing quality metrics against the target branch.</li><li>Results appear as PR comments and a merge status check.</li><li>If the PR's quality gate fails, GitHub's branch protection can block the merge.</li></ol><p><b>Why this matters:</b> Without PR decoration, reviewers have to visit SonarQube manually to check analysis results. With it, quality issues are surfaced in the same UI where the code review is already happening, resulting in significantly higher adoption and faster fix cycles.</p><p><b>Editions required:</b> Branch analysis and PR decoration are included in Developer, Enterprise, and Data Center Editions (paid tiers), and in the SonarCloud SaaS offering (with a free tier for open source projects). Community Edition (free, self-hosted) supports only main-branch analysis.</p>",
      },
      {
        q: "What are the alternatives to SonarQube?",
        difficulty: "medium",
        a: "<p>Several products compete with SonarQube in the code quality, static analysis, and security scanning space. Each has different strengths, and enterprises frequently combine two or more.</p><p><b>Direct SonarQube alternatives (broad code quality and security):</b></p><ul><li><b>Codacy:</b> SaaS platform focused on code quality automation. Simple to onboard, wide language support, tight VCS integration.</li><li><b>Code Climate Quality:</b> SaaS with a focus on maintainability metrics and technical debt visualization.</li><li><b>SemGrep:</b> Open-source pattern-based static analysis engine with a strong community rule library. Increasingly used for custom organizational rules and security scanning.</li><li><b>DeepSource:</b> SaaS platform with strong AI-assisted fix suggestions.</li></ul><p><b>Language-specific analyzers (often complementary):</b></p><ul><li>ESLint (JavaScript/TypeScript).</li><li>Pylint, Flake8, Bandit (Python).</li><li>Checkstyle, PMD, SpotBugs (Java).</li><li>rubocop (Ruby).</li><li>golangci-lint (Go).</li></ul><p>These are often run alongside SonarQube — the IDE/CI runs the native linter for immediate developer feedback, and SonarQube provides the aggregated project-level quality gate.</p><p><b>Security-focused SAST alternatives:</b></p><ul><li><b>Checkmarx:</b> Enterprise SAST with deep source code security analysis. Strong in regulated industries.</li><li><b>Fortify (OpenText):</b> Long-standing enterprise SAST product, strong in financial services and government.</li><li><b>Snyk Code:</b> AI-powered SAST focused on developer experience and remediation suggestions.</li><li><b>Veracode:</b> Enterprise SAST/DAST platform.</li><li><b>GitHub Advanced Security (CodeQL):</b> GitHub's native SAST powered by CodeQL. Increasingly popular for teams already on GitHub.</li></ul><p><b>Comparison guidance:</b></p><ul><li><b>Choose SonarQube when:</b> You want a broad quality-plus-security platform, need self-hosted deployment, value the strong \"clean as you code\" methodology, and have a diverse polyglot codebase.</li><li><b>Choose SemGrep when:</b> You need customizable pattern-based rules, want a free/open option, or need lightweight scanning in specific pipelines.</li><li><b>Choose GitHub Advanced Security when:</b> Your source is on GitHub, and you want tight platform integration for security-specific analysis.</li><li><b>Choose dedicated SAST (Checkmarx/Fortify/Snyk):</b> When security compliance or regulatory requirements demand deeper security-specific analysis than SonarQube provides.</li></ul><p><b>Frequently seen enterprise setup:</b> SonarQube for broad quality + a dedicated SAST tool for security + language-specific linters in IDEs/CI. The multi-tool approach delivers coverage that no single product matches.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "terraform",
    name: "Terraform & IaC",
    icon: "🏗️",
    kind: "qa",
    description:
      "Provisioning cloud infra as declarative code — state, modules, backends, and the safety patterns that keep you out of trouble.",
    items: [
      {
        q: "What is Terraform state and why is it critical?",
        difficulty: "medium",
        a: "<p>The <b>Terraform state</b> is a JSON file (typically <code>terraform.tfstate</code>) that acts as Terraform's record of the real-world resources it manages. It maintains a mapping between the resource addresses defined in the Terraform configuration and the actual identifiers of those resources in the target infrastructure — for example, mapping the resource <code>aws_instance.web</code> in code to the specific EC2 instance ID <code>i-0abc123def456</code>.</p><p><b>Why state is essential:</b></p><ul><li><b>Diff computation:</b> When Terraform runs <code>plan</code> or <code>apply</code>, it compares the desired state (the configuration) against the current state (the state file) against the real infrastructure (queried from the provider) to determine what actions are needed. Without state, Terraform has no way to know what already exists and would attempt to recreate everything on every run.</li><li><b>Resource metadata:</b> State stores attributes that are only known after resource creation — for example, an EC2 instance's private IP, an RDS instance's endpoint, or an auto-generated resource name. These are used as inputs to other resources.</li><li><b>Dependency tracking:</b> State records the dependency graph between resources, which drives the order in which resources are created, updated, or destroyed.</li></ul><p><b>Security consideration:</b> State files often contain sensitive values — database passwords, private keys, access tokens — even if those values are marked as sensitive in the configuration. Because of this, state files must be treated as sensitive artifacts and stored securely.</p>",
        followups: [
          {
            q: "How do you store state safely for a team?",
            a: "<p>Local state files are unsuitable for team environments because they cannot be shared, do not support concurrent operations, and provide no version history. The solution is a <b>remote backend</b> — a centralized location for state with three critical properties: locking, versioning, and encryption.</p><p><b>Recommended backends by cloud provider:</b></p><ul><li><b>AWS:</b> An S3 bucket for state storage (versioned and KMS-encrypted) combined with a DynamoDB table for state locking. Access control via IAM policies restricts who can read or write state.</li><li><b>Azure:</b> An Azure Storage Account with blob-lease-based locking and Azure encryption at rest.</li><li><b>Google Cloud:</b> A GCS bucket with object versioning and built-in state locking via GCS object generation.</li><li><b>Terraform Cloud / Terraform Enterprise:</b> A fully managed backend that provides state storage, locking, remote execution, RBAC, and full audit logging without requiring cloud-provider-specific configuration.</li></ul><p><b>Example S3 backend configuration:</b></p><pre>terraform {\n  backend \"s3\" {\n    bucket         = \"my-tf-state\"\n    key            = \"prod/vpc/terraform.tfstate\"\n    region         = \"us-east-1\"\n    encrypt        = true\n    dynamodb_table = \"terraform-locks\"\n  }\n}</pre>",
          },
          {
            q: "What is a state lock and why do you need it?",
            a: "<p>A <b>state lock</b> is a distributed lock — typically implemented as a DynamoDB item, GCS object generation, or Azure blob lease — that prevents multiple concurrent <code>terraform apply</code> or <code>plan</code> operations from executing against the same state file.</p><p><b>Why locking is essential:</b> Without locking, two engineers running <code>apply</code> simultaneously could both read the state, make different changes, and both write back — resulting in state corruption or conflicting API calls to the provider. Locks ensure serialized access to the state.</p><p><b>How it works:</b> Before any operation that reads or modifies state, Terraform attempts to acquire a lock. If another operation holds the lock, Terraform waits (or fails, depending on configuration). When the operation completes, the lock is released.</p><p><b>Handling stuck locks:</b> Occasionally a lock can become stranded — for example, if a Terraform process was killed with Ctrl+C during an apply or crashed unexpectedly. The stuck lock can be manually released with:</p><pre>terraform force-unlock &lt;LOCK_ID&gt;</pre><p><b>Caution:</b> Only use <code>force-unlock</code> after confirming that no other Terraform run is actually in progress. Force-unlocking a legitimately held lock can lead to state corruption.</p>",
          },
        ],
      },
      {
        q: "Difference between Terraform and Ansible / CloudFormation / Pulumi?",
        difficulty: "medium",
        a: "<p>Although these tools are sometimes grouped together as \"Infrastructure as Code,\" they serve different purposes and have different design philosophies.</p><p><b>Terraform:</b> A declarative infrastructure provisioning tool using HashiCorp Configuration Language (HCL). It is cloud-agnostic — supporting AWS, Azure, GCP, and hundreds of other providers — and focuses on creating, updating, and destroying infrastructure resources. Its strength is a vast provider ecosystem and a rich state model that tracks resources across long-lived infrastructure.</p><p><b>AWS CloudFormation:</b> A declarative provisioning tool using YAML or JSON, native to AWS. Advantages include deep AWS integration, no external state file (state is managed by AWS), and support for advanced AWS features like stack sets and drift detection. Drawbacks include vendor lock-in (AWS only) and slower adoption of new AWS services compared to Terraform's community-maintained provider.</p><p><b>Pulumi:</b> Similar in scope to Terraform (declarative, cloud-agnostic provisioning) but uses general-purpose programming languages — TypeScript, Python, Go, C#, Java — instead of a domain-specific language. This allows using loops, conditionals, functions, and testing frameworks that developers already know. Trade-off: the flexibility can lead to less consistent code across teams.</p><p><b>Ansible:</b> Primarily a <b>configuration management</b> tool, not a provisioning tool. Its focus is configuring and maintaining state on already-existing systems — installing packages, managing services, deploying files. It can provision cloud resources via its cloud modules, but it lacks Terraform's state model and dependency graph, making it less suited for managing infrastructure lifecycle.</p><p><b>Common architecture:</b> Many organizations use Terraform for provisioning (creating the VPC, EKS cluster, RDS instances) and Ansible for configuration (hardening the OS on EC2 instances, deploying application configurations).</p>",
      },
      {
        q: "What are Terraform Modules and how do you structure them?",
        difficulty: "medium",
        a: "<p>A <b>module</b> in Terraform is a directory containing <code>.tf</code> files that can be invoked from other Terraform configurations. Modules are the primary mechanism for code reuse and abstraction in Terraform — they encapsulate a related set of resources behind well-defined inputs and outputs.</p><p><b>Module structure:</b> A typical module directory contains:</p><ul><li><code>main.tf</code> — the core resource definitions.</li><li><code>variables.tf</code> — declared input variables with types and descriptions.</li><li><code>outputs.tf</code> — declared outputs that can be consumed by callers.</li><li><code>versions.tf</code> — required Terraform and provider versions.</li><li><code>README.md</code> — documentation (for shared modules).</li></ul><p><b>Typical repository layout:</b></p><pre>modules/\n  vpc/\n    main.tf\n    variables.tf\n    outputs.tf\n  eks/\n    main.tf\n    variables.tf\n    outputs.tf\nenvs/\n  prod/\n    main.tf         # calls modules with prod-specific inputs\n    backend.tf\n  dev/\n    main.tf         # calls the same modules with dev-specific inputs\n    backend.tf</pre><p><b>Calling a module:</b></p><pre>module \"vpc\" {\n  source = \"../../modules/vpc\"\n  cidr   = \"10.0.0.0/16\"\n  name   = \"prod\"\n}</pre><p><b>Shared modules:</b> For modules used across multiple projects or teams, publish them to a private registry — Terraform Cloud's Private Module Registry, Artifactory, or a Git repository — with Semantic Versioning tags. This enables independent versioning: modules can be improved without immediately affecting all consumers.</p>",
        followups: [
          {
            q: "How do you version and pin modules?",
            a: "<p>Modules published to a registry are versioned semantically. Consumers should always pin to a specific version to ensure reproducible builds.</p><p><b>Registry-based module with version constraint:</b></p><pre>module \"vpc\" {\n  source  = \"app.terraform.io/acme/vpc/aws\"\n  version = \"~&gt; 3.2\"           # matches &gt;= 3.2.0, &lt; 4.0.0\n  cidr    = \"10.0.0.0/16\"\n}</pre><p><b>Version constraint operators:</b></p><ul><li><code>= 3.2.1</code> — exact version.</li><li><code>&gt;= 3.2.0</code> — minimum version.</li><li><code>~&gt; 3.2</code> — pessimistic constraint at the minor level (&gt;=3.2, &lt;4.0).</li><li><code>~&gt; 3.2.1</code> — pessimistic at the patch level (&gt;=3.2.1, &lt;3.3.0).</li></ul><p><b>Git-based module with tag pinning:</b></p><pre>module \"vpc\" {\n  source = \"git::https://github.com/acme/tf-vpc.git?ref=v3.2.0\"\n}</pre><p><b>Recommendation:</b> Use exact version pinning for production environments to prevent unexpected changes. Use pessimistic constraints for shared internal modules where minor improvements should propagate automatically.</p>",
          },
        ],
      },
      {
        q: "What is `terraform plan` vs `terraform apply` vs `terraform destroy`?",
        difficulty: "easy",
        a: "<p>These three commands represent the core Terraform lifecycle operations, each with a distinct purpose and safety profile.</p><ul><li><b>terraform plan:</b> Computes and displays the differences between the current state, the target infrastructure, and the desired configuration. It is <b>read-only</b> — no infrastructure changes are made. Output shows resources to be created (<code>+</code>), updated (<code>~</code>), destroyed (<code>-</code>), or replaced (<code>-/+</code>).</li><li><b>terraform apply:</b> Executes the changes to bring the infrastructure into the desired state. It first computes a plan, presents it for confirmation, and then executes it. This is the command that actually modifies infrastructure.</li><li><b>terraform destroy:</b> Removes all resources managed by the current configuration. Effectively equivalent to setting an empty configuration and applying it. Destructive and irreversible for stateful resources.</li></ul><p><b>Recommended CI workflow:</b> To ensure the applied changes match exactly what was reviewed:</p><pre>terraform plan -out=tf.plan   # produce a saved plan\n# review the plan output\nterraform apply tf.plan         # apply the exact reviewed plan</pre><p>Using a saved plan file eliminates the risk of infrastructure changing between the plan and apply phases (which could cause the apply to differ from what was reviewed).</p><p><b>Additional useful commands:</b> <code>terraform validate</code> checks configuration syntax, <code>terraform fmt</code> formats code, and <code>terraform state list/show/rm/mv</code> allows inspection and manipulation of state.</p>",
        followups: [
          {
            q: "How would you preview changes safely in a PR workflow?",
            a: "<p>The recommended approach is to integrate Terraform plan review into the pull request workflow, so that infrastructure changes are reviewed and approved before being applied.</p><p><b>Atlantis-based workflow:</b></p><ol><li>Engineer opens a pull request modifying Terraform code.</li><li>Atlantis (a webhook-driven Terraform automation tool) automatically runs <code>terraform plan</code> and posts the diff as a comment on the pull request.</li><li>Reviewers examine the plan and code changes together, ensuring the intended infrastructure change is what will actually happen.</li><li>Once approved, an approver comments <code>atlantis apply</code>, which triggers the apply against the reviewed plan.</li></ol><p><b>Terraform Cloud / Terraform Enterprise workflow:</b> Similar mechanism using Terraform Cloud's built-in VCS integration. Plans are automatically triggered on pull requests, displayed in the Terraform Cloud UI, and applies require approval from configured team members.</p><p><b>Benefits of this pattern:</b></p><ul><li><b>Peer review of infrastructure changes:</b> Same rigor as code review.</li><li><b>No local <code>apply</code> from engineer laptops:</b> All changes flow through the reviewed path.</li><li><b>Audit trail:</b> Every applied change is linked to a pull request with reviewer approvals.</li><li><b>Consistent execution environment:</b> Plans and applies run in a controlled environment, not on engineer machines.</li></ul>",
          },
        ],
      },
      {
        q: "What is OpenTofu and why did it fork from Terraform?",
        difficulty: "medium",
        a: "<p><b>OpenTofu</b> is an open-source fork of Terraform, created in 2023 in response to HashiCorp's decision to change Terraform's license from Mozilla Public License (MPL 2.0) to the Business Source License (BSL). It is now a Linux Foundation project under the CNCF umbrella.</p><p><b>The trigger event:</b> In August 2023, HashiCorp announced that Terraform (and its other core products) would move from open source to BSL 1.1. Under BSL, the source remains visible, but commercial use that competes with HashiCorp's offerings is restricted. This affected companies building products around Terraform, Terraform Cloud alternatives, and many ecosystem tools. Within weeks, a coalition of companies — including Gruntwork, Spacelift, env0, and Scalr — announced OpenTF, which quickly became OpenTofu and was accepted into the Linux Foundation.</p><p><b>Key facts about OpenTofu:</b></p><ul><li><b>Fully compatible with Terraform 1.5.x</b> — the last MPL version. Existing configurations, state files, providers, and modules work unchanged.</li><li><b>Drop-in binary replacement:</b> <code>tofu</code> replaces <code>terraform</code> at the CLI level. Most workflows only require aliasing the command.</li><li><b>MPL 2.0 licensed</b> — remains truly open source.</li><li><b>Backed by a broad coalition</b> — reduces vendor lock-in risk that motivated the fork.</li><li><b>Diverging feature set:</b> OpenTofu 1.6, 1.7, and 1.8 have added features Terraform did not include, such as <b>state encryption</b> (native, without needing an external backend feature) and <b>provider-defined functions</b> improvements.</li></ul><p><b>Practical migration:</b></p><pre># Install OpenTofu (Linux example)\ncurl -Lo /tmp/tofu.zip https://github.com/opentofu/opentofu/releases/latest/download/tofu_linux_amd64.zip\nunzip /tmp/tofu.zip -d /usr/local/bin/\n\ntofu init\ntofu plan\ntofu apply</pre><p>State files, module source URLs, and provider blocks are unchanged. Existing Terraform Cloud users cannot use OpenTofu with Terraform Cloud (HashiCorp restricted access), but competing SaaS platforms (Spacelift, env0, Scalr, Terrateam) have added OpenTofu support.</p><p><b>Should you migrate?</b></p><ul><li><b>New projects:</b> OpenTofu is a reasonable default, particularly for open source projects, cost-sensitive teams, and those wanting stronger licensing guarantees.</li><li><b>Existing Terraform projects:</b> No urgent reason to migrate if BSL is acceptable for your use case. HashiCorp has committed to keeping Terraform functional and improving it.</li><li><b>Enterprise customers on Terraform Cloud/Enterprise:</b> Terraform remains the seamless choice for HashiCorp's paid offerings.</li></ul><p>The fork is a significant event in the IaC ecosystem — a reminder that widely-adopted open source projects have community expectations that can lead to forks when license changes are perceived as breaking trust. For most day-to-day DevOps work, either tool works identically.</p>",
      },
      {
        q: "How do you import existing infrastructure into Terraform?",
        difficulty: "medium",
        a: "<p>When adopting Terraform for infrastructure that was created manually or by other tools, you need to <b>import</b> those resources into Terraform state so future changes can be managed declaratively. Terraform provides two mechanisms.</p><p><b>1. The traditional <code>terraform import</code> command (all versions):</b></p><pre># Step 1: Write a matching resource block in .tf files\nresource \"aws_s3_bucket\" \"logs\" {\n  bucket = \"my-existing-bucket\"\n}\n\n# Step 2: Import into state\nterraform import aws_s3_bucket.logs my-existing-bucket\n\n# Step 3: Run plan to see what Terraform thinks is different\nterraform plan</pre><p>The <code>import</code> command records the resource in state but does not generate configuration. You must write the resource block yourself, matching the current settings exactly. Typically the first <code>terraform plan</code> after import shows differences — attributes you didn't yet specify. You iteratively refine the config until <code>plan</code> shows no changes.</p><p><b>2. The <code>import</code> block (Terraform 1.5+, OpenTofu supported):</b> Declarative import that lives in configuration and integrates with plan/apply.</p><pre>import {\n  to = aws_s3_bucket.logs\n  id = \"my-existing-bucket\"\n}\n\nresource \"aws_s3_bucket\" \"logs\" {\n  bucket = \"my-existing-bucket\"\n  # ... other configuration ...\n}</pre><pre>terraform plan\n# Shows: \"Terraform will perform the following actions: import my-existing-bucket to logs\"\nterraform apply</pre><p>The import block approach has meaningful advantages:</p><ul><li>Import is part of your version-controlled configuration, reviewable via PR.</li><li>Multiple imports can happen in a single apply, atomically.</li><li>Works well with CI/CD workflows.</li></ul><p><b>Config generation (Terraform 1.5+):</b> Import blocks can also <b>generate the configuration</b> for you:</p><pre>terraform plan -generate-config-out=generated.tf</pre><p>This produces a <code>generated.tf</code> file with the exact resource configuration matching the imported state — a huge time-saver when importing many resources.</p><p><b>Third-party tools for bulk import:</b></p><ul><li><b>Terraformer:</b> Reverse-engineers cloud resources into Terraform code. Especially useful for large existing environments.</li><li><b>Former2:</b> Web-based tool for generating IaC from AWS.</li><li>Cloud-native alternatives: AWS's own <code>aws-cloudformation-guard</code>, Azure's <code>aztfy</code> for Terraform generation from Azure resources.</li></ul><p><b>Best practices:</b></p><ul><li>Start with a small pilot — import a handful of resources, verify plan shows zero changes, iterate on your workflow before scaling up.</li><li>Import into a state file backed up beforehand. If something goes wrong, you can restore.</li><li>Use targeted plans (<code>-target=aws_s3_bucket.logs</code>) to focus on imported resources without noise from unrelated changes.</li><li>After successful import, review the config for opportunities to parameterize (use variables) rather than hardcoding values that were just imported.</li></ul>",
      },
      {
        q: "What is Sentinel / OPA and how do you use them with Terraform?",
        difficulty: "hard",
        a: "<p>Policy-as-Code tools enforce governance rules on Terraform configurations before they are applied — preventing misconfigurations, security issues, and cost overruns from reaching production. The two dominant options are HashiCorp's <b>Sentinel</b> (commercial, Terraform Cloud/Enterprise) and <b>Open Policy Agent (OPA)</b> with <b>Conftest</b> (open source, CNCF).</p><p><b>What policy-as-code enforces:</b></p><ul><li>All EC2 instances must use specific AMIs from an approved list.</li><li>S3 buckets must have encryption enabled and public access disabled.</li><li>Resources must have required tags (owner, environment, cost-center).</li><li>Only specific instance types are allowed (cost control).</li><li>Kubernetes clusters must have logging enabled.</li><li>No resources may be created in unapproved regions.</li></ul><p><b>Sentinel (HashiCorp Terraform Cloud/Enterprise):</b> A domain-specific policy language integrated tightly with Terraform.</p><pre># enforce-mandatory-tags.sentinel\nimport \"tfplan/v2\" as tfplan\n\nmandatory_tags = [\"Environment\", \"Owner\", \"CostCenter\"]\n\naws_instances = filter tfplan.resource_changes as _, rc {\n  rc.mode is \"managed\" and\n  rc.type is \"aws_instance\" and\n  rc.change.actions contains \"create\"\n}\n\nmain = rule {\n  all aws_instances as _, instance {\n    all mandatory_tags as tag {\n      instance.change.after.tags contains tag\n    }\n  }\n}</pre><p>Sentinel policies are attached to Terraform Cloud workspaces or organizations and run automatically before plan/apply. Failures block the operation.</p><p><b>Open Policy Agent (OPA) with Conftest:</b> OPA uses a language called Rego and works with any IaC via JSON representation.</p><pre># deny-untagged-instances.rego\npackage main\n\ndeny[msg] {\n  resource := input.resource_changes[_]\n  resource.type == \"aws_instance\"\n  resource.change.actions[_] == \"create\"\n  required := {\"Environment\", \"Owner\", \"CostCenter\"}\n  missing := required - {tag | tag := resource.change.after.tags[_]}\n  count(missing) > 0\n  msg := sprintf(\"aws_instance.%s missing tags: %v\", [resource.address, missing])\n}</pre><pre>terraform plan -out=tfplan.binary\nterraform show -json tfplan.binary > tfplan.json\nconftest test tfplan.json --policy policies/</pre><p><b>Integration in CI/CD:</b> Both approaches slot naturally into pipelines:</p><ol><li>Run <code>terraform plan</code> and export the JSON plan.</li><li>Run OPA/Sentinel policies against the plan.</li><li>If policies fail, fail the pipeline before <code>terraform apply</code>.</li></ol><p><b>Other tools in this space:</b></p><ul><li><b>Checkov:</b> Static analysis with hundreds of pre-built policies for security best practices. Excellent starting point — no policy writing required.</li><li><b>tfsec / Trivy config:</b> Similar security-focused static analyzers.</li><li><b>Terrascan:</b> Another static analyzer with a large policy library.</li><li><b>Spacelift, env0, Scalr:</b> Third-party Terraform automation platforms with built-in policy engines.</li></ul><p><b>Recommended progression:</b> Start with Checkov for out-of-the-box security policies. Add OPA/Conftest for organization-specific rules (tagging conventions, allowed regions, cost controls). Move to Sentinel only if using Terraform Cloud/Enterprise and needing tighter integration.</p>",
      },
      {
        q: "How do you publish and consume Terraform modules?",
        difficulty: "medium",
        a: "<p><b>Modules</b> are the primary mechanism for reuse in Terraform. A well-designed module encapsulates a piece of infrastructure (a VPC, an EKS cluster, a standard S3 bucket configuration) with clear inputs and outputs, so consumers do not repeat implementation details.</p><p><b>Consuming a module:</b> Modules can be sourced from many locations:</p><pre># Local filesystem\nmodule \"vpc\" {\n  source = \"./modules/vpc\"\n  cidr   = \"10.0.0.0/16\"\n}\n\n# GitHub (public or via SSH)\nmodule \"eks\" {\n  source  = \"git@github.com:example/tf-modules.git//eks?ref=v1.2.0\"\n  cluster_name = \"prod-eks\"\n}\n\n# Terraform Registry (public)\nmodule \"vpc\" {\n  source  = \"terraform-aws-modules/vpc/aws\"\n  version = \"~> 5.0\"\n  name    = \"prod-vpc\"\n  cidr    = \"10.0.0.0/16\"\n}\n\n# Private module registry (Terraform Cloud, Spacelift, custom)\nmodule \"database\" {\n  source  = \"app.terraform.io/mycompany/rds/aws\"\n  version = \"1.5.2\"\n}</pre><p><b>Anatomy of a good module:</b></p><pre>modules/eks/\n├── README.md          # documentation with examples\n├── main.tf            # primary resources\n├── variables.tf       # inputs with descriptions and validations\n├── outputs.tf         # exposed outputs\n├── versions.tf        # required Terraform and provider versions\n├── locals.tf          # internal computed values\n└── examples/          # example consumer configurations\n    ├── basic/\n    └── with-fargate/</pre><p><b>Publishing to the public Terraform Registry:</b></p><ol><li>Store the module in a public GitHub repo named <code>terraform-&lt;PROVIDER&gt;-&lt;NAME&gt;</code> (e.g., <code>terraform-aws-eks</code>).</li><li>Tag releases with semantic version tags (<code>v1.0.0</code>, <code>v1.1.0</code>, <code>v2.0.0</code>).</li><li>Sign in to registry.terraform.io with GitHub and add the repo. The registry automatically detects the semver tags as module versions.</li></ol><p><b>Publishing to a private registry:</b> Terraform Cloud, Spacelift, and other platforms provide private registries. Modules can be pushed via API, or auto-imported from private Git repositories.</p><p><b>Semantic versioning discipline:</b></p><ul><li><b>MAJOR</b> — breaking changes to input variables, outputs, or default behavior.</li><li><b>MINOR</b> — new optional variables or new outputs; backwards compatible.</li><li><b>PATCH</b> — bug fixes, no interface changes.</li></ul><p>Consumers pin versions using constraint syntax:</p><pre>version = \"~> 2.5\"    # any 2.x version >= 2.5, but not 3.x\nversion = \">= 2.5.0, < 3.0.0\"\nversion = \"2.5.0\"     # exact pin</pre><p><b>Best practices for module design:</b></p><ul><li>Keep modules focused — one logical piece of infrastructure per module.</li><li>Provide sensible defaults so simple use cases require few variables.</li><li>Expose sensible outputs — anything downstream might need.</li><li>Use variable validations for constraints (allowed values, patterns).</li><li>Never hard-code credentials or account-specific values inside modules.</li><li>Include usage examples in the <code>examples/</code> directory — these also serve as integration tests.</li><li>Test modules with <code>terraform test</code> (Terraform 1.6+) or third-party frameworks like Terratest.</li></ul><p><b>Community modules to know:</b> <code>terraform-aws-modules/*</code> (vpc, eks, rds, alb) are widely adopted and battle-tested. Start with these when possible rather than writing from scratch.</p>",
      },
      {
        q: "What are Terragrunt and Atlantis?",
        difficulty: "medium",
        a: "<p><b>Terragrunt</b> and <b>Atlantis</b> are two ecosystem tools that address gaps in Terraform's out-of-the-box experience for real-world team usage. They serve very different purposes but are often used together.</p><p><b>Terragrunt — DRY configuration for Terraform:</b> A thin wrapper around Terraform, developed by Gruntwork. It solves several problems that arise when managing many environments with Terraform:</p><ul><li><b>Duplicate backend configurations:</b> Every Terraform root module needs a backend block. Terragrunt generates them from a common template.</li><li><b>Duplicate provider configurations:</b> Providers block often looks identical across environments. Terragrunt DRYs this up.</li><li><b>Environment-specific variable files:</b> Terragrunt uses HCL configuration to inherit and override values across environments.</li><li><b>Dependencies between modules:</b> Native Terraform doesn't have great cross-root-module dependency handling. Terragrunt introduces explicit dependency blocks.</li></ul><p><b>Typical Terragrunt directory structure:</b></p><pre>infra/\n├── terragrunt.hcl          # root config with backend, provider generation\n├── prod/\n│   ├── env.hcl             # env-specific vars\n│   ├── us-east-1/\n│   │   ├── region.hcl\n│   │   ├── vpc/\n│   │   │   └── terragrunt.hcl    # inherits parents, minimal file\n│   │   └── eks/\n│   │       └── terragrunt.hcl\n└── stage/ ...</pre><pre># infra/prod/us-east-1/vpc/terragrunt.hcl\ninclude \"root\" {\n  path = find_in_parent_folders()\n}\n\nterraform {\n  source = \"git::git@github.com:example/tf-modules.git//vpc?ref=v1.2.0\"\n}\n\ninputs = {\n  cidr = \"10.0.0.0/16\"\n  name = \"prod-us-east-1\"\n}</pre><p>The <code>terragrunt run-all apply</code> command respects dependencies and applies modules in order.</p><p><b>Atlantis — GitOps for Terraform:</b> A self-hosted service that integrates Terraform with pull requests. When a developer opens a PR modifying Terraform code, Atlantis:</p><ol><li>Runs <code>terraform plan</code> for each affected root module.</li><li>Posts the plan output as a PR comment.</li><li>Enforces approvals — an authorized reviewer comments <code>atlantis apply</code> to trigger the apply.</li><li>Locks the workspace during plan/apply to prevent concurrent operations.</li></ol><p><b>Why teams use Atlantis:</b></p><ul><li><b>PR-based workflow:</b> No developer runs <code>terraform apply</code> from their laptop against production.</li><li><b>Visible plans in code review:</b> Reviewers see exactly what will change before approving.</li><li><b>Central credentials:</b> Atlantis holds cloud credentials in one place, rather than distributing them to every developer.</li><li><b>Free and open source:</b> Compared to Terraform Cloud, Atlantis is a compelling zero-cost option for teams that can self-host.</li></ul><p><b>Alternatives in this space:</b></p><ul><li><b>Terraform Cloud / Enterprise:</b> HashiCorp's SaaS/self-hosted platform. Similar functionality plus policy enforcement (Sentinel), private module registry, and workspace management.</li><li><b>Spacelift, env0, Scalr:</b> Commercial SaaS competitors with richer feature sets — better UX, policy engines, drift detection, cost estimation.</li><li><b>Terrateam:</b> A newer open-source Terraform automation tool with strong OpenTofu support.</li></ul><p><b>When to adopt each:</b></p><ul><li><b>Terragrunt</b> — when managing more than 3–4 environments and finding yourself copy-pasting Terraform boilerplate.</li><li><b>Atlantis</b> — when your team is 5+ people running Terraform, and you want PR-based governance without paying for SaaS.</li><li><b>Terraform Cloud / Spacelift / env0</b> — when you want a fully managed experience with policy, drift detection, and enterprise features, and don't want to self-host.</li></ul>",
      },
      {
        q: "What is drift and how do you detect it?",
        difficulty: "medium",
        a: "<p><b>Configuration drift</b> occurs when the real state of infrastructure diverges from what is recorded in Terraform state. Common causes include:</p><ul><li>Manual changes made through the cloud provider's console or CLI (\"ClickOps\").</li><li>Automated systems that modify resources outside of Terraform (auto-scaling groups adjusting capacity, autopatch tools changing OS versions).</li><li>Provider API changes or resource attributes that mutate on their own.</li></ul><p><b>Why drift is problematic:</b> When infrastructure differs from what Terraform expects, the next apply may attempt to \"correct\" the drift by reverting the change — potentially breaking whatever depended on the manual change. Alternatively, subsequent applies may fail with confusing errors because Terraform's assumptions no longer match reality.</p><p><b>Detecting drift:</b> Running <code>terraform plan</code> against unchanged code will show a non-empty diff if drift exists — the differences shown represent the divergence between state and reality.</p><p><b>Automated drift detection:</b> Schedule a periodic (e.g., nightly) job that runs:</p><pre>terraform plan -detailed-exitcode</pre><p>The <code>-detailed-exitcode</code> flag returns:</p><ul><li><b>0</b> — no changes needed (no drift).</li><li><b>1</b> — an error occurred.</li><li><b>2</b> — changes are planned (drift or configuration change detected).</li></ul><p>A monitoring job can alert on exit code 2, enabling proactive drift response before it accumulates or causes an outage.</p><p><b>Prevention:</b> Restrict console access, use SCPs to require Terraform for certain changes, and adopt policy-as-code tools like OPA or Sentinel to enforce that all changes flow through Terraform.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "ansible",
    name: "Ansible",
    icon: "🎯",
    kind: "qa",
    description:
      "Agentless config management — playbooks, roles, inventory, and Ansible-flavored idempotence.",
    items: [
      {
        q: "How does Ansible differ from Puppet/Chef?",
        difficulty: "medium",
        a: "<p>Ansible, Puppet, and Chef are all configuration management tools that automate the configuration of servers and infrastructure, but they differ significantly in architecture, philosophy, and operational model.</p><p><b>Ansible's key differentiators:</b></p><ul><li><b>Agentless architecture:</b> Ansible uses SSH on Linux and WinRM on Windows to connect to managed hosts. No software needs to be installed on the target systems beyond the standard system components. This eliminates the operational burden of installing, updating, and troubleshooting agent software.</li><li><b>Push-based execution:</b> The control node initiates connections to managed hosts and pushes configuration to them. In contrast, Puppet and Chef use a pull model where agents on each host periodically poll a central server for their configuration.</li><li><b>YAML-based playbooks:</b> Ansible configuration is written in YAML, which is significantly more accessible than Puppet's domain-specific language or Chef's Ruby. This lowers the barrier to entry for non-developer team members.</li><li><b>Sequential task execution:</b> Ansible executes tasks in the order they appear in the playbook (top to bottom). Puppet uses a declarative dependency graph where order is derived from resource relationships. Sequential execution is often easier to reason about but requires more explicit ordering.</li></ul><p><b>Trade-offs:</b> Ansible's push model can be slower for very large fleets (thousands of hosts) than a pull model, though projects like Ansible Automation Platform mitigate this with distributed execution. Puppet and Chef offer more mature reporting and drift-correction capabilities, but at the cost of significantly more infrastructure complexity.</p>",
        followups: [
          {
            q: "What is idempotence and how does Ansible achieve it?",
            a: "<p><b>Idempotence</b> is the property that executing an operation multiple times produces the same result as executing it once. Applied to configuration management, an idempotent playbook can be run repeatedly against the same set of hosts without causing changes beyond the first successful run.</p><p><b>Why idempotence matters:</b> Configuration management runs periodically — either scheduled or in response to changes. Without idempotence, every run would attempt to reapply all changes, potentially causing service disruptions or unbounded resource growth. With idempotence, only the actual changes are performed on each run.</p><p><b>How Ansible achieves it:</b> Ansible's built-in modules are designed to be idempotent. Before making a change, each module checks the current state and only takes action if the desired state has not yet been reached. Examples:</p><ul><li><b>package</b> — checks if the package is installed at the requested version.</li><li><b>copy</b> — compares file hashes before copying.</li><li><b>service</b> — checks the current service state before starting or stopping.</li><li><b>lineinfile</b> — checks if the line is already present in the file.</li></ul><p><b>Non-idempotent modules:</b> The <code>command</code> and <code>shell</code> modules execute arbitrary commands and cannot know whether the command has already been run. To make them idempotent, use the <code>creates</code> or <code>removes</code> parameters:</p><pre>- name: Extract archive\n  command: tar xzf /tmp/app.tar.gz -C /opt/\n  args:\n    creates: /opt/app/version.txt</pre><p>This runs the extraction only if <code>/opt/app/version.txt</code> does not exist. Whenever possible, prefer a purpose-built module over <code>command</code>/<code>shell</code>.</p>",
          },
        ],
      },
      {
        q: "Explain Playbooks, Roles, and Inventory.",
        difficulty: "easy",
        a: "<p>These three concepts form the core organizational units of an Ansible codebase. Understanding how they relate is essential to structuring maintainable automation.</p><p><b>Inventory:</b> A list of the hosts Ansible manages, organized into groups. The inventory can be static (an INI or YAML file) or dynamic (a script or plugin that queries an external system such as AWS, GCP, or a CMDB to build the host list at runtime).</p><pre># Static inventory example (INI format)\n[webservers]\nweb1.example.com\nweb2.example.com\n\n[databases]\ndb1.example.com\n\n[production:children]\nwebservers\ndatabases</pre><p>Groups can contain other groups (using <code>:children</code>), allowing hierarchical organization by environment, region, or role.</p><p><b>Playbook:</b> A YAML file that maps host groups to the automation to be performed on them. A playbook contains one or more plays, each of which targets a group and specifies tasks or roles to execute:</p><pre>- hosts: webservers\n  become: yes\n  roles:\n    - common\n    - nginx</pre><p><b>Role:</b> A reusable, self-contained unit of automation with a defined directory structure:</p><pre>roles/\n  nginx/\n    tasks/main.yml          # what to do\n    handlers/main.yml       # event-driven tasks (e.g., restart)\n    templates/nginx.conf.j2 # Jinja2 templates\n    files/                  # static files to copy\n    vars/main.yml           # role-specific variables\n    defaults/main.yml       # default variables (lowest precedence)\n    meta/main.yml           # role metadata and dependencies</pre><p>Roles are the primary unit of sharing in Ansible. Community-contributed roles are published to Ansible Galaxy and can be installed with <code>ansible-galaxy install</code>.</p>",
        followups: [
          {
            q: "What is a handler in Ansible?",
            a: "<p>A <b>handler</b> is a special type of task that runs only when explicitly notified by another task, and only once per play regardless of how many times it is notified. Handlers are the standard mechanism for expressing event-driven behavior — the classic example is restarting a service after its configuration file changes.</p><p><b>Example:</b></p><pre>tasks:\n  - name: Deploy nginx configuration\n    template:\n      src: nginx.conf.j2\n      dest: /etc/nginx/nginx.conf\n    notify: restart nginx\n\n  - name: Deploy nginx TLS certificate\n    copy:\n      src: cert.pem\n      dest: /etc/nginx/cert.pem\n    notify: restart nginx\n\nhandlers:\n  - name: restart nginx\n    service:\n      name: nginx\n      state: restarted</pre><p><b>Behavior:</b></p><ul><li>If neither the config file nor the certificate changes, the handler is never notified and nginx is not restarted.</li><li>If either or both change, the handler is notified but nginx is still restarted only once (at the end of the play).</li><li>Handlers run in the order they are defined in the handlers section, not the order in which they are notified.</li></ul><p><b>Practical benefit:</b> Handlers prevent unnecessary service disruptions. Restarting nginx for every task change would be wasteful and destabilizing; batching all restarts to the end of the play minimizes downtime.</p>",
          },
        ],
      },
      {
        q: "How do you secure secrets in Ansible?",
        difficulty: "medium",
        a: "<p>Ansible provides <b>Ansible Vault</b> as the built-in mechanism for protecting secrets, along with integrations for external secret management systems.</p><p><b>Ansible Vault:</b> Vault encrypts files or specific values with a symmetric key, allowing encrypted content to be safely committed to source control. Only someone with the vault password can decrypt the content.</p><p><b>Encrypting an entire file:</b></p><pre>ansible-vault encrypt group_vars/prod/secrets.yml</pre><p>The file is replaced with an encrypted version. It can still be edited via:</p><pre>ansible-vault edit group_vars/prod/secrets.yml</pre><p><b>Running a playbook that uses vault-encrypted content:</b></p><pre># Prompt for the password interactively\nansible-playbook site.yml --ask-vault-pass\n\n# Read the password from a file\nansible-playbook site.yml --vault-password-file=~/.vault_pass\n\n# Use a script that fetches the password from an external source\nansible-playbook site.yml --vault-password-file=./scripts/get-vault-pw.sh</pre><p><b>Encrypting individual values:</b> Rather than encrypting an entire file, individual sensitive values can be encrypted inline:</p><pre>db_password: !vault |\n  $ANSIBLE_VAULT;1.1;AES256\n  36336462373636643832396538363737...</pre><p><b>Integration with external secret managers:</b> For dynamic secrets (short-lived credentials, rotated passwords) or centralized secret management, integrate Ansible with a dedicated secrets platform:</p><ul><li><b>HashiCorp Vault</b> via the <code>community.hashi_vault</code> collection.</li><li><b>AWS Secrets Manager</b> via the <code>amazon.aws</code> collection.</li><li><b>Azure Key Vault</b> via the <code>azure.azcollection</code> collection.</li></ul><p>Secrets are fetched at playbook runtime and never stored on disk. This approach is preferred for enterprise environments because it centralizes access control, audit logging, and rotation.</p>",
      },
      {
        q: "What is Ansible Vault and how do you use it for secrets?",
        difficulty: "medium",
        a: "<p><b>Ansible Vault</b> is Ansible's built-in mechanism for encrypting sensitive data — passwords, API tokens, SSL keys, or entire files — so that they can be safely stored in version control alongside non-sensitive playbook content.</p><p><b>Basic operations:</b></p><pre># Create an encrypted file\nansible-vault create secrets.yml\n# You're prompted for a password, then dropped into an editor.\n\n# Edit an existing encrypted file\nansible-vault edit secrets.yml\n\n# Encrypt an existing file in place\nansible-vault encrypt vars.yml\n\n# Decrypt (rarely needed — playbooks decrypt automatically)\nansible-vault decrypt secrets.yml\n\n# View without editing\nansible-vault view secrets.yml\n\n# Change the password\nansible-vault rekey secrets.yml</pre><p><b>Using vault-encrypted files in playbooks:</b></p><pre># secrets.yml (encrypted content)\ndb_password: super_secret_pw\napi_token: abc123xyz</pre><pre># playbook.yml\n- hosts: db_servers\n  vars_files:\n    - secrets.yml\n  tasks:\n    - name: Create database user\n      mysql_user:\n        name: appuser\n        password: \"{{ db_password }}\"</pre><pre>ansible-playbook playbook.yml --ask-vault-pass\n# Or supply the password from a file\nansible-playbook playbook.yml --vault-password-file ~/.vault_pass</pre><p><b>Encrypting individual strings (inline vault):</b> Rather than encrypting whole files, individual values can be encrypted and embedded in otherwise-plaintext YAML:</p><pre>ansible-vault encrypt_string 'super_secret_pw' --name db_password\n# Produces:\ndb_password: !vault |\n  $ANSIBLE_VAULT;1.1;AES256\n  6535353539333134343...</pre><p>This is very useful because most of a vars file is not sensitive; encrypting only the sensitive values keeps the file diff-able and reviewable in PRs.</p><p><b>Multiple vault passwords with vault IDs:</b> Modern Ansible supports named vaults so different environments (dev, staging, prod) can have different passwords.</p><pre>ansible-vault encrypt --vault-id prod@~/.vault_pass_prod secrets-prod.yml\nansible-playbook playbook.yml --vault-id prod@~/.vault_pass_prod</pre><p><b>Best practices:</b></p><ul><li><b>Do not commit the vault password.</b> Keep it in a secure secret store or in a file that is in <code>.gitignore</code>.</li><li><b>Automate vault password retrieval.</b> A <code>vault-password-file</code> can be a script that fetches the password from HashiCorp Vault, AWS Secrets Manager, or Azure Key Vault at runtime.</li><li><b>Encrypt narrowly.</b> Prefer <code>encrypt_string</code> for individual secrets over encrypting entire files — reviews are much easier.</li><li><b>Rotate passwords periodically.</b> Use <code>ansible-vault rekey</code> and update your password store.</li><li><b>For large-scale enterprise deployments, consider external secret stores</b> like HashiCorp Vault with the <code>community.hashi_vault.vault_read</code> lookup plugin. Vault handles rotation, audit logging, and fine-grained access control better than Ansible Vault alone.</li></ul>",
      },
      {
        q: "What is Ansible AWX and Ansible Automation Platform?",
        difficulty: "medium",
        a: "<p>Ansible's core CLI tools (<code>ansible</code>, <code>ansible-playbook</code>) are excellent for individual practitioners, but running Ansible at team or enterprise scale requires additional capabilities — a web UI, RBAC, scheduling, credential management, and audit trails. <b>AWX</b> and <b>Ansible Automation Platform (AAP)</b> provide these.</p><p><b>AWX (open source):</b> The upstream project for Ansible's web-based automation platform. Runs on Kubernetes (via the AWX Operator) and provides:</p><ul><li>A web UI for launching playbooks and monitoring executions.</li><li>Users, teams, and RBAC — who can run which playbooks against which inventories.</li><li>Managed credentials — SSH keys, cloud credentials, vault passwords stored encrypted with references, not exposed to users.</li><li>Job templates — reusable configurations that bundle a playbook, inventory, credentials, and parameters.</li><li>Scheduled jobs — cron-like scheduling of playbook runs.</li><li>Notifications on job completion (Slack, email, webhooks).</li><li>Audit logs of every playbook execution.</li><li>REST API for programmatic integration.</li></ul><p><b>Ansible Automation Platform (AAP) — Red Hat's commercial offering:</b> A superset of AWX with added enterprise features:</p><ul><li><b>Automation Controller:</b> AWX with commercial support and enterprise SLAs.</li><li><b>Automation Hub:</b> Private Galaxy for hosting internal collections.</li><li><b>Event-Driven Ansible (EDA):</b> Trigger playbooks based on events from monitoring systems, Git webhooks, or message queues.</li><li><b>Automation Mesh:</b> Distributed execution across multiple locations without direct network paths back to the controller.</li><li><b>Automation Analytics:</b> Reporting and analytics dashboards on automation usage.</li><li>Certified content — Red Hat–backed collections and execution environments.</li><li>Enterprise SSO, LDAP/AD integration, compliance features.</li></ul><p><b>Common enterprise use cases:</b></p><ul><li><b>Self-service automation for non-experts:</b> Ops teams create job templates; other teams (developers, DBAs, network engineers) run them via the UI or API without needing to write playbooks.</li><li><b>Compliance-driven remediation:</b> Scheduled playbooks scan configurations and remediate drift automatically.</li><li><b>Multi-team credential isolation:</b> Different teams have access to different credentials for their respective environments, without exposing the underlying secrets.</li><li><b>Integration with ITSM:</b> ServiceNow or Jira ticket approvals trigger playbook runs via API.</li></ul><p><b>When to adopt AWX/AAP:</b></p><ul><li>You have more than 3–5 people running Ansible.</li><li>You need audit trails for compliance.</li><li>You need RBAC — different teams should not access each other's credentials.</li><li>Non-Ansible experts need to trigger automation safely.</li></ul><p><b>Alternatives:</b> Terraform Cloud, Spacelift, Rundeck, StackStorm, and organizational tools built on top of GitOps controllers all address similar orchestration needs. AWX/AAP remains the standard when Ansible is the primary automation tool.</p>",
      },
      {
        q: "What are Ansible Collections and Galaxy?",
        difficulty: "medium",
        a: "<p><b>Ansible Collections</b> are the modern packaging format for Ansible content, introduced in Ansible 2.10 (2020). They bundle related modules, plugins, roles, and playbooks into a single distributable unit. Collections replace the older model where all modules shipped with Ansible core, and are now the primary way Ansible content is distributed and consumed.</p><p><b>Why collections were introduced:</b> Historically, Ansible core included thousands of modules, making the installation large and creating a tight coupling between module releases and Ansible core releases. Bugs in an obscure module required an Ansible core release to fix. Collections decoupled these concerns — module maintainers can release independently, and users install only what they need.</p><p><b>Anatomy of a collection:</b></p><pre>my_collection/\n├── galaxy.yml              # metadata\n├── README.md\n├── plugins/\n│   ├── modules/            # custom modules\n│   ├── inventory/          # dynamic inventory plugins\n│   ├── lookup/             # lookup plugins\n│   ├── filter/             # Jinja2 filter plugins\n│   └── ...\n├── roles/                  # Ansible roles\n│   └── my_role/\n├── playbooks/              # reusable playbooks\n└── docs/</pre><p><b>Ansible Galaxy — the community hub:</b> Galaxy (<code>galaxy.ansible.com</code>) is the public registry for community collections and roles. Similar to Docker Hub for images or PyPI for Python packages.</p><p><b>Installing collections:</b></p><pre># Install a specific collection\nansible-galaxy collection install community.mysql\n\n# Install from requirements.yml\ncat &gt; requirements.yml &lt;&lt; 'EOF'\ncollections:\n  - name: community.aws\n    version: \"&gt;=6.0.0\"\n  - name: kubernetes.core\n    version: \"3.0.0\"\n  - name: hashicorp.vault\n    source: https://galaxy.ansible.com\nEOF\n\nansible-galaxy collection install -r requirements.yml</pre><p><b>Using collection content in playbooks:</b></p><pre>- hosts: web_servers\n  tasks:\n    - name: Provision MySQL user\n      community.mysql.mysql_user:\n        name: app_user\n        password: \"{{ db_password }}\"\n        priv: \"app.*:ALL\"\n\n    - name: Fetch secret from Vault\n      set_fact:\n        db_password: \"{{ lookup('hashicorp.vault.hashi_vault',\n                                'secret=secret/data/prod/db:password') }}\"</pre><p><b>Notable widely-used collections:</b></p><ul><li><b>community.general:</b> Grab bag of community-maintained modules.</li><li><b>ansible.posix:</b> Core POSIX system modules (mount, sysctl, cron, etc.).</li><li><b>community.docker:</b> Docker container and image management.</li><li><b>kubernetes.core:</b> Kubernetes resource management.</li><li><b>amazon.aws / community.aws:</b> AWS resource management.</li><li><b>azure.azcollection:</b> Azure resource management.</li><li><b>google.cloud:</b> GCP resource management.</li><li><b>ansible.netcommon:</b> Common network device modules.</li></ul><p><b>Private collections:</b> Organizations can publish internal collections to a private Automation Hub (part of AAP) or a self-hosted <code>ansible-galaxy</code> proxy. This is the standard mechanism for sharing internal automation across teams.</p>",
      },
      {
        q: "How do you ensure idempotency in Ansible playbooks?",
        difficulty: "medium",
        a: "<p><b>Idempotency</b> is a foundational property of Ansible: running the same playbook multiple times against the same target should produce the same result and should not cause unnecessary changes on subsequent runs. When done well, Ansible reports \"changed=0\" on repeated runs, indicating the desired state was already achieved and no action was taken.</p><p><b>Why idempotency matters:</b></p><ul><li>Safety — playbooks can be run repeatedly without fear of duplicate side effects.</li><li>Reliability — after an interrupted run, re-running completes the remaining work without redoing what already succeeded.</li><li>Drift correction — regular scheduled runs bring systems back to desired state.</li><li>Predictable diffs — changed hosts represent actual changes to systems.</li></ul><p><b>Most built-in modules are idempotent by default.</b> They check current state and act only if needed:</p><pre>- name: Ensure nginx installed\n  apt:\n    name: nginx\n    state: present\n\n- name: Ensure user exists\n  user:\n    name: deploy\n    shell: /bin/bash\n\n- name: Ensure config file present\n  copy:\n    src: nginx.conf\n    dest: /etc/nginx/nginx.conf\n    owner: root\n    mode: '0644'</pre><p>Each task checks current state before making changes. Running the playbook twice results in \"changed=0\" the second time.</p><p><b>The common idempotency pitfall — <code>shell</code> and <code>command</code> modules:</b> These execute arbitrary commands and cannot know what they did. Every run is reported as \"changed\".</p><pre># BAD: not idempotent — always reports changed\n- shell: mkdir /var/log/myapp\n\n# GOOD: use the file module\n- file:\n    path: /var/log/myapp\n    state: directory</pre><p><b>When shell/command is unavoidable, use <code>changed_when</code> and <code>creates</code>:</b></p><pre># Use `creates` to skip if a file already exists\n- shell: /opt/setup.sh\n  args:\n    creates: /var/lib/myapp/.initialized\n\n# Use changed_when to correctly report state\n- shell: /usr/local/bin/status.sh\n  register: status\n  changed_when: \"'CHANGED' in status.stdout\"\n\n# check_mode / dry-run compatibility\n- shell: my-command --check\n  check_mode: no\n  changed_when: false</pre><p><b>Additional idempotency techniques:</b></p><ol><li><b>Register + when:</b> Check the current state with one task, then act conditionally.<pre>- name: Check if package is installed\n  command: dpkg -s custom-package\n  register: pkg_check\n  failed_when: false\n  changed_when: false\n\n- name: Install if missing\n  command: /opt/install-custom.sh\n  when: pkg_check.rc != 0</pre></li><li><b>Handlers for restart operations:</b> Ensure services only restart when their configuration actually changed.<pre>- name: Update nginx config\n  copy:\n    src: nginx.conf\n    dest: /etc/nginx/nginx.conf\n  notify: restart nginx\n\nhandlers:\n  - name: restart nginx\n    service:\n      name: nginx\n      state: restarted</pre></li><li><b>Use <code>state:</code> semantics.</b> Most Ansible modules accept <code>state: present</code> or <code>state: absent</code>, describing desired state rather than imperative actions. Prefer these.</li><li><b>Test with <code>--check</code>:</b> Ansible's dry-run mode shows what would change without making changes. Ideal for verifying idempotency during development.</li></ol><p><b>Validation:</b> A well-written playbook should be run against a converged system and produce zero changes on the second run. If it doesn't, one or more tasks are not idempotent — investigate which and fix them.</p>",
      },
      {
        q: "What is Molecule and how do you test Ansible roles?",
        difficulty: "medium",
        a: "<p><b>Molecule</b> is the standard testing framework for Ansible roles and collections. It provides a structured way to spin up test environments (Docker containers, VMs, or cloud instances), run playbooks against them, verify results, and tear down — all as part of a CI pipeline.</p><p><b>Why testing Ansible matters:</b> Ansible playbooks change how systems are configured. A bug in a playbook can misconfigure hundreds of hosts. Testing before merging catches issues early, and Molecule provides the framework to do this consistently.</p><p><b>Molecule workflow:</b> Each Molecule scenario runs a sequence:</p><ol><li><b>lint</b> — run <code>ansible-lint</code> and <code>yamllint</code>.</li><li><b>destroy</b> — tear down any leftover test infrastructure.</li><li><b>create</b> — provision test instances (Docker containers by default).</li><li><b>prepare</b> — optional pre-setup on instances.</li><li><b>converge</b> — run the role against the instances.</li><li><b>idempotence</b> — run the role again and verify zero changes.</li><li><b>verify</b> — run assertions (Ansible playbook or Testinfra).</li><li><b>destroy</b> — clean up.</li></ol><p>Running <code>molecule test</code> executes the full sequence. Running <code>molecule converge</code> just applies the role, useful during development.</p><p><b>Typical role structure with Molecule:</b></p><pre>my_role/\n├── defaults/\n├── handlers/\n├── meta/\n├── tasks/\n├── templates/\n└── molecule/\n    └── default/\n        ├── molecule.yml    # scenario configuration\n        ├── converge.yml    # playbook that applies the role\n        └── verify.yml      # assertions</pre><p><b>Example molecule.yml:</b></p><pre>---\ndependency:\n  name: galaxy\n\ndriver:\n  name: docker\n\nplatforms:\n  - name: ubuntu-22\n    image: geerlingguy/docker-ubuntu2204-ansible\n    privileged: true\n    volumes: [/sys/fs/cgroup:/sys/fs/cgroup:rw]\n    command: /lib/systemd/systemd\n  - name: rocky-9\n    image: geerlingguy/docker-rockylinux9-ansible\n    privileged: true\n    volumes: [/sys/fs/cgroup:/sys/fs/cgroup:rw]\n    command: /usr/sbin/init\n\nprovisioner:\n  name: ansible\n\nverifier:\n  name: ansible</pre><p><b>Example verify.yml:</b></p><pre>---\n- name: Verify\n  hosts: all\n  tasks:\n    - name: Ensure nginx service is running\n      service:\n        name: nginx\n        state: started\n      check_mode: yes\n      register: nginx_status\n      failed_when: nginx_status.changed\n\n    - name: Ensure custom config is present\n      stat:\n        path: /etc/nginx/conf.d/app.conf\n      register: app_conf\n      failed_when: not app_conf.stat.exists\n\n    - name: Ensure port 80 responds\n      uri:\n        url: http://localhost\n        status_code: 200</pre><p><b>Running Molecule in CI:</b> Every push and PR to a role's repository should run <code>molecule test</code>. On failure, the pipeline blocks the merge. This provides high confidence that changes to a role will not break the systems it manages.</p><p><b>Alternatives / complementary tools:</b></p><ul><li><b>Ansible-lint:</b> Static analysis of playbooks — best practices, deprecation warnings, common mistakes.</li><li><b>ansible-playbook --check --diff:</b> Dry-run against real hosts to preview changes.</li><li><b>Testinfra / InSpec:</b> Alternative verification tools that check system state independently of Ansible.</li></ul>",
      },
      {
        q: "How do you use Ansible with cloud providers?",
        difficulty: "medium",
        a: "<p>Ansible is often used as a lightweight alternative or complement to Terraform for cloud infrastructure. Where Terraform excels at declarative provisioning with state management, Ansible excels at combined provisioning + configuration in a single flow, and at ongoing management of running infrastructure.</p><p><b>Cloud modules by provider (all in collections):</b></p><ul><li><b>AWS:</b> <code>amazon.aws</code> (core) and <code>community.aws</code> (extended). Modules like <code>ec2_instance</code>, <code>rds_instance</code>, <code>s3_bucket</code>, <code>iam_role</code>, and hundreds more.</li><li><b>Azure:</b> <code>azure.azcollection</code>. Modules for VMs, VNets, Storage, App Service, Key Vault, AKS.</li><li><b>GCP:</b> <code>google.cloud</code>. Modules for Compute Engine, GKE, Cloud SQL, GCS, IAM.</li><li><b>Kubernetes:</b> <code>kubernetes.core</code>. Manage any Kubernetes resource type (Deployment, Service, ConfigMap, custom resources).</li></ul><p><b>Example — creating and configuring an EC2 instance:</b></p><pre>- name: Provision and configure web server\n  hosts: localhost\n  gather_facts: false\n\n  tasks:\n    - name: Create EC2 instance\n      amazon.aws.ec2_instance:\n        name: web-server-01\n        instance_type: t3.medium\n        image_id: ami-0c55b159cbfafe1f0\n        key_name: my-key\n        vpc_subnet_id: subnet-abc123\n        security_group: web-sg\n        wait: true\n        tags:\n          Environment: production\n          Role: web\n      register: ec2_result\n\n    - name: Add to dynamic inventory\n      add_host:\n        hostname: \"{{ ec2_result.instances[0].public_ip_address }}\"\n        groups: web_servers\n\n- name: Configure the new instance\n  hosts: web_servers\n  gather_facts: true\n  tasks:\n    - name: Install nginx\n      package:\n        name: nginx\n        state: present\n\n    - name: Start nginx\n      service:\n        name: nginx\n        state: started\n        enabled: yes</pre><p><b>Dynamic inventory:</b> Rather than static inventory files, Ansible can query cloud APIs for its inventory. This eliminates the need to manually track instances.</p><pre># inventory/aws_ec2.yaml\nplugin: amazon.aws.aws_ec2\nregions:\n  - us-east-1\nkeyed_groups:\n  - key: tags.Environment\n    prefix: env\n  - key: tags.Role\n    prefix: role\nhostnames:\n  - tag:Name\n  - private-ip-address</pre><pre>ansible-playbook -i inventory/ configure-web.yml \\\n  -l role_web:&amp;env_production</pre><p>This targets only instances tagged <code>Role=web</code> AND <code>Environment=production</code> — no hardcoded IPs.</p><p><b>Ansible vs Terraform for cloud provisioning:</b></p><ul><li><b>Terraform:</b> Better for provisioning — strong state management, clear dependency graph, plan/apply workflow, drift detection. Preferred for infrastructure that is created once and largely static.</li><li><b>Ansible:</b> Better for configuration — running commands on hosts, orchestrating changes across a fleet, ongoing management. Provisioning modules exist but state management is weaker.</li><li><b>Combined:</b> Very common — Terraform provisions the infrastructure (VMs, networking, IAM), Ansible then configures what runs on it. This plays to each tool's strengths.</li></ul>",
      },
      {
        q: "How do variables and their precedence work?",
        difficulty: "hard",
        a: "<p>Ansible supports more than 20 different variable sources, each with a defined precedence. When the same variable is defined in multiple places, the highest-precedence value wins. Understanding the precedence order is essential for structuring maintainable playbooks.</p><p><b>Precedence order (lowest to highest — later sources override earlier ones):</b></p><ol><li><b>Role defaults</b> — variables in <code>roles/&lt;role&gt;/defaults/main.yml</code>. Lowest precedence; intended as sensible defaults that any consumer can override.</li><li><b>Inventory file variables</b> — defined in the inventory file itself.</li><li><b>Inventory group_vars</b> — files under <code>group_vars/&lt;group&gt;.yml</code>. Values for groups override inventory-file variables.</li><li><b>Inventory host_vars</b> — files under <code>host_vars/&lt;host&gt;.yml</code>. Values for specific hosts override group values.</li><li><b>Play vars</b> — declared in the play's <code>vars:</code> section.</li><li><b>Role vars</b> — variables in <code>roles/&lt;role&gt;/vars/main.yml</code>. Higher precedence than role defaults.</li><li><b>Block vars</b> — declared on a block of tasks.</li><li><b>Task vars</b> — declared on an individual task.</li><li><b>Include vars / set_fact</b> — variables set during runtime.</li><li><b>--extra-vars (-e) on the CLI</b> — highest precedence. Overrides everything else.</li></ol><p><b>Practical structuring guidance:</b></p><ul><li>Put <b>sensible defaults</b> in <code>roles/&lt;role&gt;/defaults/main.yml</code>. These should work out of the box for a typical deployment.</li><li>Put <b>environment-specific values</b> (staging vs production) in <code>group_vars/</code>.</li><li>Put <b>host-specific values</b> in <code>host_vars/</code>.</li><li>Reserve <code>-e</code> for <b>emergency overrides</b> or one-off runs — not routine configuration, since command-line overrides are not versioned.</li></ul><p>The full precedence rules also account for extra features like <code>--force-handlers</code>, connection variables, and dynamic includes; the Ansible documentation contains the authoritative complete list.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "aws",
    name: "AWS Cloud",
    icon: "☁️",
    kind: "qa",
    description:
      "Core AWS services every DevOps engineer should know — IAM, VPC, compute, and the operational sharp edges.",
    items: [
      {
        q: "Explain IAM Users, Groups, Roles, and Policies.",
        difficulty: "medium",
        a: "<p>AWS Identity and Access Management (IAM) provides four core concepts for managing access to AWS resources: Users, Groups, Roles, and Policies. Understanding how they interact is fundamental to secure AWS operations.</p><p><b>User:</b> A long-lived identity representing a specific person or application. Users have permanent credentials — either a console password or an access key ID and secret access key — and are directly authenticated against AWS. Users are appropriate for human operators who need long-term console access, but they are generally discouraged for programmatic access (see Roles below).</p><p><b>Group:</b> A collection of Users that share the same permissions. Rather than attaching policies to individual users, policies are attached to a group and all group members inherit them. Groups are purely an organizational construct — they cannot themselves be assumed and have no credentials.</p><p><b>Role:</b> A temporary identity that can be <i>assumed</i> by an entity (an AWS service, another AWS account, a federated user, or a user from your own account). When a role is assumed, AWS Security Token Service (STS) issues short-lived credentials (typically 1 hour by default, up to 12 hours) that expire automatically. Roles have no long-lived credentials of their own.</p><p><b>Policy:</b> A JSON document that describes the permissions being granted or denied. A policy contains statements, each specifying:</p><ul><li><b>Effect:</b> Allow or Deny.</li><li><b>Action:</b> The API actions covered (e.g., <code>s3:GetObject</code>).</li><li><b>Resource:</b> The AWS resources the actions apply to (identified by ARN).</li><li><b>Condition (optional):</b> Additional constraints (source IP, MFA presence, time of day).</li></ul><p><b>Best practice:</b> Prefer <b>Roles with STS</b> over long-lived IAM User keys wherever possible. Long-lived keys are a common source of breaches when accidentally committed to Git or exposed in build logs. Roles are used by:</p><ul><li>EC2 instances (via instance profiles) to grant workloads access to AWS APIs without embedded credentials.</li><li>Lambda functions (via the execution role).</li><li>EKS pods (via IAM Roles for Service Accounts — IRSA).</li><li>CI/CD systems (via OIDC federation with GitHub Actions, GitLab, etc.).</li><li>Cross-account access.</li></ul>",
        followups: [
          {
            q: "What is an IAM Role's trust policy vs permission policy?",
            a: "<p>Every IAM role has exactly one <b>trust policy</b> and one or more <b>permission policies</b>. These two policies answer different questions.</p><p><b>Trust policy:</b> Defines <i>who is allowed to assume</i> this role — the Principal(s) that AWS STS will grant temporary credentials to when they call <code>sts:AssumeRole</code>. The trust policy is attached to the role itself and is a special kind of resource-based policy.</p><p>Example — a trust policy allowing EC2 instances to assume the role:</p><pre>{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [{\n    \"Effect\": \"Allow\",\n    \"Principal\": { \"Service\": \"ec2.amazonaws.com\" },\n    \"Action\": \"sts:AssumeRole\"\n  }]\n}</pre><p><b>Permission policy:</b> Defines <i>what the assumed role can do</i> once it has been assumed. These are standard IAM policies attached to the role.</p><p>Example — a permission policy granting S3 read access:</p><pre>{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [{\n    \"Effect\": \"Allow\",\n    \"Action\": [\"s3:GetObject\", \"s3:ListBucket\"],\n    \"Resource\": [\n      \"arn:aws:s3:::my-bucket\",\n      \"arn:aws:s3:::my-bucket/*\"\n    ]\n  }]\n}</pre><p><b>End-to-end flow:</b> An EC2 instance's instance profile references this role → EC2 automatically requests temporary credentials from STS → STS checks the trust policy to confirm EC2 is allowed to assume → STS returns credentials scoped to the permission policy → the workload uses those credentials to read from S3.</p>",
          },
          {
            q: "How do you give an EKS pod access to AWS APIs?",
            a: "<p>The recommended mechanism is <b>IRSA (IAM Roles for Service Accounts)</b>, which grants IAM permissions to specific Kubernetes ServiceAccounts rather than to entire nodes. This provides fine-grained, per-pod access to AWS APIs without embedded credentials.</p><p><b>Setup steps:</b></p><ol><li><b>Enable the cluster's OIDC provider.</b> EKS clusters expose an OpenID Connect (OIDC) provider that acts as a trust anchor. This is enabled via the AWS console, CLI, or Terraform.</li><li><b>Create an IAM role with a trust policy that trusts the OIDC provider and constrains the specific Kubernetes ServiceAccount:</b><pre>{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [{\n    \"Effect\": \"Allow\",\n    \"Principal\": {\n      \"Federated\": \"arn:aws:iam::123456789012:oidc-provider/oidc.eks.us-east-1.amazonaws.com/id/EXAMPLED539D4633E53DE1B71EXAMPLE\"\n    },\n    \"Action\": \"sts:AssumeRoleWithWebIdentity\",\n    \"Condition\": {\n      \"StringEquals\": {\n        \"oidc.eks.us-east-1.amazonaws.com/id/EXAMPLED539D4633E53DE1B71EXAMPLE:sub\": \"system:serviceaccount:my-namespace:my-service-account\"\n      }\n    }\n  }]\n}</pre></li><li><b>Attach permission policies</b> to the role granting the required AWS API access (e.g., S3 read, DynamoDB write).</li><li><b>Annotate the Kubernetes ServiceAccount</b> with the role ARN:<pre>apiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: my-service-account\n  namespace: my-namespace\n  annotations:\n    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/my-role</pre></li><li><b>Pods using this ServiceAccount</b> automatically receive a projected token; the AWS SDK detects it and transparently exchanges it for temporary IAM credentials via STS.</li></ol><p><b>Security advantage:</b> Compared to older approaches like giving all pods on a node access via the node's instance profile, IRSA provides per-workload permissions and eliminates the risk of pods obtaining permissions they should not have.</p>",
          },
        ],
      },
      {
        q: "What is a VPC and how do public and private subnets differ?",
        difficulty: "medium",
        a: "<p>A <b>Virtual Private Cloud (VPC)</b> is a logically isolated section of the AWS cloud that provides full control over the network topology — IP address ranges, subnets, route tables, gateways, and security controls. Each VPC is contained within a single AWS region and can span multiple Availability Zones within that region.</p><p><b>Core VPC components:</b></p><ul><li><b>CIDR block:</b> The IPv4 address range for the VPC (e.g., <code>10.0.0.0/16</code>).</li><li><b>Subnets:</b> Sub-ranges of the VPC CIDR, each existing in a specific Availability Zone.</li><li><b>Route tables:</b> Rules governing how traffic flows out of each subnet.</li><li><b>Gateways:</b> Internet Gateway (IGW), NAT Gateway, VPN Gateway, Transit Gateway — for various forms of external connectivity.</li><li><b>Security groups and NACLs:</b> Firewalls at the instance and subnet level.</li></ul><p><b>Public subnet:</b> A subnet whose associated route table has a route with destination <code>0.0.0.0/0</code> pointing to an Internet Gateway. Resources in public subnets can be assigned public IPs and are directly reachable from the internet (subject to security group rules). Typical residents: load balancers, bastion hosts, NAT gateways.</p><p><b>Private subnet:</b> A subnet whose route table has no direct route to an Internet Gateway. Resources in private subnets cannot be reached from the internet. Outbound internet access — for OS updates, downloading dependencies, calling external APIs — is provided by routing <code>0.0.0.0/0</code> through a NAT Gateway located in a public subnet. Typical residents: application servers, EKS worker nodes, databases (if internet-reachable at all).</p><p><b>Isolated (fully private) subnet:</b> A subnet with no route to any gateway. Cannot reach the internet at all. Used for the most sensitive workloads (databases, HSMs). External access is provided only via VPC Endpoints for specific AWS services or via VPN/Direct Connect.</p><p><b>Standard three-tier pattern:</b></p><ul><li>Public subnets: ALB, NAT Gateway.</li><li>Private subnets: EC2 / EKS nodes.</li><li>Isolated subnets: RDS, ElastiCache.</li></ul><p>Subnets in each tier are typically replicated across at least two Availability Zones for high availability.</p>",
        followups: [
          {
            q: "Security Group vs Network ACL?",
            a: "<p>Both Security Groups and Network ACLs are firewall mechanisms in AWS VPC, but they operate at different levels and with different semantics.</p><p><b>Security Groups:</b></p><ul><li><b>Scope:</b> Attached to individual network interfaces (typically an EC2 instance, RDS, or Lambda).</li><li><b>Stateful:</b> Return traffic for allowed inbound connections is automatically allowed, and vice versa. You do not need explicit outbound rules for responses to allowed inbound requests.</li><li><b>Allow-only:</b> Only allow rules can be defined; there is no way to explicitly deny traffic. Traffic not matching any allow rule is implicitly denied.</li><li><b>Rule evaluation:</b> All rules are evaluated together; if any rule allows the traffic, it is allowed.</li></ul><p><b>Network ACLs (NACLs):</b></p><ul><li><b>Scope:</b> Attached to entire subnets. All traffic entering or leaving the subnet is evaluated.</li><li><b>Stateless:</b> Return traffic is not automatically allowed. Both inbound and outbound rules must explicitly permit the flow, including ephemeral ports for return traffic.</li><li><b>Allow and Deny:</b> Both types of rules are supported.</li><li><b>Rule evaluation:</b> Rules are numbered and evaluated in order; the first matching rule (allow or deny) determines the outcome.</li></ul><p><b>Practical usage:</b> Security Groups are the primary firewall used day-to-day; they are simpler, stateful, and provide instance-level control. NACLs are used less frequently, primarily for coarse subnet-wide guardrails — for example, blocking a known-malicious IP range across an entire subnet, or as a compensating control in defense-in-depth architectures. Most AWS environments rely heavily on Security Groups and use NACLs sparingly.</p>",
          },
        ],
      },
      {
        q: "How do EC2 Auto Scaling Groups work?",
        difficulty: "medium",
        a: "<p>An <b>Auto Scaling Group (ASG)</b> is an AWS service that automatically maintains a specified number of EC2 instances and scales that number up or down based on defined policies. ASGs provide the foundational elasticity and self-healing behavior for most AWS-based compute workloads.</p><p><b>Core configuration:</b></p><ul><li><b>Desired capacity:</b> The target number of running instances at any moment.</li><li><b>Minimum size:</b> The floor — the ASG will never scale below this.</li><li><b>Maximum size:</b> The ceiling — the ASG will never scale above this, regardless of load.</li><li><b>Launch Template (or Launch Configuration):</b> Defines the instance parameters — AMI, instance type, key pair, security groups, IAM instance profile, user-data script, and EBS volumes.</li><li><b>Subnets:</b> The subnets in which instances can be launched (typically spanning multiple Availability Zones for HA).</li></ul><p><b>Scaling policies:</b></p><ul><li><b>Target tracking:</b> Maintain a metric at a target value (e.g., keep average CPU at 50%). AWS handles the mechanics of scale-out and scale-in decisions. This is the recommended default.</li><li><b>Step scaling:</b> Adjust capacity in discrete steps based on threshold breaches — e.g., add 2 instances if CPU exceeds 70%, add 5 instances if it exceeds 85%.</li><li><b>Simple scaling:</b> A single adjustment triggered by an alarm.</li><li><b>Scheduled scaling:</b> Predictable capacity changes based on time (e.g., scale up for business hours).</li><li><b>Predictive scaling:</b> Machine-learning-based prediction of upcoming load.</li></ul><p><b>Health checks and self-healing:</b> ASGs continuously monitor instance health via two mechanisms:</p><ul><li><b>EC2 status checks</b> — the instance is running and responding.</li><li><b>ELB health checks</b> — the application on the instance is responding at the load balancer layer.</li></ul><p>When an instance fails a health check, the ASG automatically terminates it and launches a replacement, restoring the desired capacity without human intervention.</p><p><b>Combined with an Elastic Load Balancer target group,</b> an ASG delivers three of the most valuable properties for production workloads: <b>elasticity</b> (matching capacity to demand), <b>availability</b> (spanning multiple AZs), and <b>self-healing</b> (automatic replacement of failed instances).</p>",
      },
      {
        q: "S3 storage classes — when to use which?",
        difficulty: "easy",
        a: "<p>Amazon S3 provides multiple storage classes designed for different access patterns and cost/durability trade-offs. Choosing the appropriate class for each workload significantly impacts long-term storage cost.</p><ul><li><b>S3 Standard:</b> Default class, designed for frequently accessed data. 11 nines of durability, replicated across at least three Availability Zones. Highest per-GB cost, but no retrieval fees. Suitable for hot data serving live traffic.</li><li><b>S3 Standard-Infrequent Access (Standard-IA):</b> Lower per-GB cost than Standard, but adds per-GB retrieval fees. Still replicated across multiple AZs. 30-day minimum storage duration. Suitable for backups and older data that is accessed occasionally.</li><li><b>S3 One Zone-IA:</b> Same access pattern as Standard-IA but stored in a single AZ (not multi-AZ). Roughly 20% cheaper than Standard-IA but loses the AZ-failure resilience. Suitable for reproducible or non-critical data.</li><li><b>S3 Intelligent-Tiering:</b> Automatically moves objects between frequent, infrequent, and archive access tiers based on observed access patterns. Adds a small monitoring fee per object. Suitable when access patterns are unpredictable or vary over time.</li><li><b>S3 Glacier Instant Retrieval:</b> Archive-class pricing with millisecond retrieval times. Suitable for archival data that must occasionally be accessed immediately (e.g., quarterly reports).</li><li><b>S3 Glacier Flexible Retrieval:</b> Lower cost, with retrieval times ranging from minutes to hours depending on the retrieval tier chosen (Expedited, Standard, Bulk).</li><li><b>S3 Glacier Deep Archive:</b> Lowest-cost storage in AWS. Retrieval takes 12–48 hours. Suitable for compliance archives and long-term backups that will rarely, if ever, be accessed.</li></ul><p><b>Lifecycle rules:</b> Rather than manually managing storage class transitions, S3 lifecycle rules automate the migration of objects between classes based on age. A common pattern:</p><ul><li>0–30 days: Standard</li><li>30–90 days: Standard-IA</li><li>90+ days: Glacier Deep Archive</li></ul><p>Lifecycle rules can also automatically expire (delete) objects after a defined period, useful for log data with retention requirements.</p>",
      },
      {
        q: "How would you make an application highly available across AZs?",
        difficulty: "medium",
        a: "<p>High availability in AWS is achieved by architecting each tier of the application to survive the failure of any single Availability Zone. This requires distributing state, compute, and networking across multiple AZs.</p><p><b>Architectural pattern for AZ-resilient high availability:</b></p><ol><li><b>Compute across multiple AZs:</b> Deploy an Auto Scaling Group spanning at least two AZs, with subnets in each. The ASG will distribute instances across AZs and automatically replace instances lost during an AZ failure.</li><li><b>Load balancing:</b> Front the ASG with an Application Load Balancer (or Network Load Balancer for L4). ALBs are inherently multi-AZ — they span multiple AZs by design and automatically stop routing traffic to failed targets based on health checks.</li><li><b>Database high availability:</b> Use Multi-AZ RDS, which maintains a synchronous standby replica in a different AZ. On primary failure, RDS automatically fails over to the standby, typically within 60–120 seconds. For read-heavy workloads, add Read Replicas.</li><li><b>Shared storage:</b> Store shared application state in services that are inherently regional:<ul><li><b>Amazon S3</b> — 11 nines durability, replicated across multiple AZs.</li><li><b>DynamoDB</b> — automatically multi-AZ; consider Global Tables for cross-region.</li><li><b>EFS</b> for POSIX-shared filesystems.</li></ul></li><li><b>Session and cache state:</b> Use ElastiCache (Redis or Memcached) in Multi-AZ mode, with automatic failover, rather than storing session state on the application instances.</li><li><b>DNS-level failover:</b> Optionally use Route 53 health checks to route traffic away from unhealthy endpoints.</li></ol><p><b>Testing:</b> Highly available systems must be tested regularly. AWS Fault Injection Simulator can simulate AZ failures to validate that the architecture responds correctly. Netflix's Chaos Monkey inspired this practice.</p><p><b>Beyond a single region:</b> For workloads requiring resilience to entire regional failures, extend the pattern across regions using multi-region deployment patterns, DynamoDB Global Tables, S3 Cross-Region Replication, and Route 53 latency- or failover-based routing.</p>",
        followups: [
          {
            q: "AZ vs Region vs Edge Location?",
            a: "<p>These three concepts represent different scales of AWS's global infrastructure and are important to distinguish when designing resilient architectures.</p><ul><li><b>Region:</b> A large geographic area containing multiple isolated data centers. Examples include <code>us-east-1</code> (N. Virginia), <code>eu-west-1</code> (Ireland), <code>ap-south-1</code> (Mumbai). Regions are the top level of AWS's physical hierarchy. Most AWS services are scoped to a single region; data does not cross region boundaries unless explicitly configured.</li><li><b>Availability Zone (AZ):</b> An isolated failure domain within a region, consisting of one or more physically separated data centers with independent power, cooling, and networking. AZs within a region are connected by low-latency, high-bandwidth private links. AZs are the unit of high availability planning — a well-designed architecture survives the failure of any single AZ. Examples: <code>us-east-1a</code>, <code>us-east-1b</code>. AWS obscures the mapping of AZ letters to physical data centers across accounts to distribute load.</li><li><b>Edge Location:</b> A global network of Points of Presence (POPs) operated primarily for content delivery via CloudFront, DNS resolution via Route 53, and Web Application Firewall (WAF). Edge locations are separate from regions and are optimized for low-latency access to end users worldwide. There are many more edge locations than there are regions.</li></ul><p><b>Additional related concepts:</b> AWS also operates <b>Local Zones</b> (extensions of a region placed closer to specific metros for latency-sensitive workloads) and <b>Wavelength Zones</b> (embedded in telco 5G networks).</p>",
          },
        ],
      },
      {
        q: "What is the AWS Well-Architected Framework?",
        difficulty: "medium",
        a: "<p>The <b>AWS Well-Architected Framework</b> is Amazon's structured methodology for evaluating and improving cloud architectures. It codifies best practices across six <b>pillars</b>, providing a common vocabulary and review process for architectural decisions.</p><p><b>The six pillars:</b></p><ol><li><b>Operational Excellence:</b> Ability to run and monitor systems, improve processes, and respond to events. Practices include infrastructure-as-code, small frequent changes, automated response to events, and blameless post-incident reviews.</li><li><b>Security:</b> Protecting information and systems. Practices include least-privilege IAM, encryption in transit and at rest, defense in depth, automated security testing, and traceable audit logs.</li><li><b>Reliability:</b> Ability to recover from failures and meet demand. Practices include multi-AZ deployment, automatic recovery, load testing, backups, and chaos engineering.</li><li><b>Performance Efficiency:</b> Using compute resources efficiently. Practices include right-sizing, serverless where appropriate, caching, and continuously experimenting with new services.</li><li><b>Cost Optimization:</b> Avoiding unnecessary costs. Practices include measuring ROI per workload, using managed services, right-sizing, Reserved Instances/Savings Plans, and eliminating unused resources.</li><li><b>Sustainability</b> (added 2021): Minimizing environmental impact. Practices include maximizing resource utilization, using efficient regions, and offloading to managed services designed for efficiency.</li></ol><p><b>The Well-Architected Review process:</b> AWS provides the <b>Well-Architected Tool</b> in the AWS Console, guiding teams through 50+ questions per workload. The tool identifies high-risk issues and produces an improvement plan. Reviews are often conducted with AWS Solutions Architects (free for AWS customers).</p><p><b>Well-Architected Lenses:</b> Domain-specific extensions of the framework for particular workload types — Serverless, SaaS, IoT, Machine Learning, Financial Services, Games Industry, and others. Each Lens provides additional questions and best practices tailored to that domain.</p><p><b>Practical value:</b></p><ul><li><b>Common language:</b> When architects across teams discuss trade-offs, the six pillars provide a shared framework — \"We're increasing reliability but at some cost to operational excellence.\"</li><li><b>Explicit trade-offs:</b> Real architectural decisions always involve trade-offs. The framework makes them visible rather than implicit.</li><li><b>Onboarding new team members:</b> The framework serves as a comprehensive reference for AWS best practices.</li><li><b>Reviews find real issues:</b> Teams that conduct Well-Architected Reviews consistently identify blind spots — missing backups, over-permissive IAM, single points of failure, unused resources.</li></ul><p><b>Applicability beyond AWS:</b> Azure and GCP have their own equivalent frameworks (Azure Well-Architected Framework, Google Cloud Architecture Framework). The core ideas — pillars of concern, structured review, explicit trade-offs — are transferable across clouds. DevOps engineers should understand the concept even if working in multi-cloud environments.</p>",
      },
      {
        q: "What is the difference between CloudFormation, Terraform, and AWS CDK?",
        difficulty: "medium",
        a: "<p>Three primary tools handle Infrastructure-as-Code for AWS. Each has different design trade-offs.</p><p><b>CloudFormation (CFN):</b> AWS's native IaC service. Templates are JSON or YAML documents that CFN converts into a deployable stack.</p><ul><li><b>Native AWS integration:</b> New AWS services and features typically get CFN support at launch or shortly after.</li><li><b>Managed by AWS:</b> No state file to manage — AWS tracks the deployed state. Rollback on failure is automatic.</li><li><b>StackSets:</b> Deploy the same stack to many accounts and regions from a management account.</li><li><b>Trade-offs:</b> Verbose syntax (thousands of lines for large stacks), single-cloud only, poor secondary tooling, JSON/YAML feels awkward for complex logic.</li></ul><pre># Simplified CloudFormation snippet\nResources:\n  MyBucket:\n    Type: AWS::S3::Bucket\n    Properties:\n      BucketName: my-app-logs\n      VersioningConfiguration:\n        Status: Enabled</pre><p><b>Terraform (or OpenTofu):</b> A general-purpose IaC tool from HashiCorp using its own DSL (HCL).</p><ul><li><b>Multi-cloud:</b> A single tool for AWS, Azure, GCP, and hundreds of other providers (Datadog, GitHub, Cloudflare, Kubernetes).</li><li><b>Massive ecosystem:</b> Public module registry, extensive community modules for common patterns.</li><li><b>Mature workflow:</b> plan/apply, state management, workspaces, and rich third-party tooling (Terragrunt, Atlantis).</li><li><b>Trade-offs:</b> State file to manage carefully (S3 + DynamoDB or a managed service). Lags AWS a few weeks/months for new services. HCL is another language to learn.</li></ul><p><b>AWS CDK (Cloud Development Kit):</b> An abstraction layer over CloudFormation. Developers write infrastructure code in a general-purpose language (TypeScript, Python, Java, C#, Go). CDK synthesizes CloudFormation templates from that code.</p><ul><li><b>Real programming language:</b> Loops, functions, classes, unit tests, IDE tooling — everything you have in normal software development.</li><li><b>High-level constructs:</b> A single line like <code>new ec2.Vpc(this, 'MyVpc', { maxAzs: 3 })</code> generates a fully-configured VPC with subnets, route tables, and gateways — dozens of underlying CFN resources.</li><li><b>Solid AWS integration:</b> Inherits CFN's native AWS backing.</li><li><b>Trade-offs:</b> AWS-only (though CDK for Terraform (CDKTF) exists as a multi-cloud spin-off). Steeper learning curve when moving up the abstraction ladder.</li></ul><pre>// AWS CDK (TypeScript)\nimport * as cdk from 'aws-cdk-lib';\nimport * as s3 from 'aws-cdk-lib/aws-s3';\n\nexport class LogsStack extends cdk.Stack {\n  constructor(scope: cdk.App, id: string) {\n    super(scope, id);\n    new s3.Bucket(this, 'AppLogs', {\n      bucketName: 'my-app-logs',\n      versioned: true,\n      encryption: s3.BucketEncryption.S3_MANAGED,\n    });\n  }\n}</pre><p><b>Choosing between them:</b></p><ul><li><b>AWS-only + strong developer team:</b> CDK — best developer experience, high abstraction, unit testable.</li><li><b>Multi-cloud or already using Terraform:</b> Terraform — mature, portable, huge community.</li><li><b>AWS-only + operations-first team:</b> CloudFormation — least new tooling to adopt, no state management overhead.</li><li><b>Hybrid pattern:</b> Some organizations use Terraform for platform-level resources (networking, IAM, DNS) and CDK for application stacks, getting both benefits.</li></ul>",
      },
      {
        q: "Explain AWS Organizations, Control Tower, and multi-account strategy.",
        difficulty: "hard",
        a: "<p>Serious enterprise AWS adoption uses many accounts, not one. A multi-account strategy is now considered a best practice for security isolation, blast-radius limitation, cost tracking, and organizational scaling. AWS provides several services to make multi-account management practical.</p><p><b>Why multiple accounts:</b></p><ul><li><b>Blast radius isolation:</b> A compromised or misconfigured account cannot affect others. Different environments (dev/staging/prod) or business units cannot accidentally interfere.</li><li><b>Independent security posture:</b> Each account has its own IAM boundary. A production account can enforce stricter policies than a sandbox.</li><li><b>Clean cost attribution:</b> Costs per account are automatically distinct — no complex tag-based accounting needed to know which team spent what.</li><li><b>Quota isolation:</b> Service quotas (EC2 instances, VPCs, S3 buckets) are per-account. Splitting workloads across accounts avoids single-account quota exhaustion.</li></ul><p><b>AWS Organizations:</b> The foundational service for managing multiple accounts. Provides:</p><ul><li><b>Consolidated billing:</b> All member accounts roll up to a single payer account.</li><li><b>Organizational Units (OUs):</b> Hierarchical grouping for policy application (e.g., <code>Production/Payments</code>, <code>NonProduction/Dev</code>).</li><li><b>Service Control Policies (SCPs):</b> Guardrails that define what actions accounts can perform. SCPs are enforced above IAM — even the account root cannot bypass them. Example SCP: \"Deny use of regions outside us-east-1 and us-west-2.\"</li><li><b>Account creation via API:</b> Automated account provisioning as part of standard workflows.</li></ul><p><b>AWS Control Tower:</b> A managed landing zone service built on top of Organizations. It provisions a baseline multi-account setup with best-practice guardrails.</p><ul><li>Automatically creates security accounts (log archive, audit) and management structures.</li><li>Enables preventive guardrails (SCPs) and detective guardrails (Config rules).</li><li>Provides Account Factory for standardized account provisioning.</li><li>Integrates with AWS Single Sign-On (Identity Center) for centralized identity.</li></ul><p><b>Common multi-account structures:</b></p><pre>Root Organization\n├── Management (payer account, billing)\n├── Security\n│   ├── Log Archive (centralized CloudTrail/Config logs)\n│   └── Audit (security tooling like Security Hub, GuardDuty)\n├── Shared Services\n│   ├── Networking (Transit Gateway hub, DNS)\n│   └── CI/CD (build pipelines, artifact stores)\n├── Production\n│   ├── App-A-Prod\n│   ├── App-B-Prod\n│   └── ...\n└── Non-Production\n    ├── App-A-Dev\n    ├── App-A-Stage\n    └── ...</pre><p>Each workload team typically gets its own dev, stage, and prod accounts. Central platform teams manage the shared infrastructure accounts.</p><p><b>Cross-account access patterns:</b></p><ul><li><b>IAM roles with trust policies:</b> A user in the management account assumes a role in a target account, obtaining short-lived credentials scoped to that account's permissions.</li><li><b>AWS IAM Identity Center (successor to AWS SSO):</b> Centralized human identity, mapping users to permission sets in each account via a portal. This is now the recommended path for human access.</li><li><b>Resource-based policies:</b> Some services (S3, KMS) allow cross-account access grants at the resource level.</li></ul><p><b>Additional tools in a mature multi-account setup:</b></p><ul><li><b>AWS Config Aggregator:</b> Consolidate compliance data across all accounts.</li><li><b>Security Hub, GuardDuty, Macie:</b> Enable at the organization level with findings sent to the audit account.</li><li><b>Cost Explorer / CUR:</b> Cross-account cost analysis from the management account.</li><li><b>Transit Gateway:</b> Hub-and-spoke networking connecting VPCs across accounts.</li></ul>",
      },
      {
        q: "Explain Amazon EKS in depth.",
        difficulty: "medium",
        a: "<p><b>Amazon Elastic Kubernetes Service (EKS)</b> is AWS's managed Kubernetes offering. AWS manages the Kubernetes control plane (API server, etcd, scheduler, controller manager) as a highly available service across multiple Availability Zones. Customers manage the worker nodes and workloads.</p><p><b>Node compute options:</b></p><ul><li><b>Self-managed EC2 nodes:</b> Traditional model. You create Auto Scaling Groups, choose AMIs, and manage the node lifecycle. Provides maximum control and lowest cost, at the price of more operational work.</li><li><b>Managed Node Groups:</b> AWS manages the underlying Auto Scaling Groups, EC2 launch templates, and node updates. You choose instance types and scaling parameters. This is the default choice for most new EKS clusters.</li><li><b>Fargate:</b> Serverless pods — no nodes at all. AWS runs each pod as an isolated micro-VM. Priced per pod resource, not per node. Excellent for bursty workloads and small clusters where you do not want to manage nodes.</li></ul><p><b>Networking in EKS:</b></p><ul><li><b>Amazon VPC CNI:</b> The default networking plugin. Assigns pods real VPC IP addresses from the subnet, making pods first-class citizens in the VPC. Enables VPC-native features (Security Groups per pod, VPC flow logs, direct Route 53 resolution).</li><li><b>Alternative CNIs:</b> Cilium and Calico can be used instead of or alongside the AWS CNI for advanced networking features (network policies, eBPF-based observability, service mesh).</li><li><b>Prefix delegation:</b> A configuration that increases the number of pods per node by giving each ENI multiple IP prefixes rather than individual IPs.</li></ul><p><b>Identity integration — IRSA (IAM Roles for Service Accounts):</b> A key EKS feature. Kubernetes service accounts can be mapped to IAM roles via OIDC federation. Pods that use those service accounts automatically get short-lived AWS credentials — no long-lived keys stored in the cluster.</p><pre># A ServiceAccount that assumes an IAM role\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: s3-reader\n  annotations:\n    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/eks-s3-reader\n\n# Pods using this SA automatically get credentials for the role</pre><p><b>Add-ons:</b> EKS supports \"managed add-ons\" for core components — VPC CNI, CoreDNS, kube-proxy, EBS CSI driver, Pod Identity Agent, and others. AWS manages upgrade lifecycles for these components, simplifying maintenance.</p><p><b>EKS Auto Mode (2024+):</b> A newer offering where AWS also manages the node infrastructure automatically, similar to GKE Autopilot. You define workloads; AWS handles node provisioning, scaling, and patching. Higher price per pod, but significantly less operational overhead.</p><p><b>Common EKS considerations for DevOps:</b></p><ul><li><b>Cluster upgrades:</b> Kubernetes moves fast (minor version every ~4 months). EKS supports each version for ~14 months. A cluster on an unsupported version becomes a serious issue.</li><li><b>Cost optimization:</b> Mix on-demand, Reserved Instances/Savings Plans, and Spot Instances via managed node groups. Karpenter (AWS's autoscaler) is increasingly popular for aggressive Spot usage.</li><li><b>Ingress:</b> The AWS Load Balancer Controller creates ALBs or NLBs from Kubernetes Ingress and Service resources.</li><li><b>Secrets:</b> Prefer IRSA and AWS Secrets Manager (via CSI driver or External Secrets Operator) over Kubernetes Secrets for AWS-integrated secret handling.</li></ul>",
      },
      {
        q: "How do you optimize AWS costs?",
        difficulty: "medium",
        a: "<p>AWS cost optimization is a continuous discipline, not a one-time project. Modern cloud budgets are rarely constrained by a lack of levers — they are constrained by consistent application of well-known practices. A structured approach covers three time horizons: immediate wins, medium-term architectural changes, and ongoing hygiene.</p><p><b>Immediate wins (within a week):</b></p><ol><li><b>Delete idle resources.</b> Cost Explorer and AWS Trusted Advisor flag unused EBS volumes, unattached Elastic IPs, idle load balancers, and stopped EC2 instances still incurring EBS costs. Simply deleting these often reduces bills by 5–15%.</li><li><b>Right-size overprovisioned resources.</b> CloudWatch metrics show EC2 instances running at 5% CPU consistently. Downsize to smaller instance types. RDS instances often have similar overprovisioning.</li><li><b>Enable S3 Intelligent-Tiering or lifecycle policies.</b> Objects rarely accessed after 30 days should transition to lower-cost storage classes automatically.</li><li><b>Remove old EBS snapshots and unused AMIs.</b> Retention policies often let snapshots accumulate indefinitely.</li><li><b>Delete unused NAT Gateways or shared them across VPCs.</b> NAT Gateways are surprisingly expensive; consolidating them can save thousands per month.</li></ol><p><b>Medium-term architectural changes (weeks to months):</b></p><ol><li><b>Commit to Reserved Instances or Savings Plans.</b> For steady-state workloads, 1-year or 3-year commitments reduce EC2 and RDS costs by 30–60%. Compute Savings Plans are particularly flexible — they cover EC2, Fargate, and Lambda across instance families and regions.</li><li><b>Adopt Spot Instances for interruptible workloads.</b> Batch processing, CI runners, non-critical dev/staging environments, and stateless production workloads with proper handling can run on Spot at 60–90% discount. Kubernetes autoscalers like Karpenter make Spot use trivial.</li><li><b>Move to ARM-based Graviton instances.</b> Graviton (T4g, M7g, R7g, etc.) offers ~20% better price/performance than Intel equivalents. Most workloads recompile trivially.</li><li><b>Adopt serverless where appropriate.</b> Lambda and Fargate charge only for actual execution — a huge win for bursty or infrequent workloads that would otherwise run underutilized EC2 instances.</li><li><b>Optimize data transfer.</b> Inter-AZ, inter-region, and internet egress costs add up. Consolidate cross-AZ traffic, use S3 Transfer Acceleration selectively, and consider CloudFront to reduce egress from origin.</li></ol><p><b>Ongoing hygiene (continuous):</b></p><ol><li><b>Enforce mandatory tagging via Service Control Policies.</b> Every resource must have Owner, Environment, and CostCenter tags. Untagged resources cannot be created. This enables cost allocation reports that actually attribute spend to teams.</li><li><b>Set budgets and alerts.</b> AWS Budgets can send email/SNS alerts when spending crosses defined thresholds. Set them per account and per tag.</li><li><b>Regular cost reviews.</b> Monthly or quarterly reviews with each team owner. AWS Cost Explorer and Cost and Usage Reports (CUR) with QuickSight or Athena queries reveal trends.</li><li><b>Automate ephemeral environments.</b> Feature branch deployments, PR preview environments, and CI infrastructure should auto-destroy after use. Long-lived \"forgot to shut it down\" dev environments are common cost sinks.</li></ol><p><b>Tools worth knowing:</b></p><ul><li><b>AWS Cost Explorer:</b> Built-in cost analysis and forecasting.</li><li><b>AWS Cost Anomaly Detection:</b> ML-based alerting on unusual spend.</li><li><b>AWS Trusted Advisor:</b> Built-in checks for common optimization opportunities.</li><li><b>Third-party (Vantage, CloudZero, Cloudability, Kubecost):</b> Deeper analytics, Kubernetes-aware cost attribution, and often better UX than AWS's native tools.</li><li><b>FinOps Foundation:</b> Industry framework for cross-functional cost management practices.</li></ul><p><b>Cultural shift:</b> Sustainable cost optimization requires more than tools. Teams need visibility into their spend (via tagging and dashboards), incentives to optimize (budget accountability), and processes to review regularly. This is what the FinOps discipline formalizes.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "azure",
    name: "Azure Cloud",
    icon: "🔷",
    kind: "qa",
    description:
      "Microsoft Azure fundamentals for DevOps — resource management, identity, networking, AKS, and Azure DevOps.",
    items: [
      {
        q: "What is Microsoft Azure and how does it compare to AWS?",
        difficulty: "easy",
        a: "<p><b>Microsoft Azure</b> is Microsoft's public cloud platform, offering a comprehensive suite of Infrastructure-as-a-Service (IaaS), Platform-as-a-Service (PaaS), and Software-as-a-Service (SaaS) offerings. It competes directly with Amazon Web Services (AWS) and Google Cloud Platform (GCP), and is particularly strong in enterprises with existing Microsoft investments (Active Directory, Office 365, SQL Server, Windows Server).</p><p><b>Comparison with AWS at a service-level:</b></p><ul><li><b>Compute:</b> Azure Virtual Machines vs AWS EC2; Azure Functions vs AWS Lambda; Azure App Service vs AWS Elastic Beanstalk.</li><li><b>Storage:</b> Azure Blob Storage vs AWS S3; Azure Files vs AWS EFS; Azure Managed Disks vs AWS EBS.</li><li><b>Databases:</b> Azure SQL Database vs AWS RDS; Azure Cosmos DB vs AWS DynamoDB.</li><li><b>Networking:</b> Azure Virtual Network (VNet) vs AWS VPC; Azure Load Balancer / Application Gateway vs AWS ELB/ALB.</li><li><b>Kubernetes:</b> Azure Kubernetes Service (AKS) vs AWS EKS.</li><li><b>Identity:</b> Microsoft Entra ID (formerly Azure Active Directory) vs AWS IAM.</li></ul><p><b>Key differentiators of Azure:</b></p><ul><li><b>Enterprise integration:</b> Deep integration with Windows Server, Active Directory, and Microsoft 365. Hybrid identity via Entra Connect is a common enterprise pattern.</li><li><b>Hybrid-cloud focus:</b> Azure Arc extends Azure control-plane management to on-premises and multi-cloud resources, making it strong for hybrid scenarios.</li><li><b>Enterprise licensing:</b> Existing Windows Server, SQL Server, and Visual Studio licenses can be reused on Azure via Azure Hybrid Benefit, reducing costs significantly.</li></ul><p><b>Selection guidance:</b> Enterprises with existing Microsoft licensing and Active Directory typically favor Azure. Startups and cloud-native workloads often lean toward AWS due to its broader service catalog. GCP tends to win where data analytics and machine learning are primary drivers.</p>",
        followups: [
          {
            q: "What is the Azure equivalent of AWS's IAM?",
            a: "<p>The Azure equivalent of AWS IAM is <b>Microsoft Entra ID (formerly Azure Active Directory)</b> combined with <b>Azure RBAC (Role-Based Access Control)</b>.</p><p><b>Entra ID</b> handles identity — users, groups, service principals (equivalent to AWS IAM users and roles), and managed identities. It also provides SSO, MFA, and conditional access policies.</p><p><b>Azure RBAC</b> handles authorization — assigning built-in or custom roles at various scopes (management group, subscription, resource group, or individual resource). A role assignment in Azure combines a security principal (who), a role definition (what they can do), and a scope (where).</p><p>Unlike AWS IAM policies which are attached to identities or resources, Azure's model separates identity management (Entra ID) from resource-scoped role assignments (Azure RBAC), which is often considered cleaner but requires understanding both systems.</p>",
          },
          {
            q: "Which cloud should I choose — AWS, Azure, or GCP?",
            a: "<p>The choice depends primarily on organizational context rather than technical superiority — all three are capable, mature platforms with substantial overlap. Practical selection factors:</p><ul><li><b>Existing licensing and identity:</b> If your organization already runs Active Directory, Windows Server, and Office 365, Azure offers the smoothest integration and cost benefits via Azure Hybrid Benefit.</li><li><b>Ecosystem maturity:</b> AWS remains the largest by revenue and typically has the broadest service catalog and third-party ecosystem, making it the safest choice for cloud-native startups.</li><li><b>Data and ML focus:</b> GCP is often preferred for organizations where BigQuery, Vertex AI, and Kubernetes (which originated at Google) are strategic technologies.</li><li><b>Regional presence:</b> All three have global footprints, but specific regions may favor one over the others for compliance or latency reasons.</li><li><b>Multi-cloud reality:</b> Large enterprises frequently adopt more than one cloud provider for risk diversification, workload optimization, or acquisition-driven inheritance. DevOps skills therefore benefit from being multi-cloud aware.</li></ul>",
          },
        ],
      },
      {
        q: "Explain Azure Resource Manager (ARM) and Resource Groups.",
        difficulty: "medium",
        a: "<p><b>Azure Resource Manager (ARM)</b> is the deployment and management service for Azure. Every operation against Azure — creating, reading, updating, or deleting a resource — flows through ARM. It provides a consistent management layer above all Azure resource providers, handling authentication, authorization, tagging, policy enforcement, and dependency resolution.</p><p><b>Resource Groups</b> are the fundamental grouping mechanism in Azure. A resource group is a logical container that holds related resources — for example, a web application's storage account, virtual machines, databases, and networking. All resources in a group share the same lifecycle: they can be deployed, updated, or deleted together as a unit.</p><p><b>Key characteristics of resource groups:</b></p><ul><li><b>Region:</b> A resource group has a location (region) for its metadata, but the resources inside it can live in different regions.</li><li><b>Scope for management:</b> RBAC role assignments, policies, and locks can be applied at the resource-group level, inheriting to all resources within.</li><li><b>Lifecycle boundary:</b> Deleting a resource group deletes all resources it contains — a powerful cleanup mechanism (and a common source of accidents).</li><li><b>Tagging:</b> Resource groups and their resources can be tagged for cost allocation, ownership tracking, and automation.</li></ul><p><b>ARM Templates and Bicep:</b> ARM templates are JSON files that declaratively describe Azure resources for deployment. Bicep is a newer, cleaner DSL that compiles to ARM templates but is much more readable and provides a superior authoring experience. Both are Azure-native alternatives to Terraform for infrastructure-as-code.</p>",
        followups: [
          {
            q: "What is the Azure resource hierarchy?",
            a: "<p>Azure's resource hierarchy has four levels, from top to bottom:</p><ol><li><b>Management Groups:</b> Containers for organizing subscriptions. Used for applying policies and RBAC across many subscriptions (e.g., all production subscriptions under a \"Production\" management group). A tenant can have up to six levels of nested management groups.</li><li><b>Subscriptions:</b> The billing and quota boundary. Each subscription has its own limits, billing account, and Azure AD tenant association. Enterprises typically use multiple subscriptions to isolate environments (dev, staging, prod) and business units.</li><li><b>Resource Groups:</b> Logical containers within a subscription, as described above. Used for lifecycle grouping and RBAC assignment.</li><li><b>Resources:</b> The individual Azure objects — VMs, storage accounts, databases, VNets, etc.</li></ol><p>Policies (Azure Policy) and RBAC roles applied at higher levels are inherited by everything beneath them, so a subscription-level policy applies to all resources within all its resource groups. This inheritance model enables consistent governance at scale.</p>",
          },
          {
            q: "Difference between ARM Templates and Bicep?",
            a: "<p><b>ARM Templates</b> are the original infrastructure-as-code format for Azure — JSON documents that describe resources declaratively. They are powerful but notoriously verbose: even simple deployments require substantial JSON scaffolding, extensive quoting, and hard-to-read expressions using functions like <code>[concat(parameters('name'), 'suffix')]</code>.</p><p><b>Bicep</b> is a domain-specific language released by Microsoft in 2020 that compiles down to ARM Templates. It provides:</p><ul><li>A much cleaner, more concise syntax without JSON's syntactic overhead.</li><li>Type checking and IntelliSense in editors like VS Code.</li><li>Automatic dependency detection based on symbolic references (no manual <code>dependsOn</code> arrays).</li><li>Modules for reusable components.</li><li>100% functional parity with ARM Templates.</li></ul><p><b>Example comparison — creating a storage account:</b></p><pre>// Bicep\nresource storageAccount 'Microsoft.Storage/storageAccounts@2023-01-01' = {\n  name: 'mystorage'\n  location: 'eastus'\n  sku: { name: 'Standard_LRS' }\n  kind: 'StorageV2'\n}</pre><p>The equivalent ARM Template JSON is roughly three times longer. Microsoft now recommends Bicep for all new Azure IaC work. Terraform remains the popular multi-cloud alternative when the same team manages resources across AWS, Azure, and GCP.</p>",
          },
        ],
      },
      {
        q: "What is an Azure Subscription and how does it fit in the hierarchy?",
        difficulty: "easy",
        a: "<p>An <b>Azure Subscription</b> is a logical container for Azure resources and the primary boundary for billing, quotas, and access control. Every resource deployed in Azure belongs to exactly one subscription, and every subscription has a designated billing owner responsible for its charges.</p><p><b>Key roles a subscription plays:</b></p><ul><li><b>Billing boundary:</b> Costs are aggregated per subscription and invoiced accordingly. Enterprise Agreement (EA) and Microsoft Customer Agreement (MCA) accounts can have many subscriptions under a single billing profile.</li><li><b>Quota boundary:</b> Azure enforces per-region resource quotas at the subscription level (e.g., maximum cores per VM family, maximum public IP addresses).</li><li><b>Trust boundary:</b> A subscription is associated with a single Microsoft Entra ID tenant for authentication.</li><li><b>Access boundary:</b> Azure RBAC can be applied at the subscription scope, granting or denying access to all resources within.</li></ul><p><b>Multi-subscription patterns for enterprises:</b></p><ul><li><b>Environment isolation:</b> Separate subscriptions for dev, staging, and production, each with tailored quotas and access.</li><li><b>Business unit separation:</b> Different subscriptions for different departments or products, enabling clear cost attribution.</li><li><b>Landing Zone pattern:</b> Following Microsoft's Cloud Adoption Framework, enterprises typically create a hub subscription (shared networking, identity, security) and many spoke subscriptions (workloads).</li></ul><p>Subscriptions are grouped into Management Groups for higher-level policy and RBAC application, and multiple subscriptions can share the same Entra ID tenant for unified identity.</p>",
      },
      {
        q: "What are Availability Zones and Availability Sets in Azure?",
        difficulty: "medium",
        a: "<p>Azure provides two mechanisms for improving VM availability, which serve different purposes and offer different levels of protection.</p><p><b>Availability Sets (older):</b> A logical grouping of VMs deployed to different physical hardware (fault domains) and update domains within a single data center. The mechanism protects against:</p><ul><li><b>Fault domains:</b> Hardware failures such as power supply or network switch outages. VMs in different fault domains do not share these components.</li><li><b>Update domains:</b> Planned maintenance events (host reboots for patching). Azure updates one update domain at a time, so VMs in different update domains are not restarted simultaneously.</li></ul><p>Availability Sets protect against localized failures but do not protect against a full data center outage. They provide a 99.95% SLA.</p><p><b>Availability Zones (newer, preferred):</b> Physically separate data centers within an Azure region. Each region that supports Availability Zones has three zones with independent power, cooling, and networking, connected by high-speed low-latency links. Deploying VMs across Availability Zones protects against:</p><ul><li>Hardware failures.</li><li>Full data center outages (power loss, natural events).</li></ul><p>Availability Zones provide a 99.99% SLA and are the recommended pattern for production-critical workloads. Zone-redundant services (like Zone-Redundant Storage, ZRS) automatically replicate data across zones for higher durability.</p><p><b>Comparison to AWS:</b> Azure Availability Zones are conceptually equivalent to AWS Availability Zones. Azure Availability Sets have no direct AWS equivalent — they predate Availability Zones and are being gradually replaced.</p><p><b>Guidance:</b> For new deployments, use Availability Zones. Availability Sets remain useful in regions that do not yet support zones or for workloads with stricter co-location requirements.</p>",
        followups: [
          {
            q: "What is a Virtual Machine Scale Set?",
            a: "<p>A <b>Virtual Machine Scale Set (VMSS)</b> is Azure's equivalent of AWS Auto Scaling Groups. It manages a group of identical VMs, providing automatic scaling, health-based instance replacement, and integrated load balancing.</p><p><b>Key capabilities:</b></p><ul><li><b>Auto-scaling:</b> Scale in or out based on metrics (CPU, memory, custom metrics), schedules, or predictive analytics.</li><li><b>Zone deployment:</b> Instances can be spread across Availability Zones for high availability.</li><li><b>Rolling upgrades:</b> Automated upgrade of the underlying OS image or configuration with configurable batch sizes.</li><li><b>Health probes:</b> Unhealthy instances are automatically replaced.</li><li><b>Integration with Azure Load Balancer / Application Gateway</b> for traffic distribution.</li></ul><p>VMSS supports two orchestration modes: <b>Uniform</b> (all VMs identical, larger scale, more restrictions) and <b>Flexible</b> (more control per VM, smaller scale, more flexibility). Uniform mode is preferred for large stateless fleets; Flexible mode is preferred when individual VM customization is needed.</p>",
          },
        ],
      },
      {
        q: "Explain Azure Virtual Network (VNet) and its components.",
        difficulty: "medium",
        a: "<p>An <b>Azure Virtual Network (VNet)</b> is a logically isolated network within Azure, conceptually equivalent to an AWS VPC. It provides the foundation for private connectivity between Azure resources, and between Azure and on-premises or other networks.</p><p><b>Core components:</b></p><ul><li><b>Address space:</b> A range of private IP addresses (typically RFC 1918) assigned to the VNet, from which subnets are carved out (e.g., <code>10.0.0.0/16</code>).</li><li><b>Subnets:</b> Sub-ranges of the VNet address space, each scoped to a specific purpose (e.g., web tier, database tier). Subnets are the units at which Network Security Groups and route tables are applied.</li><li><b>Network Security Groups (NSGs):</b> Stateful firewall rules that allow or deny traffic based on source, destination, port, and protocol. Applied at the subnet or network interface level.</li><li><b>Route tables:</b> Custom routing rules that override Azure's default system routes, enabling patterns like forcing outbound traffic through a firewall appliance.</li><li><b>Service Endpoints:</b> Extend a VNet's identity to Azure PaaS services (Storage, SQL, Key Vault), providing secure access without traversing the public internet.</li><li><b>Private Endpoints:</b> A newer, more secure evolution of Service Endpoints — assigns Azure PaaS services a private IP address inside the VNet, using Azure Private Link.</li></ul><p><b>Connectivity options between VNets and external networks:</b></p><ul><li><b>VNet Peering:</b> Directly connect two VNets in the same or different regions, allowing private traffic between them without gateways.</li><li><b>VPN Gateway:</b> Site-to-site or point-to-site VPN over the public internet, encrypted with IPsec/IKE.</li><li><b>ExpressRoute:</b> Private, dedicated, high-bandwidth connection from on-premises through a Microsoft partner network — bypasses the public internet.</li><li><b>Azure Virtual WAN:</b> A hub-and-spoke networking service for large enterprises with many branch offices and VNets.</li></ul>",
        followups: [
          {
            q: "How does VNet Peering work?",
            a: "<p><b>VNet Peering</b> is Azure's mechanism for directly connecting two Virtual Networks so that resources in each can communicate as if they were in the same network. Traffic between peered VNets flows over Microsoft's backbone (not the public internet), providing low latency, high bandwidth, and no additional encryption overhead.</p><p><b>Two types:</b></p><ul><li><b>Regional VNet Peering:</b> Between two VNets in the same Azure region.</li><li><b>Global VNet Peering:</b> Between VNets in different regions. Traffic still stays on the Microsoft backbone.</li></ul><p><b>Characteristics:</b></p><ul><li>Peering must be configured on both sides of the connection.</li><li>Overlapping address spaces are not permitted between peered VNets.</li><li>Transit routing is not enabled by default — if VNet A peers with B, and B peers with C, A cannot reach C unless B is configured as a transit hub (via a Network Virtual Appliance or Azure Firewall).</li><li>Charges apply for both inbound and outbound traffic across a peering.</li></ul><p><b>Common architecture:</b> Hub-and-spoke, where a central hub VNet contains shared services (firewall, Azure Bastion, DNS) and multiple spoke VNets (workload VNets) peer with the hub. All spoke-to-spoke traffic transits through the hub for centralized inspection.</p>",
          },
        ],
      },
      {
        q: "What is Microsoft Entra ID (Azure Active Directory) and how do you use it for RBAC?",
        difficulty: "medium",
        a: "<p><b>Microsoft Entra ID</b> (formerly Azure Active Directory) is Microsoft's cloud-based identity and access management service. Every Azure subscription is associated with an Entra ID tenant, which authenticates users, groups, applications, and devices attempting to access Azure resources.</p><p><b>Core identity concepts in Entra ID:</b></p><ul><li><b>Users:</b> Individual identities (employees, contractors, guest accounts via B2B collaboration).</li><li><b>Groups:</b> Collections of users used for bulk role assignment.</li><li><b>Service Principals:</b> Non-human identities used by applications and services to authenticate to Azure — equivalent to AWS IAM users for programmatic access.</li><li><b>Managed Identities:</b> A special type of service principal automatically managed by Azure. The identity's credentials are not exposed to the workload; Azure handles rotation and lifecycle. This is the preferred approach for workloads running on Azure.</li></ul><p><b>Azure RBAC (Role-Based Access Control)</b> is layered on top of Entra ID to grant permissions to Azure resources. An RBAC assignment combines three elements:</p><ol><li><b>Security principal (who):</b> A user, group, service principal, or managed identity.</li><li><b>Role definition (what they can do):</b> A collection of allowed actions. Built-in roles include Owner, Contributor, Reader, and many resource-specific roles (e.g., Virtual Machine Contributor). Custom roles can be defined for fine-grained needs.</li><li><b>Scope (where):</b> The hierarchy level at which the assignment applies — Management Group, Subscription, Resource Group, or individual Resource. Permissions inherit downward.</li></ol><p><b>Example:</b> Assigning \"Virtual Machine Contributor\" role to a group at the resource-group scope allows all group members to manage VMs in that resource group only.</p><p><b>Best practices:</b></p><ul><li>Grant least privilege — use built-in roles when possible, custom roles only when needed.</li><li>Assign roles to groups rather than individuals for easier lifecycle management.</li><li>Use Managed Identities for workloads instead of service-principal secrets.</li><li>Enable Privileged Identity Management (PIM) for just-in-time elevation of sensitive roles.</li></ul>",
        followups: [
          {
            q: "What is a Managed Identity in Azure?",
            a: "<p>A <b>Managed Identity</b> is a special type of service principal in Microsoft Entra ID that Azure automatically manages on behalf of a workload. It eliminates the need for developers to handle credentials manually — no secrets, keys, or certificates are stored in application code or configuration.</p><p><b>Two flavors:</b></p><ul><li><b>System-assigned managed identity:</b> Created and tied to a specific Azure resource (e.g., a VM, App Service, or Function App). Its lifecycle is coupled to the resource — deleting the resource deletes the identity.</li><li><b>User-assigned managed identity:</b> A standalone Azure resource that can be assigned to multiple resources. Its lifecycle is independent, which is useful when the same identity should persist across resource re-creation.</li></ul><p><b>How it works:</b> When application code needs to authenticate to another Azure service (e.g., Key Vault, Storage), it calls the local Instance Metadata Service (IMDS) endpoint on the VM/App Service host. IMDS returns a short-lived Entra ID token for the managed identity, which the application uses to authenticate. The Azure SDK's <code>DefaultAzureCredential</code> class transparently handles this flow.</p><p><b>Example — Python accessing Key Vault:</b></p><pre>from azure.identity import DefaultAzureCredential\nfrom azure.keyvault.secrets import SecretClient\n\ncredential = DefaultAzureCredential()\nclient = SecretClient(\"https://mykv.vault.azure.net\", credential)\nsecret = client.get_secret(\"db-password\")</pre><p>No credentials appear in code. This is the equivalent of AWS IAM Roles for Service Accounts (IRSA) or EC2 instance profiles, and is the strongly recommended pattern for any workload running on Azure.</p>",
          },
          {
            q: "What is Privileged Identity Management (PIM)?",
            a: "<p><b>Privileged Identity Management (PIM)</b> is a Microsoft Entra ID feature that enables just-in-time (JIT) and time-bound access to sensitive Azure and Entra ID roles. Rather than permanently assigning privileged roles like Owner or Global Administrator, PIM lets users activate those roles only when needed, for a limited time, with optional approval and multi-factor authentication.</p><p><b>Key benefits:</b></p><ul><li>Reduces the standing attack surface — high-privilege accounts spend most of their time as regular users.</li><li>Provides an audit trail of every role activation, including justification.</li><li>Enables approval workflows for elevation, requiring peer or manager sign-off.</li><li>Enforces MFA and access reviews as part of the activation process.</li></ul><p>PIM is part of Microsoft Entra ID Premium P2 licensing and is a foundational component of a Zero Trust security posture.</p>",
          },
        ],
      },
      {
        q: "Explain Azure Kubernetes Service (AKS).",
        difficulty: "medium",
        a: "<p><b>Azure Kubernetes Service (AKS)</b> is Azure's managed Kubernetes offering. Microsoft manages the Kubernetes control plane (API server, etcd, scheduler, controller manager) as a free service; customers only pay for the worker nodes (VMs) and any add-on services they consume. AKS is Azure's equivalent of AWS EKS and GCP GKE.</p><p><b>Key features:</b></p><ul><li><b>Managed control plane:</b> Microsoft handles upgrades, patching, and high availability of the control plane. Users interact only with the standard Kubernetes API.</li><li><b>Node pools:</b> Groups of nodes with identical configuration (VM size, OS, taints). A cluster can have multiple node pools for different workload profiles — for example, a general-purpose pool, a GPU pool, and a spot-instance pool.</li><li><b>Integrated with Entra ID:</b> RBAC bindings can reference Entra ID users and groups directly, enabling centralized identity management.</li><li><b>Networking options:</b> Two CNI plugins are supported — <b>kubenet</b> (simpler, uses Azure route tables) and <b>Azure CNI</b> (each pod gets a VNet IP, enabling direct VNet integration).</li><li><b>Cluster Autoscaler and Horizontal Pod Autoscaler</b> integrated with Azure infrastructure APIs.</li><li><b>Add-ons:</b> Azure Monitor for containers, Application Gateway Ingress Controller (AGIC), Azure Policy for Kubernetes, GitOps via Flux (in AKS Fleet Manager).</li></ul><p><b>Typical usage pattern:</b></p><pre>az aks create --resource-group prod-rg \\\n  --name prod-aks \\\n  --node-count 3 \\\n  --enable-managed-identity \\\n  --enable-aad \\\n  --enable-cluster-autoscaler \\\n  --min-count 3 --max-count 10\n\naz aks get-credentials -g prod-rg -n prod-aks\nkubectl get nodes</pre><p><b>Comparison with EKS and GKE:</b> All three are managed Kubernetes services with similar core functionality. AKS is particularly strong on Entra ID integration and enterprise Windows container workloads. EKS has the largest ecosystem and mature third-party tooling. GKE is often considered the most technically advanced given Google's role in creating Kubernetes.</p>",
        followups: [
          {
            q: "What is Azure AD (Entra ID) integration in AKS?",
            a: "<p>AKS can integrate with Microsoft Entra ID (Azure AD) to authenticate Kubernetes API requests using Entra ID identities rather than static Kubernetes users or certificates. This centralizes access management with the rest of the organization's identity infrastructure.</p><p><b>How it works:</b></p><ol><li>The cluster is created with the <code>--enable-aad</code> flag (managed Entra ID integration).</li><li>Users authenticate to <code>kubectl</code> using their Entra ID credentials via a browser-based device code flow.</li><li>Kubernetes RBAC bindings reference Entra ID group Object IDs, so members of specific groups automatically get the corresponding permissions.</li><li>MFA and Conditional Access policies configured in Entra ID apply to Kubernetes access — for example, requiring MFA for production cluster access.</li></ol><p>This eliminates the need to manage kubeconfig files with embedded credentials and provides a full audit trail of who accessed the cluster through Entra ID sign-in logs.</p>",
          },
        ],
      },
      {
        q: "What is Azure DevOps and how does it compare to GitHub Actions and Jenkins?",
        difficulty: "medium",
        a: "<p><b>Azure DevOps</b> is Microsoft's suite of DevOps tools, comprising five integrated services under a single platform:</p><ol><li><b>Azure Repos:</b> Git repository hosting (or TFVC for legacy centralized VCS).</li><li><b>Azure Pipelines:</b> CI/CD service supporting builds and multi-stage deployments.</li><li><b>Azure Boards:</b> Agile work-tracking with sprints, backlogs, Kanban boards, and burndown charts.</li><li><b>Azure Test Plans:</b> Manual and exploratory test management.</li><li><b>Azure Artifacts:</b> Package management for Maven, npm, NuGet, Python, and Universal Packages.</li></ol><p><b>Azure Pipelines vs GitHub Actions:</b></p><ul><li>Both are owned by Microsoft — GitHub Actions is the newer product and receives more active investment.</li><li>Azure Pipelines has mature multi-stage YAML pipelines with approvals, environment gates, and deep enterprise features.</li><li>GitHub Actions integrates seamlessly with GitHub-hosted repos, provides a marketplace of pre-built actions, and has a simpler mental model.</li><li>Both can deploy to any target (Azure, AWS, GCP, on-prem).</li><li>Microsoft's stated direction is that new features go to GitHub Actions first; Azure Pipelines remains fully supported for existing users and enterprise scenarios.</li></ul><p><b>Azure Pipelines vs Jenkins:</b></p><ul><li>Azure Pipelines is a fully hosted SaaS with per-user pricing; Jenkins is self-hosted open source.</li><li>Azure Pipelines has built-in integrations for Azure services (App Service, AKS, Key Vault) that would require plugins in Jenkins.</li><li>Jenkins offers broader ecosystem and customization via its plugin library.</li><li>Jenkins requires operational overhead (patching, scaling, plugin management); Azure Pipelines does not.</li></ul><p><b>When to choose which:</b> For new projects, GitHub Actions if code is on GitHub. Azure Pipelines for large enterprises with existing investment or complex multi-stage requirements. Jenkins for teams that need maximum flexibility, self-hosted operation, or already have significant Jenkins infrastructure.</p>",
      },
      {
        q: "How does Azure Storage work and what are the storage types?",
        difficulty: "medium",
        a: "<p><b>Azure Storage</b> is Microsoft's foundational cloud storage service, offering multiple data services under a single storage account. It is designed for massive scale, durability, and low cost.</p><p><b>The four primary services within a storage account:</b></p><ul><li><b>Blob Storage:</b> Object storage for unstructured data — images, videos, backups, logs, static websites. Equivalent to AWS S3 or GCP Cloud Storage. Supports three blob types: Block Blobs (files), Append Blobs (log-style writes), and Page Blobs (used as backing storage for Azure VMs' managed disks).</li><li><b>File Storage:</b> Fully managed SMB and NFS file shares that can be mounted from Windows, Linux, or macOS. Equivalent to AWS EFS or FSx.</li><li><b>Queue Storage:</b> Simple asynchronous message queues for decoupling application components. Equivalent to AWS SQS.</li><li><b>Table Storage:</b> A NoSQL key-value store for semi-structured data. Largely superseded by Azure Cosmos DB for new workloads.</li></ul><p><b>Access tiers for Blob Storage</b> — used to optimize cost based on access patterns:</p><ul><li><b>Hot:</b> Frequently accessed data. Highest storage cost, no per-access retrieval fee.</li><li><b>Cool:</b> Infrequently accessed, minimum 30-day storage. Lower storage cost, per-access retrieval fee.</li><li><b>Cold:</b> Rarely accessed, minimum 90-day storage. Even lower storage cost.</li><li><b>Archive:</b> Rarely accessed, minimum 180-day storage. Lowest storage cost but retrieval takes hours (rehydration).</li></ul><p><b>Redundancy options</b> — determine how data is replicated for durability:</p><ul><li><b>LRS (Locally Redundant Storage):</b> 3 copies in a single data center.</li><li><b>ZRS (Zone-Redundant Storage):</b> 3 copies across 3 Availability Zones in the same region.</li><li><b>GRS (Geo-Redundant Storage):</b> LRS in primary region + async replica in secondary region.</li><li><b>GZRS (Geo-Zone-Redundant Storage):</b> ZRS in primary region + async replica in secondary region.</li></ul><p>Lifecycle policies can automatically transition blobs between tiers or delete them based on age, similar to S3 lifecycle rules.</p>",
      },
      {
        q: "What is Azure Key Vault and how do you integrate it with applications?",
        difficulty: "medium",
        a: "<p><b>Azure Key Vault</b> is Microsoft's managed service for securely storing secrets, cryptographic keys, and TLS/SSL certificates. It provides hardware security module (HSM)-backed key storage, centralized secret management, and full audit logging — eliminating the need to store sensitive data in code, configuration files, or environment variables.</p><p><b>Three types of stored items:</b></p><ul><li><b>Secrets:</b> Arbitrary text values such as database passwords, API keys, or connection strings. Retrieved as opaque strings.</li><li><b>Keys:</b> Cryptographic keys (RSA, EC) used for signing or encryption operations. The private key material never leaves Key Vault — applications submit data to Key Vault for signing.</li><li><b>Certificates:</b> Full X.509 certificates, including automatic renewal through integrated CAs like DigiCert or Let's Encrypt.</li></ul><p><b>Access model:</b> Key Vault uses either <b>Azure RBAC</b> (recommended for new deployments) or the legacy <b>access policies</b>. RBAC provides fine-grained roles like Key Vault Secrets User, Key Vault Reader, and Key Vault Administrator, applied at scope.</p><p><b>Integration patterns:</b></p><ol><li><b>Managed Identity (preferred):</b> The application's managed identity is granted Key Vault access. The application uses the Azure SDK to fetch secrets at runtime with no credentials in code:<pre>from azure.identity import DefaultAzureCredential\nfrom azure.keyvault.secrets import SecretClient\n\ncredential = DefaultAzureCredential()\nclient = SecretClient(\"https://myvault.vault.azure.net\", credential)\ndb_password = client.get_secret(\"db-password\").value</pre></li><li><b>Kubernetes Secret Store CSI Driver:</b> Mount Key Vault secrets as files inside pods on AKS, so containers see them as regular files. Secrets are synced with automatic rotation.</li><li><b>Application Configuration integration:</b> Azure App Configuration and App Service can reference Key Vault secrets directly in configuration.</li><li><b>Key Vault References in ARM/Bicep:</b> Templates can reference secrets at deployment time without exposing them in template files.</li></ol><p><b>Best practices:</b> Enable <b>soft delete</b> and <b>purge protection</b> to prevent accidental or malicious deletion, enable <b>diagnostic logging</b> to a Log Analytics workspace for auditing, and rotate secrets regularly using Event Grid notifications for automation.</p>",
      },
      {
        q: "How do you set up CI/CD for an Azure application?",
        difficulty: "medium",
        a: "<p>Azure supports multiple CI/CD approaches, with the most common combinations being GitHub Actions targeting Azure, Azure Pipelines, or Jenkins. A modern, cloud-native approach typically uses <b>GitHub Actions with OpenID Connect (OIDC) federation to Azure</b> for authentication, eliminating the need for long-lived service principal secrets.</p><p><b>Example workflow — deploying a container to AKS via GitHub Actions:</b></p><pre>name: Deploy to AKS\n\non:\n  push:\n    branches: [main]\n\npermissions:\n  id-token: write   # required for OIDC\n  contents: read\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Azure login via OIDC\n        uses: azure/login@v2\n        with:\n          client-id: ${{ secrets.AZURE_CLIENT_ID }}\n          tenant-id: ${{ secrets.AZURE_TENANT_ID }}\n          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}\n\n      - name: Build and push image\n        run: |\n          az acr login -n myregistry\n          docker build -t myregistry.azurecr.io/myapp:${{ github.sha }} .\n          docker push myregistry.azurecr.io/myapp:${{ github.sha }}\n\n      - name: Set AKS context\n        uses: azure/aks-set-context@v3\n        with:\n          resource-group: prod-rg\n          cluster-name: prod-aks\n\n      - name: Deploy\n        run: |\n          kubectl set image deployment/myapp \\\n            myapp=myregistry.azurecr.io/myapp:${{ github.sha }}</pre><p><b>Key components of a mature Azure CI/CD setup:</b></p><ul><li><b>Source of truth in Git:</b> Application code and infrastructure code (Bicep/Terraform) versioned together.</li><li><b>OIDC federation:</b> GitHub-to-Azure trust configured so workflows authenticate without stored secrets.</li><li><b>Azure Container Registry (ACR):</b> Private container registry for the organization's images.</li><li><b>Environment approvals:</b> Production deployments require manual approval via GitHub Environments or Azure DevOps.</li><li><b>Bicep or Terraform for infrastructure:</b> Applied through the same pipeline for consistent IaC.</li><li><b>Observability integration:</b> Application Insights instrumentation deployed with the application.</li></ul><p><b>Alternative for GitOps:</b> Rather than <code>kubectl apply</code> in the pipeline, use ArgoCD or Flux to sync from a Git repository, following the pull-based GitOps model.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "gcp",
    name: "Google Cloud (GCP)",
    icon: "🟠",
    kind: "qa",
    description:
      "Google Cloud Platform for DevOps — projects, IAM, GKE, Cloud Storage, Cloud Build, and data-native services.",
    items: [
      {
        q: "What is Google Cloud Platform (GCP) and how does it compare to AWS and Azure?",
        difficulty: "easy",
        a: "<p><b>Google Cloud Platform (GCP)</b> is Google's public cloud offering, built on the same infrastructure that powers Google Search, YouTube, and Gmail. It is the third-largest hyperscale cloud provider after AWS and Azure, with particular strength in data analytics, machine learning, and Kubernetes.</p><p><b>Service-level comparison with AWS and Azure:</b></p><ul><li><b>Compute:</b> Compute Engine (VMs) vs EC2/Azure VMs; Cloud Run (serverless containers) vs Fargate/Container Apps; Cloud Functions vs Lambda/Azure Functions.</li><li><b>Storage:</b> Cloud Storage vs S3/Blob Storage; Persistent Disks vs EBS/Managed Disks; Filestore vs EFS/Azure Files.</li><li><b>Databases:</b> Cloud SQL vs RDS/Azure SQL; Firestore/Cloud Spanner vs DynamoDB/Cosmos DB; BigQuery (no direct equivalent — GCP's flagship analytics warehouse).</li><li><b>Networking:</b> VPC (globally scoped, unique to GCP) vs regional VPCs in AWS/Azure; Cloud Load Balancing (global, anycast) vs ELB/ALB.</li><li><b>Kubernetes:</b> Google Kubernetes Engine (GKE) — the original and most mature managed Kubernetes; often considered the technical leader given Google's role in creating Kubernetes.</li><li><b>Identity:</b> Cloud IAM (resource-focused) vs AWS IAM (identity-focused) / Azure Entra ID.</li></ul><p><b>Key differentiators of GCP:</b></p><ul><li><b>Global network:</b> GCP's network is unique — VPCs are global by default (subnets are regional). This enables truly global load balancing with a single anycast IP.</li><li><b>Data and analytics:</b> BigQuery is a fully managed, serverless data warehouse that scales to petabytes with no infrastructure to manage. Widely regarded as best-in-class.</li><li><b>Machine learning:</b> Vertex AI provides an integrated ML platform, and GCP has significant investment in AI-optimized hardware (TPUs).</li><li><b>Container leadership:</b> As the home of Kubernetes, GCP's container tooling — GKE, Cloud Run, Anthos — is very mature.</li></ul><p><b>Common usage pattern:</b> GCP is often adopted for specific workloads (data analytics, ML) alongside AWS or Azure for general infrastructure, giving rise to multi-cloud strategies.</p>",
      },
      {
        q: "Explain the GCP resource hierarchy (Organization, Folders, Projects, Resources).",
        difficulty: "medium",
        a: "<p>GCP's resource hierarchy is central to how permissions, billing, and organizational policies are applied. There are four levels, from top to bottom:</p><ol><li><b>Organization:</b> The root of the hierarchy, corresponding to a company or entity. Created when a Google Workspace or Cloud Identity account is linked to GCP. All resources ultimately belong to an organization.</li><li><b>Folders:</b> Optional grouping units within an organization. Used to organize projects by department, environment, or business unit. Folders can be nested (up to 10 levels), enabling flexible organizational structures.</li><li><b>Projects:</b> The primary organizational unit in GCP. A project is a billing boundary, resource container, and permission boundary — analogous to an AWS account or Azure subscription. Every GCP resource belongs to exactly one project.</li><li><b>Resources:</b> The individual GCP objects (VMs, storage buckets, GKE clusters, databases). Each resource is created within a project.</li></ol><p><b>Policy inheritance:</b> IAM policies and organization policies applied at higher levels are inherited by everything beneath. Assigning a role at the organization level grants it across all folders, projects, and resources.</p><p><b>Why projects matter operationally:</b></p><ul><li>Each project has its own set of enabled APIs, quotas, and billing account.</li><li>Projects provide strong isolation — resources in different projects cannot communicate by default.</li><li>Projects can be deleted as a unit, cleaning up all contained resources — useful for temporary environments.</li></ul><p><b>Multi-project patterns for enterprises:</b></p><ul><li>Separate projects for dev/staging/prod for a single application.</li><li>Separate projects per team or business unit for cost attribution.</li><li>Shared VPC pattern where one \"host\" project owns the network and multiple \"service\" projects consume it.</li></ul>",
        followups: [
          {
            q: "How does GCP billing work at the project level?",
            a: "<p>Every GCP project is associated with a <b>Billing Account</b>, which is the entity that receives charges. Costs for all resources created within a project accrue to that project's billing account.</p><p><b>Key characteristics:</b></p><ul><li>A billing account can be linked to many projects, allowing consolidated billing across an organization.</li><li>Billing accounts can be organized into Billing Sub-Accounts for further separation.</li><li>Detailed cost breakdown is available at the billing account, project, service, or SKU level via BigQuery export.</li><li><b>Budgets and alerts</b> can be configured at the billing account or project scope to notify when spending thresholds are crossed.</li><li>Cost anomaly detection can identify unusual spend patterns.</li></ul><p>A common enterprise pattern is one billing account per business unit or cost center, with many projects underneath. This enables clear cost attribution while maintaining centralized billing management.</p>",
          },
        ],
      },
      {
        q: "Explain GCP IAM and how it differs from AWS IAM.",
        difficulty: "medium",
        a: "<p><b>Google Cloud IAM</b> is GCP's identity and access management service. It follows a resource-centric model that is fundamentally different from AWS IAM's identity-centric model, and this distinction is important to understand.</p><p><b>Core concepts in GCP IAM:</b></p><ul><li><b>Member (or Principal):</b> An identity — a Google account (user), Google Group, Service Account, domain, or the special <code>allUsers</code> / <code>allAuthenticatedUsers</code> pseudo-identities.</li><li><b>Role:</b> A named collection of permissions. Three types:<ul><li><b>Primitive roles:</b> Owner, Editor, Viewer — legacy, very broad.</li><li><b>Predefined roles:</b> Fine-grained roles maintained by Google (e.g., <code>roles/storage.objectViewer</code>).</li><li><b>Custom roles:</b> Roles defined by the customer with specific permission sets.</li></ul></li><li><b>Policy:</b> A binding between a member and a role, attached to a resource. Grants the specified member the specified role's permissions on that resource.</li></ul><p><b>Difference from AWS IAM:</b></p><ul><li><b>AWS IAM</b> attaches policies to identities (users, groups, roles), which define what those identities can do to any resource. It is <i>identity-focused</i>.</li><li><b>GCP IAM</b> attaches policies to resources (or resource hierarchy nodes like projects), granting specified identities specified roles on those resources. It is <i>resource-focused</i>.</li></ul><p>This has practical implications: in GCP, giving a user access to a specific storage bucket requires setting a policy on that bucket. In AWS, it requires editing a policy attached to the user. Neither is objectively better; they simply reflect different design philosophies.</p><p><b>Service Accounts:</b> The equivalent of AWS IAM roles for programmatic access. A service account is both an identity and can be granted roles, and it can be attached to compute resources (Compute Engine, GKE workloads, Cloud Functions) so workloads inherit its permissions without embedded credentials.</p><p><b>Workload Identity:</b> GCP's equivalent of AWS IRSA — allows GKE pods to authenticate as GCP service accounts without requiring node-level credentials or JSON key files. Strongly recommended over service account keys for security.</p>",
      },
      {
        q: "What is Google Kubernetes Engine (GKE)?",
        difficulty: "medium",
        a: "<p><b>Google Kubernetes Engine (GKE)</b> is Google's managed Kubernetes service and the original managed Kubernetes offering — launched in 2014, before AWS EKS or Azure AKS. Because Google created and open-sourced Kubernetes, GKE is often considered the most technically advanced of the managed Kubernetes services.</p><p><b>Two operating modes:</b></p><ul><li><b>GKE Standard:</b> Traditional model where the user chooses node pools, VM sizes, and scaling policies. Provides maximum control and is priced per node.</li><li><b>GKE Autopilot:</b> Fully managed mode where Google manages nodes automatically — users only define workloads. Google handles node provisioning, sizing, scaling, and patching. Priced per pod resource request. Recommended for most new workloads because it reduces operational overhead significantly.</li></ul><p><b>Key features:</b></p><ul><li><b>Automated upgrades:</b> Control plane and nodes can be auto-upgraded on release channels (Rapid, Regular, Stable).</li><li><b>Multi-cluster support via Fleet:</b> Manage many GKE clusters as a single logical fleet with unified config and policy.</li><li><b>Workload Identity:</b> Pods authenticate as GCP service accounts securely without embedded credentials.</li><li><b>Regional and Zonal clusters:</b> Regional clusters replicate the control plane across three zones for higher availability.</li><li><b>Container-native networking:</b> GKE uses VPC-native networking where pods get real IP addresses from the VPC, integrating cleanly with GCP networking.</li><li><b>Binary Authorization:</b> Attestation-based supply chain security — only signed, verified images can be deployed.</li></ul><p><b>Comparison with EKS and AKS:</b> GKE typically leads on features and Kubernetes version freshness. EKS has the largest ecosystem and third-party integrations. AKS is strongest for Windows containers and Microsoft-stack integration.</p>",
        followups: [
          {
            q: "What is GKE Autopilot and when should you use it?",
            a: "<p><b>GKE Autopilot</b> is a fully managed operating mode of GKE where Google handles all node-level operations. In Autopilot, the user only defines Kubernetes workloads (Deployments, StatefulSets, etc.); Google automatically provisions, scales, and patches the underlying nodes based on the workloads' resource requests.</p><p><b>Autopilot handles automatically:</b></p><ul><li>Node provisioning and sizing.</li><li>Node scaling up and down.</li><li>Node OS patching and Kubernetes upgrades.</li><li>Node security hardening.</li><li>Resource allocation optimization.</li></ul><p><b>Pricing:</b> Autopilot is priced based on the CPU, memory, and ephemeral storage requested by running pods — not by the underlying VMs. This aligns cost directly with workload demand.</p><p><b>Trade-offs:</b> Autopilot enforces constraints that Standard mode does not — restricted node access, limited DaemonSets, certain kernel-level features unavailable. These constraints exist because Google manages the nodes. For workloads that fit within Autopilot's model — which is most general web/API workloads — the operational simplification is significant.</p><p><b>When to choose Autopilot:</b> New GKE deployments, general-purpose workloads, teams without deep Kubernetes operations expertise. Standard mode is preferred for specialized workloads requiring node-level customization, privileged access, or specific hardware (GPUs, TPUs).</p>",
          },
        ],
      },
      {
        q: "Explain GCP Cloud Storage and its storage classes.",
        difficulty: "easy",
        a: "<p><b>Google Cloud Storage</b> is GCP's unified object storage service — the equivalent of AWS S3 and Azure Blob Storage. It provides highly durable, globally accessible storage for any type of unstructured data, from bytes to petabytes.</p><p><b>Fundamental concepts:</b></p><ul><li><b>Buckets:</b> Top-level containers that hold objects. Bucket names are globally unique across all of GCP.</li><li><b>Objects:</b> The individual files stored in buckets, each identified by a name and containing arbitrary bytes.</li><li><b>Location:</b> A bucket has a location, which is either a specific region, a dual-region (two specific regions), or a multi-region (a continent-scale grouping like <code>US</code> or <code>EU</code>).</li></ul><p><b>Storage classes</b> — trade off storage cost against access frequency and retrieval fees:</p><ul><li><b>Standard:</b> Frequently accessed data. Highest storage cost, no retrieval fee, no minimum storage duration.</li><li><b>Nearline:</b> Data accessed less than once per month. Lower storage cost, per-GB retrieval fee, 30-day minimum storage.</li><li><b>Coldline:</b> Data accessed less than once per quarter. Even lower storage cost, higher retrieval fee, 90-day minimum storage.</li><li><b>Archive:</b> Data accessed less than once per year. Lowest storage cost, highest retrieval fee, 365-day minimum storage.</li></ul><p>Unlike some competitors, all Cloud Storage classes offer <b>identical millisecond retrieval latency</b> — you never wait hours for cold data. The trade-off is purely economic, driven by storage cost vs retrieval cost.</p><p><b>Object Lifecycle Management:</b> Similar to S3 lifecycle rules, Cloud Storage supports automatic transitions between classes and automatic deletion based on age, version count, or custom conditions.</p><p><b>Additional features:</b> Versioning (retain deleted or overwritten object versions), Object Holds (compliance-driven retention preventing deletion), Signed URLs (time-limited authenticated download links), and Customer-Managed Encryption Keys (CMEK) via Cloud KMS.</p>",
      },
      {
        q: "What is BigQuery and why is it significant for GCP?",
        difficulty: "medium",
        a: "<p><b>BigQuery</b> is GCP's fully managed, serverless data warehouse — one of Google's flagship products and a primary reason many organizations adopt GCP. It is designed for analytical (OLAP) workloads at massive scale, with no infrastructure to provision or manage.</p><p><b>What makes BigQuery distinctive:</b></p><ul><li><b>Serverless architecture:</b> No clusters, no nodes, no capacity planning. Queries automatically scale to the resources they need. You pay for storage and for query processing (either on-demand per bytes scanned, or via reserved slot commitments).</li><li><b>Separation of storage and compute:</b> Data is stored in Google's Colossus filesystem; queries run on ephemeral Dremel compute engines. This decoupling enables massive parallelism and independent scaling.</li><li><b>Standard SQL support:</b> Uses standard ANSI SQL (with some Google-specific extensions), so analysts and data engineers can use familiar tools without learning a new query language.</li><li><b>Petabyte scale:</b> BigQuery routinely handles single queries scanning hundreds of terabytes in seconds.</li><li><b>Integration:</b> Native connections to Data Studio (visualization), Dataflow (streaming ETL), Looker (BI), and machine-learning tools including BigQuery ML for running ML models directly in SQL.</li><li><b>Streaming inserts:</b> Data can be inserted continuously with near-real-time query availability.</li></ul><p><b>Pricing model:</b></p><ul><li><b>On-demand:</b> Pay per byte scanned by queries (~$5 per TB, with the first TB per month free).</li><li><b>Slot-based reservations:</b> Reserve dedicated capacity (slots) at predictable monthly cost.</li></ul><p><b>Common DevOps relevance:</b> BigQuery is often the target of GCP's billing exports, allowing organizations to run detailed cost analytics on their cloud spend. It is also a common destination for logs exported from Cloud Logging.</p>",
      },
      {
        q: "What is Cloud Build and how does it fit into GCP's CI/CD story?",
        difficulty: "medium",
        a: "<p><b>Cloud Build</b> is GCP's managed CI/CD service, providing serverless build execution for container images, packages, and general-purpose CI/CD workflows. It is the native alternative to Jenkins, GitHub Actions, or Azure Pipelines when targeting GCP.</p><p><b>Core characteristics:</b></p><ul><li><b>Serverless:</b> No build servers to provision or manage. Cloud Build allocates ephemeral compute for each build.</li><li><b>YAML-configured:</b> A <code>cloudbuild.yaml</code> file in the source repository defines a sequence of build steps, each running as a container.</li><li><b>Container-native:</b> Each step is a container image, allowing arbitrary tooling. Google provides many pre-built builder images (Docker, kubectl, gcloud, Maven, Node, Python, etc.).</li><li><b>Integrated with source:</b> Triggers on pushes or tags in Cloud Source Repositories, GitHub, GitLab, or Bitbucket.</li><li><b>Integrated with GCP:</b> Native integration with Artifact Registry (image storage), GKE and Cloud Run (deployment targets), and Secret Manager (credentials).</li></ul><p><b>Example cloudbuild.yaml:</b></p><pre>steps:\n  - name: 'gcr.io/cloud-builders/docker'\n    args: ['build', '-t', 'us-central1-docker.pkg.dev/$PROJECT_ID/apps/myapp:$SHORT_SHA', '.']\n\n  - name: 'gcr.io/cloud-builders/docker'\n    args: ['push', 'us-central1-docker.pkg.dev/$PROJECT_ID/apps/myapp:$SHORT_SHA']\n\n  - name: 'gcr.io/cloud-builders/gke-deploy'\n    args:\n      - run\n      - --filename=k8s/\n      - --image=us-central1-docker.pkg.dev/$PROJECT_ID/apps/myapp:$SHORT_SHA\n      - --location=us-central1\n      - --cluster=prod-gke\n\ntimeout: '1200s'</pre><p><b>GCP CI/CD ecosystem:</b> Beyond Cloud Build, GCP integrates with Cloud Deploy for continuous delivery to GKE/Cloud Run, Artifact Registry for image storage, and Binary Authorization for policy-based deployment gates. Many teams also use GitHub Actions or GitLab CI targeting GCP for their broader tooling ecosystem while using Cloud Build for GCP-native, image-heavy workloads.</p>",
      },
      {
        q: "What is Cloud Run and when should you use it instead of GKE?",
        difficulty: "medium",
        a: "<p><b>Cloud Run</b> is GCP's fully managed serverless platform for running containers. It sits between traditional serverless (Cloud Functions, AWS Lambda) and container orchestration (GKE), offering the container flexibility of Kubernetes without the operational overhead.</p><p><b>Key characteristics:</b></p><ul><li><b>Any container image:</b> As long as your container listens on the port specified by the <code>PORT</code> environment variable and starts within a few seconds, it can run on Cloud Run — regardless of language or framework.</li><li><b>Scale-to-zero:</b> When no requests are arriving, Cloud Run scales the deployment to zero instances, and you pay nothing. Cold starts are typically fast (sub-second for small images).</li><li><b>Auto-scaling to thousands:</b> Cloud Run automatically scales up in response to traffic, with configurable minimum and maximum instance counts.</li><li><b>Per-request billing:</b> You pay for the CPU, memory, and requests consumed during actual execution, not for idle capacity.</li><li><b>Fully managed:</b> No cluster to manage, no nodes to patch, no networking to configure.</li><li><b>Integrated with GCP:</b> Native integration with Cloud Build, Artifact Registry, IAM, Cloud SQL, and VPC.</li></ul><p><b>When to choose Cloud Run over GKE:</b></p><ul><li>Stateless HTTP APIs and web applications.</li><li>Event-driven workloads triggered by Pub/Sub or Cloud Storage events.</li><li>Workloads with variable or bursty traffic where scale-to-zero saves cost.</li><li>Small teams that want container flexibility without Kubernetes operational overhead.</li></ul><p><b>When to prefer GKE:</b></p><ul><li>Complex multi-service applications with intricate networking or storage requirements.</li><li>StatefulSets, DaemonSets, or workloads needing precise Kubernetes control.</li><li>Long-running batch jobs.</li><li>Workloads requiring specific hardware (GPUs, TPUs) with fine control.</li></ul><p><b>Cloud Run for Anthos:</b> A variant that runs Cloud Run workloads on GKE clusters, giving you the Knative-style developer experience with the flexibility of Kubernetes.</p>",
      },
      {
        q: "How does networking work in GCP compared to AWS?",
        difficulty: "medium",
        a: "<p>GCP's networking model differs fundamentally from AWS in several important ways. Understanding these differences is essential to designing GCP architectures effectively.</p><p><b>Key differences:</b></p><ul><li><b>Global VPC:</b> A GCP VPC is <b>global by default</b> — it spans all GCP regions with a single logical network. Subnets are regional, but they all belong to the same global VPC. In AWS, VPCs are scoped to a single region, and cross-region communication requires VPC peering or Transit Gateway.</li><li><b>Global load balancing:</b> A single Cloud Load Balancer can serve traffic from any GCP region using anycast IP. This means one global IP can route users to the nearest region automatically. AWS requires more complex configurations (Global Accelerator, Route 53 latency-based routing) to achieve similar behavior.</li><li><b>Firewall rules are VPC-scoped:</b> GCP firewall rules apply at the VPC level with target tags or service accounts identifying which resources they affect. AWS security groups are attached to individual network interfaces.</li><li><b>Automatic subnets:</b> By default, GCP creates auto-mode VPCs with pre-defined subnets in every region. Most organizations prefer custom-mode VPCs for explicit control over IP allocation.</li></ul><p><b>Standard GCP networking components:</b></p><ul><li><b>VPC:</b> Global logical network.</li><li><b>Subnets:</b> Regional IP ranges within a VPC.</li><li><b>Firewall rules:</b> VPC-wide allow/deny rules.</li><li><b>Cloud Router:</b> Dynamic BGP routing for VPN and Interconnect.</li><li><b>Cloud NAT:</b> Managed NAT gateway for outbound internet from private instances.</li><li><b>Cloud Interconnect:</b> Private connectivity between on-premises and GCP (equivalent to AWS Direct Connect).</li><li><b>Cloud VPN:</b> IPsec VPN tunnels for site-to-site or client VPN.</li><li><b>Shared VPC:</b> Multiple projects share a central VPC — enables clean separation of network administration from workload administration.</li></ul>",
      },
      {
        q: "What is Anthos and where does it fit in GCP's offering?",
        difficulty: "hard",
        a: "<p><b>Anthos</b> is Google's hybrid and multi-cloud application platform, built on Kubernetes. It extends GCP's tooling and management capabilities to workloads running on-premises, in other clouds (AWS, Azure), or at the edge. Anthos is Google's answer to enterprises that need consistent Kubernetes operations across heterogeneous environments.</p><p><b>Core Anthos components:</b></p><ul><li><b>Anthos clusters (on-prem):</b> Google-managed Kubernetes on your own hardware — GKE running on VMware vSphere or bare metal.</li><li><b>Anthos on AWS / Azure:</b> GKE-managed Kubernetes running on AWS EC2 or Azure VMs, providing GKE consistency across clouds.</li><li><b>Anthos Config Management:</b> GitOps-based policy and configuration syncing across all clusters, using tools like Config Sync and Policy Controller (Gatekeeper).</li><li><b>Anthos Service Mesh:</b> Google's managed Istio distribution, providing traffic management, security (mTLS), and observability across services regardless of where they run.</li><li><b>Cloud Run for Anthos:</b> Run Knative-based serverless workloads on any Anthos cluster.</li><li><b>Migrate for Anthos:</b> Tools for migrating existing VM-based workloads into containers running on Anthos.</li></ul><p><b>Where it fits in GCP's offering:</b> While core GCP services target cloud-native workloads on GCP, Anthos targets organizations with hybrid or multi-cloud needs. Common use cases include enterprises with existing on-premises datacenter investments transitioning gradually to cloud, and companies requiring consistent operations across multiple public clouds for regulatory or contractual reasons.</p><p><b>Comparison with alternatives:</b> AWS Outposts and Azure Arc/Azure Stack are the equivalent hybrid offerings from other clouds. All three are premium-priced products aimed at large enterprises rather than typical cloud-native startups. Anthos differentiates on its Kubernetes-native architecture and consistent operating model regardless of underlying infrastructure.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "monitoring",
    name: "Monitoring & Observability",
    icon: "📊",
    kind: "qa",
    description:
      "Metrics, logs, traces, and the SLO discipline that turns noisy alerts into meaningful signals.",
    items: [
      {
        q: "Monitoring vs Observability — what's the difference?",
        difficulty: "medium",
        a: "<p><b>Monitoring</b> and <b>observability</b> are related but distinct concepts. Monitoring is a subset of observability, focused on watching known conditions; observability is the broader capability of understanding system behavior from external signals.</p><p><b>Monitoring:</b> The practice of watching pre-defined signals for known failure modes. Monitoring answers specific questions defined in advance — \"Is CPU utilization above 90%?\", \"Is the error rate above 1%?\", \"Is disk usage above 85%?\". Dashboards and alerts are configured for these known conditions. Monitoring is highly effective for known unknowns — problems you anticipate can occur and want to detect.</p><p><b>Observability:</b> A property of a system that enables asking <i>new</i>, previously unanticipated questions about its state from the outside — without deploying new code or adding new instrumentation. Observability is achieved by producing rich telemetry (metrics, logs, and traces) that can be sliced and correlated at query time. Observable systems are effective for unknown unknowns — investigating novel problems whose signature you did not anticipate.</p><p><b>Practical distinction:</b></p><ul><li>Monitoring tells you <i>what</i> is broken (a symptom).</li><li>Observability tells you <i>why</i> it is broken (the root cause).</li></ul><p><b>Complementary use:</b> Mature systems combine both. Monitoring provides early warning through alerts on known-important signals. Observability provides the exploratory tooling to diagnose the underlying cause when alerts fire — or when users report issues that no alert caught.</p><p><b>Cultural shift:</b> Observability requires investment in instrumentation and tooling upfront (structured logging, distributed tracing, high-cardinality metrics) but pays dividends in incident response time and the ability to understand novel failure modes in complex distributed systems.</p>",
        followups: [
          {
            q: "What are the three pillars of observability?",
            a: "<p>The three traditional pillars of observability are metrics, logs, and traces. Each provides a different view of system behavior, and together they enable comprehensive analysis.</p><ol><li><b>Metrics:</b> Numeric time-series data — measurements of specific quantities over time. Metrics are highly compressed, cheap to store, and efficient to query at scale. They answer questions like \"what is the request rate?\" or \"what is the 99th-percentile latency?\" Common tooling: Prometheus, InfluxDB, Datadog.</li><li><b>Logs:</b> Discrete, timestamped events emitted by applications and systems. Logs contain rich context (which user, which request, which parameters) that is impractical to include in metrics due to cardinality. They answer questions like \"what error occurred for user X at 10:32am?\" Common tooling: Loki, the ELK stack (Elasticsearch, Logstash, Kibana), Splunk.</li><li><b>Traces:</b> Records of individual requests as they flow through a distributed system, showing the sequence of operations and their timing across services. Distributed traces answer questions like \"why is this specific request slow?\" or \"which service is the bottleneck?\" Common tooling: Tempo, Jaeger, Zipkin, OpenTelemetry.</li></ol><p><b>Modern additions:</b> The observability community has increasingly recognized additional signal types worth first-class treatment:</p><ul><li><b>Events</b> — significant occurrences such as deployments, feature flag changes, or configuration updates. Overlaying events on metrics dashboards helps correlate changes with anomalies.</li><li><b>Continuous profiles</b> — CPU, memory, and lock profiles captured continuously in production, enabling investigation of performance issues at the code level. Tools include Pyroscope, Parca, and Datadog Continuous Profiler.</li></ul>",
          },
        ],
      },
      {
        q: "How does Prometheus work?",
        difficulty: "medium",
        a: "<p><b>Prometheus</b> is an open-source metrics collection and time-series database that has become the de facto standard for monitoring in Kubernetes environments. Its architecture centers around a pull-based scrape model and a powerful query language (PromQL).</p><p><b>Data collection (pull model):</b> Prometheus periodically scrapes HTTP endpoints exposed by monitored targets. Each target exposes its metrics at a well-known endpoint (typically <code>/metrics</code>) in a simple text-based exposition format. Prometheus fetches this endpoint at a configured scrape interval (typically 15–60 seconds).</p><p><b>Service discovery:</b> Rather than manually configuring every target, Prometheus dynamically discovers what to scrape via service discovery integrations:</p><ul><li><b>Kubernetes</b> — pods, services, endpoints via the Kubernetes API.</li><li><b>Consul, etcd, Zookeeper</b> — via their respective APIs.</li><li><b>Cloud providers</b> — EC2 instances via the AWS API, GCE instances, Azure VMs.</li><li><b>DNS-based</b> — SRV records.</li><li><b>Static configuration</b> — for fixed targets.</li></ul><p><b>Storage:</b> Metrics are stored in a local time-series database optimized for the workload of large numbers of writes and range queries. Data is stored in 2-hour blocks and compacted over time. For long-term storage and horizontal scaling, projects like Thanos, Cortex, and Mimir extend Prometheus with remote object storage backends.</p><p><b>Alerting:</b> Alerts are defined as PromQL expressions with duration thresholds. When an expression evaluates to true for the specified duration, Prometheus emits an alert. Alerts are routed to <b>AlertManager</b>, a separate component that handles:</p><ul><li>Deduplication (multiple Prometheus instances firing the same alert).</li><li>Grouping (batching related alerts).</li><li>Routing (different alerts to different receivers — PagerDuty, Slack, email).</li><li>Silencing (temporarily suppressing alerts during known maintenance).</li><li>Inhibition (suppressing alerts when a related, more severe alert is firing).</li></ul>",
        followups: [
          {
            q: "Why is Prometheus pull-based instead of push?",
            a: "<p>The pull-based model was a deliberate design choice by Prometheus's authors. It offers several practical advantages over push-based systems:</p><ul><li><b>Target health as a side effect:</b> If Prometheus cannot scrape a target, that itself is a signal — the target is either down or unhealthy. In a push model, missing data is ambiguous — did the target stop pushing because it is broken, because the network is broken, or because the metric was never emitted?</li><li><b>Simpler multi-server operation:</b> Multiple Prometheus servers can independently scrape the same targets without coordination. A single target does not need to know or care about how many Prometheus servers are monitoring it. In a push model, targets would need to know all endpoints to push to.</li><li><b>Simpler firewall model:</b> Prometheus initiates outbound connections to scrape targets. In many environments, allowing Prometheus to reach out is easier than opening inbound holes on every workload.</li><li><b>Client simplicity:</b> Application code only needs to expose an HTTP endpoint; it does not need to manage buffering, retries, or delivery to a backend.</li><li><b>Rate limiting and load protection:</b> Prometheus controls its own load; a misconfigured client cannot overwhelm Prometheus with high-frequency pushes.</li></ul><p><b>Handling short-lived jobs:</b> The pull model does have an inherent limitation — jobs that complete before Prometheus can scrape them. For batch jobs, cron jobs, or serverless functions, the <b>Pushgateway</b> serves as a bridge: the job pushes its metrics to Pushgateway, which then exposes them for Prometheus to scrape. However, Pushgateway is designed only for this specific case; using it as a general-purpose intake would defeat the design of the system.</p>",
          },
          {
            q: "What are the 4 Prometheus metric types?",
            a: "<p>Prometheus defines four metric types, each suited to a different kind of measurement.</p><ol><li><b>Counter:</b> A cumulative value that only ever increases (or resets to zero when the process restarts). Used for counts of events — total requests served, total errors, total bytes transmitted. Named with the <code>_total</code> suffix by convention.<pre>http_requests_total{method=\"GET\",status=\"200\"} 12847</pre></li><li><b>Gauge:</b> A value that can go up or down. Used for instantaneous measurements — current memory usage, current temperature, current number of active connections.<pre>memory_usage_bytes{instance=\"web-1\"} 3421552640</pre></li><li><b>Histogram:</b> A sampling of observations distributed across configurable buckets, along with a total sum and count. Used for measuring distributions such as request latency or response size. The bucket structure enables server-side computation of any percentile across instances via <code>histogram_quantile()</code>.<pre>http_request_duration_seconds_bucket{le=\"0.1\"} 24054\nhttp_request_duration_seconds_bucket{le=\"0.5\"} 33444\nhttp_request_duration_seconds_bucket{le=\"1.0\"} 100392\nhttp_request_duration_seconds_sum 88394.4\nhttp_request_duration_seconds_count 144320</pre></li><li><b>Summary:</b> Similar to a histogram, but computes quantiles on the client side. The result is a fixed set of quantiles (e.g., 50th, 95th, 99th percentile) exposed as pre-computed values. Summaries cannot be aggregated across instances (percentiles are not aggregable in general).</li></ol><p><b>Guidance:</b> Prefer <b>histograms</b> for latency and other continuous distributions. Their bucket-based representation can be aggregated across instances and enables computing any percentile at query time. Summaries are appropriate when only a fixed set of pre-computed quantiles is needed and aggregation is not required.</p>",
          },
        ],
      },
      {
        q: "What is an SLI, SLO, and SLA?",
        difficulty: "medium",
        a: "<p>These three concepts — SLI, SLO, and SLA — are the core vocabulary of Service Level Management, a discipline pioneered by Google SRE and now widely adopted. They form a hierarchy from measurement to internal target to external commitment.</p><ul><li><b>SLI (Service Level Indicator):</b> A quantitative measure of some aspect of service level. It is a metric that reflects user experience of the service. Examples: request success rate (fraction of requests returning a non-5xx response), request latency at the 99th percentile, availability of a critical endpoint. A good SLI directly correlates with user-perceived quality.</li><li><b>SLO (Service Level Objective):</b> An internal target for an SLI over a defined time window. Examples: \"99.9% of requests successful over any 30-day window\", \"99th-percentile latency below 300ms\". SLOs are aspirational but achievable; they represent the reliability the team commits to internally.</li><li><b>SLA (Service Level Agreement):</b> A <i>contractual</i> commitment made to customers, typically with financial consequences for missing the target (e.g., service credits). SLAs are usually more relaxed than SLOs — for example, an internal SLO of 99.95% availability may back a customer-facing SLA of 99.9%. The gap provides a buffer for the team to detect and respond to issues before they become contract violations.</li></ul><p><b>Error budget:</b> The gap between the SLO target and 100% is the <b>error budget</b>. For a 99.9% SLO, the error budget is 0.1% — approximately 43 minutes of downtime per 30 days. The error budget is the amount of unreliability the team is permitted to consume.</p><p><b>Why this framework matters:</b> SLIs, SLOs, and error budgets provide a data-driven basis for engineering decisions. Instead of arguing subjectively about how much reliability work versus feature work to do, teams use the current error budget consumption to decide. A team with plenty of budget can take risks; a team burning budget must slow down and stabilize.</p>",
        followups: [
          {
            q: "What is an error budget policy and how do you use it?",
            a: "<p>An <b>error budget policy</b> is an explicit team agreement that defines what happens as the error budget is consumed. It transforms error budgets from an abstract concept into a concrete engineering practice.</p><p><b>Typical policy structure:</b></p><ul><li><b>Budget healthy (&lt; 50% consumed):</b> Normal operations. Feature work proceeds. Deployments follow standard cadence.</li><li><b>Budget concerning (50–100% consumed):</b> Additional scrutiny. Deployment cadence may slow. Reliability work is prioritized.</li><li><b>Budget exhausted (&gt; 100% consumed):</b> All non-critical feature work halts. The team focuses exclusively on reliability improvements until the budget recovers.</li></ul><p>This policy converts what would otherwise be subjective judgment (\"we should focus on reliability now\") into a data-driven, pre-agreed response. It also protects engineers from being pressured into unsafe deployments during difficult periods.</p><p><b>Burn-rate alerting:</b> Rather than only alerting when the budget is exhausted, mature teams alert on the <b>rate</b> at which the budget is being consumed. Two common tiers:</p><ul><li><b>Fast burn:</b> If the current rate would exhaust the monthly budget within a few hours, page the on-call engineer immediately (indicates an active incident).</li><li><b>Slow burn:</b> If the current rate would exhaust the monthly budget within a few days, generate a lower-urgency notification (indicates a chronic degradation worth investigating).</li></ul><p>Burn-rate alerts detect both acute outages and gradual reliability degradation, providing much more actionable signals than simple threshold alerts on individual metrics.</p>",
          },
        ],
      },
      {
        q: "What is OpenTelemetry and why is it becoming the standard?",
        difficulty: "medium",
        a: "<p><b>OpenTelemetry (OTel)</b> is a CNCF observability framework that provides a unified specification, SDKs, and collector infrastructure for generating and exporting telemetry data — traces, metrics, and logs — from applications. It is the result of merging the earlier OpenTracing and OpenCensus projects, and has rapidly become the industry standard for observability instrumentation.</p><p><b>Why OpenTelemetry matters:</b> Before OTel, each observability vendor (Datadog, New Relic, Dynatrace, AppDynamics) required their own proprietary instrumentation library. Switching vendors or supporting multiple backends meant re-instrumenting applications. OTel breaks this lock-in — you instrument once with vendor-neutral OTel libraries and export to any compatible backend.</p><p><b>The three signals OTel covers:</b></p><ul><li><b>Traces:</b> Distributed request flows across services, showing spans and their timing relationships.</li><li><b>Metrics:</b> Numerical measurements aggregated over time (counters, gauges, histograms). OTel metrics are now generally available and interoperable with Prometheus.</li><li><b>Logs:</b> Structured or unstructured messages. OTel logs are the most recent signal to reach GA, and adoption is still growing.</li></ul><p><b>Core components:</b></p><ol><li><b>OTel API:</b> The interface applications use — <code>tracer.startSpan()</code>, <code>meter.recordCounter()</code>. Vendor-neutral.</li><li><b>OTel SDK:</b> The implementation of the API for each language (Go, Java, Python, JavaScript, .NET, Ruby, Rust, etc.). Handles sampling, batching, and exporting.</li><li><b>OTel Collector:</b> A stand-alone process that receives telemetry from applications, processes it (batching, filtering, transformation), and exports to any backend. Runs as a sidecar, DaemonSet, or gateway.</li><li><b>Exporters:</b> Backend-specific plugins that translate OTel data into vendor formats (Datadog, Jaeger, Prometheus, Elastic, Grafana Cloud, and many more).</li></ol><p><b>Instrumentation approaches:</b></p><ul><li><b>Auto-instrumentation:</b> Language-specific agents automatically instrument common frameworks (HTTP clients, database drivers, message queues). For Java, this is a single JVM agent. For Python and Node.js, it is a library that hooks into common frameworks. No code changes required.</li><li><b>Manual instrumentation:</b> Application code explicitly creates spans and records metrics. Higher effort but produces richer, business-aware telemetry.</li><li><b>eBPF-based auto-instrumentation:</b> Newer tools like OpenTelemetry eBPF and Grafana Beyla auto-instrument applications from outside the process, requiring no application changes at all.</li></ul><p><b>Example — instrumenting a Go HTTP handler:</b></p><pre>import (\n    \"go.opentelemetry.io/otel\"\n    \"go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp\"\n)\n\ntracer := otel.Tracer(\"my-service\")\n\nhandler := otelhttp.NewHandler(\n    http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n        ctx, span := tracer.Start(r.Context(), \"handle-request\")\n        defer span.End()\n        // ... application logic ...\n    }),\n    \"http.handler\",\n)</pre><p><b>Why this matters strategically:</b> OpenTelemetry decouples instrumentation from backends. Organizations can start with an open-source backend (Grafana Tempo + Prometheus + Loki), evaluate commercial vendors (Datadog, Honeycomb, Chronosphere) without reinstrumenting, and even send telemetry to multiple backends simultaneously. This portability is a major shift in observability economics.</p>",
      },
      {
        q: "What is the Grafana LGTM stack?",
        difficulty: "medium",
        a: "<p>The <b>Grafana LGTM stack</b> is Grafana Labs' open-source observability platform, offering an integrated set of best-in-class open source tools for the four primary observability signals. LGTM is an acronym:</p><ul><li><b>L — Loki:</b> Log aggregation, designed to be highly cost-efficient by indexing only labels (not full text).</li><li><b>G — Grafana:</b> The visualization and dashboarding layer — the front-end for all the other components.</li><li><b>T — Tempo:</b> Distributed tracing backend, storing spans efficiently with support for OpenTelemetry, Jaeger, and Zipkin protocols.</li><li><b>M — Mimir:</b> Long-term, horizontally scalable Prometheus metrics storage. A drop-in replacement for Prometheus at multi-tenant scale.</li></ul><p><b>Why LGTM is compelling:</b></p><ul><li><b>Open source:</b> No licensing costs. Deployable anywhere.</li><li><b>Cloud-native:</b> Designed for Kubernetes, horizontal scaling built-in.</li><li><b>Cost-efficient by design:</b> Loki's label-only indexing means log storage cost is a fraction of Elasticsearch. Mimir's chunk storage optimizes long-term Prometheus retention.</li><li><b>Standard protocols:</b> Prometheus remote_write, OpenTelemetry, Loki API, Jaeger — nothing proprietary.</li><li><b>Correlation-native:</b> Grafana links traces, logs, and metrics with shared identifiers, so a spike in a metric can link directly to example traces and log lines.</li></ul><p><b>Typical deployment on Kubernetes:</b></p><pre>helm repo add grafana https://grafana.github.io/helm-charts\n\nhelm install grafana grafana/grafana -n observability\nhelm install loki grafana/loki-stack -n observability\nhelm install tempo grafana/tempo-distributed -n observability\nhelm install mimir grafana/mimir-distributed -n observability\n\n# Feed data in via:\n# - promtail (logs to Loki)\n# - OpenTelemetry Collector (traces to Tempo, metrics to Mimir)\n# - Prometheus / Grafana Agent (metrics)</pre><p><b>Grafana Cloud:</b> Grafana Labs also offers a hosted SaaS version of LGTM. For teams that don't want to operate the stack themselves, this is a substantially cheaper alternative to Datadog for equivalent capability, particularly on log-heavy workloads.</p><p><b>How LGTM compares to alternatives:</b></p><ul><li><b>vs Elastic Stack (Elasticsearch + Logstash + Kibana):</b> LGTM is much cheaper for logs (label indexing vs full-text). Kibana is more powerful for log search-heavy use cases; Grafana is more powerful for cross-signal correlation.</li><li><b>vs Datadog / New Relic / Dynatrace:</b> LGTM lacks the polish, ML-based features, and integrations of commercial APMs, but is dramatically cheaper. Trade-off: more operational burden self-hosted.</li><li><b>vs Prometheus + Jaeger + separate log store:</b> LGTM provides integration and single-pane-of-glass Grafana; assembling equivalent tooling yourself is possible but requires more integration work.</li></ul><p><b>Adoption trend:</b> LGTM is gaining rapid traction as organizations look for observability cost control. It is not a silver bullet — Datadog remains dominant in many enterprises — but it is now a serious contender, particularly for cost-conscious cloud-native teams.</p>",
      },
      {
        q: "Explain SRE golden signals and RED/USE methodologies.",
        difficulty: "medium",
        a: "<p>Observability produces enormous volumes of data. To make it actionable, engineers use structured frameworks that identify the most important signals for detecting problems. Three widely-used frameworks are the <b>Four Golden Signals</b>, <b>USE method</b>, and <b>RED method</b>.</p><p><b>Four Golden Signals (Google SRE):</b> The four key metrics for user-facing systems.</p><ol><li><b>Latency:</b> Time to serve a request. Track separately for successful and failed requests — a 50 ms failure and a 500 ms success mean different things.</li><li><b>Traffic:</b> Rate of requests through the system. Requests per second, transactions per second.</li><li><b>Errors:</b> Rate of failed requests. Explicit failures (HTTP 5xx), implicit failures (successful HTTP 200 with wrong content), and policy failures (e.g., latency exceeding SLO).</li><li><b>Saturation:</b> How \"full\" the service is. CPU utilization, memory pressure, thread pool utilization, connection pool utilization. Systems degrade before hitting 100%.</li></ol><p><b>The Four Golden Signals are dashboards-per-service.</b> Every user-facing service should have a dashboard with these four signals prominently displayed.</p><p><b>USE method (Brendan Gregg — for resources):</b> Applied to system-level resources (CPU, memory, disk, network).</p><ul><li><b>Utilization:</b> Average time the resource was busy servicing work (percentage).</li><li><b>Saturation:</b> Degree to which the resource has extra work it can't service (usually queue length or waiting time).</li><li><b>Errors:</b> Count of error events on the resource.</li></ul><p>USE is checklist-oriented: for every hardware resource, ask about all three. Iterate through CPU, memory, disks, network interfaces, storage controllers.</p><p><b>RED method (Tom Wilkie — for services):</b> A simpler, service-centric variant of the Four Golden Signals.</p><ul><li><b>Rate:</b> Requests per second.</li><li><b>Errors:</b> Error rate.</li><li><b>Duration:</b> Distribution of request latencies.</li></ul><p>RED is a subset of the Four Golden Signals (omits saturation). Its virtue is simplicity — three signals apply universally to any request/response service and can be extracted from a load balancer or reverse proxy without cooperation from the service itself.</p><p><b>When to use each:</b></p><ul><li><b>Four Golden Signals:</b> The default for user-facing services. Best balance of completeness and simplicity.</li><li><b>USE method:</b> When investigating resource-level issues (a CPU-bound host, a saturated disk).</li><li><b>RED method:</b> When you have limited visibility into services and need a baseline. Also excellent for infrastructure-level dashboards (e.g., a fleet of Ingress controllers).</li></ul><p><b>SLIs, SLOs, SLAs:</b> These frameworks feed into higher-level SRE constructs.</p><ul><li><b>SLI (Service Level Indicator):</b> A quantitative measure of some aspect of service quality (e.g., \"proportion of requests that succeed within 300ms\").</li><li><b>SLO (Service Level Objective):</b> A target for SLIs (e.g., \"99.9% over 30 days\").</li><li><b>SLA (Service Level Agreement):</b> A contractual promise to customers, typically looser than the internal SLO to provide margin.</li></ul><p>A mature observability practice defines SLIs derived from Golden Signals or RED metrics, tracks SLO compliance over rolling windows, and uses <b>error budgets</b> (100% minus SLO) to manage the trade-off between reliability and feature velocity.</p>",
      },
      {
        q: "What is distributed tracing and how does it work?",
        difficulty: "medium",
        a: "<p><b>Distributed tracing</b> is the observability technique that follows a request as it flows through multiple services in a distributed system. It answers questions that metrics and logs alone cannot: \"Which service is slow?\" \"Where in this 20-service call chain does the failure originate?\" \"What was the code path for this specific request?\"</p><p><b>Core concepts:</b></p><ul><li><b>Trace:</b> A record of a single request's journey through the system. Composed of many spans.</li><li><b>Span:</b> A single operation within a trace — an HTTP call, a database query, a function invocation. Each span has a start time, end time, name, and attributes.</li><li><b>Parent-child relationships:</b> A span that triggers other spans is their parent. This creates a tree of spans within a trace, showing causal relationships.</li><li><b>Context propagation:</b> The mechanism by which a trace ID and span ID flow from one service to another, typically via HTTP headers (<code>traceparent</code>, <code>tracestate</code> per W3C Trace Context).</li></ul><p><b>How a trace is constructed:</b></p><ol><li>Service A receives a request. It has no incoming trace context, so it generates a new trace ID and creates a root span.</li><li>Service A calls Service B. It attaches <code>traceparent</code> HTTP header containing the trace ID and its own span ID.</li><li>Service B extracts the trace context, creates a new span (child of A's span), does its work, and calls Service C similarly.</li><li>Each service exports its spans to the tracing backend (Jaeger, Zipkin, Tempo, Datadog APM, etc.).</li><li>The backend assembles all spans with the same trace ID into a single trace, displaying it as a Gantt chart of span durations.</li></ol><p><b>Practical trace visualization:</b> A trace in Jaeger or Tempo shows something like:</p><pre>Trace: 6f4a2e...\n[========== HTTP GET /checkout (450ms) ==========]\n  [== validate cart (10ms) ==]\n  [======= call payment-svc (250ms) ==============]\n    [==== stripe API (200ms) ====]\n  [== update order-svc (100ms) ==]\n    [== database insert (80ms) ==]\n  [== call notification-svc (50ms) ==]</pre><p>This immediately shows that the payment service dominates latency, and that the Stripe API call is the specific bottleneck within it.</p><p><b>Sampling:</b> Tracing every request is expensive. Production systems typically sample:</p><ul><li><b>Head-based sampling:</b> Decision made at the start of the trace. Simple (e.g., \"trace 1% of requests\") but may miss rare error traces.</li><li><b>Tail-based sampling:</b> Decision made after the trace completes. Can prioritize traces with errors or slow durations. Requires buffering, typically done by the OTel Collector.</li></ul><p><b>OpenTelemetry as the standard:</b> Distributed tracing was historically fragmented across OpenTracing, OpenCensus, and vendor-specific libraries. OpenTelemetry has consolidated the field — nearly all modern tracing backends accept OTel format.</p><p><b>Common tracing backends:</b></p><ul><li>Grafana Tempo (open source, cheap object-storage backing).</li><li>Jaeger (CNCF graduated project, self-hosted).</li><li>Zipkin (original distributed tracing system, still viable).</li><li>Datadog APM, Honeycomb, New Relic, Dynatrace, Chronosphere (commercial SaaS).</li><li>AWS X-Ray, Google Cloud Trace, Azure Application Insights (cloud-native).</li></ul>",
      },
      {
        q: "How do you deal with alert fatigue?",
        difficulty: "medium",
        a: "<p><b>Alert fatigue</b> is the phenomenon where responders become desensitized to alerts because there are too many, too many are false positives, or too many are non-actionable. Alert-fatigued teams miss real problems, respond slowly, or ignore alerts entirely. It is one of the most common failure modes in observability practices.</p><p><b>Symptoms of alert fatigue:</b></p><ul><li>Engineers routinely acknowledge and dismiss alerts without investigating.</li><li>The on-call rotation is dreaded because pages come every night.</li><li>Real incidents are missed because they were buried in noise.</li><li>Alert channels (Slack, email) are muted or filtered out.</li><li>Trust in the monitoring system erodes.</li></ul><p><b>Root causes and remediation:</b></p><ol><li><b>Alerts fire on symptoms that aren't actually user-impacting.</b> Example: alerting on CPU &gt; 80% when the service is meeting its SLOs.<br><b>Fix:</b> Alert on SLI violations (user-visible impact) rather than system metrics. SLO-based alerting is the modern best practice — page only when error budget is being consumed faster than sustainable.</li><li><b>Alerts fire on transient issues that self-resolve.</b><br><b>Fix:</b> Add hysteresis — require the condition to persist for N minutes before alerting. Use <code>for: 5m</code> in Prometheus, or equivalent in other tools.</li><li><b>Multiple alerts fire for the same underlying incident.</b><br><b>Fix:</b> Alert routing and grouping — Alertmanager and PagerDuty group related alerts. Use dependencies: don't page when a downstream service is down because an upstream service (with its own alert) is down.</li><li><b>Alerts are non-actionable — the responder doesn't know what to do.</b><br><b>Fix:</b> Every alert must include a runbook link and a clear \"what to do\" in the description. If no action is possible, it shouldn't be an alert — it should be a dashboard.</li><li><b>Historical alerts are never reviewed or cleaned up.</b> Alerts accumulate over years, and no one prunes.<br><b>Fix:</b> Regularly review alert rules. If an alert has fired 100 times in 6 months and every response was \"no action needed\", delete it.</li></ol><p><b>The SLO-based alerting model (from Google SRE Book):</b></p><ul><li>Define SLIs (e.g., proportion of requests succeeding within latency budget).</li><li>Define SLOs (e.g., 99.9% over 30 days).</li><li>Calculate error budget = 100% − SLO.</li><li>Alert only when the current burn rate would exhaust the error budget too quickly.</li><li>Use multi-window, multi-burn-rate alerts: fast burn (short window, high threshold) for immediate issues; slow burn (long window, low threshold) for gradual degradation.</li></ul><pre># Fast burn: 2% budget consumed in 1 hour → page immediately\n- alert: HighErrorBudgetBurnRate\n  expr: |\n    (\n      sum(rate(http_requests_total{status=~\"5..\"}[1h])) /\n      sum(rate(http_requests_total[1h]))\n    ) &gt; (14.4 * 0.001)  # 14.4x sustainable burn rate\n  for: 2m\n  labels:\n    severity: page\n  annotations:\n    runbook_url: https://runbooks/high-error-rate</pre><p><b>Alert severity tiers:</b></p><ul><li><b>Page:</b> Wakes the on-call at 3 AM. Must be user-impacting, actionable, and urgent.</li><li><b>Ticket:</b> Creates a ticket for review during business hours. Non-urgent issues that still need investigation.</li><li><b>Info / dashboard:</b> Doesn't notify anyone. Available on demand.</li></ul><p><b>Cultural practice — the alert retrospective:</b> After every page, ask: was this actionable? Was the runbook helpful? Could this have been prevented? Compile lessons learned and modify alerts and code accordingly. Good on-call is a continuous improvement discipline, not just responsive firefighting.</p>",
      },
      {
        q: "What is eBPF-based observability (Cilium Hubble, Pixie)?",
        difficulty: "hard",
        a: "<p>A new generation of observability tools built on <b>eBPF</b> is transforming how DevOps teams gain visibility into applications running in Kubernetes. These tools instrument applications automatically from the kernel level — no code changes, no sidecars, no service mesh required. They are particularly valuable in environments where traditional instrumentation is impractical.</p><p><b>The core insight:</b> Every application-level operation eventually results in a system call (send/recv/read/write) that the kernel can observe. eBPF programs can hook these syscalls and reconstruct application-level protocols (HTTP, gRPC, MySQL, PostgreSQL, Kafka, Redis) from wire-level traffic. This provides deep visibility with essentially zero performance overhead and no application changes.</p><p><b>Major eBPF-based observability tools:</b></p><ul><li><b>Cilium Hubble:</b> The observability layer for Cilium (a CNI that uses eBPF). Provides real-time visibility into service-to-service traffic — Layer 3/4 (IP, TCP, UDP) and Layer 7 (HTTP, gRPC, DNS, Kafka). Shows a service map of every flow in the cluster.</li><li><b>Pixie (now open source, was Pixie Labs → New Relic):</b> Auto-instruments Kubernetes applications for HTTP/gRPC latency, DNS resolution, database queries, CPU flame graphs, and more — all via eBPF. No agents to install per-application; a single DaemonSet observes everything.</li><li><b>Parca / Pyroscope:</b> Continuous profiling of running applications, producing CPU flame graphs for the entire cluster. Identifies performance hotspots that would require manual profiling otherwise.</li><li><b>Grafana Beyla:</b> Auto-instruments Go, Rust, Java, and Node.js applications to produce OpenTelemetry-compatible traces and metrics without code changes. A modern alternative to OTel auto-instrumentation.</li><li><b>Coroot:</b> Full-stack observability platform using eBPF for metrics, traces, and logs.</li><li><b>Tetragon (Isovalent):</b> Runtime security observability, focused on detecting suspicious kernel-level events.</li></ul><p><b>Advantages of eBPF observability:</b></p><ol><li><b>Zero application changes.</b> No SDK to integrate, no library to import, no code to modify. This is especially valuable for legacy applications, off-the-shelf third-party services, and polyglot environments.</li><li><b>Language-agnostic.</b> Works equally well for Go, Java, Python, Node.js, Rust, C++, and beyond. Instrumentation happens below the language runtime.</li><li><b>Minimal performance overhead.</b> Well-written eBPF programs consume microseconds per event and run in the kernel — much cheaper than typical instrumentation.</li><li><b>Complete coverage.</b> No blind spots from missing instrumentation. Every syscall is observable.</li><li><b>Real-time visibility.</b> eBPF operates continuously, so data is available immediately for diagnosis.</li></ol><p><b>Limitations and trade-offs:</b></p><ul><li><b>Requires modern kernels.</b> eBPF programs generally need Linux 5.x+ kernels. This is rarely a problem on modern managed Kubernetes but can block on-prem or older systems.</li><li><b>TLS encryption is a challenge.</b> HTTPS traffic is encrypted at the syscall level. Tools use various techniques (uprobes into TLS libraries, or requiring userspace hooks) to observe encrypted traffic — with varying success across languages.</li><li><b>Application context is limited.</b> eBPF sees wire-level protocol semantics but not business context. Traditional application-level instrumentation still provides richer, business-aware telemetry.</li><li><b>Newer tooling ecosystem.</b> Some tools are still maturing compared to established players like Datadog and Prometheus.</li></ul><p><b>Practical adoption pattern:</b> Many organizations combine both approaches. eBPF observability provides baseline coverage across all services with zero effort; targeted OpenTelemetry instrumentation adds business-level context to critical services where it justifies the effort.</p>",
      },
      {
        q: "How would you correlate logs, metrics, and traces?",
        difficulty: "hard",
        a: "<p>Correlating the three pillars of observability — logs, metrics, and traces — is what transforms them from independent data sources into a coherent debugging environment. The key is a <b>shared identifier</b> that ties observations of the same request or operation across all three data types.</p><p><b>The shared identifier:</b> The standard practice is to use a <b>trace ID</b> generated by an OpenTelemetry-compatible tracing system, and to propagate that trace ID everywhere:</p><ul><li><b>Into every log line:</b> When a service handles a request, it extracts the trace ID from the incoming context and includes it as a structured field (<code>trace_id=abc123...</code>) on every log message emitted during that request.</li><li><b>Onto metrics via exemplars:</b> Prometheus supports \"exemplars\" — links from a histogram bucket to a specific trace ID that contributed a sample to that bucket. This allows jumping from a metric spike directly to an example slow request.</li><li><b>Across service boundaries:</b> When one service calls another, the trace ID is propagated as an HTTP header (W3C Trace Context standard: <code>traceparent</code>). The downstream service continues the trace under the same ID.</li></ul><p><b>Practical debugging workflow enabled by correlation:</b></p><ol><li>An alert fires — the 99th-percentile latency has spiked.</li><li>Open the metric in Grafana. The dashboard shows exemplars — small dots on the latency graph representing specific slow traces.</li><li>Click an exemplar dot. Grafana opens the corresponding distributed trace in Tempo, showing the timeline of the slow request across all services it touched.</li><li>Identify the slow span. Click a link from that span to Loki, which displays all log messages with the same trace ID from the affected service.</li><li>Read the logs to identify the root cause — a slow database query, a rate-limited external API, a deadlock, or an unexpected error.</li></ol><p><b>Tooling that makes this seamless:</b> Grafana's tight integration with Loki (logs), Prometheus (metrics), and Tempo (traces) enables this cross-signal navigation natively. Datadog, New Relic, and Dynatrace provide similar correlation in their commercial suites.</p><p><b>Prerequisites:</b> Achieving this workflow requires investment in consistent instrumentation. Every service must be OpenTelemetry-instrumented; every log format must include structured fields; propagation headers must be forwarded correctly through every service boundary. The payoff is dramatic reduction in mean time to resolution for complex incidents.</p>",
      },
    ],
  },

  /* ============================================================ */
  {
    id: "networking",
    name: "Networking & Shell",
    icon: "🌐",
    kind: "qa",
    description:
      "DNS, TCP, load balancing, and the shell scripting fundamentals that keep infrastructure glued together.",
    items: [
      {
        q: "Walk me through what happens when you type example.com into a browser.",
        difficulty: "medium",
        a: "<p>The apparent simplicity of typing a URL and seeing a page belies a complex sequence of network operations spanning DNS, transport, security, and application layers. A complete answer covers each stage.</p><ol><li><b>DNS resolution:</b> The browser needs to convert <code>example.com</code> into an IP address. Lookup proceeds through a cache hierarchy:<ul><li>Browser DNS cache.</li><li>Operating system's resolver cache.</li><li>Recursive resolver (typically the ISP, or a public resolver like 8.8.8.8 or 1.1.1.1).</li><li>If not cached, the recursive resolver queries the DNS hierarchy: root nameservers → TLD nameservers (<code>.com</code>) → authoritative nameservers for <code>example.com</code>.</li><li>The authoritative nameserver returns the A record (IPv4) or AAAA record (IPv6).</li></ul></li><li><b>TCP connection establishment:</b> The browser opens a TCP connection to the returned IP address on port 443 (HTTPS). This requires a three-way handshake: SYN → SYN-ACK → ACK.</li><li><b>TLS handshake:</b> Over the newly established TCP connection, the browser and server perform a TLS handshake:<ul><li>Negotiate the TLS version and cipher suite.</li><li>Exchange keying material (typically via Diffie-Hellman for forward secrecy).</li><li>The server presents its certificate; the browser validates the certificate chain up to a trusted root CA.</li><li>Verify the certificate matches the requested hostname (SNI).</li></ul>TLS 1.3 combines these steps for faster setup.</li><li><b>HTTP request:</b> The browser sends an HTTP request over the encrypted TLS connection — the request line (<code>GET / HTTP/2</code>), headers (Host, User-Agent, Accept, Cookie, etc.), and optional body.</li><li><b>Request routing:</b> The request typically flows through:<ul><li>A <b>CDN edge server</b> that may serve cached content directly.</li><li>A <b>load balancer</b> at the origin that distributes traffic across application instances.</li><li>An <b>application server</b> that processes the request.</li></ul></li><li><b>Server-side processing:</b> The application may query databases, caches, or other backend services to construct the response.</li><li><b>Response delivery:</b> The response is streamed back over the same TLS connection.</li><li><b>Browser rendering:</b> The browser parses the HTML, discovers linked resources (CSS, JavaScript, images), requests them (often reusing the same TCP/TLS connection via HTTP/2 or HTTP/3 multiplexing), builds the DOM and CSSOM, executes JavaScript, and paints the page.</li></ol><p><b>Common interview follow-ups</b> drill into any one of these stages — DNS caching, TLS versions, HTTP/2 vs HTTP/3, or how a CDN reduces latency.</p>",
        followups: [
          {
            q: "Difference between A record, CNAME, and ALIAS?",
            a: "<p>These are DNS record types used to map domain names to destinations, each with distinct behavior and constraints.</p><ul><li><b>A record:</b> Maps a domain name directly to an IPv4 address. Example: <code>example.com. A 93.184.216.34</code>. This is the most direct form of mapping — a single DNS lookup returns the target IP. Fast and unambiguous.</li><li><b>AAAA record:</b> The IPv6 equivalent of an A record — maps a name to an IPv6 address. Example: <code>example.com. AAAA 2606:2800:220:1:248:1893:25c8:1946</code>.</li><li><b>CNAME (Canonical Name) record:</b> Maps a domain name to another domain name rather than to an IP address. When a resolver looks up a name with a CNAME, it must perform an additional lookup on the target name. Example: <code>www.example.com. CNAME example.com.</code> A key constraint: <b>CNAME cannot be used at the zone apex</b> (the bare domain, e.g., <code>example.com</code>) — the DNS specification prohibits mixing a CNAME with other record types at the same name, and the apex requires SOA and NS records.</li><li><b>ALIAS / ANAME record:</b> A provider-specific record type that behaves like a CNAME but is legal at the zone apex. When queried, the DNS provider resolves the target internally and returns A/AAAA records to the client, so the apex constraint is not violated. Examples: <b>AWS Route 53 Alias records</b>, <b>Cloudflare CNAME flattening</b>, <b>DNSimple ALIAS</b>. These are essential for using cloud load balancers (which are addressed by DNS name rather than static IP) at the root of your domain.</li></ul>",
          },
          {
            q: "How does DNS caching work and what does TTL control?",
            a: "<p>Every DNS record includes a <b>Time-To-Live (TTL)</b> value — an integer number of seconds indicating how long resolvers are permitted to cache the response. TTL is the primary knob that controls the trade-off between DNS query load and change propagation speed.</p><p><b>Caching hierarchy:</b> DNS responses are cached at multiple layers:</p><ul><li>Application (e.g., browser DNS cache).</li><li>Operating system stub resolver.</li><li>Recursive resolver (ISP, corporate, or public).</li></ul><p>Each layer honors the TTL and only re-queries after the TTL expires. Most DNS traffic is answered from cache; only cache misses generate authoritative queries.</p><p><b>TTL trade-offs:</b></p><ul><li><b>Short TTL (e.g., 60 seconds):</b> Changes propagate quickly worldwide (roughly within one minute). Downside: many more DNS queries reach authoritative servers, increasing DNS traffic and load.</li><li><b>Long TTL (e.g., 86400 seconds / 24 hours):</b> Very low DNS query volume. Downside: DNS changes take up to a day to fully propagate; problematic for migrations, failovers, or emergency IP changes.</li></ul><p><b>Migration pattern:</b> Before a planned DNS change, temporarily lower the TTL well in advance — for example, lower from 24 hours to 60 seconds a week before the migration. Wait for the old long TTL to age out of all caches (24 hours in this case). Perform the migration; DNS changes now propagate quickly. After the migration is stable, raise the TTL back up to reduce ongoing query load.</p><p><b>Provider behavior:</b> Some resolvers may cap TTLs (both minimum and maximum) for operational reasons, so extreme values may not have the intended effect universally.</p>",
          },
        ],
      },
      {
        q: "TCP vs UDP — when to use each?",
        difficulty: "easy",
        a: "<p>TCP and UDP are the two most common transport-layer protocols. They share the same IP layer beneath them but differ significantly in the guarantees they provide.</p><p><b>TCP (Transmission Control Protocol):</b></p><ul><li><b>Connection-oriented:</b> Uses a three-way handshake to establish a connection before data can flow.</li><li><b>Reliable:</b> Guarantees delivery of every byte. Lost packets are automatically retransmitted.</li><li><b>Ordered:</b> Data arrives in the same order it was sent.</li><li><b>Flow control:</b> Adapts to receiver capacity to prevent overwhelming the destination.</li><li><b>Congestion control:</b> Adapts to network conditions to avoid contributing to congestion.</li><li><b>Higher overhead:</b> Handshake latency, header size (~20 bytes), and the state maintained on both ends.</li></ul><p><b>UDP (User Datagram Protocol):</b></p><ul><li><b>Connectionless:</b> No handshake — packets are simply sent.</li><li><b>Unreliable:</b> Packets may be lost, duplicated, or arrive out of order. The application handles reliability if it needs it.</li><li><b>Low overhead:</b> 8-byte header, minimal per-packet processing, no connection state.</li><li><b>No flow or congestion control:</b> The application is responsible if these are needed.</li></ul><p><b>When to use TCP:</b> Any workload where lost or reordered data is unacceptable — HTTP/1 and HTTP/2, SSH, database connections (PostgreSQL, MySQL, Redis), file transfers, email (SMTP, IMAP).</p><p><b>When to use UDP:</b> Latency-sensitive workloads where occasional loss is acceptable or the application handles reliability itself — DNS (small queries, fast responses), VoIP and video streaming (a lost frame is preferable to buffered stalling), online gaming, DHCP, and modern protocols like QUIC that build reliability into userspace.</p>",
        followups: [
          {
            q: "What is HTTP/3 and why is it built on UDP?",
            a: "<p><b>HTTP/3</b> is the latest major version of the HTTP protocol, standardized in 2022. It runs on top of <b>QUIC</b> (Quick UDP Internet Connections), a transport protocol that runs over UDP rather than TCP. This design decision addresses several long-standing limitations of TCP-based HTTP.</p><p><b>Why not TCP?</b> HTTP/2 introduced multiplexing — many logical streams over one connection. However, because HTTP/2 rides on TCP, the loss of a single TCP packet blocks <i>all</i> streams until the retransmission arrives, since TCP guarantees in-order byte delivery. This is called <b>head-of-line blocking at the transport layer</b>. Attempting to fix this within TCP is not feasible because TCP is implemented in operating system kernels and evolves slowly.</p><p><b>QUIC's approach:</b> QUIC implements its own reliability, congestion control, and stream multiplexing on top of UDP. Because streams are managed at the QUIC layer (rather than delegated to TCP), a lost packet only blocks its own stream — other streams continue independently. Key features:</p><ul><li><b>Elimination of transport-layer head-of-line blocking.</b></li><li><b>Integrated TLS 1.3 handshake:</b> The connection setup and encryption are combined, reducing round trips.</li><li><b>0-RTT resumption:</b> Repeat connections can send application data on the very first packet.</li><li><b>Faster connection establishment:</b> A new QUIC connection can be established in a single round trip; combined TCP+TLS traditionally requires two or three.</li><li><b>Connection migration:</b> QUIC connections are identified by a connection ID rather than by IP+port, allowing them to survive changes in the underlying network (e.g., switching from WiFi to cellular).</li><li><b>Userspace implementation:</b> QUIC lives in application space, not the kernel — so improvements can be deployed with the application rather than requiring OS updates.</li></ul><p><b>Trade-offs:</b> QUIC has higher CPU cost than TCP (encryption of every packet, no kernel offload), and some middleboxes and firewalls treat UDP more restrictively than TCP. Adoption is growing but is not yet universal.</p>",
          },
        ],
      },
      {
        q: "Write a shell script that rotates old log files.",
        difficulty: "medium",
        a: "<p>A robust log rotation script compresses recent-but-old logs, deletes very old compressed logs, and uses safe scripting practices. The following implementation follows those principles:</p><pre>#!/usr/bin/env bash\nset -euo pipefail\n\nLOG_DIR=\"${1:-/var/log/myapp}\"\nRETAIN_DAYS=\"${2:-14}\"\nDELETE_AFTER_DAYS=\"${3:-90}\"\n\n# Compress .log files older than RETAIN_DAYS days\nfind \"$LOG_DIR\" -type f -name '*.log' -mtime +\"$RETAIN_DAYS\" -print0 |\n  xargs -0 -r gzip -9\n\n# Delete .gz files older than DELETE_AFTER_DAYS days\nfind \"$LOG_DIR\" -type f -name '*.gz' -mtime +\"$DELETE_AFTER_DAYS\" -delete\n\necho \"Rotation complete at $(date -Iseconds)\"</pre><p><b>Design notes:</b></p><ul><li><b>Configurable via arguments</b> with sensible defaults using the <code>${var:-default}</code> parameter expansion syntax. The script works out of the box and can be customized without editing.</li><li><b>Safety flags via <code>set -euo pipefail</code>:</b> Exit on any error, error on unset variables, and propagate pipeline failures. Standard practice for any production shell script.</li><li><b>Null-delimited find/xargs pipeline (<code>-print0</code> / <code>-0</code>):</b> Handles filenames containing spaces, newlines, or unusual characters safely. Never parse <code>find</code> output through a plain shell loop or unquoted expansion.</li><li><b>xargs <code>-r</code> flag:</b> Prevents xargs from running gzip when there is no input, avoiding a spurious \"missing operand\" error.</li><li><b>gzip <code>-9</code>:</b> Maximum compression. Log files typically compress well and are read infrequently, so trading CPU for smaller storage is usually worthwhile.</li><li><b>ISO-8601 timestamp in the log message:</b> Machine-parseable and unambiguous timezone-wise.</li></ul><p><b>Production considerations:</b> For most real systems, prefer the system's <code>logrotate</code> daemon or systemd's built-in journal rotation over custom scripts — these tools handle edge cases like open file descriptors, signals to running processes, and rotation frequency more robustly.</p>",
        followups: [
          {
            q: "What does `set -euo pipefail` do?",
            a: "<p><code>set -euo pipefail</code> enables three Bash options that dramatically improve script safety. Each addresses a different class of common shell-scripting bug.</p><ul><li><b>-e (errexit):</b> Exit the script immediately if any command exits with a non-zero status. Without this, a script continues executing after a command fails, potentially producing incorrect or corrupt results. Exceptions: commands in conditions (<code>if</code>, <code>while</code>), commands joined with <code>||</code> or <code>&amp;&amp;</code>, and commands followed by <code>||</code> for explicit handling.</li><li><b>-u (nounset):</b> Treat references to unset variables as errors. Without this, a typo like <code>$USR</code> instead of <code>$USER</code> silently expands to an empty string, producing puzzling behavior — for example, <code>rm -rf /$FOO/dir</code> becomes <code>rm -rf /dir</code> if FOO is unset.</li><li><b>-o pipefail:</b> Make a pipeline's exit status the last non-zero exit status among its commands, or zero if all succeeded. By default, a pipeline's exit status is only the last command's status. Consider <code>cat missing.txt | wc -l</code> — without pipefail, this succeeds (wc succeeds with 0 lines) even though cat failed.</li></ul><p><b>Combined effect:</b> Bash without these flags is a \"keeps going forever\" shell — errors are ignored, typos are silent, and pipeline failures are masked. Enabling these flags brings Bash close to the safety expectations of a modern programming language.</p><p><b>Recommendation:</b> Treat <code>set -euo pipefail</code> at the top of every non-trivial shell script as mandatory. For maximum robustness, some engineers also add <code>IFS=$'\\n\\t'</code> to prevent word-splitting surprises.</p>",
          },
        ],
      },
      {
        q: "How would you debug a service that's slow only for some users?",
        difficulty: "hard",
        a: "<p>Debugging a partial performance problem — one that affects only a subset of users — requires systematically narrowing down what distinguishes the affected population from the healthy population. This is fundamentally different from a total outage and requires observability data that can be sliced by user attributes.</p><p><b>Step-by-step investigation approach:</b></p><ol><li><b>Segment the affected population.</b> Use the APM or observability platform to slice request-latency metrics by:<ul><li>Geographic region or country.</li><li>ISP or ASN.</li><li>Device type or operating system.</li><li>Browser and browser version.</li><li>User account tier or feature flag exposure.</li></ul>Identify what characterizes the slow users. A concentrated geographic pattern suggests a networking or CDN issue; a device-specific pattern suggests a client-side or codec issue.</li><li><b>Check DNS resolution per region.</b> DNS provider issues can silently degrade performance in specific regions. Use RIPE Atlas or a synthetic monitoring service to verify authoritative DNS response times from multiple geographic vantage points.</li><li><b>Verify CDN edge health.</b> If the affected users share a geographic region, the CDN edge serving that region may be misbehaving. Check CDN-specific metrics for cache hit rate, edge origin latency, and error rate per POP. Many CDN outages affect only certain edge locations.</li><li><b>Compare distributed traces between fast and slow requests.</b> Pull traces from both populations and look for differences:<ul><li>Are the slow traces spending more time in a specific service?</li><li>Is a database query showing higher latency for certain query parameters?</li><li>Are external API calls slower for certain callers?</li></ul></li><li><b>Investigate network-path issues.</b> Consider:<ul><li><b>MTU / PMTU black-holing:</b> Some networks incorrectly drop ICMP fragmentation-needed messages, causing packets larger than the effective MTU to disappear silently. Symptom: connections work fine for small requests but hang on larger transfers.</li><li><b>BGP routing changes</b> that route certain ISPs' traffic through unexpectedly long or congested paths.</li><li><b>IPv4 vs IPv6 asymmetry</b> — one address family may have significantly worse performance.</li></ul></li><li><b>Check TLS-related timing.</b> An expired intermediate certificate, an OCSP responder outage, or a certificate chain that requires additional roundtrips can affect certain clients. Symptoms include long \"initial connection\" phases in browser network panels.</li><li><b>Correlate with recent changes.</b> Overlay recent deployments and feature flag rollouts on the timeline of the degradation. A partial rollout to specific users (e.g., 10% canary or region-scoped rollout) is a common cause of slice-specific slowness.</li><li><b>Cache and data locality.</b> If the affected users are in a region with a smaller cache warmth, their requests may be hitting cold caches more often. Similarly, cross-region reads to a primary database from a distant region can add significant latency.</li></ol><p><b>Debugging discipline:</b> Form a hypothesis about what distinguishes the affected users, test it with data, and iterate. Do not attempt to \"guess and check\" fixes — partial problems can waste substantial time if approached without a hypothesis-driven method.</p>",
      },
      {
        q: "What is a service mesh and how does Istio (or Linkerd) work?",
        difficulty: "hard",
        a: "<p>A <b>service mesh</b> is a dedicated infrastructure layer that transparently handles service-to-service communication in a distributed system — providing traffic management, security, and observability without requiring changes to application code. Service meshes have become a standard component of production Kubernetes at scale.</p><p><b>The problem service meshes solve:</b> In a microservice architecture, dozens or hundreds of services communicate over the network. Every service needs to implement: retries and timeouts, mutual TLS for encryption, distributed tracing propagation, circuit breaking, rate limiting, and traffic routing (canary, blue-green). Reimplementing these in every service and every language is impractical. Service meshes centralize this into infrastructure.</p><p><b>Architecture — sidecar pattern:</b> The most common service mesh model injects a lightweight proxy (a \"sidecar\") into every application pod. The application's outbound traffic transparently flows through the sidecar, which handles all the mesh functionality.</p><pre>Pod\n├── application container\n└── sidecar proxy (Envoy for Istio, Linkerd2-proxy for Linkerd)\n\nApplication ↔ sidecar ↔ network ↔ sidecar ↔ Application</pre><p><b>Istio</b> — the most feature-rich service mesh:</p><ul><li><b>Data plane:</b> Envoy proxies as sidecars in every pod (or as ambient waypoints with the newer sidecar-less architecture).</li><li><b>Control plane:</b> <code>istiod</code> — configures all Envoy proxies with the desired routing, security, and telemetry policies.</li><li><b>Rich traffic management:</b> Weighted routing, header-based routing, mirroring, fault injection, retries with jitter, circuit breaking, outlier detection.</li><li><b>Security:</b> Automatic mTLS between all services (traffic is encrypted and mutually authenticated by default), fine-grained authorization policies.</li><li><b>Observability:</b> Automatic metrics, traces, and access logs for every service-to-service call.</li></ul><p><b>Linkerd</b> — the simpler alternative:</p><ul><li>Custom Rust-based lightweight proxy (Linkerd2-proxy), much smaller and faster than Envoy.</li><li>Focuses on core features: mTLS, retries, observability, canary via traffic shifting.</li><li>Notably easier to install, operate, and understand than Istio.</li><li>Backed by Buoyant, CNCF graduated project.</li></ul><p><b>Example — Istio traffic shifting for a canary release:</b></p><pre>apiVersion: networking.istio.io/v1beta1\nkind: VirtualService\nmetadata:\n  name: my-app\nspec:\n  hosts: [my-app.example.com]\n  http:\n    - route:\n        - destination:\n            host: my-app\n            subset: v1\n          weight: 90\n        - destination:\n            host: my-app\n            subset: v2\n          weight: 10   # 10% canary to v2</pre><p><b>Trade-offs of adopting a service mesh:</b></p><ul><li><b>Complexity:</b> A service mesh adds operational overhead. Sidecars consume CPU/memory. Debugging is harder because traffic flows through more hops. Istio in particular has a substantial learning curve.</li><li><b>Latency:</b> Every hop adds a few milliseconds. Usually negligible but can matter for latency-sensitive workloads.</li><li><b>Value proposition:</b> Worth it when you have 20+ microservices needing consistent security, retry, and observability policies. Overkill for small deployments.</li></ul><p><b>Alternatives and evolution:</b></p><ul><li><b>Ambient mesh (Istio):</b> A newer sidecar-less architecture where mesh functionality lives on shared per-node proxies (ztunnel) plus optional per-service waypoints. Reduces the sidecar footprint significantly.</li><li><b>Cilium Service Mesh:</b> Uses eBPF to provide mesh functionality without sidecars. Ties mesh directly to CNI.</li><li><b>Consul Connect:</b> HashiCorp's service mesh, tightly integrated with Consul service discovery.</li><li><b>AWS App Mesh, Azure Service Fabric Mesh:</b> Cloud-native offerings.</li></ul>",
      },
      {
        q: "Explain how Kubernetes networking works (CNI plugins).",
        difficulty: "medium",
        a: "<p>Kubernetes networking is a common area of confusion because Kubernetes itself does not implement networking — it defines requirements and delegates the actual implementation to a <b>Container Network Interface (CNI) plugin</b>. Different CNI plugins produce different network behaviors, and the choice matters significantly for production clusters.</p><p><b>Kubernetes networking requirements (from the spec):</b></p><ol><li>Every pod gets its own IP address.</li><li>Pods on the same node can communicate with all other pods on the cluster without NAT.</li><li>Nodes can communicate with pods (and vice versa) without NAT.</li><li>The IP a pod sees itself as is the same IP other pods see it as.</li></ol><p>This gives Kubernetes its \"flat network\" model — no port mapping between the pod's port and the node's port. Every pod is a first-class network endpoint.</p><p><b>What a CNI plugin does:</b> When a pod is scheduled to a node, kubelet calls the CNI plugin to:</p><ol><li>Assign the pod an IP address.</li><li>Create the necessary network interfaces (typically a virtual ethernet pair — one end in the pod's network namespace, the other on the host).</li><li>Set up routing so the pod can reach other pods and services.</li><li>Optionally, apply NetworkPolicies (firewall rules).</li></ol><p><b>Common CNI plugins:</b></p><ul><li><b>Calico:</b> The most widely deployed CNI in production. Uses BGP for routing between nodes; can operate in overlay mode (VXLAN/IPIP) or direct-routing mode. Strong NetworkPolicy support (Calico-specific extensions plus standard Kubernetes NetworkPolicy). Now offers an eBPF data plane for higher performance.</li><li><b>Cilium:</b> The rising star. Uses eBPF for routing, load balancing, and policy enforcement — no iptables. Provides Layer 7 policy (HTTP, gRPC, Kafka), integrated observability (Hubble), and service mesh capabilities. Increasingly the default in managed Kubernetes.</li><li><b>Flannel:</b> Simple overlay network using VXLAN. Very easy to set up. No NetworkPolicy support out of the box. Good for learning and simple deployments.</li><li><b>Weave Net:</b> Encrypted mesh network. Simple but less commonly chosen for large-scale deployments now.</li><li><b>AWS VPC CNI:</b> The default for EKS. Assigns real VPC IPs to pods, making them first-class VPC citizens with Security Group support.</li><li><b>Azure CNI:</b> Similar model for AKS.</li><li><b>Google Cloud (GKE):</b> Uses a customized CNI internally with VPC-native networking.</li></ul><p><b>Overlay vs underlay networking:</b></p><ul><li><b>Overlay:</b> Pod traffic is encapsulated (VXLAN, IPIP) inside packets that traverse the physical network. Simple to deploy; works on any underlying network. Small overhead from encapsulation.</li><li><b>Underlay / direct routing:</b> Pods share IP space with the underlying network, and physical infrastructure routes pod IPs directly (via BGP or cloud provider integration). Lower overhead but requires infrastructure cooperation.</li></ul><p><b>NetworkPolicy:</b> Kubernetes provides a standard NetworkPolicy resource for firewall-like traffic restrictions:</p><pre>apiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-frontend\n  namespace: prod\nspec:\n  podSelector:\n    matchLabels:\n      app: api\n  ingress:\n    - from:\n        - podSelector:\n            matchLabels:\n              app: frontend\n      ports:\n        - protocol: TCP\n          port: 8080</pre><p>NetworkPolicies are enforced by the CNI plugin. Not all CNIs support them — for example, Flannel does not, so combining Flannel with Calico's policy-only mode is a common pattern.</p><p><b>Choosing a CNI:</b></p><ul><li>Small clusters, learning environments: Flannel is simplest.</li><li>Production, self-managed clusters: Calico or Cilium.</li><li>Managed Kubernetes: Use the cloud provider's default unless you have a strong reason to replace it.</li><li>Modern greenfield deployments: Cilium is increasingly the preferred choice for its performance and eBPF-based feature set.</li></ul>",
      },
      {
        q: "How does Cilium and eBPF-based networking work?",
        difficulty: "hard",
        a: "<p><b>Cilium</b> is a CNCF graduated CNI plugin that uses <b>eBPF</b> to implement networking, security, and observability for Kubernetes. It has become the de facto standard for high-performance, security-conscious cloud-native networking, and is now the default in many managed Kubernetes offerings (Google Autopilot, Azure AKS with Cilium data plane, and others).</p><p><b>Why Cilium is significant:</b></p><p>Traditional Kubernetes networking relies on <code>iptables</code> for services, network policies, and load balancing. iptables was designed in the 1990s for stateful firewalling of a few dozen rules; production Kubernetes clusters can have <b>tens of thousands</b> of iptables rules. Performance degrades linearly, updates take seconds to propagate, and debugging is nightmarish. Cilium replaces this entire stack with eBPF programs attached to kernel network hooks — fast, scalable, and dynamically updatable.</p><p><b>What Cilium provides:</b></p><ol><li><b>CNI networking:</b> Pod-to-pod connectivity with eBPF-based routing. No iptables. Faster than any iptables-based CNI.</li><li><b>Kubernetes Services:</b> Replaces kube-proxy with an eBPF implementation. Service IPs are handled by eBPF programs that redirect traffic to backend pods with essentially zero overhead.</li><li><b>Network Policies:</b> Standard Kubernetes NetworkPolicy plus CiliumNetworkPolicy — which extends to Layer 7 (HTTP method, path, headers, gRPC service/method, Kafka topic, DNS name).</li><li><b>Encryption:</b> Transparent encryption between nodes via WireGuard or IPsec — no service mesh required.</li><li><b>Observability (Hubble):</b> Real-time flow visibility for every packet, at Layer 3/4 and Layer 7. Shows a service map of the entire cluster.</li><li><b>Service Mesh:</b> Optional sidecar-less service mesh functionality using per-node proxies plus optional waypoint proxies.</li><li><b>Cluster mesh:</b> Connect multiple Kubernetes clusters into a single flat network with service discovery across clusters.</li></ol><p><b>Example — Layer 7 network policy allowing only specific HTTP paths:</b></p><pre>apiVersion: cilium.io/v2\nkind: CiliumNetworkPolicy\nmetadata:\n  name: api-l7\nspec:\n  endpointSelector:\n    matchLabels:\n      app: api\n  ingress:\n    - fromEndpoints:\n        - matchLabels:\n            app: frontend\n      toPorts:\n        - ports:\n            - port: \"8080\"\n              protocol: TCP\n          rules:\n            http:\n              - method: GET\n                path: /api/products.*\n              - method: POST\n                path: /api/orders</pre><p>This policy blocks any HTTP method or path that isn't explicitly allowed — enforced in the kernel by eBPF programs. Standard NetworkPolicy cannot express this.</p><p><b>Performance benefits:</b> For a large cluster (500+ services, tens of thousands of endpoints), Cilium can be several times faster than iptables-based CNIs on latency-sensitive workloads. The savings compound with scale.</p><p><b>Adoption pattern:</b></p><ul><li>Managed Kubernetes: increasingly the default. GKE Dataplane V2, AKS with Cilium, and OpenShift can all use Cilium as their CNI.</li><li>Self-managed Kubernetes: Cilium is the recommended choice for new production clusters unless there is a specific reason to choose otherwise.</li><li>Migration path from Calico or Flannel: possible but requires care — cluster networking migrations are non-trivial.</li></ul><p><b>Isovalent</b> (now part of Cisco) is the commercial company behind Cilium and provides enterprise support, additional features, and the closed-source Enterprise edition.</p>",
      },
      {
        q: "Explain load balancing algorithms and when to use each.",
        difficulty: "medium",
        a: "<p>Load balancers distribute incoming traffic across multiple backend servers to improve availability and utilization. The choice of algorithm affects fairness, latency, cache locality, and failure handling. Modern L7 load balancers (Envoy, NGINX, HAProxy, cloud ALBs) support many algorithms; understanding the trade-offs helps choose the right one for a given workload.</p><p><b>Common algorithms:</b></p><ol><li><b>Round Robin:</b> Sends each new request to the next backend in rotation. Simplest algorithm. Assumes all backends are equally capable and requests are equally expensive.<ul><li>Pros: Simple, evenly distributed for uniform requests.</li><li>Cons: Poor with heterogeneous backends or variable-cost requests.</li></ul></li><li><b>Weighted Round Robin:</b> Same as round robin but with backend weights. A backend with weight 3 gets 3 requests for every 1 request to a weight-1 backend.<ul><li>Use: Blue-green deployments, canary releases with weighted traffic shifting, heterogeneous fleets.</li></ul></li><li><b>Least Connections:</b> Sends the next request to whichever backend currently has the fewest active connections. Naturally distributes load to less-busy backends.<ul><li>Pros: Adapts to variable request duration.</li><li>Cons: Requires connection tracking; misbehaves if connections are pooled or long-lived (a backend with many idle keep-alive connections looks \"busy\").</li></ul></li><li><b>Least Time / Least Response Time:</b> Sends to the backend with the lowest average response time. More accurate than Least Connections for load balancing based on actual capacity.<ul><li>Use: When request duration varies significantly and connection tracking alone is misleading.</li></ul></li><li><b>Consistent Hashing (Ring Hash, Maglev):</b> Hashes some request attribute (client IP, URL path, session ID) to always send matching requests to the same backend.<ul><li>Use: Cache locality — repeatedly serving the same URL from the same backend maximizes cache hit rate. Also used for stateful services where session affinity matters.</li><li>Key benefit: When backends are added or removed, only a small fraction of hashes are remapped (unlike simple modular hashing).</li></ul></li><li><b>Random:</b> Picks a random backend. Simple and surprisingly good in practice, especially with a large fleet.<ul><li>Variant: <b>Random with two choices (\"power of two\"):</b> Pick two random backends, send to whichever has fewer connections. Nearly as good as least-connections without needing global connection tracking.</li></ul></li><li><b>Header/URI-based routing:</b> Route based on HTTP header values or URL path. Not really \"load balancing\" — this is <b>content-based routing</b>. Used for multi-tenant services, A/B testing, and API gateway patterns.</li></ol><p><b>Session affinity (sticky sessions):</b> Ensures subsequent requests from the same client go to the same backend. Common techniques:</p><ul><li><b>Cookie-based:</b> The load balancer sets a cookie identifying the backend. Standard for HTTP.</li><li><b>Source IP hash:</b> The client's IP is hashed to a consistent backend. Simpler but breaks when clients share a NAT.</li><li><b>Header-based:</b> A custom header (e.g., session ID) drives affinity.</li></ul><p><b>Health checks:</b> Load balancers should exclude unhealthy backends. Two check types:</p><ul><li><b>Active health checks:</b> The LB periodically probes each backend (e.g., HTTP GET /healthz). Simple but adds overhead.</li><li><b>Passive health checks:</b> The LB observes real request responses and marks backends unhealthy on repeated failures. No probing overhead but slower to detect issues before they impact real requests.</li></ul><p><b>Practical guidance:</b></p><ul><li>Default choice: Round Robin or Random with two choices — simple and effective.</li><li>Long-lived connections (gRPC, WebSockets): Least Connections is dangerous — prefer explicit connection limits per backend.</li><li>Cache-heavy workloads: Consistent Hashing on URL or key.</li><li>Stateful services: Session affinity (though consider whether the state should be externalized instead).</li><li>Multi-region: Latency-based routing (DNS or Anycast) to route users to the nearest region, then normal load balancing within the region.</li></ul>",
      },
      {
        q: "How do you troubleshoot DNS issues?",
        difficulty: "medium",
        a: "<p>DNS problems are among the most common causes of production incidents in distributed systems. The classic aphorism \"it's always DNS\" reflects reality: DNS underlies almost every network operation, and small misconfigurations cause outsized impact. Systematic troubleshooting quickly narrows down the cause.</p><p><b>Layered troubleshooting approach:</b></p><ol><li><b>Verify the symptom.</b> Confirm what is actually failing:<ul><li>Does <code>ping hostname</code> fail because DNS resolution fails, or because the resolved IP is unreachable?</li><li>Is the failure intermittent or consistent? Does it affect all users or a subset?</li><li>Does <code>ping IP_address</code> succeed? If yes, it's a DNS-only issue. If no, it's a network reachability issue independent of DNS.</li></ul></li><li><b>Query DNS directly with <code>dig</code> or <code>nslookup</code>.</b></li></ol><pre># Basic resolution\ndig example.com\ndig +short example.com\n\n# Query a specific record type\ndig MX example.com\ndig TXT example.com\ndig AAAA example.com\n\n# Bypass local caches — query authoritative directly\ndig @8.8.8.8 example.com\ndig @1.1.1.1 example.com\n\n# Trace the delegation path from root to authoritative\ndig +trace example.com\n\n# Query with reverse DNS\ndig -x 8.8.8.8</pre><p><b>3. Compare resolver behavior.</b> Query multiple resolvers to isolate whether the issue is at your local resolver, an upstream resolver, or the authoritative servers:</p><ul><li>Local resolver: <code>dig example.com</code></li><li>Public: <code>dig @8.8.8.8 example.com</code> and <code>dig @1.1.1.1 example.com</code></li><li>Authoritative: Find NS records with <code>dig NS example.com</code>, then <code>dig @ns1.example.com example.com</code>.</li></ul><p>If public resolvers return different results than your local resolver, the local resolver is the problem (stale cache, misconfiguration). If authoritative servers return different results than public resolvers, the public resolvers have stale caches. If authoritative servers are wrong, the actual DNS records need fixing.</p><p><b>4. Check TTL and caching behavior.</b> DNS caching is a frequent source of confusion:</p><pre>dig example.com | grep -i \"; ANSWER\" -A1\n# Shows record with its remaining TTL</pre><p>If records were recently changed but old values persist, TTL is the reason. TTL of 3600 means up to 1 hour of caching downstream. Plan record changes ahead by lowering TTL 24 hours before the change.</p><p><b>5. Check for common Kubernetes DNS issues.</b> Inside Kubernetes clusters, CoreDNS handles internal DNS:</p><ul><li><code>kubectl exec &lt;pod&gt; -- nslookup kubernetes.default</code> — should return the Service ClusterIP.</li><li><code>kubectl logs -n kube-system deployment/coredns</code> — check CoreDNS logs.</li><li>NDOTS setting (default 5) causes non-fully-qualified names to first be tried with search domains appended, which can be slow. Common tuning point.</li><li><code>ExternalName</code> Services and stub domains can cause unexpected resolution behavior.</li></ul><p><b>6. Common DNS failure modes and their signatures:</b></p><table><tr><th>Symptom</th><th>Likely cause</th></tr><tr><td>SERVFAIL response</td><td>DNSSEC validation failure or authoritative server error</td></tr><tr><td>NXDOMAIN response</td><td>Record doesn't exist (may be legitimate or a misconfiguration)</td></tr><tr><td>Resolver timeout</td><td>Authoritative unreachable or resolver overloaded</td></tr><tr><td>Intermittent failures</td><td>One of multiple NS servers is broken; round-robin masks it partially</td></tr><tr><td>Stale records</td><td>TTL not yet expired at intermediate resolvers</td></tr><tr><td>Different results per query</td><td>Split-horizon DNS or GeoDNS returning different answers to different clients</td></tr></table><p><b>7. Tools for deeper DNS visibility:</b></p><ul><li><b>DNSViz</b> (dnsviz.net): Graphical DNSSEC and delegation analyzer.</li><li><b>MXToolbox:</b> Web-based tools for DNS record inspection.</li><li><b>RIPE Atlas:</b> Query DNS from thousands of vantage points worldwide — invaluable for identifying region-specific issues.</li><li><b>tcpdump port 53:</b> Capture DNS queries and responses to see exactly what is happening at the network level.</li></ul>",
      },
      {
        q: "What is Zero Trust networking?",
        difficulty: "medium",
        a: "<p><b>Zero Trust</b> is a security model that discards the traditional \"trusted internal network vs untrusted external network\" boundary. Instead, <b>every</b> request — regardless of its origin — must be authenticated, authorized, and encrypted. The model was popularized by Google's BeyondCorp architecture and is now the dominant security paradigm for modern cloud infrastructure.</p><p><b>Traditional model (\"castle and moat\") — and why it failed:</b></p><ul><li>A perimeter firewall separates internal (trusted) and external (untrusted).</li><li>Once inside the network, services trust each other broadly.</li><li>Corporate VPNs extend the \"inside\" to remote employees.</li></ul><p>This model failed because:</p><ul><li>Attackers who breach the perimeter (via phishing, supply-chain attacks, or exposed services) gain broad lateral movement inside.</li><li>Insider threats are not addressed.</li><li>Cloud, SaaS, and mobile eliminate the meaningful distinction between \"inside\" and \"outside\".</li><li>Multi-tenant environments cannot have a clear perimeter.</li></ul><p><b>Core Zero Trust principles:</b></p><ol><li><b>Never trust, always verify.</b> Every request is authenticated, regardless of where it comes from.</li><li><b>Least privilege access.</b> Users and services get only the specific access they need, not blanket network access.</li><li><b>Assume breach.</b> Design assuming attackers may already be inside; limit blast radius.</li><li><b>Verify explicitly.</b> Use multiple signals — identity, device health, location, behavior — for access decisions.</li><li><b>Continuous verification.</b> Access is not a one-time gate; it's continuously reassessed.</li></ol><p><b>Practical Zero Trust in DevOps environments:</b></p><ol><li><b>Identity-based access, not IP-based.</b> Users authenticate via SSO with MFA; services authenticate via mTLS, JWT tokens, or platform-managed identities. Network location does not grant trust.</li><li><b>Encrypt everything in transit.</b> TLS for HTTPS, mTLS between services, WireGuard/IPsec for network-level encryption. No cleartext, even inside the VPC.</li><li><b>Micro-segmentation via NetworkPolicy.</b> Even inside a Kubernetes cluster, pods should have explicit ingress rules — not general connectivity.</li><li><b>Service mesh for automatic mTLS.</b> Istio, Linkerd, or Cilium provides mutual TLS between all services by default, without application changes.</li><li><b>Just-in-time access to production.</b> No standing SSH access; access is granted on request, with approval, for a limited window. Tools: Teleport, Boundary, StrongDM.</li><li><b>Least-privilege IAM.</b> Every workload has a specific service account with narrowly-scoped permissions. No shared cluster-admin credentials.</li><li><b>Continuous authentication.</b> Session tokens are short-lived. Long-lived credentials (static API keys) are eliminated in favor of federated identities (OIDC, IRSA, Workload Identity).</li><li><b>Device posture checks.</b> Access requires the client device to meet security standards (patched OS, disk encryption, endpoint security running).</li><li><b>BeyondCorp-style application access.</b> Employees access internal apps through an identity-aware proxy (Google IAP, Cloudflare Access, Pomerium, Teleport) rather than via VPN.</li></ol><p><b>Zero Trust for developers:</b> Instead of VPN into the corporate network, developers authenticate through an identity-aware proxy to reach dev/staging environments. Kubernetes access is via short-lived tokens issued by SSO. Production access requires just-in-time elevation with peer approval.</p><p><b>Common tools in a Zero Trust stack:</b></p><ul><li><b>Identity:</b> Okta, Microsoft Entra ID, Auth0.</li><li><b>Zero Trust network access:</b> Cloudflare Access, Google IAP, Pomerium, Tailscale, Teleport.</li><li><b>Service-to-service mTLS:</b> Istio, Linkerd, Cilium, Consul Connect.</li><li><b>Just-in-time production access:</b> Teleport, Boundary, sudolikeaboss.</li><li><b>Secrets management:</b> Vault, AWS Secrets Manager, external-secrets-operator.</li></ul><p><b>The trajectory:</b> Zero Trust is not a product to buy — it's an architectural direction. Most large organizations are on a multi-year journey from traditional perimeter security toward Zero Trust. Understanding the principles and being able to explain how a specific implementation fits into the model is now a baseline expectation for security-conscious DevOps roles.</p>",
      },
    ],
  },
];
